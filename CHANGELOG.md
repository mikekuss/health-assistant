# Changelog

All notable changes to Health Assistant are documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

> **Workflow:** every user-facing change adds a line under `## [Unreleased]` at
> commit time (see [docs/RELEASE_PROCESS.md](docs/RELEASE_PROCESS.md)). At release
> time, `## [Unreleased]` is renamed to `## [vX.Y.Z] - YYYY-MM-DD` and a fresh
> `## [Unreleased]` is opened above it.

## [Unreleased]

### Fixed
- **Saving an existing integration's settings no longer fails with HTTP 500.** `POST /integrations/{domain}/config-flow?integration_id=…` (the Edit Configuration form) raised `UnboundLocalError: cannot access local variable 'generated'` after the commit, because the show-once machine-secret dict was bound only on the create-new-instance branch. The change was saved, but the UI reported an error. `generated` is now initialised before the create/update branch, so updates return the normal success payload (without secrets). Regression test in `backend/tests/test_integrations.py`.

## [v0.8.0] - 2026-09-20

### Added
- **Task Assignments adopt the §15 defaults-first grouping (desktop parity)** — the modality slots (Global Default Fallback / text, Document Parsing / vision, Voice Input / stt) lead as a "Default models" section; the eleven derived tasks (NLP, chat, Magic Fill, …) follow as "Other tasks". The AI-config page's Providers / Models / Task Assignments / Agent Settings sub-tabs now use the library `SegmentedTabs` (replacing the hand-rolled button strip).

- **BYOK one-click provider setup (§15, family plan 17 Phase 3)** — connect a provider from a neutral preset tile grid (OpenAI, OpenRouter, Groq, Mistral, DeepSeek, Ollama-local; Gemini/Anthropic overlay-disabled until their builders land) with guided key setup, fetch-first validation, curated model curation, gap-fill defaults at USER scope (chat→default, vision→ocr, stt→transcription), per-slot rebind pickers on the success panel, and a per-provider "Set up automatically" review dialog. i18n-routed §15 error enum with `suspectedVendor` mis-paste hints; actionable "no model assigned" deep-link into the settings surface. New endpoints: `GET /ai-config/provider-presets`, `POST /ai-config/providers/{preset_key}/setup`, `PUT /ai-config/providers/{provider_id}/set-default`. Migration `b1y2o3k4s5e6` adds `ai_providers.preset_key` and migrates model capabilities to the family vocabulary (`audio_input` → `stt`; full set `text|vision|tools|stt|tts|embeddings`) — round-trip tested. Vendored `contracts/ai-presets.json` + sync script + `check-byok-contract.sh` gate (CI: `byok-contract.yml`); uniform 12-case contract matrix + USER-scope isolation cases in `backend/tests/test_ai_provider_setup_contract.py`. Setup touches catalogs only — no PHI, no key/prompt telemetry.

- **Mock chat AI for demos & screenshots (no API key)** — new `mock` provider type: `MockMedicalChatModel` (`app/ai/chat_models.py`) is a deterministic scripted LangChain model that drives the real LangGraph chat engine with real DB tools in a 3-turn plan (recent examinations → examination details → markdown answer with `citation://` OBSERVATION/EXAMINATION chips). `seed_demo.py` provisions it idempotently (provider + `mock-medical` model + TENANT-scope `chat` assignment; skip with `HA_AI_MOCK=0` — a UI-configured provider outranks it), so the AI chat demo and the `ai-chat` screenshot work out of the box after every DB reset. New `backend/scripts/mock_chat.py` chats with it from the terminal.

### Changed
- **assistant-ui 0.36.1 → 0.43.0** — catch-up to the family version (study/desktop run ^0.42). Drop-in: no app code changes required by the bump. Unlocks the library's `DesktopMark` + desktop-aware family defaults (`FamilyBadge` default order now includes Desktop, so the About page lists it automatically).
- **Sidebar family menu lists Desktop Assistant** — new row (library `DesktopMark`, linking to neuronection.com/en/desktop/) alongside Health / Career / Study.
- **Tailwind CSS 3 → 4** — CSS-first configuration: `@theme` carries the brand/dark palettes, `xs` breakpoint, z-index scale, and animations (tailwind.config.js deleted); `@tailwindcss/postcss` replaces the tailwindcss+autoprefixer PostCSS chain; class-based dark mode via `@custom-variant dark`; `tailwindcss-animate` → `tw-animate-css` (same `animate-*` classes; its `@import` must precede `@plugin` directives); `tailwind-merge` v3 for v4-aware merging. ~217 files codemodded by `@tailwindcss/upgrade` (shadow/ring scale renames), plus removal of dead `bg-opacity-*` utilities (6 inline-style no-ops; CorrelationChart tooltip → `bg-white/95`). Verified: build, lint 0 errors, vitest 651/651.
- **websockets 15 → 16.1** — max version compatible with langgraph-sdk's `<17` cap (17.x ignored until it relaxes); plus python-slugify 9, xxhash 4 (dependabot #128/#133/#135).
- **Backend dependency batch** — langchain-openai 1.6.2 (with openai 2.54, staying under the langchain `<3` cap), fastmcp 4, bcrypt 5, + the grouped python/npm minor-and-patch updates (dependabot group PRs #127/#123). FE build/lint/651 tests + BE 3124 green.
- **Dependabot ignores extended** — `openai >=3` (langchain-openai cap), `transformers >=5`, `torch >=2.2` (deliberate AI-pipeline versioning), `tailwind-merge >=3` (pairs with held Tailwind 3), `eslint >=9` (flat-config migration).
- **Dropped the direct `pydantic_core` pin** — it's a transitive of pydantic (exact-pinned there); a direct pin only invites orphan bumps (this bit us twice: core 2.49.0 vs pydantic 2.13.x → 2.46.5).
- **Build chain majors: vite 6 → 8, @vitejs/plugin-react 4 → 6, typescript-eslint 7 → 8** (dependabot #122/#114/#117+#121). vite 8 needed a clean lockfile resolution (rolldown/babel peer chain); ts-eslint 8's stricter `no-unused-expressions` rule required three small statement fixes (ExaminationGroupManager, NotificationManagement). FE build + lint + 651/651 tests green.
- **CI now runs on PRs** — `docker-publish.yml` gains a `pull_request` trigger (test suite only; image publishing stays gated to push/tag events), so Dependabot PRs finally carry real checks instead of empty rolls.
- **Dependabot: grouped minor+patch updates** (one resolved PR per ecosystem for `python-*` / `npm-*` families) — prevents the split-PR `ResolutionImpossible` breaks (langchain-core, pydantic-core) seen today; majors stay individual PRs. New ignores: `tailwindcss >=4` (deliberate CSS-first migration), `uuid-utils >=1` (violates langchain's <1.0 cap).
- **recharts 2 → 3** — adopted the major bump (dependabot #100); the only break was `ResponsiveContainer`'s `height` prop, now `number | \`${number}%\`` — narrowed the `height` prop on `CorrelationChart` and `LineChart` accordingly (all call sites pass numbers).
- **Standardize on Node.js 24 LTS** — the frontend Docker image (`node:20-alpine` → `node:24-alpine` in both build and runtime stages), the security-scan workflow (Node 20 → 24, matching test.yml), the README dev requirement (Node 24+), and a new `frontend/.nvmrc`. `@types/node` moves to the 24.x line in the frontend (and gains `engines: node >=24` in frontend + ts-sdk), with a Dependabot ignore rule keeping `@types/node` on the 24 line. Closes the types→26 Dependabot PRs (#51, #62): types follow the runtime, and Node 26 isn't LTS until Oct 2026.


- **`@neuronection/assistant-ui` ^0.18.0 → ^0.36.1** — 18 minor versions of the family UI library (nav/SidebarNav baseline, SegmentedTabs, chat components, token/overlay polish). Fixes the biomarker-detail crash `Tooltip must be used within TooltipProvider`: library `InfoTooltip` (hover mode) now renders its own `TooltipProvider` so it works standalone (library changeset staged, ships ≥ 0.36.2; app suite verified against the fixed build).
- **Dev-server fix: pre-bundle the excluded library's whole dependency tree** — the assistant-ui 0.36.x update brought CJS packages into the `optimizeDeps.exclude` chain (`@tiptap/react` importing the `use-sync-external-store/shim/with-selector` wrapper, `tiptap-markdown` importing `markdown-it-task-lists`, `katex`), and raw-served CommonJS crashes pages with `doesn't provide an export named …` on any cold dep cache. `vite.config.ts` now generates the `optimizeDeps.include` list from the excluded package's own `dependencies` (skipping subpath-only packages like `@tiptap/pm`), plus explicit `zustand/vanilla|traditional|shallow` and `use-sync-external-store` entries for reactflow's raw chain.
- `scripts/version_manager.py` synced from the dev family template
  (`TEMPLATE_VERSION` stamp, `--version`; config-order fix so `--version`
  works without a config file).
- **UI capture pipeline moved to `scripts/ui-capture/` (family-standard, config-driven)** — the Playwright runner (`capture.mjs`), gallery generator (`gallery.mjs`) and capture wrapper (`capture_ui.sh`) are now vendored family scripts configured entirely through `scripts/ui-capture/ui-capture.config.json` (URLs/port-env fallback, demo auth, localStorage session injection, frozen clock, GIF order, seed command); the scene catalog moved from `frontend/tests-e2e/ui-capture/scenes.mjs` to `scripts/ui-capture/scenes.mjs` and demo seeding to `scripts/ui-capture/seed_demo.sh`. `./scripts/capture_ui.sh` stays as a thin shim (flags unchanged), `npm run capture:ui*` keeps working, and `docs/SCREENSHOTS.md` regenerated with byte-identical scene blocks (banner only).

- **AI architecture (ADR-0008, Phase 8): legacy chat loop decommissioned** — `run_reasoning_loop` and the `AI_AGENT_ENGINE` flag/tenant dial are deleted; the LangGraph engine (`app/ai/graphs/chat_agent.py`) is the only chat reasoning path (event/SSE contract unchanged). `?flow_events` now defaults to `true` on `/ai-assistance/stream` and `/resume` (the parameter remains as an override); legacy sentinel frames keep flowing until the frontend is family-events-only in a later release.

### Fixed
- **BYOK setup is now scope-aware** — running one-click setup from `/admin/system/ai-config` (or the tenant AI config surface) creates the provider + slot assignments at the SYSTEM/TENANT layer instead of always creating a personal USER-scope row invisible on that page (`ProviderSetupRequest.scope`, guarded by `check_scope_access`; adoption and gap-fill stay strictly within the requested scope, and `set-default` binds at the provider's own scope). The setup modal also now fetches the freshly persisted models directly for its rebind pickers (fixes a contradictory "no models were persisted" panel), ships the missing `settings.ai.close` label, and moves its actions into the dialog footer so Add and Edit provider share the same layout. Preset forms gain an explicit **Set up manually** action (creates the provider row as-is — no catalog fetch, no model curation, no default binding); editing the preset's base URL switches the form to manual-only.

- **Library dialogs rendered pinned top-left** — `@layer assistant-ui` was declared *before* `@import 'tailwindcss'`, so the later `base` layer's preflight (`* { margin: 0 }`) outranked every library class rule (cascade-layer order beats specificity): PanelModal dialogs (Edit Provider, the BYOK setup modal, ModelRegistry's catalog modal, …) lost their `sm:mx-auto`/`sm:my-auto` centering and `rounded-*`. `index.css` now pins the layer order explicitly (`theme, base, assistant-ui, components, utilities`) so preflight < library < app utilities.
- **Tailwind 4 dev-mode: library CSS layered (root cause of broken styling)** — the app's v4 utilities live in `@layer utilities`, which loses to ANY unlayered CSS; the prebuilt `assistant-ui/styles.css` (imported unlayered, containing a few of its own utilities) therefore overrode app variants (`sm:hidden`/`sm:block` never applied — e.g. the Browse & Connect modal rendered mobile chips on desktop with a 614px-tall stadium pills). The library stylesheet now imports as `@import '...styles.css' layer(assistant-ui)` from index.css (removed from main.tsx). Also switched the build to the official `@tailwindcss/vite` plugin (postcss.config.js removed) per v4 docs for Vite projects. **Dev servers must be restarted** to pick this up.
- **Missing translations for biomarker precision settings** — `settings.precision_below_10/_3/_1` (+`_desc`) and `settings.biomarker_precision(_desc)` were added as literal dotted keys *inside* the nested `settings` object of the locale files (lookup path `settings.settings.*` → miss → raw keys shown in Settings panels). Renamed to proper nested keys in EN + EL (backend `settings_definitions.py` label/description keys unchanged).
- **Settings subnav no longer moves between tab pages** (`/settings/*`, `/admin/system/settings`, `/admin/tenant/settings`) — mirrors study-assistant's stable-subnav behavior: the global header pill now renders the same shape on every tab (removed the inconsistent back button from AI Configuration and My Account; Integrations gained a proper PageHeader instead of shrinking the header to a path-derived title), the settings shell resets the scroll container on tab change so the sticky nav rail stops drifting with stale scroll offsets, and `/profile` moved inside the user settings shell (pathless layout route, same URLs) so switching to Profile no longer unmounts the entire subnav.
- **Fresh pip resolve failed after the pydantic batch** — Dependabot merged `pydantic-core==2.49.0` (#90) and `pydantic==2.13.5` (#96) separately, but pydantic 2.13.5 pins `pydantic-core==2.46.5` exactly; core aligned to 2.46.5 and the full requirements.txt resolves cleanly again.
- **Stale `@types/marked` / `@types/qrcode.react` removed** — both libraries ship their own TypeScript types; the `@types/*` devDependencies were dead weight Dependabot kept bumping (closed #91, #99). React 19 bumps are now Dependabot-ignored (>=19) — that migration is deliberate, not a drive-by bump (#101).
- **pip-audit advisories in backend pins** — tornado 6.5.7 → 6.5.8 (PYSEC-2026-3928, GHSA-wwv5-g3v4-889x, GHSA-8423-8fgw-73vq) and aiosmtplib 5.1.1 → 5.1.2 (PYSEC-2026-3805); Security Scan `pip-audit (backend)` job green again.
- **CI dependency failures from the Dependabot batch** — two merged bumps broke CI gates that local verification missed: (1) `langchain==1.4.0` requires `langchain-core>=1.6.0` but requirements.txt pinned core 1.4.8 (pip ResolutionImpossible on fresh install in CI) — core aligned to 1.6.3 and the full set now resolves and installs cleanly; (2) TypeScript 7 (#69) crashes the `@typescript-eslint` plugin during `npm run lint` ("Cannot read properties of undefined (reading 'Intrinsic')") — TypeScript held back on 5.9.x with a Dependabot ignore (>=6) until typescript-eslint supports it.
- **About page LinkedIn icon after lucide-react 1.x** — lucide removed brand icons in 1.0 (merged via dependabot #42), which broke the build (`Linkedin` no longer exported). Added an app-local `LinkedinIcon` SVG (same geometry as the former lucide icon) used by both About-page links.
- **`biomarker-detail` UI capture shot the wrong page** — the scene still navigated to the retired `/biomarkers/catalog` (now a redirect to `/catalogs?type=biomarker`), where the scripted `text=Total Cholesterol` click only selected a catalog row, so `docs/images/biomarker-detail-desktop.png` showed the Catalogs workspace instead of the detail view. It now deep-links to `/biomarkers/details/{biomarkerId}`, resolved deterministically via a new `biomarkerId` path token (`GET /biomarkers/slug/cholesterol-total`).

- **Fixed inline chat citations (react-markdown v10 regression)** — `defaultUrlTransform` strips the non-standard `citation://` protocol, so inline `[Ref: …]` citations degraded to plain links; the chat markdown renderer now passes a `urlTransform` that preserves them, restoring EXAMINATION/OBSERVATION chips for real LLM answers too.
### Security (P0 hardening, audit 2026-09-11)
- **Chat session ownership enforced (S-1)** — a client-supplied `session_id` is now verified to belong to the caller (`ChatSessionService.get_owned_session`) before any chat write or checkpointer attach; foreign sessions return 404 on the chat endpoints, and `save_message(owner_user_id=…)` refuses to write into another member's session (previously a user could inject messages into, and attach a LangGraph checkpoint thread to, another user's chat session).
- **Provider `api_base` SSRF guard (S-2)** — `fetch-external-models` and provider create/update now run `api_base` through the SDK net-guard (`assert_safe_url`), blocking DNS names that resolve internally (the gap the previous literal-IP endpoint check couldn't close). Self-hosted local LLM stacks keep working via `INTEGRATION_ALLOWED_HOSTS` / `INTEGRATION_BLOCK_PRIVATE_RANGES=false` or `DEBUG`.
- **Rate limits on LLM endpoints (S-4)** — new per-user fixed-window limiter (`rate_limit_user`); `/ai-assistance/assist|stream|transcribe` and the HITL `resolve`/`resume` streams are capped (30/5 min, 15/5 min for transcription) with 429 + `Retry-After`. Degrades open when Redis is unreachable, like the auth limiter.
- **Prompt-injection guard blocks by default (S-5)** — `high`-risk inputs (2+ injection patterns) are now rejected at the chat boundary instead of log-and-proceed; opt out via `PROMPT_GUARD_BLOCK_HIGH=false`.
- **Demo-token claim check (S-7)** — tokens minted by `/auth/demo-login` are rejected the moment `DEMO_MODE` is turned off (previously a stale demo token kept full user-level access on a production instance).
- **Frontend dependency fixes** — `npm audit fix` resolves all 7 advisories (3 high, build-chain: `browserslist`, `fast-uri`, `js-yaml`, `postcss-selector-parser`); `npm audit` is now clean.
- **PHI residue removed from localStorage (FE-1)** — the dead `documentSlice` (imported nowhere; had been persisting filenames, server file paths, and patient IDs to `localStorage['documents']` on every mutation) is deleted along with its store exports; the logout wipe keeps defensively clearing the historical `'documents'` key.
- **Route-level code splitting (FE-2)** — all 80 route components now load via `React.lazy` + a lightweight `Suspense` fallback (Login, Setup, Layout and the instance-registration barrels stay eager). The entry bundle drops from 5.06 MB to 3.78 MB (−25%) and each page ships as an on-demand chunk; follow-up entry-vendor splitting (react-quill / markdown stack via the statically-reachable shared layout chain) is tracked in the audit.

## [v0.7.0] - 2026-09-04


- **AI architecture (ADR-0008, Phase 6.2/6.3): family flow events + `FlowStatusCard` adoption** — the graph chat adapter now dual-emits the family event vocabulary (`flow_started`, `node_started`/`node_finished`, `interrupt`, `flow_finished`, `flow_failed`) alongside the legacy sentinel protocol, gated behind a new additive `?flow_events=true` query parameter on `/ai-assistance/stream` and `/resume` (legacy clients are byte-identical). The chat UI opts in and renders a live `FlowStatusCard` while a graph turn streams — Thinking → Using tools → Waiting for you → Wrapping up — with the interactive HITL review card hosted in the card's detail slot while the turn is live (after completion the persisted cards render exactly as before). The Task Monitor page adopts `FlowStatusCard` for active extraction pipelines: each processing examination renders its real stages (Aggregating → Analyzing text → Defining ontology → Persisting results) with failure state and error detail. Requires `@neuronection/assistant-ui` ^0.18.0 (FlowStatusCard).

- **AI architecture (ADR-0008, Phases 3.5/3.6): the LangGraph chat engine is now the default** — `AI_AGENT_ENGINE` defaults to `graph` (the original loop generator stays available as rollback via setting/env). The engine is tenant-resolvable for shadow rollouts: `TenantModel.settings.ai_agent_engine` overrides the system setting which overrides the env default (`AIAssistanceService._get_agent_engine`), so a single dev tenant can run `graph` while the platform stays on `loop`. Parity proof shipped in the same change: service-level tests run chat tool turns and HITL ask_user flows under BOTH engines asserting identical sentinel sequences up to the card (with the ratified terminal-card delta on graph), checkpoint durability across a full checkpointer reopen (resume-after-restart), and node-level / mid-flow (`update_state(as_node=…)`) graph tests per the LangGraph testing guide. The SSE adapter (3.4) required no further change — interrupt terminal-event handling shipped with 3.3.

- **Feature: per-model reasoning-effort setting** — the model registry gains a "Reasoning effort" option (`none` / `minimal` / `low` / `medium` / `high`, stored on the model row's `settings` JSONB and forwarded to the provider by the model factory). This lets reasoning-family models (GPT-5 / o-series) serve tool-calling chat by setting effort to `none`, instead of failing with an opaque provider 400 — no per-model special-casing in code; the model row remains the single source of configuration. The setting threads through `get_llm` and the OCR/NLP factory paths. UI: the existing model add/edit modal shows a Reasoning-effort select (localized EN/EL, custom values supported).

- **Fix: actionable chat error when the assigned model can't run chat tools** — providers like OpenAI reject function-tool requests from reasoning-family models (GPT-5 / o-series) with an opaque HTTP 400, which previously surfaced as a generic "error while responding" message. The stream classifier now maps provider 400s to a new stable `model_config` error code, and the chat UI shows localized (EN/EL) guidance: the model can't run chat tools, assign a tool-capable model (e.g. GPT-4o) to the Chat or Default task under Settings → AI Configuration, then retry. No raw provider text is exposed (the guidance text is app-owned).

- **AI architecture (ADR-0008, Phase 3.3): `ask_user` pauses the turn (graph engine) — interrupt/resume over checkpointed state** — under `AI_AGENT_ENGINE=graph` an `ask_user` card now ends the stream (terminal-card SSE) with the run checkpointed (`thread_id` = chat session id via the lifespan-held store); `/resume` delivers the resolution summary as `Command(resume=...)` so the model continues the same conversation instead of starting a fresh turn. `propose_*` review cards keep continue-after-propose; `HitlTask` rows + idempotent 409s remain the only dedup. Fault tolerance lands as ratified: `agent_step` retries transient connection/timeout errors, `tool_exec` never retries (clinical tools are not idempotent), and errors bubble to the SSE error classifier unchanged. Graph state is checkpoint-serializable-only (runtime handles ride in `config.configurable`); loop engine untouched (default engine is still `loop`).

- **AI architecture (ADR-0008, Phase 3.2): LangGraph chat-engine mirror behind `AI_AGENT_ENGINE`** — the agentic-chat reasoning loop now exists as a LangGraph StateGraph (`app/ai/graphs/chat_agent.py`: `agent_step` → `tool_exec` → capped routers → `finalize`) emitting the exact same event/SSE-sentinel vocabulary as the original `run_reasoning_loop`, proven event-for-event by parity tests (tool calls, HITL proposals with proactive persistence and trimmed feedback, provider-delta quirks, iteration cap, SSE output). Selected by the new `AI_AGENT_ENGINE` setting (`loop` default — zero behavior change until the Phase 3.5 shadow run); all three call sites (chat stream, HITL resume, non-streaming chat) are engine-agnostic via `chat_engine_iter`.

- **AI architecture (ADR-0008, Phase 3.1): LangGraph scaffold** — `app/ai/graphs/` package plus a process-lifespan-held `AsyncPostgresSaver` checkpoint store (pool opened once in the FastAPI lifespan, boot-time idempotent `setup()`, and drain-before-close so in-flight graph runs finish before the pool closes). New deps: `langgraph` 1.2.11 (patch bump from 1.2.6), `langgraph-checkpoint-postgres` 3.1.2, `psycopg[binary,pool]` 3.3.5 (psycopg2 for Alembic stays). New daily Celery beat job `prune_checkpoint_threads` keeps the checkpoint tables bounded: the tables carry no timestamps, so thread age derives from the owning chat session — sessions idle beyond `AI_CHECKPOINT_RETENTION_DAYS` (default 30, new setting) and orphan threads are pruned; the job is a no-op until the tables exist and never touches HITL card data. No graphs wired yet (the chat-agent graph is Phase 3.2) — zero user-visible behavior change.

- **AI architecture (ADR-0008, Phase 2): the model factory is now the single construction site and the only LangChain/provider-SDK import location** — zero user-visible behavior change. `backend/app/ai/providers/factories.py` moved to the canonical `backend/app/ai/chat_models.py` (registry + `get_llm` semantics unchanged); the OCR/NLP processors no longer hardcode `ChatOpenAI` fallbacks and are injected with their chat model (`get_ocr_processor`/`get_nlp_extractor` resolve assignments and build via the factory); the speech-to-text endpoint no longer imports the `openai` SDK — the gateway-owned `app/ai/assistance/stt.py` (plain `httpx` multipart) owns the transport, and the streaming-error classifier maps provider exception classes by MRO instead of importing them (SSE error codes unchanged). The vendored `check-ai-alignment.sh` CI gate is now **strict** (0 findings) and ships in-tree with its workflow.

- **Dependencies:** `@neuronection/assistant-ui` ^0.17.0 (publishes the compact `SidebarNav` density the v0.6.0 short-viewport sidebar shipped against — the release build failed `tsc` against the published 0.16.0 types).

## [v0.6.0] - 2026-09-02

- **Compact sidebar on short viewports** — at ≤720px window height the nav drops to the library's new `SidebarNav` `compact` density and the footer drops to a compact variant (Neuronection mark + wordmark link, smaller Fund/About pills, version; the family panel hides; CreateMenu stays) so the patient-record tree stays fully visible (library change pending the next `@neuronection/assistant-ui` release).
- **Sidebar footer is now a project block** — "Part of Neuronection" (mark + wordmark link), the three family assistants (Health / Career / Study — library marks, aligned names, external links, the current app emphasized), Fund + About pills and the app version (from `frontend/package.json`); the block collapses to icons in the collapsed rail and ships dark-mode variants. The Fund popup reuses the library `SponsorCard` (Buy Me a Coffee + GitHub star channels in `config/funding.tsx`, also wired into the About page). The About entry moved out of the main nav into the footer's About pill (`/about` itself is unchanged); the standalone `AppVersion` line left the sidebar (it still renders on the auth screens). Product copy lives in the app locales (en + el) — the library stays presentational per ADR-006.
- **Dependencies:** `@neuronection/assistant-ui` ^0.16.0 (compact SponsorCard redesign, clickable current-app FamilyBadge, `SidebarNav.secondaryItems`).

- **The AI Configuration settings were rebuilt on the shared family library** (`@neuronection/assistant-ui`), retiring the three largest hand-rolled settings surfaces (~1,500 lines of bespoke form/list code) in favour of the same components that power study- and career-assistant:
  - **Providers** are now compact cards with an inline connection-test row (Test → Connected/Failed with a live model count) and a dialog built on the library `ProviderForm`: write-only API key (the stored key is never rendered), Local/Cloud deployment toggle, jurisdiction country picker, and the compliance fields (company name/website). Provider type is pinned read-only when editing.
  - **Models** are now managed through the library `ModelRegistry`: one provider card per endpoint, an add/edit modal that lists the provider's live catalog (with automatic capability guessing for `whisper`/vision models), beautified display names, capability chips (`Text` / `Vision` / `Audio Input`), clearable temperature and max-tokens, a description field, and a per-row enabled checkbox. Adding every unregistered catalog model is one click.
  - **Task assignments** use the library `TaskAssignmentPicker`: the global default fallback gets its own Fallback picker row, each task row offers only models with the capability the backend requires (OCR → vision, transcription → audio, everything else → text), and clearing an assignment falls back to the default.
  - All AI-config strings are translated (English + Greek); scope handling (System/Organization/Personal) is unchanged.

- **The web UI now runs on the shared family design system** (`@neuronection/assistant-ui` 0.8.0, also powering study- and career-assistant). Every generic UI primitive — buttons, modals, popovers, menus, comboboxes, date/time pickers, tables, chips, search inputs, form blocks — is now a thin re-export of the library instead of a local copy, and the app's identity (light and dark) lives entirely in a small `theme.css` of token overrides. Notable improvements riding along: the bespoke `LegacyPopover` overlays are gone in favour of accessible library popovers; the record selectors (anatomy, taxonomy, doctor, organization, unit, category) are async comboboxes with consistent filtering; dialogs gained consistent Escape/focus-restore behavior; and look-alike internals got honest names (`DashboardCardTitle`, `DocumentCard`, `SetupStepList`, `ActiveTaskBadge`) so they can't be confused with the library components they resemble.

- **Fix: frontend container crashed with `Could not read package.json`.** Both `docker-compose.standalone.yml` and `docker-compose.prod.yml` overrode the frontend image's command with `npm run preview` — a Vite dev/preview command that needs the source tree and `package.json`. But the published `health-assistant-frontend` image is production-only: it ships the built SPA at `/app/dist` and serves it with `serve` (its default CMD on port 3000), with no `package.json`. The override made the container crash-loop (`ENOENT /app/package.json`) and nginx served nothing. Both compose files now drop the `command:` override so the image's built-in `serve` CMD runs, matching the nginx `proxy_pass http://frontend:3000`.

- **Fix: `install.sh` could start stale local images from a previous install.** The script only ran `docker compose up -d`, and Compose pulls an image only when it's absent locally — so re-running `install.sh` on a host with old images from an earlier install silently used those stale images (missing recent fixes). It now runs `docker compose pull` before `up -d` to refresh to the latest published images; new `--no-pull` flag skips the pull for offline reuse. (`update-docker.sh` already pulled.)
  - **New `--reset` flag: force-refresh stack data even when `.env` already exists.** The leftover-volume guard only fired when `.env` was freshly generated; a re-run over an existing `.env` + stale/corrupt volumes had no way to start clean. `install.sh --reset` now stops the stack and deletes the `postgres_data` + `redis_data` volumes (uploads with `--reset-all`) before pulling and starting fresh. `--yes` skips the confirmation prompt. `.env` is always preserved.

- **Fix: Flower could not connect to Redis (`HELLO must be called with the client already authenticated`).** Both compose files set Flower's `CELERY_BROKER_URL` to `redis://redis:6379/0` — no password — while the Redis container runs with `--requirepass`, so the Celery monitoring UI crashed on startup (Flower's healthcheck then failed and the UI showed an error page). The broker URL now includes the password (`redis://:${REDIS_PASSWORD}@redis:6379/0`), matching worker/beat.

- **Fix: `migrate` service failed with `No 'script_location' key found in configuration`.** The backend Docker image (`docker/Dockerfile`) never copied `backend/alembic.ini`, so `alembic upgrade head` inside the `migrate` container had no `script_location` and exited 255 — the backend, worker, and beat then never booted on a fresh `install.sh` run. The image now ships `alembic.ini` at `/app/backend/alembic.ini`, where Alembic expects it.
  - Cleanup: the backend/worker Dockerfiles' application-code `COPY`s are consolidated into a single multi-source line so a future new `backend/` file (like `alembic.ini`) can't silently miss the image again; `backend/alembic.ini` no longer carries the stale placeholder `sqlalchemy.url = …admin123…` (the URL is injected at runtime by `alembic/env.py:get_url()` from `app.core.config`).

## [v0.5.0] - 2026-08-23

- **Beginner-first install & setup overhaul.** A single command now takes a fresh clone to a running Docker stack, and advanced options are hidden from first-timers without losing any feature.
  - New `scripts/install.sh` — one-command Docker install: pre-flight checks (cwd, Docker, daemon, compose), generates `.env` interactively (Quick Start default), starts the standalone stack, waits for the backend healthcheck, then prints the app URL + a one-click first-run setup link. Idempotent (never overwrites `.env`); flags `--env-only` / `--no-wait`.
  - New `scripts/update-docker.sh` — documented Docker update path in one command: `git pull --ff-only` (warn-and-continue so a dirty tree never bricks a running install), image pull, restart, wait-for-healthy. Flags `--no-pull` / `--no-wait`.
  - `scripts/setup_env.py` — new 3-option menu with **Quick Start** as the default: it asks only the public app URL and writes a production-oriented `.env` aligned to the standalone stack (`APP_ENV=production`, `DEBUG=false`, `TRUSTED_PROXY_COUNT=1`, `DEMO_MODE=false`); domain/LAN URLs automatically get `SETUP_TOKEN_MODE=env` with a printed one-click `/setup?token=…` URL, localhost keeps the tokenless path; an optional advanced gate drops into prefilled Full Setup. Full Setup's environment default flipped to production and its next-steps now point at `docker-compose.standalone.yml`. Quick mode no longer writes the demo/dev-tooling credentials. Also fixed a latent bug where the `.env.example` prose comment `# APP_ENV=production + DEMO_MODE=true).` was rewritten as a stray duplicate `APP_ENV=` line.
  - `docs/INSTALL.md` rewritten beginner-first — a strictly linear Quick Start built around `./scripts/install.sh`; the setup-token modes matrix, headless CLI admin, seeding prose, and anatomy expansion packs moved out of the first-run path into an Advanced & Reference section. README production quick-start now leads with `install.sh`. Plan: `dev/plans/install-setup-beginner-overhaul-2026-08-23.md`.
  - **Fix: fresh-install migrate failure on hosts with a leftover Postgres volume.** `install.sh` (and `update-docker.sh` when it has to regenerate a deleted `.env`) now detects a `postgres_data` volume left behind by a previous install on the same Docker host. The postgres container only applies `POSTGRES_PASSWORD` on first init of an empty data dir, so a freshly generated `.env`'s new password doesn't match the old volume's stored one — `alembic upgrade head` then dies with `password authentication failed for user "admin"` and the backend never boots. The new shared guard (`check_leftover_db_volume` in `scripts/lib-docker.sh`) resolves the compose project, checks the postgres volume on fresh `.env` generation, and prompts to reset it for a clean install (destructive — offers `docker compose down` first if the old stack still runs) or aborts so the user can restore their previous `.env`.

- **Fix: bridge pairing for mandatory machine secrets.** Instances created before the HMAC hardening (or whose creation-time plaintext was never shown) were locked out of the API proxy with `401: API secret not configured`. New owner-scoped `POST /integrations/instance/{id}/rotate-secret` (`field=api_secret|webhook_secret`) mints a fresh secret, stores it Fernet-encrypted with the instance id as context, and returns the plaintext **once** — the previous secret dies immediately. The web app's "Connect your mobile app" card now has a *Show pairing code* action that rotates + renders a QR/connection code carrying all three segments (`base_url|instance_id|api_secret`) — the exact form the Android app's scanner and paste field already accept (`Onboarding.parseQr`). Plan: `dev/plans/bridge-secret-pairing-2026-08-18.md`.

- **Fix: telemetry biomarker trends silently failing since the long-format migration.** Every telemetry trends query (`/analytics/trends` with `aggregation=1 hour|1 day|1 month`) errored with `no top level time_bucket_gapfill in group by clause` — `GROUP BY bucket` resolved to the source column instead of the gapfill expression — and the service fell back to raw FHIR observations, so dashboards showed observation-shaped data instead of OHLC telemetry. Three fixes: (1) the gapfill expression is now the GROUP BY key (positional) with explicit start/end so gap-filling actually works; (2) bound params are explicitly cast (`timestamptz`/`uuid`) — asyncpg's inference resolved them as `text` through the overloaded gapfill signature; (3) new migration `r1e2a3l4t5i6` enables **realtime aggregation** on the three telemetry continuous aggregates (they were `WITH NO DATA` + `materialized_only=true`, so `telemetry_monthly` returned zero rows while the raw hypertable held 260k — realtime union of live buckets is the TimescaleDB-recommended dashboard pattern). Verified against live data: all three aggregation paths now return real OHLC telemetry.

- **Fix: fresh `pip install -r requirements.txt` failed** after the cryptography 50 bump — the unused `fastapi-mail` dependency (never imported; email delivery is not wired) caps `cryptography<50` and made pip's resolver fail on clean environments (Docker builds included). Removed the dead dependency; resolution + `pip-audit` both clean now.

- **Docs: aligned all documentation with the security-hardening changes (2026-08 audit follow-through).**
  - `docs/API.md` — JWT/session-token model rewritten (1h TTL, `token_kind`/`jti`, DB-revalidating refresh, logout kills the access token), single-use invites with 30-day cap, bootstrap moved to `/auth/setup`, `TRUSTED_PROXY_COUNT` rate-limit note, and the inbound machine-routes table rewritten for mandatory HMAC secrets + mandatory timestamp + query-string MAC + minimal unsigned `/status`; `expires_in` examples 86400→3600.
  - `docs/INTEGRATIONS_SDK.md` §3.16 + secret-handling section — mandatory auto-provisioned per-instance secrets (shown once, Fernet + row-binding), route-level timestamp/query enforcement.
  - `docs/INTEGRATIONS_FRAMEWORK.md` — webhook flow rewritten (UUID routes, never authenticates).
  - `docs/API_LAYERS.md` — Layer-1 session token description updated (claims, TTL, revocation).
  - `docs/TENANCY_AND_USER_MANAGEMENT.md` — invite single-use semantics, SYSTEM_ADMIN grant restrictions (`/users` blocked for tenant admins), role-change token revocation.
  - `docs/SEEDING_AND_DEMOS.md` — the `DEMO_MODE_ACCEPT_UNAUTHENTICATED` fail-closed gate.
  - `docs/INSTALL.md` — `create_system_admin.py --password` is required (no `admin123` fallback).
  - `docs/MOBILE_SYNC.md` + bridge SDK docs (`authentication.md`, `troubleshooting.md`, Kotlin README) — mandatory-secret language, canonical form incl. query string + mandatory timestamp, unsigned-`/status` minimal payload + clock resync.
  - `.env.example` — `TRUSTED_PROXY_COUNT`, `ENABLE_API_DOCS`, `DEMO_MODE_ACCEPT_UNAUTHENTICATED`, disabled-by-default `MCP_STDIO_ALLOWED_COMMANDS`, placeholder-refusal notes for `SECRET_KEY`.
  - `dev/audits/setup-token-modes.md` — deprecation banner for `setup_url_hint` (audit C-1); frontend `Setup.tsx` comment cleaned up.

- **Security hardening (P2 of the 2026-08-18 audit): CI scanning + hardening tail.**
  - New `.github/workflows/security-scan.yml`: weekly + PR-triggered `pip-audit` (strict) and `npm audit` (moderate threshold) and a full-history `gitleaks` secret scan.
  - Global-search ILIKE patterns escape `%`/`_` wildcards (API-L4) — wildcard-stuffed queries no longer force match-everything scans.
  - Deleted the dead `app/core/middleware.py` auth stub (wrong role comparison, unreferenced — a trap for future use; AUTH-L5).
  - The audit report (`dev/audits/SECURITY-AUDIT-2026-08-18.md`) now carries a §10 remediation-status record: 5/5 Critical, 16/16 High fixed; deferred items (offline-cache encryption, token key separation, net_guard IP pinning, MFA, cookie sessions) are explicitly documented as non-blocking with mitigations.

- **Security hardening (P1 follow-ups of the 2026-08-18 audit).**
  - **AUTH-H1:** rate-limit identity now honors `X-Forwarded-For` only up to a configured `TRUSTED_PROXY_COUNT` (default 0 = direct exposure, header ignored) — spoofed XFFs can no longer mint fresh brute-force buckets.
  - **AUTH-L4:** WebSocket auth no longer accepts `?token=` query strings at all (subprotocol-only; JWTs no longer reach proxy logs/browser history).
  - **API-L2:** remaining 500-details that echoed exception internals (`examinations` extraction trigger, image edit) replaced with generic messages.
  - **Docs:** INSTALL.md TLS section rewritten around the new `nginx-TLS.conf` flow (the old text claimed in-stack TLS termination that didn't exist); the prod checklist documents auto-provisioned webhook secrets, `TRUSTED_PROXY_COUNT`, `REDIS_PASSWORD`, and `ENABLE_API_DOCS`; the nonexistent `APP_CSP_CONTENT` reference is gone.

- **Security hardening (Batch 6 of the 2026-08-18 audit): dependency vulnerabilities — both ecosystems now scan clean.**
  - Python: `pillow` 12.2.0→12.3.0 (14 advisories, image-parsing surface), `aiohttp` 3.14.1→3.14.3 (3), `cryptography` 49.0.0→50.0.0 (1), `pyasn1` 0.6.3→0.6.4 (3), `mcp` 1.28.0→1.28.1; unused `python-jose`/`ecdsa` removed from the local env. `pip-audit`: **0 known vulnerabilities**.
  - Frontend: `react-router-dom` 6.30.4→7.18.2 (open-redirect + SSR hydration injection advisories; the removed `future` flag was the only code change needed), plus `postcss`/`nanoid`/`js-yaml`/`brace-expansion`/`fast-uri` transitives via `npm audit fix`. `npm audit`: **0 vulnerabilities**; build + 636 tests green after the major bump.

- **Security hardening (Batch 5 of the 2026-08-18 audit): configuration, containers, TLS posture.**
  - **C-5:** a root `.dockerignore` (committed, un-gitignored) keeps PHI (`uploads/`), secrets (`.env*`, keys), `venv/`, caches and dev docs out of image layers; all Dockerfiles use multi-stage builds with allowlisted `COPY` paths and run as a non-root `ha` user (no compilers in runtime images).
  - **C-5/CFG:** the settings `.env` tree walk-up is disabled outside dev — a baked-in container `.env` can no longer silently downgrade boot guards; production must use real env vars or an explicit `HA_ENV_FILE`.
  - **CFG-H1:** production refuses to boot with placeholder/weak `SECRET_KEY` values (the `.env.example` literal previously passed) — ≥32 chars + entropy required; the DB-password blocklist now also rejects `secure_password_here`.
  - **CFG-H2:** a TLS-terminating nginx template ships (`docker/nginx-TLS.conf`: 80→443 redirect + ACME webroot + modern ciphers); the HTTP-only `nginx.conf` is explicitly marked dev/VPN-only and no longer publicly proxies `/docs`.
  - **CFG-H6:** `DEMO_MODE=true` outside dev refuses to boot unless `DEMO_MODE_ACCEPT_UNAUTHENTICATED=true` is set explicitly (was a log-warning-only auth bypass).
  - **CFG-M1/M2:** Redis runs with `requirepass` in prod + standalone compose (`REDIS_PASSWORD` required, healthchecks authenticated); the dev-db compose binds Postgres/Redis to 127.0.0.1 instead of 0.0.0.0.
  - **CFG-M4:** `DEBUG=true` with a non-dev `APP_ENV` refuses to boot (SQL echo logs PHI with bound parameters).
  - **API-L1/CFG-L1/CFG-L2:** Swagger/Redoc disabled outside dev unless `ENABLE_API_DOCS=true`; `TrustedHostMiddleware` pins the app to `APP_URL`/`FRONTEND_URL` hosts in production; CORS origins now resolve through pydantic settings (no more `os.getenv` bypass) and `PATCH` is allowed.
  - `scripts/setup_env.py` generates `REDIS_PASSWORD`, writes `.env` with 0600 permissions, and prints the new key.

- **Security hardening (Batch 4 of the 2026-08-18 audit): frontend XSS chains + token lifecycle.**
  - **FE-H1/H3/M6:** all 12 raw-HTML sinks (examination/patient notes, allergy/medication descriptions, biomarker info, catalog rich text, custom SVG icons) now render through DOMPurify (`utils/sanitize.ts`); the Quill editor sanitizes both markdown-converted and pasted HTML on write.
  - **FE-H2:** AI chat no longer disables react-markdown's URL sanitizer (`urlTransform` override removed — `javascript:` links from LLM output were clickable).
  - **FE-H4:** the 401-refresh interceptor now excludes `auth/refresh` (killed an infinite request-recursion loop), uses a single-flight lock shared across concurrent 401s, runs on a bare axios instance, and persists the rotated refresh token.
  - **FE-M2/M5/L4:** logout wipes the tenant-switch `originalAccessToken`/`originalRefreshToken` (previously survived logout due to a case-sensitive sweep), the `documents` PHI cache, and persisted AI/settings stores; the sweep is now case-insensitive.

- **Security hardening (Batch 3 of the 2026-08-18 audit): integrations framework, MCP client, bridge machine surfaces.**
  - **C-4:** MCP `stdio` transport disabled by default (`MCP_STDIO_ALLOWED_COMMANDS=""`) — it was user-configurable remote code execution (`python -c …` passed the command allowlist). Interpreter inline-code flags (`-c`/`-e`/`-p`/…) are rejected at the argument level even when an operator re-enables stdio.
  - **INT-H1/H2:** webhook + API-proxy routes fail closed — the integration UUID is no longer accepted as a credential. Instances are auto-provisioned with a high-entropy HMAC secret at creation (Fernet-encrypted with row-binding, shown once in the create response).
  - **INT-H3/H4:** SSRF net-guard now covers the `fhir_server` DocumentReference attachment fetch (previously a raw `httpx.get` bypass that could read internal network endpoints and store the bytes as a user-readable document) and MCP http/sse URLs (private/loopback/link-local targets blocked).
  - **INT-M1:** webhook bare-MAC deliveries get a Redis replay guard (10-min window); the canonical timestamped scheme remains fully replay-proof.
  - **INT-M2:** API-proxy HMAC now requires `X-Api-Timestamp` (no more unlimited replay) and covers the query string (query tampering on captured requests rejected; Kotlin SDK signing alignment).
  - **INT-M3:** unsigned `GET /status` returns only `{status, server_time}` — no cursor/SDK-version/frontend-URL oracle (signed status probes get the full payload; `server_time` lets skewed SDK clocks resync).
  - **INT-M4:** OAuth `redirect_uri` pinned to `APP_URL` instead of the client `Host` header (DCR registration poisoning). Machine routes enforce a 40 MiB body cap; error details no longer echo internals.

- **Security hardening (Batch 2 of the 2026-08-18 audit): API attack surface — intra-tenant PHI boundaries, file-write traversal, SSRF.**
  - **C-3:** backup-restore document filenames are extension-validated against the upload allowlist — a crafted `documents.json` could previously write arbitrary files via `../` in the "extension".
  - **API-H1:** `POST /import/ocr` no longer accepts a client-supplied `api_base` (the platform key was exfiltrated to attacker URLs); all import endpoints now cap request bodies at `MAX_UPLOAD_SIZE` and never echo exception strings.
  - **API-H2:** patient-scope exports access-check every requested `patient_id` — a USER could previously export any tenant patient's full record (incl. raw documents) by UUID.
  - **API-H3:** AI chat + tool listing validate the client-supplied patient context via `check_patient_access` before any tool pipeline binds it.
  - **API-H4/H5:** document `preview` enforces owner for USER role; `presign` + `dicom-metadata` fetch tenant-scoped (tenant ADMIN/MANAGER can no longer cross tenants via the admin short-circuit).
  - **API-M2:** `GET /observations/{id}` enforces the USER patient gate; **API-M3:** global search results (patients, examinations, documents, events) are restricted to the USER's own linked patients; **API-M4:** notification triggers listed/deleted/test-fired by a USER are limited to their own (created-by or own-patient) triggers.

- **Security hardening (Batch 1 of the 2026-08-18 pre-exposure audit, `dev/audits/SECURITY-AUDIT-2026-08-18.md`): authentication & token lifecycle.**
  - **C-1:** `GET /auth/setup-status` no longer returns the live setup token in `setup_url_hint` (env mode) — anonymous callers could bootstrap the instance with it; the launcher composes the one-click URL itself.
  - **C-2:** tenant ADMIN/MANAGER can no longer escalate to `SYSTEM_ADMIN` via `PUT/POST /api/v1/users`; the service layer refuses the role change too, and MANAGERs can no longer change roles at all.
  - **H2:** `/auth/refresh` re-loads the user row on every refresh — deleted/deactivated users are refused, and claims (email/tenant/role) are rebuilt from the DB; user deletion and role changes revoke all live refresh + session tokens.
  - **H3:** refresh tokens are rejected as bearer access tokens everywhere (`verify_access_token` enforces the `type=refresh` split); invite/presigned/api tokens are refused on domain routes.
  - **H4:** `is_active=false` now blocks login, demo-login, and refresh (previously "disabled" users kept full access).
  - **H5:** tenant-switch access/refresh tokens are properly typed and jti-registered (revocable; refresh preserves + re-validates the switch context).
  - **M1/M2/M5:** uniform login timing (dummy bcrypt), invite validated before the email-exists check (kills invite-free email enumeration), uniform `invalid_client` OAuth error with timing equalization.
  - **M3:** invite tokens are single-use (Redis jti consumption, fail-closed) with the TTL capped at 30 days.
  - **M4:** session access tokens now carry a server-registered `jti`; `logout` kills the live access token itself and `logout-all` wipes sessions + refresh tokens. Default access-token TTL reduced 24h → 1h (`JWT_EXPIRATION_HOURS`).
  - **L1:** bcrypt input is consistently truncated to 72 bytes (removes pin/venv behavior drift); `create_system_admin.py` no longer defaults to `admin123` (explicit `--password` required).

- **PWA: Android installability + offline navigation — PNG manifest icons (incl. maskable), service-worker navigation fallback, pinch-zoom unlocked.** Hardening pass driven by the new Android companion hand-off entry points ("Ask the assistant" Home card + "Open web dashboard" Profile row, landed in the `app/` repo) which deep-link into the PWA via Chrome Custom Tabs. (1) The manifest previously declared only SVG icons with `purpose: "any maskable"` — Chrome's SVG-maskable handling is inconsistent, weakening install prompts; the manifest now ships raveled PNGs `pwa-192/512.png` (any, rounded-square art from `icon.svg`) + `pwa-192/512-maskable.png` (full-bleed gradient variant, waveform verified inside the 40% safe circle); PWA shortcuts and the iOS `apple-touch-icon` (SVG unsupported there) point at `pwa-192.png`. (2) `src/sw.ts` registers a workbox `NavigationRoute` bound to the precached `/index.html` — every in-app navigation now works offline (SPA shell served, client router takes over); the previously dead `runtimeCaching` blocks under `injectManifest` remain inert by design (offline data still flows through the Dexie queue + repository caches). Dev is excluded (`import.meta.env.PROD`) so the dev server keeps routing/HMR. Verified headless-Chrome: offline `/dashboard` + `/ai-assistant` both return 200 + shell. (3) `index.html` viewport drops `maximum-scale=1.0, user-scalable=no` — pinch-zoom is an a11y requirement, not a trade.

- **Feature: bridge mobile rich-content reads — biomarker `info`, `GET /documents/{id}/text`, richer exam detail (mobile rich-text R4).** Three additions that let the Android companion render the platform's rich clinical text natively instead of stripping it to plain text. (1) `GET /biomarkers` items now carry `info` — the Markdown education text (`BiomarkerDefinition.info`, LLM-generated) the web frontend already renders; the app renders it in a new "About this biomarker" section via its Markwon pipeline. (2) New `GET /documents/{id}/text` returns `{id, extracted_text, status, truncated}` — the OCR-extracted Markdown of a document, capped at 512 KiB (`truncated: true` flags the cut); patient-scoped via `_bound_document` (cross-patient → error, soft-deleted excluded). (3) `GET /examinations/{id}` now also returns `category` (via the `category_concept` selectin relationship), `lab_name` (organization name), and `external_id` — breaking wire addition, both repos land together (no other consumers). Kotlin SDK bumped to **0.5.0**: `BiomarkerSummary.info`, `ExaminationSummary.category/labName/externalId`, new `DocumentText` model + `getDocumentText(id)` reader. 4 new DB-backed tests pin info passthrough, text read + truncation flag, cross-patient isolation, exam-detail fields; 3 new SDK decode tests. Bridge docs updated. Companion app changes land in the `app/` repo (shared readers + Room v7 `info` column + UI surfaces).

- **UX: biomarker detail page — status-tinted latest value, delta badge, status in header, dead buttons removed.** Four small UI improvements to the biomarker detail page that increase information density without adding controls. (1) Removed the dead Share/Print buttons in the sticky toolbar — they had no `onClick` handlers. (2) The interpretation status (Normal/High/Low/Warning) now appears as a colored pill in the PageHeader subtitle next to the category/telemetry badges, so the user sees the latest reading's status before scrolling to a tab. (3) The big "Latest Result" digit in the Patient Snapshot is now tinted by status (red for High/Abnormal, blue for Low, amber for Warning, neutral for Normal) via a new `getStatusTextColor` helper in `biomarkerUtils.ts` (mirrors `getStatusColorClass` but text-only — no badge chrome). (4) A neutral delta badge under the latest value shows `↑/↓ X.X% vs prev` computed from the last two numeric readings — direction is shown by the arrow, color is left neutral so the user combines it with the latest-value tint (avoids the "lower is always better" bias — wrong for HDL, vitamin D, etc.). STATE biomarkers and single-reading series gracefully skip the delta. New i18n keys `delta_vs_previous`, `delta_vs_previous_tooltip`, `latest_status_tooltip` (en/el). 4 new unit tests pin `getStatusTextColor`.

- **UX: biomarker detail page — remove duplicate KPI strip + add mobile Snapshot tab.** The 4-card KPI strip above the tabs (Latest Result / Clinical Reference / Avg Overall / Total Records) duplicated the right-column Patient Snapshot card — visible one below the other on mobile, forcing a long scroll past redundant numbers. Removed the strip from this page (the `BiomarkerKpiStrip` component itself is retained — `ObservationDetail` still uses it). The Patient Snapshot card body was extracted into a reusable `BiomarkerSnapshotCard` (single source of truth for the layout) and now renders in two contexts: the desktop right sidebar (xl+, unchanged) and a new mobile-only **Snapshot** tab that appears in the tab strip below xl alongside Trend / Clinical / History / Insights / Relations / Technical. The right sidebar is hidden below xl; the Snapshot tab bundles the migration progress banner so it stays reachable on mobile. Viewport-aware tab sanitization: a deep link to `/snapshot` on a desktop viewport falls back to Trend, and crossing the xl boundary while on Snapshot auto-switches to Trend (URL-synced). New i18n keys `biomarkers.tab_snapshot` + `biomarkers.tab_snapshot_desc` (en/el).

- **Feature: bridge unified `/changes` delta endpoint (mobile-bridge-v2 Phase 5).** One round-trip replaces N per-type reads; powers the Android companion's pull-to-refresh + the 15-min wake-up poll. `GET /changes?since=<ISO>&types=<csv>&limit=` queries each requested type by `updated_at > since` AND the bound patient + tenant + `deleted_at IS NULL`, returns `{data: {medications, allergies, vaccines, clinical_events, documents, examinations}, cursor, cached_at, since}`. The cursor advances to `max(updated_at)` across the batch when at least one row matched (null = nothing changed, client re-uses same `since`). `types` filters to a subset (unknown → HTTP 400); default is all six; `since` defaults to now − 7 days for bounded first-pull. **No migrations needed** — every target table already has `updated_at` via `TimestampMixin` (auto-updated on UPDATE, indexed). Limitation documented: deletions (soft or hard) aren't represented — client periodically does a full re-sync to discover them. Kotlin SDK gains `getChangesRaw(since, types, limit)` (raw JSON — the response shape diverges from `ReadEnvelope<List<T>>`). 9 new DB-backed tests pin: since-filtering, cursor advance, null cursor on empty, cross-patient isolation, types filter, unknown-type rejection, default 7-day window, since-echo.

- **Fix: pre-existing bug in `document_service.delete_document` — raw-SQL `text(":doc_id::uuid")` poisoned the transaction under asyncpg.** SQLAlchemy's `text()` bindparam regex `:name(?!:)` deliberately skips a name immediately followed by `:`, so `:doc_id::uuid` reached asyncpg as a literal SQL string, hit a Postgres syntax error on the `:`, the except logged a warning + swallowed, and the subsequent `db.delete(document) + commit` aborted with `InFailedSQLTransactionError`. Documented as the root cause whenever a doc-with-observations delete previously 500'd. Replaced with an ORM `delete(Observation).where(document_id == ...)` — same behaviour, no raw SQL, no bindparam parser interaction.

- **Feature: bridge mutations + extraction status — DELETE exam/doc, POST extract, GET status/logs (mobile-bridge-v2 Phase 4).** The bridge now exposes the mutation + extraction paths the Android companion needs for its Records-overhaul screens. All patient-scoped via the existing `_bound_examination`/`_bound_document` helpers (cross-patient → HTTP 400 *before* any side effect — no file unlink, no row delete, no OCR dispatch). New arms: `DELETE /examinations/{id}` (hard delete + cascade: unlinks every document's file + defensive bulk delete of orphan observations/medications), `DELETE /documents/{id}` (file unlink + row delete + observation cleanup via the now-fixed `delete_document`), `POST /documents/{id}/extract` (dispatches the OCR/NLP pipeline — best-effort Celery, returns `{job_id, message}`), `GET /documents/{id}/extract/status` (live `{status, progress, error_message}`), `GET /examinations/{id}/status` (exam-level extraction state + per-document status array, mirrors the PWA endpoint), `GET /examinations/{id}/logs` (TaskLog entries for the exam + its documents). 12 new DB-backed tests pin: row removal, cross-patient isolation for every path, OCR job-id return + status flip, exam status documents array, log tenant filtering. *(PUT/PATCH edits + image transforms + exam-level extract deliberately deferred — they have richer validation/side-effects; the high-value mutation surface for v2 is delete + re-extract + status.)*

- **Feature: bridge clinical-record reads — medications / allergies / vaccines / clinical-events / doctors (mobile-bridge-v2 Phase 3).** The bridge now exposes the high-value daily-use clinical reads the Android companion needs for its Today screen + Home safety cards. All new paths HMAC-gated (no exemptions added), all patient-scoped via `_bound_patient_id` (the bound patient + tenant + `deleted_at IS NULL`), all reuse the existing ORM models (no migrations, no model changes). New arms: `GET /medications?limit=`, `GET /allergies?active=&limit=` (active-only by default — `?active=false` returns resolved history), `GET /vaccines?limit=`, `GET /clinical-events?status=&limit=` (flat list — no nested relations for speed), `GET /clinical-events/{id}` (full nested `to_dict()` with type_details/examinations/observations/anatomy_links/occurrences, eager-loaded to avoid async N+1), `GET /doctors?limit=` (tenant-wide address book — doctors aren't patient-scoped). 12 new DB-backed tests pin happy-path + cross-patient/cross-tenant isolation for every path. The Kotlin SDK gains `Medication`/`Allergy`/`Vaccine`/`ClinicalEvent`/`Doctor` models + typed readers (`getMedications`, `getAllergies(active)`, `getVaccines`, `getClinicalEvents(status)`, `getClinicalEventRaw`, `getDoctors`); 5 new SDK parity tests pin wire-shape decoding.

- **Feature: Kotlin SDK typed readers + `ReadEnvelope<T>` (mobile-bridge-v2 Phase 2).** The Kotlin SDK now ships typed data classes + convenience methods for every bridge read path, so app code stops hand-decoding `requestText` JSON. New models (`Models.kt`): `ReadEnvelope<T>` (generic `{data, cursor, cached_at}`), `BiomarkerSummary`, `ObservationPoint` (+ `CodeableConcept`, `Coding`, `Quantity` for the FHIR-shape fields), `ExaminationSummary`. New `BridgeClient` methods: `readEnvelope<T>(path, serializer)` + `readDetail<T>(path, serializer)` generic helpers, plus the typed wrappers `getObservationsLatest(limit)`, `getObservations(biomarker, since, until, limit)`, `getBiomarkers(limit)`, `getExaminations(limit)`, `getExamination(id)`, `listDocumentsForExam(examId)`, `listDocuments(examinationId, limit)`. 6 new SDK parity tests pin envelope + detail + FHIR-code decoding.

- **Feature: bridge documents content / detail / preview / patient-wide list (mobile-bridge-v2 Phase 1).** The Health Assistant Bridge now exposes the document binary + metadata paths the Android companion needs to actually render documents it uploads — previously the bridge returned metadata only and the app couldn't open what it uploaded. New paths, all HMAC-gated (no exemptions) and scoped to the bound patient via `_bound_document` (cross-patient → HTTP 400, soft-deleted excluded): `GET /documents?examination_id=&limit=` (patient-wide list, optional exam filter, ≤500), `GET /documents/{id}` (enriched metadata), `GET /documents/{id}/content` (binary response body, MIME from filename), `GET /documents/{id}/preview?page=N` (JPEG page render via the existing `convert_to_images` pipeline, or the stored bytes for images). The under-exam `GET /examinations/{id}/documents` list is now enriched with `content_type`, `file_size`, and `examination_id`. The Kotlin SDK gained `BridgeClient.requestBytes(method, path) -> ByteArray` (the binary primitive) + `getDocumentContent(id)` / `getDocumentPreview(id, page?)` conveniences + a `DocumentSummary` model. SDK bumped to `0.3.0` (`manifest.json sdks.kotlin` bumped in lockstep). 8 new DB-backed tests pin: enriched payload, patient-wide list + exam filter, detail, detail cross-patient isolation, content bytes round-trip, content cross-patient isolation, image preview pass-through, soft-deleted exclusion.

- **Fix: client-disconnect during request body read no longer logs as a server error.** When a client (typically the Android companion, a browser tab, or any webhook/api-proxy caller) closed the connection before the body finished streaming — mobile app backgrounded, network drop, user cancel — the `starlette.requests.ClientDisconnect` raised by `await request.body()` escaped unhandled, tripping the generic 500 `GLOBAL ERROR` path (full stack trace + `correlation_id`) followed by the noisy empty-message `Database session error:` log from the `get_db` rollback. Added a dedicated global `ClientDisconnect` handler in `app/main.py` that logs the method + path at INFO (no stack trace, no correlation id — it's expected client behaviour, not a server fault) and returns nginx-style **499 Client Closed Request**. Covers every endpoint that reads a request body or streams (webhook ingestion, integration API proxy, document/exam uploads, SSE), not just the two `integrations.py` callsites where the symptom surfaced.

- **Feature: bridge reads are now telemetry-aware + catalog-driven (biomarker read alignment).** The Health Assistant Bridge's `GET /observations` and `GET /observations/latest` now return the bound patient's **telemetry** rows (heart rate / steps / SpO₂ — which live in the TimescaleDB hypertable, previously invisible to the bridge) alongside FHIR observations, merged into one latest-**per-biomarker** list that obeys `limit`. `GET /biomarkers` returns the full catalog with `is_telemetry`, `reference_range_min/max`, `value_type`, and the corrected `unit` (from `preferred_unit.symbol` — the old `default_unit` getattr always emitted `null`). Every observation row carries a flat `{low, high}` `reference_range`, `interpretation`, `relative_score`, `biomarker_id`/`biomarker_slug`, and `biomarker_value_type` (STATE rows keep `value_string`/`value_codeable_concept`) — telemetry rows synthesize the same shape from the biomarker definition, so mobile clients never branch on source. `biomarker=` now accepts the slug too. DB-backed tests pin telemetry series reads, per-biomarker merge, shape normalization, and cross-patient/tenant isolation.

- **Docs: Mobile Sync overview + Health Connect sync subsystem documented.** Added a public `docs/MOBILE_SYNC.md` (+ `docs-tree.json` entry) covering the Android companion's Health Connect → Bridge → backend pipeline, the offline-first outbox, the history-window + cursor model, and the privacy posture. Updated the `mobile` skill (`.opencode/skills/mobile/SKILL.md`) with a full §12 on the Health Connect sync subsystem (pluggable sources, the walking-window read + cursor-defaults-to-floor gotcha, the permission-rationale-activity requirement, settings/monitoring, the drain loop) and refreshed the app `README.md` + `android/AGENTS.md` module maps.

- **Feature: configurable client-connect URL for mobile-app onboarding.** The QR's base URL is now server-authoritative and resolves as: a per-instance **"Connect URL"** (new optional field on the bridge config flow) → a server-wide **`mobile.client_base_url`** SystemSetting (new "Mobile app" section in the SystemSettings admin page, SYSTEM_ADMIN-only) → the `APP_URL` env var → the URL you're browsing from. `APP_URL` itself stays an env var (it's the OAuth `iss` token-issuer claim — editing it at runtime would invalidate tokens), so the dedicated `mobile.client_base_url` is the presentation-only override. Exposed via a new unauthenticated `GET /api/v1/config/public` endpoint (`{app_url, client_base_url, demo_mode}`). The bridge instance detail's QR card consumes the resolved URL.

- **Feature: QR + connection code on the bridge instance detail for mobile-app onboarding.** A Health Assistant Bridge instance's overview tab now shows a "Connect your mobile app" card with a QR encoding `base_url|integration_id` and a one-click-copy connection code, so users onboard the Android companion by scanning (or pasting the code) instead of typing a UUID. The API secret is encrypted at rest + masked, so it isn't embedded in the QR — when one is set, the card directs the user to paste it once in the app's manual section. Added `qrcode.react` + a `BridgeConnectCard` component rendered for `health_assistant_bridge` instances.

- **Feature: bridge read/management paths + Kotlin SDK + document uploads.** The Health Assistant Bridge integration (the mobile/headless-client surface) gained typed **read** and **management** endpoints alongside its existing push (`/status`, `/map`, `/sync`) — all HMAC-gated when `api_secret` is set and scoped to the patient the instance is bound to: `GET /observations/latest`, `GET /observations` (time series), `GET /biomarkers` (catalog), `GET /examinations` + `GET /examinations/{id}` (list/detail), `POST /examinations` (idempotent create via the canonical service + `external_id` dedup), and `GET`/`POST /examinations/{id}/documents` (document list + base64 upload, idempotent via the existing `(tenant, patient, integration, external_id)` dedup, 25 MiB cap). Fixed a latent auth bug: `GET /status` is now correctly exempt from HMAC verification even with a secret set (it's the connectivity + SDK-discovery probe) — aligning the server with the docs and the SDKs. Added a **Kotlin SDK** (`integrations/health_assistant_bridge/kotlin-sdk/`, `io.healthassistant:kotlin-sdk`) with HMAC signing whose parity with the Python `sign_request` is golden-vector-tested. Backend coverage: mock-based provider tests + a DB-backed cross-patient isolation + idempotent-upload test. Docs: `api-reference.md` (all new paths + envelope + upload format), `authentication.md` (gating table), `client-setup.md` (Kotlin SDK section) updated.

- **Docs: removed the stale `MOBILE_SYNC_APP.md` React Native companion-app proposal.** It documented a fabricated `POST /api/v1/wearable/data` endpoint, `wearable_service.py`, and `WearableSyncPayload` as if they existed (they never did) and proposed a stack (React Native/Expo) that the project is not pursuing. Deleted the file and its references in `docs-tree.json`, `PROJECT_STRUCTURE.md`, `ARCHITECTURE.md`, and `README.md`. The shipped mobile-bridge surface remains the [`health_assistant_bridge`](integrations/health_assistant_bridge/) integration (Python + TS SDKs); a first-party native companion app, when it lands, will be documented against its real, implemented architecture.

- **Change: graph filters on desktop now open as a slide-in side panel; minimap disabled on phones.** Two graph-view UX refinements. **(1)** The four graph-filter sections (Types, Kinds, Relations, Dim) no longer occupy an always-visible box above the canvas on desktop — they were distractive and ate vertical space. They now hide behind the toolbar's Filters button (visible in graph mode on all viewports) and open as a right-side overlay panel with slide-in animation, a close button, and Escape-to-close; the graph canvas keeps full width and stays interactive while the panel is open (modeless). Mobile keeps its bottom sheet. The toolbar Filters button is now a toggle (clicking when open closes it). **(2)** The React Flow `<MiniMap>` overview (bottom-right preview) is now disabled below the `sm` breakpoint (phones) where the canvas is too small for it to be useful.

- **Fix: graph "Dim" filter chips were hard to read.** The dim/kind toggle chips under the graph filter used an overly faint `text-gray-300` at `text-[9px]` for their inactive state, making the labels nearly illegible. Brought them in line with the Kind chips: `text-[10px] font-bold`, readable `text-gray-500 dark:text-gray-400` inactive, stronger `bg-gray-500` active, `rounded-full`, and a proper dark-mode hover.

- **Feature: consolidated, mobile-friendly filter UX for the Catalogs page.** Several connected improvements that reclaim vertical space on small screens while keeping every filter accessible. **(1) Toolbar facet collapse on mobile** — the per-type facet chips (kind/class/category) hide behind a "Filters" toggle button (`<sm`) with an active-filter dot; desktop shows them inline as before. **(2) 3-way List | Cards | Graph segmented control** — replaces the separate list/card view toggle *and* the standalone List/Graph exploration row with one compact control (icon-only on mobile, icons+labels on desktop); the underlying state model is preserved (viewMode stays local, graph mode stays URL-driven). **(3) Graph filter bottom sheet on mobile** — the four graph filter sections (Types, Kinds, Relations, Dim) collapse into a slide-up sheet with header + scrollable sections + Clear-all/Done footer, so the graph canvas keeps the full viewport; desktop keeps the inline filter rows. The Dim row was also moved up into the filter group (was a stray row below the canvas). **(4) Single mode-aware Filters button** — the toolbar now owns one Filters button whose target switches with the mode: it opens the facet chips in List/Cards mode (which hide in graph mode where they don't apply) and the graph filter sheet in Graph mode. This required lifting the graph-filter state out of `CatalogOntologyGraph` into `CatalogWorkspace` (the graph component is now controlled via props) so the toolbar badge + trigger and the canvas read the same state; `CatalogToolbar` gained an `onFiltersClick` prop for the external-trigger path. No filters were removed — all six graph filters (types, kinds, relations, dim, isolated, depth) and the list-mode facets are intact. `tsc --noEmit` + ESLint clean; 198 catalog/filter tests pass.

- **Fix: Dashboard layout + add-card dropdowns now close on outside click.** The layout-selector menu and the edit-mode "Add card" menu on the dashboard stayed open when clicking elsewhere on the page — neither had a click-outside handler, only a toggle button. Added the standard `useRef` + document `mousedown` listener pattern already used by the header user menu, so both dropdowns dismiss on any outside click (and opening one closes the other, since each click is "outside" the other's ref).

- **Change: tighter `StickyToolbar` spacing on mobile.** The page action toolbar's vertical padding (`py-3`→`py-2`), margin-below (`mb-6`→`mb-3`), section/item gaps (`gap-4`→`gap-2`, `gap-6`→`gap-3`, `gap-3`→`gap-2`), and non-sticky variant are reduced on small screens (`<sm`) so the toolbar occupies noticeably less vertical space on phones; desktop (`sm+`) is byte-for-byte unchanged. Inner button sizing (touch targets) is preserved — only the container's own chrome is trimmed.

- **Feature: hide-on-scroll-down toolbar for more usable screen space.** The page action toolbar (`StickyToolbar`, rendered above content on list/detail pages with actions, filters, or details) now slides out of view when the user scrolls down and reappears the moment they scroll back up — the standard pattern used by Chrome, Reddit, and iOS Safari. Scoped to the toolbar only; the main page `Header` (title + patient context) stays put so users never lose orientation. Implementation auto-detects the real scroll container (the app scrolls inside `<main>`, not the window) by walking the DOM for the first actually-scrollable ancestor. A 12px direction threshold prevents flicker on momentum / micro-scrolls, and the toolbar is always revealed within 80px of the top so it never hides on the page's initial identity. The slide uses `translateY` (no layout shift), reuses the existing 300ms transition, and degrades gracefully to the always-visible behavior when `sticky={false}` or no scroll parent is found. `tsc --noEmit` + ESLint clean.

## [v0.4.3] - 2026-08-06

- **Change: breadcrumb path split between header and sidebar for a mobile-friendly layout.** The breadcrumb navigation is now distributed by viewport to keep the header compact on small screens without losing context. On desktop (`lg+`), the breadcrumb renders in the header bar above the page title as before. On mobile and tablet (below `lg`), the breadcrumb moves into the sidebar drawer below the logo, so the header shows only the page title — which wraps naturally (no truncation or ellipsis dots). The sidebar auto-expands the parent group of the active sub-item so the highlighted navigation entry is always visible without manual toggling. The `Breadcrumbs` component itself wraps naturally and never truncates text. `tsc --noEmit` clean.

- **Fix: `value_type`/`supports_multi_state` lost their DB-level defaults in the consolidated baseline.** The state-biomarkers migration (`s1t2a3t4e5b6`) intended `value_type ... DEFAULT 'quantity'` and `supports_multi_state ... DEFAULT false`, but the consolidated baseline (`8ddb7ef7ca4d`) recreated both columns NOT NULL **without** the `DEFAULT` clause. Since the baseline runs first, the incremental migration's guarded `ADD COLUMN ... DEFAULT` was skipped (`_column_exists` short-circuit), so fresh installs (incl. the CI test DB) ended up with NOT NULL and no server default — raw-SQL inserts and any non-ORM write path failed with a spurious `NotNullViolationError` instead of the constraint they were testing. The ORM model only carried a Python-side `default=` (bypassed by raw SQL). Restored `server_default` on both columns in the baseline migration AND the model. Three previously-failing audit tests (`test_biomarker_reference_range_order_enforced`, `test_same_slug_allowed_across_tenants_but_not_within`, `test_two_null_tenant_rows_with_same_slug_collide`) now pass unmodified; full suite 2832 green; `alembic check` shows no column/default diff.

## [v0.4.2] - 2026-08-06

- **Change: biomarkers trends page moved from `/analytics/trends` to `/biomarkers`.** The page (whose sidebar label was already "Biomarkers") now lives at the canonical `/biomarkers` route that previously existed as a duplicate rendering the same `<BiomarkerTrends />` component. The legacy `/analytics/trends` **frontend route is removed** with no redirect (greenfield — no users to migrate). All internal links repointed: sidebar nav, dashboard cards (`AnomalyCard`, `HealthSummaryCard`), `BiomarkerSummary` (3 spots), the `CorrelativeAnalytics` breadcrumb, plus stale path references in code comments and `docs/GETTING_STARTED_GUIDE.md`. The backend REST endpoint `GET /api/v1/analytics/trends` and its `analyticsService.ts` caller are **unchanged** — the API path is a separate concept from the browser route. `tsc --noEmit` + ESLint clean.

- **Feature (breaking): state biomarkers — categorical values alongside the numeric default.** A new `value_type` discriminator on `BiomarkerDefinition` (`quantity` | `state`) makes qualitative results (Positive/Negative/Detected/Susceptible/Within Limits/Indeterminate) first-class. STATE biomarkers declare their accepted values via an `allowed_states` join to a new universal `biomarker_states` catalog (22 codes seeded from HL7 v3-ObservationInterpretation, SNOMED CT, and FHIR DataAbsentReason — no invented vocabulary). The `is_normal` flag on each join row replaces numeric reference ranges for categoricals. **Multi-state panels** via FHIR R4 `component[]` (e.g. one state per organism in a microbiology culture) — opt-in with `supports_multi_state=True`. **Hard value-shape contract** enforced at every write path (REST, OCR, integration sync, FHIR import): STATE biomarkers reject `value_quantity`/`value_string`, accept only `valueCodeableConcept` with a coding from `allowed_states`; mismatches raise HTTP 422. **FHIR-native storage** (no parallel qualitative layer) — `Observation.value_codeableConcept` was already on the model; this finishes the plumbing. **Telemetry hard-blocked** at three layers (DB CHECK + endpoint guard + migration-task backstop) — `telemetry_data.value` is Float NOT NULL. **FHIR R4 facade** gains `value-concept`, `value-string`, `component-code` search params. **AI extractor** uses sibling `value_state_code` fields (not a loosened `value: float`) — zero blast radius on downstream numeric code. The latent `valueCodeableConcept` drop on FHIR import round-trip is fixed. SDK gains `set_value_codeable_concept(code, system, display?)`. Two new analytics helpers: `get_biomarker_state_history` (chronological state list) + `get_multi_state_history` (per-component tracks for panels). `_get_observation_status` short-circuits to the STATE branch before any `float()` — pre-fix a Positive result silently badged "Normal". See `dev/plans/state-biomarkers-2026-08-05.md` and the new [docs/STATE_BIOMARKERS.md](docs/STATE_BIOMARKERS.md).

## [v0.4.1-rc.1] - 2026-08-05

- **Docs: document `DEMO_MODE` in SEEDING_AND_DEMOS.md §8.** Covers enabling the flag, auto-seeding on boot, credential-free `/auth/demo-login`, the demo banner with "Exit demo" link, and deterministic UUIDs for session persistence across daily resets. SEO description in `docs-tree.json` updated to mention `DEMO_MODE`.

- **Fix: demo session breaks after daily reset — deterministic UUIDs + auto-logout.** The daily demo reset (`reset-demo.sh`) wiped the DB volume + re-seeded with new random UUIDs, so outstanding JWTs referenced non-existent tenant/user IDs — `/auth/validate` still passed (stateless) but every data query returned empty. Two-layer fix: (1) `seed_demo.py` now uses fixed UUIDs for the demo tenant (`11111111-…`) + user (`22222222-…`), so a reset recreates the same entities the tokens point at; (2) `App.tsx` detects a stale session (`getCurrentUser()` 404) when the JWT carries `demo: true` and auto-logs out — the login page then auto-calls `demo-login`, so the visitor never has to click anything.

- **Feature: demo mode (`DEMO_MODE=true`) — credential-free auto-login + persistent banner.** A new orthogonal env flag (`DEMO_MODE`) spins up a fully self-contained demo instance: on backend boot, the lifespan auto-runs `scripts/seed_demo.py` (idempotent — the existing one-shot `demo-seed` compose service is now redundant but harmless), creating the demo tenant + `demo@healthassistant.local` admin + synthetic clinical data. A new `POST /auth/demo-login` endpoint mints access + refresh tokens **with no credentials** (stamped with a `demo: true` JWT claim that survives `/auth/refresh`); it 404s when `DEMO_MODE` is off so the route is inert on normal instances. `GET /auth/setup-status` now reports `demo_mode`, which the frontend `Login` page uses to auto-call `/auth/demo-login` and skip the login form entirely (the `/login` route is never shown). A new pinned `DemoBanner` component (modelled on `TenantSwitchBanner`) sits above the header on every authenticated page announcing "Demo mode — data is synthetic…" with an "Exit demo" link to the main product site. The demo creds are config-first-class (`DEMO_USER_EMAIL`/`DEMO_USER_PASSWORD`) but alias the legacy `HA_DEMO_EMAIL`/`HA_DEMO_PASSWORD` env names (via pydantic `AliasChoices`) so the existing demo docker-compose and the UI screenshot-capture tooling keep working unchanged. A boot validator logs a loud red-boxed warning whenever `DEMO_MODE` is on so it's never left enabled accidentally on a real instance. 5 new backend tests cover the demo-login contract (404 when off, 503 when unseeded, happy-path JWT claim, setup-status flag on/off).

- **Docs: sync skills + docs to the telemetry long-format rewrite.** Updates 4 skill files (the AI's source of truth — `clinical-data` §4.1-4.6 + debugging/recap, `project-overview` §3.4, `backend` model table + `migrate_biomarker_data` row + `sync_active_integrations` row + deleted-scripts note, `integrations` §8.4) and 3 user-facing docs (`MOBILE_SYNC_APP.md` upload payload → long-format `{timestamp, slug, value, unit?, patient_id?}`, `INTEGRATIONS_FRAMEWORK.md` telemetry-split helper, `PROJECT_STRUCTURE.md` scripts count/list) so they no longer describe the dropped `heart_rate`/`steps`/`calories` dedicated columns + JSONB `data` catch-all, the deleted `_METRIC_COLUMNS` alias map, the deleted `scripts/migrate_heart_rate.py`, or the removed `device_id → UserIntegration → user_id → Patient` attribution chain (now `patient_id` is read directly off each telemetry row). Without this, the next AI session touching telemetry would get stale guidance.

- **Change (breaking): telemetry hypertable rewritten to long-format (drops dedicated `heart_rate`/`steps`/`calories` columns + JSONB catch-all).** The `telemetry_data` TimescaleDB hypertable is rebuilt in the canonical long-format shape — one row per `(timestamp, device, slug)` with explicit `value Float` + `unit Text` + `patient_id UUID` columns. Replaces the asymmetric wide+JSONB hybrid (dedicated Float columns for 3 "first-class" metrics + a JSONB `data` catch-all that gave every other telemetry biomarker — SpO2, glucose, sleep stages — second-class storage). **Modularity win:** adding a new telemetry biomarker is now a row-only change — flip `BiomarkerDefinition.is_telemetry = True` and the integration sync starts emitting rows with no DDL and no service-layer branching (previously required editing ~5 files: the `_METRIC_COLUMNS` alias map, `apply_telemetry_split`'s slug→column branch, `analytics_service`'s dual-path SQL, and writing new CAgg DDL). The new `tests/test_telemetry_new_biomarker_regression.py` verifies `spo2` (a brand-new biomarker with no historical dedicated column) flows end-to-end through upload → read → summary → trends with **zero code changes**. **Continuous aggregates are generic:** the two hardcoded CAggs (`AVG(heart_rate), AVG(steps), AVG(calories)`) are replaced by three generic `GROUP BY slug` CAggs (hourly/daily/monthly — the monthly CAgg covers the `last-12-months`/`all-time` analytics buckets that previously hit the raw hypertable). One definition covers every current and future telemetry biomarker. **Patient attribution simplified:** `patient_id` is now persisted on every telemetry row at insert time, killing the fragile `device_id → UserIntegration → user_id → Patient` resolver chain in `migrate_biomarker_data` (~100 lines deleted); telemetry→FHIR migration now reads `tr.patient_id` directly. **Upload contract (breaking):** `TelemetryDataPoint` is now `{timestamp, slug, value, unit?, patient_id?}` — one point per metric/timestamp (was: one point with `heart_rate`/`steps`/`calories`/`data` fields). The integration SDK already produces one Observation per metric, so the mapping is 1:1. `upload_telemetry_data` now uses SQLAlchemy Core bulk insert with `ON CONFLICT DO NOTHING` chunked at 5000 rows. **Hot-path indexes:** `(tenant_id, slug, timestamp)` for analytics + `(tenant_id, device_id, timestamp)` for per-device reads; compression segmented by `tenant_id, device_id, slug`. **Files:** new migration `t1e2l3o4n5g6_telemetry_long_format` + baseline `8ddb7ef7ca4d` updated so fresh installs match; `models/telemetry_model.py`, `schemas/telemetry.py`, `services/telemetry_service.py`, `services/integration_sync_service.py`, `services/analytics_service.py`, `services/import_service.py`, `workers/tasks.py`, `api/v1/endpoints/telemetry.py` rewritten; `scripts/migrate_heart_rate.py` + `scripts/delete_migrated_heart_rate.py` deleted (superseded); 4 test files rewritten + 1 new regression test + 3 collateral test fixes. 2715 backend tests pass; ruff clean. See `dev/plans/telemetry-long-format-2026-07-28.md` for the full plan.

- **Feature: parallel `test`-branch deployment in the Gitea Actions pipeline + parameterised compose container names.** The deploy workflow (`.gitea/workflows/deploy.yml`) now triggers on pushes to `test` as well as `main`/`master`, and a `test` push deploys a **fully separate stack on the same host** — driven by the `TEST_*` Gitea secrets. Branch-aware `env:` selects, per `github.ref_name == 'test'`: `STACK_NAME` (`health-assistant-test` vs `health-assistant`), `DEPLOY_PATH` (`TEST_DEPLOY_PATH` vs `DEPLOY_PATH`), the five host ports (`TEST_BACKEND_PORT`/`TEST_FRONTEND_PORT`/`TEST_FLOWER_PORT`/`TEST_POSTGRES_PORT`/`TEST_REDIS_PORT` vs the unprefixed defaults), and a branch-specific floating image tag (`:test` vs `:latest`, so the two branches no longer thrash the same `:latest` pointer — deploys always pin to `${{ github.sha }}` regardless). Distinct per-branch fallbacks prevent a missing secret from silently reusing a production port. **Critical compose change:** `docker/docker-compose.prod.yml` had hardcoded `container_name:` on all 8 services (`health-assistant-postgres` etc.), which bypass Docker Compose's project-name namespacing — two stacks on one host would have collided at `docker compose up` ("container name already in use"). All eight are now `${STACK_NAME:-health-assistant}-<service>`, so the production stack is **byte-for-byte unchanged** (default prefix = current names) while the test stack gets `health-assistant-test-*`. Named volumes and the bridge network were already isolated by the compose project name (= deploy-dir basename), so only `container_name` needed parameterising. `concurrency: deploy-${{ github.ref }}` already scoped per-branch, so main and test runs don't cancel each other. App-level secrets (SECRET_KEY, POSTGRES_PASSWORD, VAPID, AI keys, SMTP) remain shared across both branches by design — only deployment topology is branched.

- **Fix: setup-token retrieval command dropped the actual token.** The first-run setup token is logged on its own indented line *below* the `Setup token (required if accessing remotely):` label, so the documented `docker compose ... logs backend | grep -i "setup token"` matched the label line and the "Retrieve later" echo line but **filtered out the token line between them** — operators saw every line except the one they needed. Every surface that printed or quoted the retrieval command now uses `grep -i -A 1 "setup token"` so the token line (one line after the label) is included: the boot banner in `main.py` (both `log` mode and the `env`-fallback path), the post-grace mint in `setup_token.py`, the `POST /auth/setup` 403 error detail, the `.env.example` comment, and `docs/INSTALL.md`. The Setup wizard UI hint additionally now references the actual compose file (`docker compose -f docker/docker-compose.standalone.yml logs backend …`) instead of the bare `docker compose logs backend` — the bare form fails with "no configuration file provided" on the documented default (standalone) install where the compose file lives at `docker/docker-compose.standalone.yml`.

## [v0.4.0-rc.1] - 2026-07-27

- **Feature: unified notification preferences — per-kind mute, inline controls, consolidated settings hub.** Replaces the fragmented two-store preference model (tiered settings for sources/channels + ad-hoc JSONB for integration types) with one unified kind-based contract. A new `NotificationKindRegistry` (`backend/app/services/notification_kind_registry.py`) is the single source of truth that maps every notification to an addressable "kind" (`source:{SOURCE}`, `channel:{CHANNEL}`, or `integration:{integration_id}:{type_id}`) with its label, manage URL, and mutability flag. `emit()` now auto-stamps a `payload.preferences` hint onto each notification (best-effort, non-blocking) so the frontend can render an inline **"Turn off this kind"** button + **"Notification settings"** link directly in the notification detail modal (and the bell dropdown) — no emitter changes needed, no frontend kind-derivation. Two new endpoints: `GET /notifications/preferences[?integration_id=]` (all addressable kinds + their `enabled` state) and `PUT /notifications/preferences/{kind_id}` (enable/disable any kind, routing the write to the right store). Safety-critical kinds (`SYSTEM_ERROR`, critical `SYSTEM`/`CLINICAL`) are immutable — the mute button is hidden, the manage link stays. The consolidated **Settings tab** in the Notification Center (`/notifications/settings`) replaces the old `/settings/notifications` page: master toggle + push setup + every source/channel kind + every integration instance's types grouped per-instance with deep-links — all read from one unified endpoint via the new `useNotificationPreferences` hook (module-level cached, optimistic mutations). 20 new backend tests + 13 new frontend tests; 2702 backend / 602 frontend green.

- **Change (breaking): notification preferences moved to `/notifications/settings`; per-integration pref keys now per-instance; two integration endpoints removed.** Three breaking changes (greenfield — no users). **(1) Route move:** `/settings/notifications` is deleted; the canonical preferences surface is `/notifications/settings` (a Settings tab in the Notification Center). The `settingsNav.ts` "Notifications" entry is removed; notifications is now a self-contained hub (Inbox / All / Rules / Reminders / Settings / Admin). **(2) Per-instance keying:** integration notification pref keys change from `notifications.integration.{domain}.{type_id}` to `notifications.integration.{integration_id}.{type_id}` — fixes the URL-vs-key mismatch where the settings UI was per-instance (`/settings/integrations/:id?tab=notifications`) but the toggle was per-domain (muting one Fitbit silently muted all of the same provider). Two integrations of the same provider are now independently mutable. **(3) Removed endpoints:** `GET /integrations/notification-types` + `PUT /integrations/{domain}/notification-types/{type_id}` are deleted; their callers (the IntegrationDetail notifications tab + the central settings hub) now use `GET /notifications/preferences[?integration_id=]` + `PUT /notifications/preferences/{kind_id}`. The frontend `integrationService.getNotificationTypes` / `updateNotificationTypePref` methods + their types are removed.

- **Fix: clear all pre-existing ruff errors (42 → 0) + fix flaky `test_ai_config_step_has_sub_steps_payload`.** Backend ruff is now fully clean. Fixes across 20 files: **F821** (real bug — `CatalogScope` referenced but never imported in `catalogs/adapters.py`); **F401** (unused imports incl. `HitlTaskStatus`, `selectinload`, `func`); **F841** (dead locals — replaced bare `role` with `current_user.role` at all call sites in `documents.py`; removed dead `STANDARD_RANGES` dict, `tenant_filter` stub, unused `edges`/`kind`/`fhir_response`); **E711/E712** (SQLAlchemy `!= None` → `.isnot(None)`, `== True` → `.is_(True)`); **E722** (bare `except:` → `except Exception:`); **E741** (ambiguous `l` → `logger`); **E402** (12 misplaced module-level imports moved to top — verified safe, no circular-dep risk). Also fixes the flaky `test_ai_config_step_has_sub_steps_payload`: other suite tests (`test_ai_config_simple`, `test_api_key_encryption`) create SYSTEM-scope AI providers without cleanup; the `_tenant_ai_config` evaluator's SYSTEM-scope fallback saw them and flipped `done=True`. The test now wipes AI tables at the start for order-independence. 2702 backend tests pass (was 2701 + 1 flaky).

- **Feature: modern integrations catalog popup + setup-wizard manual-complete + SYSTEM_ADMIN integrations-review step.** Three related UX improvements to integrations + the setup wizards. **(1) Integrations catalog popup (`/settings/integrations`):** the old horizontal-scroll category strip + always-visible card grid is replaced by a focused **"Browse & Connect" launcher card** that opens a new `BrowseIntegrationsModal` (`components/integrations/`) — a modern popup with an **autofocus search box** (filters by name / description / domain / category), a **vertical category sidebar** with per-category counts (no more horizontal scroll; collapses to chips on mobile), and a **scrollable card grid** with per-card "Connected" badges for already-connected domains. The active-connections list ("Your Integrations") stays on the page unchanged; the popup is the discovery surface. Click-outside + Esc close; matches the existing `ConfigFlowModal` shell (Portal + overlay + zoom-in). **(2) SYSTEM_ADMIN wizard integrations-review step:** new `system.integrations_review` role step (`setup_checklist_service._system_integrations_review`) prompts the admin to review the catalog at `/admin/system/integrations` — a `redirect` step, `optional` so it never blocks the completion ring, auto-completes once any `SystemIntegration` row exists (the admin has toggled something), and can be manually dismissed. Registered in `ROLE_CHECKLISTS[SYSTEM_ADMIN]`; route added to the `EXPECTED_ROLE_ROUTES` regression guard. **(3) Manual-complete for wizard steps:** users can now mark ANY step as checked/completed manually — for steps the evaluator can't detect ("configured differently", "genuinely doesn't apply"). Backend: new `StepResult.manually_completed` flag (True only when completion is via override, not the evaluator); persisted per-user in `UserModel.settings["setup.manual_complete"][scope]` where scope is `"role"` for role steps and `"patient:<entity_id>"` for patient steps (so overrides don't leak between patients); folds into the effective `completed = evaluator_completed OR manually_completed`; new `POST /setup/checklist/manual-complete` endpoint validates the step id belongs to the caller's scope (no arbitrary writes). Frontend: new `ManualCompleteToggle` component (`components/setup/steps/`) wired into both `RoleSetupWizard` + `PatientSetupWizard` below the `StepRenderer` — shows "Mark as complete" (incomplete), "Marked manually · Undo" (overridden), or hides entirely (genuinely complete). i18n keys (`setup.manual_complete` / `manual_undo` / `manually_completed_hint` + `setup.steps.system.integrations_review` + `setup.step_desc.system.integrations_review`) in en + el. 6 new backend tests (manual override mark/clear/reject + evaluator-already-complete non-flagging + endpoint roundtrip + new step default/completed states); tsc + eslint + build clean; frontend setup suite 31 pass; backend setup-checklist + integrations suites 59 pass.

- **Change: integrations are enabled by default; only a SYSTEM_ADMIN can disable them via `/admin/system/integrations`.** Inverts the system-integration enablement semantic. Previously every discovered integration was invisible until a SYSTEM_ADMIN explicitly enabled it (a `SystemIntegration` row with `is_enabled=True`). Now integrations are **enabled the moment they are discovered on the filesystem** — a `SystemIntegration` row is only written when an admin acts on it, and the only "off" state is an explicit `is_enabled=False`. This removes the hidden first-run step where freshly-added integrations silently didn't appear; they now show up in the catalog and registry automatically, and the admin console becomes the opt-OUT path (its original intent). **Centralised in one place:** new `app/services/system_integration_service.py` exposes `is_domain_disabled(db, domain)` and `get_disabled_domains(db)` encoding the "missing row == enabled" rule so the registry, the user-facing endpoints, and the admin console all ask the same question. **Registry** (`integration_registry.initialize`): loads every discovered domain except those in the disabled set (was: loaded only the explicitly-enabled set). **User-facing endpoints** (`integrations.py`): `GET /integrations/available`, `GET/POST /{domain}/config-flow`, and the OAuth loader gate on `is_domain_disabled` instead of `is_enabled == True`. **Admin console** (`admin_integrations.py`): `GET /admin/integrations` reports `is_enabled: true` for discovered domains with no DB row; `POST /{domain}/disable` now creates the `is_enabled=False` row if none exists (previously a no-op when no row existed, which would have been a bug under the new semantic) and triggers a registry reload so the provider unloads immediately. The frontend `SystemIntegrations.tsx` needs no changes — the default-on state renders correctly with the existing toggle. `scripts/enable_integration.py` docstring updated to reflect the default-on behaviour. No data migration needed: existing `is_enabled=True` rows still mean "enabled", existing `is_enabled=False` rows still mean "disabled", and the only semantic change is for rows that DON'T exist (previously disabled-by-default, now enabled-by-default — exactly the requested behaviour). 2 new registry tests (default-on + explicit-disable-skip) + 1 new endpoint test (disabled domain hidden from `/available`); existing registry/endpoint tests updated to patch the new helpers. Full backend suite passes (2672 passed, 1 pre-existing unrelated isolation failure).

- **Feature: multi-step patient create/edit form (`PatientFormWizard`).** The old 5-field patient modal (firstName, lastName, gender, birthDate, mrn) is replaced by a **3-page category wizard** covering the full FHIR patient profile — the same fields the setup wizard's patient sections handle, now in the main create/edit flow. **Step 1 (Basic info):** name, sex, date of birth, MRN — the minimum to create. **Step 2 (Contact info):** FHIR `0..*` address (repeatable rows: street/city/postal/country), telecom (phone/email, repeatable), emergency contact. **Step 3 (Additional demographics):** race / ethnicity (CDC OMB dropdowns from `GET /setup/extension-catalog`), preferred language, insurance provider. The wizard uses a **unified draft state** — navigating between pages never loses data; no per-section save buttons. **Create mode:** step 1 creates the patient, steps 2–3 are optional (with a "Skip & finish" shortcut). **Edit mode:** all pages show existing data; "Save" persists all accumulated changes in one `updatePatient` call. Replaces the old `FormModal` in both `PatientList` (create) and `PatientDetail` (edit) — one component, two entry points. The old per-page `formData` state + `handleSubmit` handlers are removed. i18n keys under `patient_form.*` in en + el. tsc + eslint + build clean; 589 tests pass.

- **Refactor: accordion collapsible card UI for the wizard drawer + patient step uses existing UI (no duplication).** Two focused changes. **(1) Accordion cards in the popup drawer:** the flat step list + separate action bar is replaced by a **collapsible card accordion** — each step is a card; clicking one expands it (showing description, sub-step progress for guided steps, Recheck + Open buttons) and collapses the others. Collapsed cards show a compact preview (title + status icon + sub-step count or "optional" tag). The first incomplete step auto-expands on load. The accordion is more intuitive and scannable than the previous split layout — the user sees all steps at a glance and drills into any one without losing context. **(2) Patient step uses the existing UI:** deleted `PatientCreationWizard` (which duplicated the patient form inline — the same anti-pattern the AI config was flagged for). The `tenant.first_patient` and `user.linked_self_patient` steps are now `redirect` → `/patients?new=patient` (the existing quick-create flow). The patient is configured in ONE place; the wizard guides there and tracks completion via recheck — exactly like the AI config pattern. Backend route-regression test now covers the patient redirect routes again; `setup.derived_hint` i18n added (en + el) for auto-checked steps. Backend 24 pass; vitest setup tests 26 pass; tsc + eslint + build clean.

- **Fix: SYSTEM_ADMIN patient step + wizard default-step + drawer UX (click-outside-collapse, exit button, compact info).** Five focused fixes. **(1)** SYSTEM_ADMIN now has `tenant.first_patient` in their checklist — system admins need to set up patients in their own tenant too. **(2)** Main wizard page default step was landing on the LAST step (the only incomplete mandatory one, skipping optionals) — now opens on the first incomplete step **including optional** (same fix as the drawer). **(3)** Click-outside the popup drawer now **auto-collapses to badge** (was: only mobile had a backdrop that closed it; desktop had no click-outside). The overlay is on all screen sizes; clicking it collapses (not closes) so the wizard is one click away. **(4)** The X close button is removed from the header — replaced by a two-button bottom bar: **"Open full wizard"** (navigates to `/setup/wizard` + closes drawer) and **"Exit wizard"** (fully closes + deactivates the floating badge so it stops nagging). The header has only the minimize (collapse) button. **(5)** Popup drawer shows **compact step info** — title + a styled description/notes text (from new `setup.step_desc.*` i18n keys, en + el) + for guided sub-step steps a progress summary ("1 of 3 done") + a single "Open" button to the first incomplete sub-step's route. The full 3-card guided checklist is for the main wizard page only — the popup stays scannable. Backend 25 pass; tsc + eslint + build clean.

- **Refactor: AI config is a guided redirect (no duplicate forms) + wizard minimize-to-badge + dismissible floating badge.** Three focused changes from user feedback. **(1) No duplicate AI forms:** deleted `AIConfigWizard` (which embedded `ProviderManager`/`ModelsPage`/`TaskAssignment` inline — a duplicate of the settings page). The AI config step is now `external_config` with `payload_hint.sub_steps` as a list of `{id, done, route}` — the new `GuidedExternalStep` renderer shows a 3-card guided checklist (provider → model → tasks), each with done/todo status + an "Open"/"Manage" button that navigates to the real AIConfig page at the right `?tab=`. The user configures AI in ONE place (the settings page); the wizard guides them there. The `StepRenderer` auto-dispatches to `GuidedExternalStep` when `payload_hint.sub_steps` exists, else falls back to `RedirectStep`. **(2) Minimize-to-badge from the wizard page:** the full-page wizard (`/setup/wizard`) now has a minimize button (PanelRightClose icon, in the footer) that navigates to dashboard + opens the drawer in collapsed floating-badge mode — same UX as the drawer's own minimize. **(3) Dismissible floating badge, only after explicit wizard use:** new `setupWizardActive` UI-store flag (non-persistent — resets on reload). The floating badge only appears when the user has explicitly opened the wizard. A dismiss (X) button on the badge sets `setupWizardActive = false` so it stops nagging until the wizard is reopened. The wizard page sets the flag `true` on mount. Backend test updated to assert the new `sub_steps` list shape (id/done/route per entry); full setup-checklist suite 25 pass; tsc + eslint + build clean.

- **Feature: inline sub-wizards (patient creation + AI config) + non-blocking wizard drawer.** Major UX redesign of the setup wizard based on iterative feedback. The wizard now does most of its work **inline** via sub-wizards instead of redirecting away — the user never loses the wizard context. **(1) Patient creation sub-wizard (`PatientCreationWizard`):** the `tenant.first_patient` and `user.linked_self_patient` steps are now `inline_form` (were `redirect`). When activated, the wizard panel renders a 3-step inline sub-wizard: basic info (name/gender/birthDate → `createPatient`) → contacts (reuses `ContactsSection`) → additional demographics (reuses `ExtensionsSection`). Auto-selects the newly created patient. Reusable in both the role wizard and (future) tenant admin flows. **(2) AI config sub-wizard (`AIConfigWizard`):** the 3 flat AI steps are collapsed back into a single `inline_form` step (`tenant.ai_config` / `system.ai_config`) carrying `payload_hint.sub_steps` (per-sub-step completion: provider/model/assignment). The wizard panel renders a 3-step inline sub-wizard embedding the existing `ProviderManager` → `ModelsPage` → `TaskAssignment` components at the right scope. **(3) Shared `SubWizardShell`:** compact horizontal step indicator (dots with done/active states) + Previous/Next nav + content render-prop — reusable for any future sub-wizard. **(4) Non-blocking wizard drawer:** redesigned as a persistent side panel (no dark backdrop on desktop — the user interacts with the settings page underneath). Add minimize button (collapses to a floating progress pill bottom-right), a floating reopen button when closed + setup is incomplete, and fixed the default-step bug (was opening on the last step — now opens on the first incomplete step including optional). New UI store state `setupDrawerCollapsed`. **(5) Backend:** the AI steps carry `payload_hint.sub_steps` so the frontend knows which parts are already configured; the route-regression test covers the remaining redirect steps; 2 new tests assert the AI sub_steps payload + the patient-creation steps are `inline_form`. i18n keys added under `setup.subwizard.*` in en + el. Full vitest 589 pass (1 unrelated flaky timeout in isolation passes); backend setup-checklist 25 pass; tsc + eslint + build clean.

- **Fix: setup-wizard redirect routes + completed-step reopen UX.** Three UX improvements to the setup wizard based on user feedback. **(1) Persistent wizard drawer (`SetupWizardDrawer`):** clicking a step's "Open"/"Manage" CTA from `/setup/wizard` now navigates to the target page AND opens a right-side drawer that stays mounted across navigation (follows the existing `AIDrawer` pattern — rendered globally in `Layout`, toggled via `useUIStore.setupDrawerOpen`). The drawer shows: the completion ring, a compact step list (clickable, colour-coded by status), the active step's "Open"/"Manage" button, a **"Recheck"** button (re-polls `GET /setup/checklist` so the step flips green in-place), Prev/Next nav, and auto-advances when the active step completes. Auto-rechecks on `window.focus` — the user completes the action in the main area, returns to the tab, and the step is already green. "Back to full wizard" returns to `/setup/wizard`. The user no longer loses wizard context when opening a settings page. **(2) Actionable "derived" steps:** `system.first_user` (was `derived` → showed "no action needed here") is now `redirect` → `/admin/system/users`; `system.first_tenant` is now `redirect` → `/admin/system/tenants`. Only `system.catalog_seeded` stays `derived` (genuinely automatic). **(3) AI config guided sub-steps:** the single `tenant.ai_config` / `system.ai_config` steps are split into three granular steps each — `ai_provider` → `ai_model` → `ai_task_assignment` — each deep-linking to the right tab on the AIConfig page via `?tab=providers|models|tasks`. The `AIConfig` page now reads + syncs `?tab=` in the URL (via `useSearchParams`) so wizard steps land on the right tab and the tab survives refresh. Backend evaluators: `_has_ai_provider` (active provider of the right scope) + `_has_ai_model` (active model joined to a provider of the right scope) + the existing task-assignment check. The route-regression test covers all 14 role-step routes. New i18n keys `setup.recheck`/`setup.back_to_wizard` in en + el. Full vitest suite 589 pass; backend setup-checklist 23 pass; tsc + eslint + build clean.

- **Fix: setup-wizard redirect routes + completed-step reopen UX.** Two issues on the wizard's redirect/external_config steps. **(1) Wrong routes:** three role-step `payload_hint.route`s pointed at non-existent frontend paths, so clicking the CTA fell through to the catch-all `*` → `<Dashboard>` → "no patient selected". Fixed: `system.ai_config` `/admin/ai` → `/admin/system/ai-config`; `tenant.ai_config` `/settings/ai` → `/admin/tenant/ai-config`; `tenant.member_invited` `/settings/members` → `/admin/tenant/users`. New backend regression test `test_redirect_and_external_config_steps_carry_correct_routes` asserts every redirect/external_config step's route matches the real `App.tsx` route — a wrong route can't silently land on the dashboard again. **(2) Completed steps had no reopen path:** the step panel showed only "This step is complete." with a vague "Go" button. `RedirectStep` now always renders a clear CTA — **"Open"** when incomplete, **"Manage"** when complete (with a gear icon + neutral styling) — so the user can review or change the configured settings anytime. The completion hint reads "Completed — you can review or change these settings anytime." The obsolete `actionLabel` prop is removed from `StepRenderer`/the wizard pages (`RedirectStep` resolves its own labels via i18n). New i18n keys `setup.action_open`/`action_manage`/`optional`/`redirect_hint_complete`/`redirect_hint_incomplete` in en + el. 1 new StepRenderer test (completed-step "Manage" CTA + navigation); full vitest suite 589 pass; backend setup-checklist suite 23 pass; tsc + eslint clean.

- **Feature: role setup wizard + "Resume Setup" entrypoints (iteration 3).** Every authenticated user now gets a role-aware guided checklist at `/setup/wizard` — the patient wizard's sibling. Reuses the same modular toolkit (`SetupLayout` / `Stepper` / `StepRenderer`) so it was added with zero new primitives: it reads `GET /setup/checklist` (no entity) → the role steps for the calling user (USER / ADMIN / MANAGER / SYSTEM_ADMIN), defaults to the first incomplete mandatory step, and reuses the per-kind renderers. Most role steps are `redirect` / `external_config` / `derived`, so the role wizard is a guided tour: each step links to the right place (`/patients?new`, `/admin/ai`, `/settings/members`, …) and flips green on return. **Entrypoints:** "Resume Setup" item added to the Header user-menu dropdown (between Profile and Settings); the stale "A guided setup wizard is coming soon." placeholder in `NoPatientState` — the exact gap users saw on a fresh instance with no patient context — is replaced with an accurate "Run setup wizard" CTA button (gradient-styled to read as the primary onboarding path) that routes to `/setup/wizard`. The i18n key `common.no_patient_setup_hint` ("…coming soon.") is removed; `common.no_patient_setup_action` ("Run setup wizard") added in en + el. 5 new tests for `NoPatientState` (asserts the stale hint is gone + the CTA renders + adapts to has/empty-patients); full vitest suite 589 pass; `tsc --noEmit` + eslint + `vite build` clean.

- **Feature: in-app patient setup wizard (iteration 2 — the wizard UI).** The backend checklist contract shipped in iteration 1 now has a frontend. The "guided setup wizard is coming soon" placeholder is replaced by a real, reopenable, deep-linkable wizard at `/patients/:id/setup` + a completion card on `PatientDetail`. **Design:** the wizard IS the advanced form (design D4 — no parallel form); state is backend-derived (design D2 — every save re-polls `GET /setup/checklist`, so steps flip green in-place with no local completion state); the UI is kind-driven (design D7 — each `StepResult.kind` has one renderer). **Modular toolkit** (`components/setup/`): `SetupProgressRing` (SVG arc), `Stepper` (vertical clickable step list), `SetupLayout` (two-pane responsive shell, reused by the role wizard next), `StepRenderer` (kind dispatcher with a graceful fallback so new backend kinds never crash the UI), per-kind renderers (`RedirectStep`, `DerivedStep`), and inline-form sections (`DemographicsSection`, `ContactsSection`, `ExtensionsSection`) registered in a `INLINE_SECTIONS` map (adding a section = one entry; the wizard page + StepRenderer are untouched). **Extensions section** is driven by `GET /setup/extension-catalog` so the client never hardcodes CDC OMB race/ethnicity codes — `omb_category` renders a dropdown, `code` a dropdown, `string` free text. **Contacts section** covers FHIR `0..*` address + telecom (repeatable rows) + emergency contact. Every section persists via `PUT /patients/:id` then calls `onSaved()` → the parent re-polls. **Routing:** `/patients/:patientId/setup` added to `App.tsx`. **`SetupChecklistCard`** on `PatientDetail`'s sidebar shows the completion ring + next-incomplete-step hint + an "Open setup wizard" CTA; collapses to a "Setup complete ✓" badge at 100%. **i18n:** new `setup.*` keys in en + el covering every backend step title. 33 new frontend tests (ring clamping, stepper navigation, renderer dispatch + fallback, section registry coverage, ExtensionsSection catalog load + save patch shape); full vitest suite 584 pass; `tsc --noEmit` + eslint + `vite build` clean.

- **Feature: setup wizard — extension catalog endpoint + OMB race/ethnicity/language seed (iteration 2 backend enabler).** The patient setup wizard's "extensions" section needs to render the right inputs (OMB race/ethnicity dropdowns, language picker, insurance free-text) without the client hardcoding CDC codes or the supported-extension keys. New `GET /api/v1/setup/extension-catalog` returns `[{key, title_i18n_key, value_type, cardinality, options?}]` derived from the single-authority `SUPPORTED_PATIENT_EXTENSIONS` registry (`fhir_extensions.py`) + a new CDC OMB seed at `backend/data/seeds/omb_race_ethnicity.json` (5 OMB race + 2 ethnicity + 17 common language codes). `value_type` ∈ `omb_category` (dropdown of CDC codes) | `code` (dropdown) | `string` (free text). The service (`SetupChecklistService.get_extension_catalog`) is the only place that maps extension-key → value_type + picklist source, so iteration 4 adds doctor extensions with zero frontend change. New schemas `ExtensionCatalogItem`/`ExtensionOption`/`ExtensionCatalogResponse` in `schemas/setup_checklist.py`. 4 new test cases in `tests/test_setup_checklist_service.py` (catalog shape, OMB options present, unsupported-entity rejection, endpoint smoke); full suite 22 pass.

- **DX: every script under `scripts/` now supports `-h`/`--help`.** All 12 scripts (2 Python + 10 shell) respond to both `-h` and `--help` with a tailored usage message (purpose, prerequisites, flags, examples) and exit 0. Unknown args print a "try --help" hint and exit 1. `version_manager.py` already had argparse help (unchanged); `reset-dev-db.sh` already had a `print_help` (unchanged). New: `setup_env.py` gains a module docstring + `-h`/`--help` early exit + a non-interactive `--mode=1|2` flag (so CI/automation can skip the prompts). `run-dev.sh` gains `print_help` + `-h`/`--help` (existing flags `--force-stop`/`--no-admin`/`--force-celery` unchanged). `run-docker.sh`, `clean_logs.sh`, `capture_ui.sh`, and the five `test_*.sh` smoke-test scripts each gain a header docblock + `print_help` + arg parser following the existing `reset-dev-db.sh` pattern (sed-extracted header). `capture_ui.sh` pre-scans for `-h`/`--help` before forwarding other flags to the Playwright node script (so its own flags like `--scene` still pass through). Shellcheck-clean on all new code (existing pre-existing SC2086/SC1091 warnings in script bodies untouched).

- **Feature: setup-token modes — `SETUP_TOKEN_MODE` four-mode deployment-time choice for the first-run wizard (store / automation / LAN / VPN).** Replaces the implicit "print a one-time token to container logs and require it for non-localhost" guard with four explicit, operator-chosen modes so storefront bundlers (Umbrel/Runtipi/CasaOS/Cosmos), manual Docker installs, enterprise/LAN installs, and VPN-only setups each get a frictionless but still first-claim-protected first-run UX. Design rationale: `dev/audits/setup-token-modes.md`. **New env vars** (`backend/app/core/config.py`): `SETUP_TOKEN_MODE` (default `log`), `SETUP_BOOTSTRAP_TOKEN` (for `env` mode), `SETUP_TOKEN_GRACE_MINUTES` (default 30, for `time` mode). Config validator rejects unknown modes, refuses grace < 1 minute, and falls back to `log` if `env` is selected with an empty bootstrap token (warns; doesn't refuse to boot). **`backend/app/core/setup_token.py`** rewritten: `current_mode()`, `seed_from_env()`, `mark_boot_time()`/`is_within_grace_window()` + per-mode `is_setup_token_required()` decision tree (localhost + dev/test escape hatch preserved). `time` mode lazily mints + logs a token once the grace window expires (audit D5 — no dead state). `clear()` (called by `POST /auth/setup` on success) invalidates the active token in every mode — one-shot semantics preserved (the system still refuses re-bootstrap via `_is_initialized → 410`). **`backend/app/main.py`** lifespan dispatches per mode (`env` seeds silently from env var so logs aren't the channel for the secret; `time` logs the grace window; `disabled` logs a security warning; `log` keeps the current behaviour). **`SetupStatus` response** gains `token_mode` + `setup_url_hint` (`backend/app/schemas/auth.py`); `setup_status` endpoint (`backend/app/api/v1/endpoints/auth.py`) surfaces them so the launcher + wizard can tailor UX from one round-trip (`setup_url_hint` only non-null in `env` mode + uninitialized + token still active — contains `/setup?token=<value>` ready to hand the user). **Frontend** (`frontend/src/pages/Auth/Setup.tsx`) reads `?token=` from the URL on mount → auto-fills the field (one-click launcher flow) and picks one of four tailored hint strings per `token_mode` (env / time / log / disabled). **`.env.example`** documents the modes table; **`scripts/setup_env.py`** Full Setup mode prompts for the mode and prints the ready-to-use launcher URL when `env` is chosen. **`docs/INSTALL.md`** gains the modes table + store-bundle recipe. **Tests** (`backend/tests/test_setup_token_modes.py` — 23 new) cover all four modes, env-mode seeding + empty fallback, time-mode within-window / expired / lazy-mint / clear-resets-mint-flag, disabled never-required, setup_status `token_mode` for each mode, `setup_url_hint` only in env mode, suppressed after initialize, config validator rejecting bad mode names + grace < 1. Existing 12 auth-setup tests pass unchanged. Frontend `tsc --noEmit` + `eslint` clean.

- **Feature: in-app guided-setup wizard (iteration 1 — schema + backend service + audit).** A modular, role-aware, backend-derived setup-checklist system that drives a future in-UI wizard for first-run role onboarding (System Admin / Tenant Admin / User) and per-entity advanced-form wizards (Patient, later Doctor/Organization). Iteration 1 ships the foundation; the wizard UI itself lands in a later iteration on the same branch. Design rationale at `dev/audits/setup-wizard-design.md`. **(1) Patient `extensions` JSONB column** (`backend/app/models/fhir/patient.py` + migration `s1e2t3u4p5w6`) holds local-keyed FHIR R4 extensions — race (US Core), ethnicity (US Core), preferred_language (IHE), insurance_provider (HA). The new `backend/app/services/fhir_extensions.py` registry is the single authority: `SUPPORTED_PATIENT_EXTENSIONS` (4 entries) with per-extension `build`/`parse`/`validate` callables; `to_fhir_extension_list` projects the local map onto a canonical FHIR `extension[]` array (validated by `assert_valid_fhir` and round-tripped via the R4 facade); `from_fhir_extension_list` inverts canonical input on imports. `Patient.to_dict()` exposes `extensions`; `Patient.to_fhir_dict()` surfaces canonical `extension[]`. `PatientCreate`/`PatientUpdate`/`PatientResponse` accept the new field; `fhir_service.create_patient`/`update_patient` validate via the registry and reject malformed extensions. Insurance is intentionally a `string` extension for now; a proper FHIR `Coverage` resource is roadmap. **(2) `SetupChecklistService`** (`backend/app/services/setup_checklist_service.py`) — two pluggable evaluator registries (`ROLE_CHECKLISTS`, `ENTITY_CHECKLISTS`); adding a step = one evaluator function + one registry tuple entry (the endpoint, schema, and later UI are untouched). Role steps: user (preferences_language, linked_self_patient), tenant (first_patient, first_doctor, first_org, ai_config, member_invited), system (first_tenant, catalog_seeded, ai_config, first_user). Entity (patient) steps: birth_date, address, telecom, emergency_contact, race_or_ethnicity, preferred_language, insurance_provider, allergies, current_medications (ACTIVE only), current_events (covers "current pains" via ClinicalEvent). All aggregation steps + several preferences are `optional=True` ("no allergies" is a valid end-state) so they don't pull the mandatory `completion` ratio below the floor. Adding a step is one evaluator function + one registry tuple entry — endpoint/schema/UI are untouched. Authoritative state is always derived from the underlying data; no onboarding-state table to drift. **(3) Endpoint** `GET /api/v1/setup/checklist[?entity=patient&entity_id=<uuid>]` returns `SetupChecklistResponse` (`SetupChecklistResponse`/`StepResult` Pydantic schemas in `backend/app/schemas/setup_checklist.py`) — RBAC reuses `check_patient_access` (USER=own patients only, ADMIN/MANAGER/SYSTEM_ADMIN=tenant-scoped). **(4) Tests** (`tests/test_patient_extensions.py` — 19 tests; `tests/test_setup_checklist_service.py` — 18 tests) cover registry shape, validate/build/parse round-trip, `assert_valid_fhir` accepts extension-bearing Patient, checklist completion flips as data is added, optional/mandatory ratio semantics, USER cross-patient 403, endpoint smoke. Full related suite (178 tests across patient/FHIR-converter/audit/FHIR-R4-phase1/elements) green. New user doc `docs/SETUP_WIZARD.md` + `docs/docs-tree.json` entry under "Getting Started".

- **Integration: FHIR Server syncs the full patient record (Conditions, Encounters, Documents, Medications, Allergies, Immunizations) + interactive remote-patient picker.** Implements `dev/plans/fhir-server-multi-resource-sync-2026-07-23.md`. The outbound FHIR integration previously pulled/pushed only `Observation`; it now ingests the rest of the record through the canonical write services (same RBAC, audit, dedup as UI writes), and adds a first-class way to select which remote FHIR patient is synced. **Phases 1–3 (wire up existing SDK hooks):** `fhir_server` now overrides `supports_clinical_events`/`pull_clinical_events` (Condition → Health Journeys), `supports_examinations`/`pull_examinations` (Encounter → Examinations), `supports_documents`/`pull_documents` (DocumentReference → OCR, with attachment fetch + auto-link to a just-pulled Encounter), and `supports_hitl_proposals`/`pull_hitl_proposals` (unmapped remote LOINC/SNOMED codes queue as Define-Biomarker cards; a seen-codes cursor + `handle_proposal_resolution` keep it idempotent). The Observation-only search was refactored into a generic `_search_resource` with a **per-resource-type `_lastUpdated` cursor** (`last_updated:Condition`, …) so one slow resource can't starve the others; a `pull_resources` config field lets an operator opt an instance out of any type. **Phase 4 (Route A — new SDK hooks):** the medication/allergy/immunization models lacked the integration-dedup parity that events/exams/docs had, so this adds it end-to-end — migration `f1m2u3l4t5i6` (source_integration_id + external_id columns + partial unique indexes on `fhir_medications`/`fhir_allergy_intolerances`/`patient_immunizations`); the three `add_patient_*` services refactored to the canonical `TokenData` + dedup-kwargs shape (patient-access check + `created_by` provenance + `IntegrityError` race recovery, matching `create_event`/`create_examination`); three new SDK hook pairs (`supports_medications`/`pull_medications`, `supports_allergies`/`pull_allergies`, `supports_immunizations`/`pull_immunizations`) + re-exports; three engine wiring blocks in `run_sync`. A new `integrations/fhir_server/mappers.py` holds the pure FHIR→schema mappers for all seven resource types. **Remote-patient picker:** the custom-action endpoint now accepts an optional input body (backward-compatible kwargs); a `find_patient` action (modal hint `patient_picker`) searches the server by name/MRN with **MRN auto-suggest from the local patient**, and `select_patient` sets `remote_patient_id`; `_remote_patient` now resolves explicit-config → SMART-token → none. Frontend gains a `PatientPickerModal` (search + click-to-select) and a generic `modal` action hint; the `ConfigFlowModal` now renders array fields with `items.enum` as a toggle-card multi-select (reusable by any integration) instead of free-text chips. 17 new backend tests (40 total in the provider suite); full suite green (2469 passed). Per-integration docs split into `integrations/fhir_server/docs/` (7 pages + `docs-tree.json`); `INTEGRATIONS_SDK.md` documents the treatment-resource hooks + action input/modal.

- **Docs: documented the OAuth2 endpoints + synced STATUS/ARCHITECTURE/the backend skill with the API access-layers work.** The public `docs/API.md` (REST reference) was missing the new `/oauth/*` surface — added an "OAuth2 — FHIR facade client auth" section (token/revoke/client-CRUD table, SMART scope syntax, curl examples for `/oauth/token` → facade call) + a Contents entry; bumped the brittle handler count to "300+ across 37 modules". `docs/STATUS.md` facade bullet now states the OAuth2 client-credentials + SMART auth is implemented (no longer implied-deferred) and the session/api boundary is code-enforced. `docs/ARCHITECTURE.md` facade section documents the auth model + OAuth-client Provenance agent. The `backend` opencode skill §2.3 now covers `get_api_principal` / `require_session_token` / `require_fhir_scopes` / `create_api_access_token`, the `session_gated_router` boundary, the api-token `TokenData` claims, and records the `/auth/service-account` + `get_current_user_with_tenant_override` retirement — so future AI sessions don't reach for removed helpers.

- **UI: Admin "API Clients" page — manage OAuth2 facade clients (create, rotate secret, scope, revoke).** Phase 1.9 of the API access-layers plan. New `pages/Admin/OAuthClients.tsx` (route `/admin/tenant/oauth-clients`, gated to `ADMIN`/`MANAGER`/`SYSTEM_ADMIN`) lets a tenant administrator register an external system as an OAuth2 client of the FHIR R4 facade without touching the CLI. The create/edit form takes a display name + free-form SMART scopes (with one-click presets: `system/*.read`, `system/*.*`, `patient/Observation.read`, …) and conditionally collects a bound patient id when `patient/` scopes are present. The plaintext `client_secret` is surfaced **exactly once** in a dismissible reveal modal (with copy buttons + a "store it now" warning) on both create and rotate-secret. Per-row actions: edit, rotate secret, enable/disable, delete (the latter two via the global confirmation modal). Backed by a typed `services/oauthService.ts` (pattern B object literal) + `types/oauth.ts`. Sidebar gains the item under Administration → Tenant section (MANAGER now also sees the Administration group, scoped to just this item). i18n key `admin.api_clients` added in en + el. `tsc --noEmit` + `eslint` + `vite build` clean.

- **Security: retired the long-lived `/auth/service-account` JWT path in favor of OAuth2 clients.** Phase 3 of the API access-layers plan (`dev/plans/api-access-layers-2026-07-23.md`). `POST /auth/service-account` is removed — it minted a tenant-wide role JWT (up to 365 days) with no scopes, which conflicted with the new least-privilege model. External system interop now goes through `POST /oauth/clients` (admin registers a scoped client) + `POST /oauth/token` (short-lived, SMART-scoped access tokens). Cleanup: removed the `UserModel.is_service_account` column (+ migration `d1r2o3p4s5a6`), the service-account login-reject check, the now-dead `get_current_user_with_tenant_override` helper (the facade uses `get_api_principal`), and the `TokenData.is_service_account` field. Facade Provenance agent label updated "Service Account" → "OAuth Client". `docs/API.md` dropped the `/auth/service-account` row. Full suite green (2452 passed).

- **Security/Architecture: the FHIR R4 facade is now a scoped, external-only API surface (OAuth2 client-credentials + SMART-on-FHIR scopes); the domain REST API is frontend/mobile-only.** Two phases of the API access-layers plan (`dev/plans/api-access-layers-2026-07-23.md`). **(1) OAuth2 client-credentials + boundary gate:** new `oauth_clients` model + migration, `POST /api/v1/oauth/token` (RFC 6749 §4.4; form/JSON/HTTP-Basic client auth), `POST /api/v1/oauth/revoke` (RFC 7009 jti blocklist via `token_store`), and admin client CRUD (`/api/v1/oauth/clients` — list/create/rotate-secret/patch/delete with SMART scope validation). `app/core/scopes.py` parses/validates SMART scopes (`system/` and `patient/` contexts registrable; `user/` + launch deferred to Stage 4). Access tokens are short-lived JWTs (`aud`, `iss`, `token_kind=api`, `scope`, `jti`, optional `bound_patient_id`). `get_api_principal` is the facade auth dependency (api-only, audience + revocation checks); a new `require_session_token` guard is applied to the domain router group so **api tokens are rejected on every domain endpoint** with 401. The facade switched from the session-based `get_current_user_with_tenant_override` to `get_api_principal` — **session JWTs (the frontend) are rejected on the facade**; the `X-Tenant` override on the facade is dropped (operators probe via a per-tenant `system/*.*` client). `decode_access_token` disables PyJWT's auto-audience check (the audience gate lives in `get_api_principal`). **(2) SMART scope enforcement + discovery:** every facade CRUD route is now scope-gated by `require_fhir_scopes` (read vs write per resource; `system/*.*`, `system/Observation.read`, etc.) returning a 403 FHIR `OperationOutcome` on mismatch; `patient/` scoped clients are narrowed to their bound patient via a patient-compartment predicate in the facade crud layer (search/read/update/delete filtered; cross-patient writes rejected). `GET /.well-known/smart-configuration` advertises the client-credentials grant + endpoints; the CapabilityStatement `rest.security` carries the SMART-on-FHIR extension + oauth-uris. The facade is no longer reachable with a session JWT — verified zero `/fhir/R4` calls in the frontend. Full backend suite green (2452 passed).

- **Docs: documented the API access layers — domain REST (frontend/mobile) vs. FHIR R4 facade (public, external) vs. Integrations Framework (per-provider), and retracted the domain REST API as an integrator contract.** New `docs/API_LAYERS.md` is the canonical "which API do I use?" decision guide: the three-layer model (internal domain REST for first-party clients, the public FHIR R4 facade with OAuth2 client-credentials + SMART-on-FHIR scopes for external clinical systems, and the Integrations Framework for per-platform sync), the audience/shape/auth/stability comparison table, and the decision tree for integrators. `docs/API.md` intro retracted the "any external integrator that prefers a domain-shaped REST contract" claim — the domain router is now documented as frontend/mobile-only with a callout pointing external integrators at the facade. `docs/FHIR_R4_FACADE.md` dropped the SMART-on-FHIR deferral (no longer "Stage 4 — uses JWT auth for now"), documented the OAuth2 client-credentials auth model, and added an "Authentication & SMART scopes" section (token flow, SMART scope vocabulary, discovery endpoints, client-attributed Provenance). `docs/INTEGRATIONS_FRAMEWORK.md` gained a scope-boundary note clarifying the framework is for per-provider inbound sync, not a general-purpose public REST gateway. `docs/docs-tree.json` adds the new `api-layers` entry under "Integrations & API" (first in the section, as the integrator entry point). Sets the written contract for the subsequent OAuth2 + SMART enforcement work.

- **AI Chat — HITL resolved cards surface outcome details + hide the internal `[HITL RESOLUTION FEEDBACK]` message from the UI.** Two related improvements to what a human-in-the-loop task card shows *after* the user resolves it. **(1) Outcome details in resolved cards:** the collapsed "Confirmed/Dismissed" card previously discarded the `resolved.result` data every handler already records. A new optional `renderOutcome(task)` on the handler registry renders the actual changes made — anatomy imports show **clickable "N nodes" / "N edges" chips that open a popup** listing exactly which structures/edges were imported, with an "X added · Y updated" breakdown from the import stats (+ an error count when the import reported errors); catalog-definition and patient-instance proposals (biomarker/medication/allergy) show "N links created" / "N links failed" from the graph-edge persistence; `ask_user` cards show a **collapsible "N answers" toggle** that expands to each question prompt → its answer, formatted by kind (freetext, option labels for single/multi-choice, candidate names for catalog/instance refs); failed cards surface `resolved.error`. The shared `HitlOutcomeDetail` chip renderer + `OutcomeDetailModal` portal popup live in a new `outcome.tsx`, deliberately kept out of `registry.tsx` to avoid a runtime circular import between the registry and the handlers (the test suite caught it). i18n keys added under `ai_chat.hitl.outcome.*` in en + el. **(2) Hide `[HITL RESOLUTION FEEDBACK]` from the UI on reload:** the resume continuation persists a structured `[HITL RESOLUTION FEEDBACK]` user message so the LLM receives the user's task-card outcomes on its next turn — but `GET /sessions/{id}/messages` returned every message verbatim, so on page refresh that internal LLM instruction rendered as a chat bubble. The message is now tagged `content.kind="hitl_feedback"` when saved, and a new `is_internal_message()` helper filters it **at the HTTP endpoint boundary** — NOT in the shared `ChatSessionService.get_session_messages` (which `reconstruct_history` still calls to build the LLM's context): the LLM keeps the message, only the UI omits it. The helper also recognizes the `[HITL RESOLUTION FEEDBACK]` text prefix, so sessions saved *before* the tag are cleaned up with no migration. **Tests:** 2 new backend tests (`is_internal_message` unit + GET `/messages` endpoint filtering); existing `test_hitl_resume.py` (16) + `test_ask_user.py` + `test_chat_session_resumable.py` (46) pass; 34 frontend HITL tests pass. tsc + eslint clean.

- **Docs: synced 4 skills + AI_SYSTEM.md with the anatomy/catalog/graph work shipped this branch.** The deep-dive skills (loaded at the start of every future AI session) and the public AI_SYSTEM.md had drifted from the implementation across 5 features. **`ai-pipeline/SKILL.md`:** tool count 28→39 (8 `propose_*` not 5); added `allergies.py` domain + 4 allergy read tools to the table; `explore_catalog_relations` entry now documents the `types` filter + `truncated`/`node_count`/`edge_count` return + depth clamp (1–3) + LLM edge cap (150); added `propose_record_allergy` + `propose_define_allergy` + `propose_anatomy_graph_generation` (pre-flight search, in-tool generation, `note` channel, schema v3) to the tool table; link-proposals note corrected "four of the six"→"six of the seven"; documented the generic HITL `note` channel (`__hitl__` → `_hitl_proposal_note` → `_hitl_llm_feedback(task, note)`). **`hitl-task-cards/SKILL.md`:** `schema_version` annotation updated (2 for most, 3 for anatomy); documented the `note` channel in §2.1; added the "proposal-time generation + graph view" pattern (§3.5 pattern 3) covering `GenerateAnatomyGraphHandler` — lazy `useState` init from `proposed_payload.generated`, Graph/Table toggle, ConceptGraphView color-coding (indigo=new, slate=existing), and the ReactFlow fixed-height gotcha (`style={{ height: 450 }}` not `flex-1`). **`clinical-data/SKILL.md`:** `traverse()` description updated — return shape now includes `truncated`/`node_count`/`edge_count`; documents `endpoint_type_whitelist` post-filter + the `limit+1` probe pattern + the LLM-facing `limit=150` + depth clamp. **`docs/AI_SYSTEM.md`:** §4.1 anatomy row rewritten (dedup at two layers, proposal-time generation, Graph/Table view, color-coding); `explore_catalog_relations` bullet documents `types` filter + `truncated` flag + depth clamp + token-safety. **`frontend/SKILL.md`:** added `<PromoteScopeButton>` paragraph (permission-gated dropdown, `promoteCatalogItem` service, 409 collision surfacing, one component for all catalogs).

- **Performance + UX: anatomy graph proposal opens instantly + graph visualization.** Two issues addressed: (1) opening the anatomy graph HITL card took many seconds because the LLM generation (`define_anatomy_graph`) ran synchronously when the modal opened; (2) the user could only see the proposed graph as an editable table, with no visual of how proposed nodes relate to existing ones. **Fix 1 — generation moved to proposal time:** the `propose_anatomy_graph_generation` tool now resolves the LLM via `AIProviderService.get_llm(TaskType.DEFINE_ANATOMY_GRAPH, ...)` (deferred leaf-module imports — no import cycle) and calls `define_anatomy_graph` inside the tool body, embedding the result in `proposed_payload.generated`. The frontend handler reads this synchronously via a lazy `useState` initializer → the modal opens with zero LLM latency. If the in-tool generation fails (provider down, parse error), `generated=None` and the handler falls back to the existing client-side `getAIAssistance` call with the Retry button (graceful degradation — no UX regression). The chat turn itself is slower (the tool blocks on the LLM call, the chip shows "executing"), but the card is instantly usable when it appears — a better tradeoff since the user is already waiting for the chat response. `schema_version` bumped 2→3. **Fix 2 — graph visualization:** the handler now has a Graph/Table view toggle (default: Graph). The Graph tab renders the existing `ConceptGraphView` (ReactFlow, already bundled) with the proposed nodes/edges mapped from the editable draft, color-coded: **indigo** for new nodes (will be added on import), **slate** for nodes that already exist in the catalog (from the pre-flight snapshot's `existing.node_slugs`). The graph updates live as the user edits the table. The Table tab retains the full editable node/edge grid. A legend explains the color-coding. **Tests:** new backend test mocks the LLM + generation call, asserts the `{nodes, edges}` is embedded at proposal time + the note reports "Generated N nodes"; new frontend test asserts the handler opens instantly from a pre-generated payload (`getAIAssistance` NOT called) + the graph mock renders. Existing tests updated for the graph-default view + the ConceptGraphView mock (ReactFlow needs `ResizeObserver`, not in jsdom). tsc + eslint + ruff clean; 100 backend + 156 frontend tests pass.

- **Performance + token-safety: `explore_catalog_relations` hardening (4 fixes).** The LLM graph-traversal tool was DB-efficient (single recursive CTE, indexed, cycle-safe) but **token-unsafe for deep/wide graphs**: a depth-2 anatomy query ran ~7K tokens, hub biomarkers ~14K, and the silent 500-edge SQL cap could spike to ~42K tokens with no signal to the LLM. Four high-leverage fixes landed, all in the shared `catalog_graph_service.traverse` + the `explore_catalog_relations` tool (every catalog benefits, no per-type logic). **(A) Truncation flag:** `traverse()` now fetches `limit + 1` rows (the standard probe pattern — no extra round-trip) and returns `{truncated: bool, node_count, edge_count}` alongside `{start, nodes, edges}` so the LLM knows when the graph was cut and can narrow further. Mirrors the existing `whole_catalog_graph` truncation pattern. **(B) Destination-type filter:** new `types: Optional[List[str]]` param on `explore_catalog_relations` (e.g. `types=["anatomy"]` to see only the anatomy sub-graph around a biomarker). Implemented as a Python post-filter in `traverse()` via the new `endpoint_type_whitelist` service param — keeps the full traversal reach so the path exists, then prunes edges/nodes to only those touching a whitelisted endpoint type. The biggest single token reducer: cuts a 200-edge cross-catalog result to ~20. **(C) Depth clamp:** the tool now clamps `max_depth` to 1–3 (matching its docstring — the service allows 1–5 and the tool previously clamped nothing, so the LLM could request depth 5 and saturate the cap). `max_depth=10` no longer crashes. **(E) Lower LLM edge cap:** the tool passes `limit=150` to `traverse()` (down from the 500 service default) — ~8K-token ceiling instead of ~42K. The workspace Graph UI keeps the 500 default (it renders visually, not into an LLM context). **Prompt:** the "SEARCH BEFORE GENERATING ANATOMY" guidance now tells the LLM to pass `types=["anatomy"]` to `explore_catalog_relations` for focused, token-cheap results. **Net effect:** a typical depth-2 anatomy query drops from ~7K → ~1–2K tokens. **Tests:** 4 new — truncation metadata present + counts match, `types` filter prunes non-matching edges (metformin→concept TREATS survives `types=["concept"]`, drops under `types=["anatomy"]`), depth clamp prevents the depth=10 crash, `traverse()` truncation flag via `limit+1` probe. 99 backend tests pass; ruff clean (one pre-existing `F841` in `whole_catalog_graph` untouched).

- **Feature: anatomy graph generation deduplicates against the existing catalog.** The `propose_anatomy_graph_generation` tool generated blind — it had no awareness of structures/edges already in the anatomy catalog, so it could propose duplicates (mitigated only by the import's slug upsert, which silently overwrites rather than skips). Now deduplication happens at **two layers**. **(1) LLM-driven search (prompt guidance):** the CHAT + GENERAL system prompts now carry a dedicated "SEARCH BEFORE GENERATING ANATOMY" section instructing the LLM to call `search_catalogs` (`types="anatomy"`) to find the target, then `explore_catalog_relations` (depth 2) to see its existing sub-graph, BEFORE calling `propose_anatomy_graph_generation` — so the agent can tell the user what already exists and scope the proposal to filling gaps (both chat tools are already registered + cover the anatomy type). **(2) Tool-internal safety net:** the `propose_anatomy_graph_generation` tool itself now does a pre-flight search — looks up the target (tenant + global), traverses its 2-hop neighborhood via `anatomy_service.get_anatomy_graph`, and embeds a compact snapshot (root + existing node slugs + slug-resolved edges + counts) in `proposed_payload.existing`. A one-line summary is surfaced to the LLM via a new generic **HITL `note`** channel (`{"__hitl__": true, "task": {...}, "note": "..."}`) so even if the LLM forgets to search, the note tells it what exists. **(3) Dedup-aware generation:** the frontend handler passes the snapshot as `context.existing` to `getAIAssistance`, and the `define_anatomy_graph` assistance prompt now includes a "these structures/edges ALREADY EXIST — generate only the MISSING detail, reference existing nodes as edge endpoints" block. When nothing matches, the note says "from scratch" and generation runs unchanged. **Generic plumbing:** `_hitl_llm_feedback` now accepts an optional `note` (prepended to the standard wait-message); any `propose_*` tool can attach one via the `note` sibling in its `__hitl__` return. `schema_version` bumped 1→2 for the anatomy task (additive `existing` field). **Tests:** new backend test seeds a heart + left-ventricle + PART_OF edge, calls the tool, asserts the snapshot embeds both slugs + the slug-resolved edge + the note reports counts; updated the no-existing test to assert `existing is None` + "from scratch" in the note; new prompt-guard test asserts the CHAT + GENERAL prompts reference `search_catalogs` + `explore_catalog_relations` + the anatomy type. New frontend tests: the handler passes `context.existing` when present (and `undefined` when not); the summary chip shows "N exist / M edges" from the pre-flight snapshot. tsc + eslint + ruff clean; 92 backend + 155 frontend tests pass.

- **Feature: catalog scope parity for anatomy + generic promote UI (DRY).** Anatomy had the full `CatalogScope` infrastructure (`scope`/`tenant_id`/`AuditMixin` columns, a registered `AnatomyCatalogAdapter`, a working `POST /catalogs/anatomy/{id}/promote`) but the **domain `/anatomy` write endpoints bypassed the policy** — they used a stale binary `tenant_id`-only check and had a latent bug where `POST /anatomy` stamped `scope=SYSTEM` on tenant rows (violating the `scope=SYSTEM ⇔ tenant_id IS NULL` invariant). The promote path existed in the backend but was **unreachable from the UI** — no frontend button for any catalog. This work closes both gaps **without adding any anatomy-specific scope/promote logic** (the DRY principle: anatomy delegates to the same shared policy every other catalog uses). **Backend — domain parity fix:** `POST /anatomy` now calls `DEFAULT_CATALOG_POLICY.assign_create_scope` (SYSTEM_ADMIN→system, ADMIN/MANAGER→tenant, USER→user); `PATCH`/`DELETE /anatomy` call `check_modify` (creator-or-ADMIN for user-scope, ADMIN for tenant, SYSTEM_ADMIN for system). The legacy `is_global = tenant_id is None` checks are removed. `AnatomyStructureResponse` now exposes `scope` + `created_by` (were stripped from the API response). **Backend — shared promote hardening:** the duplicated `promote_scope` in `BiomarkerCatalogAdapter` (a near-verbatim copy of the base) is collapsed into a shared `_apply_scope_transition` helper that both adapters call — one RBAC check, one tenant_id/scope flip, one audit row. A new `_check_slug_collision` guard runs before the flip: if another row at the target scope tier already owns the same slug, it raises `CatalogConflict` (mapped to HTTP 409 with the conflicting item's id + name, so the client can offer "open existing"). Same-scope transitions skip the check (no-op). `BiomarkerCatalogAdapter` gains `model = BiomarkerDefinition` (was unset — the adapter overrode every method that referenced it, but the inherited collision guard needs it). **Frontend — generic promote:** new `promoteCatalogItem(type, id, scope)` in `catalogService.ts` (one function, six catalog types). New `PromoteScopeButton` component — a dropdown of allowed transitions mirroring `check_promote` (SYSTEM_ADMIN→all, ADMIN/MANAGER→user↔tenant, USER→hidden), wired into the catalog workspace detail header next to the Edit button. On 409 it surfaces the conflicting item's name inline. 8 i18n keys in en + el under `catalogs.promote.*`. **Tests:** new `test_anatomy_domain_scope.py` (8 — role-derived scope on create, `check_modify` RBAC on update/delete incl. creator-only user-scope rule); new `test_catalog_promote_collision.py` (3 — 409 on collision, success when unique, same-scope no-op); new `PromoteScopeButton.test.tsx` (7 — permission matrix, select→promote→onPromoted, 409 surfacing). The existing parametrized `test_catalog_scope_access.py` (39, all catalog types incl. biomarker's refactored promote) still passes. tsc + eslint clean; 108 backend + 153 frontend tests pass. **Breaking:** `POST /anatomy` no longer returns `scope=SYSTEM` for tenant rows; the response now includes `scope` + `created_by`. See `dev/plans/anatomy-scope-parity-2026-07-22.md`.

- **Feature: anatomy graph generation HITL — end-to-end chat review card for AI-driven anatomy expansion.** The `propose_anatomy_graph_generation` chat tool was dead code: exposed to the LLM and firing an inbox notification, but with no frontend handler (rendered as the "Unsupported action" fallback), no system-prompt description, and no generate+import wiring on confirm. This completes the full chain using the existing backend primitives (`define_anatomy_graph` assistance + `/anatomy/import`). **Backend:** `propose_anatomy_graph_generation` added to the HUMAN-IN-THE-LOOP tool list in all three system prompts (`CHAT`, `RESUME`, `GENERAL`) with SYSTEM_ADMIN-only-on-confirm guidance; no tool-registry, DB, or `/resolve` changes (the "AI never writes" model is preserved — `/resolve` still records the outcome only). New `backend/tests/test_hitl_anatomy_graph.py` (4 tests — factory registration, `__hitl__` marker + `task_type`/`status`/`proposed_payload` contract, inbox-notification fire, read-only no-DB-writes). **Frontend:** new `GenerateAnatomyGraphHandler.tsx` — generates the graph on mount via `getAIAssistance({task_type:'define_anatomy_graph'})`, surfaces an editable node/edge table (add/remove rows, relation dropdown sourced from `ConceptRelationType`), imports via `anatomyService.importGraph` on Approve, then `resolveHitlTask` (surfaces a clear 403 "only system admins can import" error for non-admins). Registered in `registry.tsx` (`generate_anatomy_graph`, `Network` icon, indigo accent). The `AIAssistanceRequest.task_type` union extended with `'define_anatomy_graph'` (was missing — the assistance call was unreachable from the typed frontend). Stale `AnatomyImportNode` type fixed: replaced the removed `category: AnatomyCategory` enum with `class_concept_slug` + added `display` (the import schema had moved to class concepts; the frontend type never caught up). New `GenerateAnatomyGraphHandler.test.tsx` (12 tests — summary renderer, generate-on-mount, generation failure + retry, approve → importGraph → resolveHitlTask confirmed, 403 error pill, reject → dismissed, registry wiring). 21 i18n keys in en + el under `ai_chat.hitl.generate_anatomy_graph.*`. **Design:** frontend-driven two-step (generate → review/edit → import → resolve), mirroring the catalog-definition handler pattern — deliberately NOT server-side generate+import in `/resolve` (would break the documented "no writes in /resolve" contract). Generation effect excludes `t` from deps so a language switch doesn't re-trigger a paid LLM call. Global-ontology warning surfaced in the modal (anatomy import writes `tenant_id=NULL` rows shared across all people). See `dev/plans/anatomy-graph-hitl-2026-07-22.md`. tsc + eslint clean; 4 backend + 12 frontend tests pass.

- **Docs: corrected stale HITL/anatomy claims + added implementation plan for the `generate_anatomy_graph` task type.** An audit of the HITL proposal tools found that `propose_anatomy_graph_generation` (`backend/app/ai/tools/hitl_proposals.py`) was dead code end-to-end — the tool was exposed to the LLM and fired an inbox notification, but had no frontend handler, no system-prompt description, and no generate+import wiring on confirm. The docs (`AI_SYSTEM.md`, `SEEDING_AND_DEMOS.md §6.4`, `INSTALL.md`) claimed it as shipped. **Doc fixes:** `AI_SYSTEM.md` §4.1 table row corrected (now genuinely ✅ shipped after the feature above landed); the "four of the six `propose_*` tools accept `links`" count corrected to "six of the seven" (the two allergy tools `propose_record_allergy` + `propose_define_allergy` gained `links` when allergies reached medication parity); `SEEDING_AND_DEMOS.md §6.4` rewritten to accurately describe the end-to-end HITL flow; `INSTALL.md` corrected its "produces a review card" claim. **Plan:** `dev/plans/anatomy-graph-hitl-2026-07-22.md` — the locked design (frontend-driven two-step, preserves the "AI never writes" model), the stale-type fix, and the union extension.

- **Feature: allergies brought to full medication parity — modernized service layer, dedicated list/detail pages, full AI tooling, HITL proposals, FHIR Substance facade.** The `/alerts` page was allergy-only and limited (cross-patient view, no add/edit-in-place, no catalog linkage, no AI). This work brings every surface to parity with medications (the reference "max features" pattern), with intentional breaking changes since no one is using the project yet. **Backend (Phase 1):** `allergy_service.py` rewritten from own-session+raw-dict to **db-injected + Pydantic-schema params** (matches `medication_service.py`). New service surface: `get_allergy` (single instance), `get_allergy_usage` (cross-patient join for the detail-page tab), `reprocess_allergy` (AI re-enrich seam — gracefully no-ops when no NLP backend is wired). `AllergyIntoleranceBase/Update` now take `Dict[str, Any]` code (not the strict `AllergenCode`) + `field_validator`s (empty→None, reactions list coerce) mirroring `MedicationRecordBase`. `AllergyCatalogResponse` exposes `scope` / `class_concept_*` / `tenant_id` parity with `MedicationCatalogResponse`. `AllergyCatalog` gained **`to_fhir_dict()` projecting to FHIR `Substance`** (the canonical FHIR resource for an allergen; FHIR has no dedicated "allergen catalog" resource — Substance is the closest fit, mapping `category` enum → FHIR substance-category codes). `AllergyCatalog` registered in `app/facade/registry.py` as `Substance` (read-only, like `Medication`). New endpoints: `GET /allergies/{id}` (single), `GET /allergies/catalog/{id}/usage`, `POST /allergies/catalog/{id}/reprocess`. **Latent-bug fix:** `check_allergy_access` + `check_medication_access` now filter `deleted_at.is_(None)` (both previously missed it despite `SoftDeleteMixin` — symmetric latent bug; the facade was already soft-deleting correctly but the access helpers could still hand back tombstones). **AI backend (Phase 2):** new `app/ai/tools/allergies.py` with 4 read-only chat tools mirroring `medications.py` — `get_current_allergies`, `get_patient_allergy_history`, `get_allergy_catalog_details`, `search_allergens`. Two new HITL proposal tools in `hitl_proposals.py`: `propose_record_allergy` (patient-instance, task_type `add_allergy`) + `propose_define_allergy` (catalog, task_type `create_allergy_definition`), both honoring the existing `LINK_SCHEMA` for allergy (`INDICATES`/`CORRELATES_WITH` concept, `AFFECTS` anatomy). System prompts (`CHAT` + `RESUME` + `GENERAL`) updated with the two new propose_* tools + CATALOG-vs-INSTANCE guidance for allergies. **Frontend service (Phase 3):** `allergyService.ts` expanded to match `medicationService.ts` — `getCatalogAllergy`, `updateCatalogAllergy`, `deleteCatalogAllergy`, `getAllergyUsage`, `reprocessAllergy`, `getAllergy` (single). TS types now declare the backend's **UPPERCASE enum values** (`AllergyCategory`/`Criticality`/`ClinicalStatus`/`ReactionSeverity`) — the old lowercase types were a bug, not a contract (the DB stores uppercase per `app/models/enums.py`; the old TS lowercase types only worked because everything was `any`-cast through `Partial<>` payloads). All call sites updated for the casing fix (most use `(x ?? '').toUpperCase()` for backwards-tolerant reads). **Frontend forms (Phase 4):** new headless `components/patients/AllergyForm.tsx` — `forwardRef` with imperative `submit()`, `prefill` + `onSubmit`/`onCancel`/`onReject` contract (HITL-embeddable via `showHeader`/`showActions`), AI assist, `CatalogItemPicker`, inline "define new allergen" path, status/criticality/category segmented controls, reactions editor, `<LinksSection>` for graph edges. `AllergyModal.tsx` reduced to a 68-line thin wrapper. **Frontend pages (Phase 5):** new `pages/Allergies/AllergyList.tsx` (patient-scoped, mirrors `MedicationList` — status filter, grid/list toggle, catalog shortcut, add button, active/resolved sectioning, `?new=allergy` intent). New `pages/Allergies/AllergyDetail.tsx` (allergen catalog detail mirroring `MedicationDetail` — 3 tabs: info / reactions / management, edit-in-place, AI reprocess, cross-patient usage table). New `components/allergies/AllergyCard.tsx` (compact card mirroring `MedicationCard` with kebab action menu, AI actions dropdown, criticality/status pills). New `components/ui/AllergyAIActions.tsx` with 4 AI prompts (explain/reactions/alternatives/severity). `currentAllergyId` threaded end-to-end through `uiSlice` + `AIActionsDropdown` + `AIChatInterface` + `prompts.py` so the chatbot knows which allergen the user is viewing. **Routes/Sidebar:** `/alerts` → redirect to `/allergies`; new `/allergies` + `/allergies/details/:allergyId` routes. Sidebar relabels "Allergy alerts" → "Allergies". Legacy cross-patient `pages/Dashboard/AllergyAlerts.tsx` **deleted** (the dashboard `AllergyAlertsCard` + `AllergySummary` now link to `/allergies`). **HITL frontend (Phase 6):** new `handlers/CreateAllergyHandler.tsx` (patient-instance, hosts `AllergyForm`) + `handlers/CreateAllergyDefinitionHandler.tsx` (catalog, hosts controlled catalog `AllergyForm`); both registered in `registry.tsx` (`add_allergy` rose accent, `create_allergy_definition` amber accent). **i18n:** 28 new top-level `allergies.*` keys + 10 new `allergies.modal.*` + `allergies.ai_actions.{explain,reactions,alternatives,severity}` + `ai_chat.hitl.{add_allergy,create_allergy}` — in BOTH `en` and `el`. **Docs:** `API.md` (3 new endpoint rows), `FHIR_R4_FACADE.md` (resource count 19 → 20, Substance row), `AI_SYSTEM.md` (search_allergens bullet + 2 new task types in the HITL table), `GETTING_STARTED_GUIDE.md` (`/alerts` → `/allergies`). 2395 backend tests pass; 531 frontend tests pass; tsc + eslint clean. **Decision:** no `examination_id` on `AllergyIntolerance` (unlike `Medication`) — allergies relate to many visits over their lifetime (noted, discussed, reacted, resolved), so a single FK is the wrong model; longitudinal visit context goes through Clinical Events.

- **CI/CD: hardened the GitHub image-publish workflow (`.github/workflows/docker-publish.yml`).** Bumped the Docker actions to current majors now that this workflow runs on GitHub-hosted runners (no self-hosted act-runner compatibility concern, unlike the Gitea pipeline): `docker/build-push-action@v5`→`@v7` (v6 is already superseded), `docker/metadata-action@v5`→`@v6`, `docker/login-action@v3`→`@v4`. Added a `concurrency` block grouped by `${{ github.ref }}` with `cancel-in-progress: true` — a newer push to `main` cancels a stale publish run, while tag-driven release publishes (`refs/tags/*`) form their own groups and never cancel each other or a main build. `actions/checkout@v4` left as-is (current). Brings the GitHub publish path to parity with the Gitea hardening.

- **Docs: `INSTALL.md` documents the container image model — GHCR by default, custom registries, and building from source.** The compose files are pull-only (no `build:` step) and default to `ghcr.io/health-assistant-io/health-assistant`, but INSTALL.md never said where images come from or that the source is configurable — so a self-hoster who doesn't use the project's Gitea CI had no documented "just pull the images" path even though it already works. Added a **"Container images & custom registries"** subsection under Production Deployment: explains that every flavor (and the quick start) pulls pre-built images from GHCR by default, with a table of the `REGISTRY` / `REPOSITORY` / `IMAGE_TAG` env vars to redirect to a private registry/mirror, point at a fork's namespace, or pin a release tag for reproducible deploys; plus a "run from source" path (build the default tags locally so `compose up` reuses them instead of pulling) for offline/fork/no-registry cases. Closes the non-Gitea deployer gap.

- **CI/CD: Gitea Actions deploy pipeline unblocked — job image pulled from Docker Hub instead of `docker.gitea.com`.** The runner timed out fetching job containers (`Get "https://docker.gitea.com/v2/": context deadline exceeded`), stalling every job at the pull step. `.gitea/workflows/deploy.yml` now pins `container.image: gitea/runner-images:ubuntu-latest` (the Docker Hub mirror of the default image — Docker CLI included so `build-and-push` still runs; `-slim` has no Docker CLI, `-full` is a 16 GB overkill) on all four jobs, and gains `concurrency` (cancel superseded runs per branch) and least-privilege `permissions` (`contents: read`, `packages: write`). Stale actions bumped: `docker/login-action@v2`→`v3`, `appleboy/ssh-action` + `scp-action` `@master`→`@v1` (master branches are unpinned); `checkout@v4` / `setup-python@v5` / `setup-node@v4` left as-is (current and known-good on Gitea's act runtime — avoided jumping to the days-old v7 majors). `docs/CI_CD_SETUP.md` §3A documents the canonical runner-side fix: set `runner.labels` (`ubuntu-latest:docker://gitea/runner-images:ubuntu-latest`, plus 22.04/24.04) in the runner's `config.yaml` (Gitea 1.21+) so every workflow inherits the image, applied via `docker compose restart runner` + `docker pull`. The per-job `container:` blocks stay as defense-in-depth so the pipeline isn't solely dependent on out-of-repo runner config.

- **Dev: `./scripts/run-dev.sh --no-admin` skips admin creation so the first-run setup wizard can be exercised.** The dev script previously always ran `create_system_admin.py` (`admin@healthassistant.local` / `admin123`), which left a user row in the DB — so `GET /auth/setup-status` returned `initialized=true` and the new `/setup` wizard could never be reached in dev. New `--no-admin` flag gates that step; default behavior is unchanged. The script prints a hint pointing at the wizard URL and at `psql … TRUNCATE users, tenants …` for resetting a DB that already has the admin. Documented in `DEVELOPMENT.md` alongside `reset-dev-db.sh` (the clean-slate path: `reset-dev-db.sh -y --migrate` then `run-dev.sh --no-admin`). bash syntax + arg parsing verified.

- **Feature: first-run setup wizard (browser-based admin bootstrap) + `/auth/register` locked to invite-only.** Eliminates the single biggest install UX cliff — creating the first admin previously required `docker compose exec … create_system_admin.py`. Now a fresh install is detected automatically: visiting the app URL redirects to a **setup wizard** (`/setup`) where you pick the admin email, password, and organization name, and you're logged in immediately. **Backend:** new `GET /auth/setup-status` (returns `{initialized, setup_token_required}` — drives the login-vs-setup redirect) and `POST /auth/setup` (the only bootstrap path — creates the initial tenant + `SYSTEM_ADMIN`, returns login tokens). **First-claim race closed:** on boot, if the DB has zero users, a one-time **setup token** is generated and printed to the backend container logs; the wizard requires it for non-localhost/non-dev requests so a stranger can't claim an internet-exposed fresh instance before the owner. Localhost and dev deployments skip the token. New `app/core/setup_token.py` (in-memory token — the backend is a single HTTP process; move to Redis if multi-worker HTTP ever lands). `POST /auth/register` is now **invite-only** — the open no-`tenant_id` bootstrap branch is removed (moved into `/auth/setup`); every registration requires a `tenant_id` + valid invite token. This also removes the "anyone can create a tenant as ADMIN" hole that existed on every later registration. The advisory-lock race protection from the old register path is preserved in `/auth/setup` (re-check inside the lock). **Frontend:** new `pages/Auth/Setup.tsx` wizard (organization name, email, password+confirm, conditional setup-token field with the `docker compose logs … grep` retrieval hint); `Login.tsx` now calls `setup-status` on mount and redirects to `/setup` when uninitialized, and the dead `<Link to="/register">` (no such route/page ever existed) is replaced with an invite-only note; `/setup` route added to `App.tsx` unauthenticated block; `api/axios.ts` no-token-warning exclusion list updated. **Breaking:** the `POST /auth/register` no-`tenant_id` bootstrap path no longer exists — any client relying on it must use `POST /auth/setup` (first-run) or invite tokens (subsequent users). **Tests:** new `tests/test_auth_setup.py` (10 tests — setup-status variants, happy-path SYSTEM_ADMIN creation + token return, 410/403/409 guards, advisory-lock acquisition, register-lockdown); `test_auth.py::test_register_success` rewritten as `test_setup_success` against the new endpoint; `test_auth_register_isolation.py` lost its three register-bootstrap tests (moved to setup) and gained the invite-only contract docstring. All 33 auth tests pass; 531 frontend tests pass; tsc + eslint clean. **Docs:** `INSTALL.md` step 6 now leads with the wizard (CLI demoted to a "Headless / automation" callout), First-Time Sign-In rewritten, and a `grep "setup token"` snippet added; `README.md` quick start updated; `GETTING_STARTED_GUIDE.md` §1 references the wizard as the first-login experience. `create_system_admin.py` is unchanged and remains the IaC/automation path.

- **Docs: restructured `docs/INSTALL.md` + new `docs/GETTING_STARTED_GUIDE.md` (end-user walkthrough).** INSTALL.md had several defects that tripped first-time installers, all fixed in this pass. **(1) Step 7 URL bug** — "Access the application" pointed at `http://localhost:3000` (the dev port) and `http://localhost:8000/docs`, but the standalone compose serves everything behind Nginx on port 80; corrected to `http://localhost` (UI), `/docs` (Swagger), `/health`, and `/flower/`, with a note pointing bring-your-own-proxy / dev users to the right ports. **(2) Credentials story** — there are *no* default login credentials (by design; the operator chooses them via `create_system_admin.py --email … --password …`), but the docs never said so. Added an explicit callout in step 6 stating the `admin@example.com`/`securepassword` values are placeholders, you log in with whatever you passed, and the script's argparse fallbacks (`sysadmin@health-assistant.local` / `admin123`) apply only if you run it bare. **(3) Anatomy digression split out** — the ~40-line "Anatomy Graph Expansion Packs" block (JSON schema, CLI flags, AI sub-graph generation) sat inside Quick Start step 6 and derailed the boot-the-app flow; moved to a new `## Optional: anatomy expansion packs` appendix at the end, with a one-line pointer left in step 6. **(4) "Updates" rewritten for Docker** — the section described `git pull && pip install` in a venv (the contributor flow), not the Docker path the quickstart uses; replaced with the `git pull → setup_env.py (if needed) → compose pull → compose up -d` sequence, with the venv path demoted to a clearly-labelled "From source (development)" subsection. **(5) "First-Time Setup & Linking" split into two real bootstrap paths** — the old section conflated `create_system_admin.py` (direct DB write, SYSTEM_ADMIN + named tenant) with the open-registration path; rewrote as "Path A — the admin script" vs "Path B — open registration (first registrant bootstraps a tenant as SYSTEM_ADMIN)". Dropped "Household Tenant" positioning prose (→ "a new tenant", audience-neutral per the docs skill). **(6) Misc cleanup** — removed the redundant "Configuration" section (restated step 4); folded the Redis health-check bug-confession into a one-line note; gave the dangling `certbot` code blocks a `#### TLS with Let's Encrypt` heading under the Security Checklist; consolidated the fragmented single-command Troubleshooting code blocks; fixed the API-docs URL in the old Support block; added a `## See also`. **(7) README consistency** — `README.md` quick start used `--tenant "My Household"` (positioning drift) while INSTALL.md used `--tenant "My Organization"`; aligned both to "My Organization" and added a no-default-credentials note pointing at INSTALL.md. **New doc:** `docs/GETTING_STARTED_GUIDE.md` — a task-oriented end-user guide that picks up where INSTALL.md leaves off (the install gets you to a login screen; this gets you through the first hour of actually using the app). Covers sign-in & roles, the per-person context switcher + creating/linking the active person, optional LLM setup (`/settings/ai-config`), uploading a lab report with the OCR/extraction pipeline, creating an examination, reading biomarker trends, tracking medications/vaccinations/allergies (catalog-vs-instance distinction), starting a health journey, connecting a wearable (`/settings/integrations`), the propose-never-writes AI assistant + Magic Fill, and personalization (PWA, draggable dashboard, notifications, languages). Includes two mermaid diagrams: a "first-hour" flowchart of the recommended step order, and a sequence diagram of the upload → Celery → LLM → review-card pipeline. All routes/paths verified against the frontend router (`frontend/src/App.tsx`) and sidebar (`Sidebar.tsx`). Registered in `docs/docs-tree.json` under "Getting Started" between Installation and Architecture. Follows the docs skill positioning rules (Beta, no HIPAA/GDPR claims, FHIR stated-not-leading, AI always paired with the human-in-the-loop wall). Every shell command in both docs is now in its own fenced block (one command per ```bash fence) so the website's per-block copy button copies a single, paste-ready command — no multi-command blocks remain.

- **Frontend: shared `CodingSystemSelect` + single coding-systems registry (DRY).** The coding-system dropdown was reimplemented in every form that needs one — `VaccineForm` used a plain free-text input (no dropdown at all), `BiomarkerForm` and `ClinicalEventForm` hardcoded `<option>` lists, and `AnatomyForm` + `ConceptForm` each declared their own local `const CODING_SYSTEMS = [...]` array (which had already drifted: anatomy offered LOINC, concept offered MeSH/FMA, etc.). **New single source of truth:** `config/codingSystems.ts` — one registry of nine systems (LOINC, SNOMED CT, CVX, ICD-10, ATC, RxNorm, MeSH, FMA, custom), each with its label, canonical FHIR `system` URL, and hint, plus a `CODING_SYSTEMS_BY_DOMAIN` map (biomarker / vaccine / clinical_event / medication / anatomy / concept) so each entity surfaces only the systems relevant to it (vaccines default to CVX, anatomy now correctly offers SNOMED/FMA instead of LOINC, concepts offer the full set). Helpers: `getCodingSystem`, `getCodingSystemsForDomain`, `getCodingSystemLabel`. **New component:** `components/ui/CodingSystemSelect.tsx` — a `<select>` bound to the registry via a `domain` prop (or explicit `systems`), with `includeEmpty` for nullable fields and a `className` override so each form keeps its own look while the option *list* stays shared. **Refactored all five forms** to use it: `VaccineForm` (now a proper CVX dropdown), `BiomarkerForm`, `ConceptForm`, `AnatomyForm`, and `ClinicalEventForm` (keeps its chunky styled chrome + chevron; only the option list is shared). Removed the two local `CODING_SYSTEMS` constants and three hardcoded `<option>` blocks. Also removed the free-text "Target Diseases" input from the patient-instance vaccination "define new" panel — disease association is a **catalog** concern expressed through the knowledge graph (`PREVENTS` edges via `<LinksSection srcType="immunization">` and the seeded `kind=disease` concepts), not free text on a patient dose. tsc + eslint clean; 531 frontend tests pass.

- **Frontend + Backend: vaccinations page overhaul — modular form/card/modal aligned with the Medication pattern, plus examination linking.** The vaccinations page was a bare-bones list with a hand-rolled modal and a custom vaccine dropdown that bypassed the shared picker infrastructure. Rewritten to mirror `MedicationList`/`MedicationModal`/`MedicationCard`, composing existing primitives instead of reimplementing them. **New modular package `components/vaccinations/`:** `VaccinationForm` (self-contained header/body/footer like `MedicationForm`) now picks the vaccine via the **`CatalogItemPicker`** (single, type `vaccine`, cards mode) with an inline "Define new vaccine" flow (creates a catalog entry through `createCatalogItem('vaccine', …)`), and links the visit via **`InstanceField`** (single, type `examination`) — the "instance selector" the form was missing; `VaccinationModal` hosts the form in the shared `<Modal>` and owns the API writes (add/edit immunization, optional catalog creation, and graph-link persistence via `createLinksFor('immunization', …)` after the record commits); `VaccinationCard` mirrors `MedicationCard` (icon tile, status badge, portal action menu with Edit/Delete/Open-in-catalog); `vaccinationStatus.ts` is the single source of truth for status badge/tile colours; barrel `index.ts`. **Page rewrite (`pages/Vaccinations/VaccinationList.tsx`):** status filter (all/completed/not-done/entered-in-error), grid/list view toggle, completed-vs-other grouping, "Open Catalog" button (`/catalogs?type=vaccine`), `?new=vaccination` create-intent, full `t()` i18n. **Schema fields now exposed in the form** (previously missing): `status`, `manufacturer`, `location`, `note`, plus `examination_id` and `dose_number`/`lot_number`/`administered_at`. **Backend:** added `examination_id` (FK → `examinations.id`, `ON DELETE CASCADE`) to `PatientImmunization` — model + `to_dict()` + FHIR R4 `Immunization.encounter` projection (mirrors `fhir_medications.examination_id`); wired through `PatientImmunizationCreate`/`Update` schemas + `vaccine_service`; Alembic migration `v1a2c3c4i5n6` (chains off head `n1o2t3i4f5y6`). **i18n:** new `vaccinations` namespace in en + el. **Tests:** 2 new backend tests (model-level FHIR encounter projection + endpoint create→read→clear round-trip for `examination_id`); 13 vaccine tests pass; tsc + eslint + ruff clean.

- **Frontend: catalog list strips rich-text markup from descriptions (HTML/Markdown → plain text).** The catalog item list (`CatalogBrowser`, used by both the workspace list pane and the picker-browse modal) rendered `item.description` as a raw string — so a description stored as HTML (`<p>Take with food</p>`) or Markdown (`**Take twice daily**`) showed the tags/markers as literal characters in the row. **Fix:** both the list and grid/card modes now pass the description through `toPlainText(item.description)` before rendering, producing a clean single-line snippet (CSS `line-clamp-2` still handles visual truncation). This is plain-text only — no formatted prose in the list, matching the compact-row contract (formatted prose stays in `CatalogItemCard` and `CatalogItemInfo` via `FormattedText`). **Enabler:** extended `toPlainText` in `utils/textFormat.ts` to also strip Markdown — previously it only stripped HTML and left Markdown markers (`**`, `#`, `-`, `[]()`) intact (a documented limitation). Markdown is now parsed to HTML via `marked` (already a dependency) then the existing DOM-based strip handles both formats in one pass. `toSnippet` (which wraps `toPlainText`) inherits the improvement, so `InstanceCard`, `examinationAdapter`, `eventAdapter`, and other compact-snippet callers also strip Markdown now. Widened `toPlainText`'s param type to `unknown` for the polymorphic `CatalogItem` shape (field values are loosely typed). 512 tests pass; tsc + eslint clean.

- **Fix: portalled popovers hidden behind six custom-rolled modals (z-index).** The relation-type dropdown (`RelationTypeSelect`, `z-popover` = 1050) opened behind the modal in the AI chat HITL "define" proposal form — its portalled popover painted *under* the form content instead of on top. Root cause: six modals that bypass the shared `<Modal>` component (`z-modal` = 1000) hardcoded `z-[1100]` (= 1100, same as `z-drawer`) for their overlay, defeating the codebase-wide contract that portalled popovers (`z-popover` = 1050) float above modals. Since 1050 < 1100, *any* popover or nested detail-modal opened inside these modals was hidden. **Fix:** changed all six overlays from the `z-[1100]` magic number to the canonical `z-modal` token — `HitlTaskModal` (the reported surface), `AIToolsModal`, `ChatInspector`, `CreateBiomarkerModal`, `ExaminationUpload` (preview spinner), `ExaminationGroupManager` (preview spinner). Now 1050 > 1000 holds everywhere and the shared `<Modal>`/custom-modal behavior is consistent. The AI drawer (`z-[550]` range) and toasts (`z-toast` = 1200) are unaffected. `tsc --noEmit` + eslint clean (the 9 lint warnings in `ExaminationUpload.tsx` are pre-existing `useEffect`-dep warnings, unrelated).

- **Frontend: catalog picker/relations UX pass — dropdown no longer clips, selected items show their type + formatted fields.** Three related fixes to the components backing catalog "Related items" editing (`RelationTypeSelect`, `LinksSection`, `CatalogRelationsEditor`, `CatalogItemCard`). **(1) Fixed the relation-type dropdown being clipped at the viewport/modal bottom edge.** `RelationTypeSelect` always opened below its trigger (`top: rect.bottom + 4`), so a chip near the bottom of a form/modal had its dropdown cut off. Added a `placement` state (`'top'|'bottom'`) computed in the positioning `useLayoutEffect`: when `spaceBelow < ~340px` and `spaceAbove` is larger, the popover flips above the trigger, anchored via CSS `bottom` (distance from viewport bottom) so it grows upward without needing a measured height. Mirrors the existing `CitationButton.tsx` flip pattern. Animation direction also flips (`slide-in-from-bottom-1` vs `slide-in-from-top-1`). **(2) Switched `<LinksSection>` (create/edit forms) and `<CatalogRelationsEditor>` (edit-mode, existing item) to `displayMode="cards"`.** Previously both rendered picks as compact chips, where the relation dropdown *replaced* the type chip — so the user couldn't see whether a picked related item was a medication, biomarker, concept, etc. The existing `CatalogItemCard` (purpose-built for this, with type icon + type label + name + scope + key fields, and an `actions` slot that `CatalogItemPicker` already wired the relation dropdown into) is now used in both surfaces. Edit mode and create/edit forms are now consistent. **(3) `CatalogItemCard` richtext fields now render as formatted prose.** Replaced the `toSnippet(raw, 160)` plain-text collapse with `<FormattedText>` (the auto-detecting renderer already used by the full `CatalogItemInfo` detail view — detects HTML/Markdown/plain via `detectTextFormat` and renders with `prose-sm` styling). A `max-h-28 overflow-hidden` cap keeps the compact card bounded; full text remains one click away via the card's "open in catalog" button. `tsc --noEmit` + eslint clean.

- **Docs: aligned `docs/AI_SYSTEM.md` + `docs/API.md` with the link-proposal system + catalog/instance tool rename.** Two doc files updated to reflect the link-proposal + rename work shipped earlier on this branch. **(1) `docs/AI_SYSTEM.md`** — added two paragraphs after the task-types table: a "Catalog vs instance disambiguation" paragraph explaining the system-prompt section that pairs the `define_*` / `prescribe_*` / `record_*` verbs with trigger phrases; and a "Link proposals (create + link in one confirmation)" paragraph documenting the optional `links[]` parameter on four `propose_*` tools, the `LINK_SCHEMA` single-source-of-truth matrix in `app/ai/tools/propose_link.py`, the drop-and-report validator (`build_link_specs`), the `get_link_schema` LLM tool, and the frontend `<LinksSection>` + `GET /api/v1/concept-edges/schema` UI discovery path. **(2) `docs/API.md`** — added a row to the `concept-edges` endpoint table for `GET /concept-edges/schema?src_type=&dst_type=`: pure-metadata discovery (no DB hit, no tenancy), returns the matrix slice in one of three shapes (full list / per-source map / per-pair list), powers the LLM `get_link_schema` tool + the frontend `<LinksSection>` dropdown filter. **Skills updated locally (gitignored)**: `.opencode/skills/ai-pipeline/SKILL.md` (tool list with new names + `get_link_schema`; new `propose_link.py` module section; tool count 24→25; CATALOG-vs-INSTANCE note; `GET /concept-edges/schema` endpoint §6.3), `.opencode/skills/hitl-task-cards/SKILL.md` (recipe §3 split into "catalog form pattern" vs "ClinicalEventForm pattern"; example uses `propose_prescribe_medication`; new link-proposal wiring note), `.opencode/skills/clinical-data/SKILL.md` (resolver coverage 7/11 → 11/11; new `LINK_SCHEMA` paragraph), `.opencode/skills/frontend/SKILL.md` (catalog forms section: `typeMeta` now optional; new "Single source of truth per concept" + "Graph links via `<LinksSection>`" paragraphs; new shared-infrastructure bullet list for `conceptEdges.ts` / `useLinkSchema` / `CatalogItemPicker.getRelationsForType` / `filterRelationGroups`).

- **Fix: "Related items" section appeared twice in catalog create forms.** After the previous form consolidation (which moved `<LinksSection>` INTO each catalog form), the `CatalogWorkspace` was still rendering its own external `<LinksSection>` for create mode — so users saw two identical sections stacked. Root cause: when I moved the section into the forms in the consolidation commit, I forgot to remove the workspace's external instance. **Fix:** removed the workspace's external `<LinksSection>` (and its now-unused `LinksSection` import — `createLinksFor` + `selectionsToLinkInputs` stay because `performSave` still uses them). The form-internal `<LinksSection>` is the single source of truth, fed by `values.links` / `onChange({links: next})`. **Bonus:** added `<LinksSection>` to the four remaining catalog forms (`AllergyForm` srcType='allergy', `VaccineForm` srcType='immunization', `AnatomyForm` srcType='anatomy', `ConceptForm` srcType='concept') so every catalog type gets the same "Related items" UX in both the workspace create flow AND the HITL propose flows. Each catalog form now owns its own `<LinksSection>` driven by the LINK_SCHEMA `srcType` for that concept. **Verified:** `tsc --noEmit` clean; 512 frontend tests pass; eslint clean.

- **Refactor: one form per catalog type (single source of truth).** Eliminates the long-standing form drift between the catalog-workspace create flow and the HITL "Define X" proposal flow. **Before:** `catalog/forms/MedicationForm.tsx` (controlled, `CatalogItemFormProps`) for the workspace, `patients/MedicationDefinitionForm.tsx` (uncontrolled, `prefill`+`onSubmit`) for HITL — same `MedicationCatalog` row created two different ways, with different field types (RichTextField vs plain textarea), different state models, and after Phase 3 different feature sets (only the HITL form had `<LinksSection>`). Same drift existed for biomarker (`catalog/forms/BiomarkerForm.tsx` vs `examinations/BiomarkerDefinitionForm.tsx`). The HITL skill explicitly calls this out as wrong: "The form rendered inside the modal must be a separate, headless component… ClinicalEventForm is the canonical example — it is shared by both the manual ClinicalEventModal and the HITL HitlTaskModal." **Now:** the catalog forms (`catalog/forms/*.tsx`) are the single source of truth. The catalog-workspace pattern (`CatalogItemFormProps: {values, onChange, mode}` — controlled, no internal state) wins because 5 forms already use it (medication, biomarker, allergy, vaccine, concept, anatomy), the workspace infrastructure (audit, scope, history, save) is built around it, and it composes better than `prefill`+`onSubmit`. **Changes:** (1) `<LinksSection srcType="medication">` moved into `catalog/forms/MedicationForm.tsx` — driven by `values.links` / `onChange({links: next})`. The catalog workspace already wires this through Phase 3's `editing.links` stash, so it just works for the manual create flow too. (2) Same for `catalog/forms/BiomarkerForm.tsx` with `srcType="biomarker"`. (3) `typeMeta` made optional in `CatalogItemFormProps` (catalog forms don't actually read it — only the workspace passes it). (4) `CreateMedicationDefinitionHandler` rewritten to manage draft state + render `<MedicationForm values={draft} onChange={...} mode="create" />` + own footer (cancel/reject/submit). (5) Same for `CreateBiomarkerDefinitionHandler`. (6) `CreateBiomarkerModal` (the legacy examinations-page manual create flow) rewritten to use `<BiomarkerForm>` too — was the third silent entry point. (7) **Deleted** `patients/MedicationDefinitionForm.tsx` and `examinations/BiomarkerDefinitionForm.tsx` (the old HITL-only forms). Net: -740 LOC of duplicated form chrome, +1 single source of truth per concept. **Behavior preserved:** AI-assist button on the catalog form is deferred (was on the HITL form only; will return as a catalog-forms-wide feature in a follow-up). The patient-prescription `MedicationForm` at `components/patients/MedicationForm.tsx` is **unchanged** — it's a different concern (prescribes a drug to a patient, has dosage/frequency fields). **Verified:** `tsc --noEmit` clean; 512 frontend tests pass; eslint clean.

- **AI HITL: wire `<LinksSection>` into create forms (Phase 3 of the `propose-link-hitl` plan).** Connects the Phase-1 backend (AI proposes links in a single `propose_*` call) and Phase-2 frontend (`<LinksSection>` component) into the actual user-facing forms so the long-promised "create X AND its links in one confirmation" UX finally works. **Six surfaces touched: (1) `MedicationDefinitionForm`** — gained a `links` field in prefill + payload and renders `<LinksSection srcType="medication">` after primary fields (collapses when the matrix offers no destinations). **(2) `CreateMedicationDefinitionHandler`** — `proposalToPrefill` maps the backend's `{dst, relation, properties, duplicate_of}` snapshot to `CatalogSelection[]`; `handleConfirm` calls `createLinksFor('medication', created.id, links)` after `addCustomMedication` returns the new id; per-link `{ok, edge_id?, error?}` results aggregated into the `resolveHitlTask` audit payload + `onResolved` card state. **(3) `BiomarkerDefinitionForm` + (4) `CreateBiomarkerDefinitionHandler`** — same pattern with `srcType="biomarker"`; the `links` field is stripped before calling `biomarkerService.createBiomarker` (the create endpoint doesn't know about it; we persist edges separately). **(5) `MedicationForm` + (6) `CreateMedicationHandler`** (patient prescription) — same pattern; the AI's `propose_prescribe_medication` tool attaches links to the resolved **catalog entry** (the drug), not the prescription row. The handler persists links via `createLinksFor('medication', catalog_id, links)` using the resolved catalog_id (existing OR newly-created via `createCatalogItem` when `is_new_catalog_entry`). **CatalogWorkspace:** the catalog create modal's hardcoded "Save the item first, then you can add relations." placeholder is gone. Create mode now renders `<LinksSection srcType={active.edge_endpoint_type}>` driven by the catalog descriptor, with the selections stashed on the draft (`editing.links`). `performSave` calls `createLinksFor(active.edge_endpoint_type, savedId, links)` after the primary create returns `savedId`. Edit mode is unchanged — `CatalogRelationsEditor` still handles it (loads + persists edges immediately, which is correct for an existing row). **Summary chips:** each HITL `renderSummary` now appends a `<LinksSection mode="summary">` block under the primary chips when the proposal carried links, so the compact chat card shows what's being linked at a glance. **i18n:** new `ai_chat.hitl.links.*` keys added to both `en/common.json` and `el/common.json` (section title, search placeholder, empty/error states, "Link exists" badge). **ClinicalEventForm integration deliberately skipped:** the LINK_SCHEMA's `clinical_event_type` rows apply to the event TYPE (a catalog row), not the event INSTANCE the user creates in this flow; the semantics of attaching links to an instance via this flow are unclear and worth a separate design pass. **Verified:** `tsc --noEmit` clean; 512 frontend tests pass; eslint clean on every touched file.

- **Fix: medication citation 404 when AI references a catalog entry.** Hovering a `[Ref: medication=<uuid>]` chip produced `GET /api/v1/medications/<uuid> → 404` whenever the UUID referenced a catalog definition (returned by `search_medications` / `get_medication_catalog_details`) rather than a patient prescription. Root cause: medication is the only citation type with a dual nature — the same `[Ref: medication=...]` syntax covers both patient prescriptions (`fhir_medications`, fetched via `/medications/<id>`) AND catalog definitions (`medication_catalog`, fetched via `/medications/catalog/<id>`) — but `CitationButton` only knew about the instance endpoint. The AI's catalog-side tools emit catalog IDs, so any chat answer that cited catalog details 404'd on hover. Fix: `CitationButton.tsx` now tries the instance endpoint first, falls back to the catalog endpoint on 404, and tracks which kind was resolved in a new `citationKind` state. Navigation routes accordingly — the existing `/medications/details/<id>` for prescriptions, the catalog workspace (`/catalogs?type=medication&item=<id>`) for catalog entries. Both navigation paths (the inline `handleOpenOriginal` and the `getEntityUrl` helper) updated. Other citation types (observation, biomarker, examination, event, document) are unambiguous and unchanged. Defensive layer — fixes the symptom regardless of which tool the AI used; future AI-prompt improvements to disambiguate citation types won't be blocked by this fix. All 512 frontend tests pass; tsc + eslint clean.

- **Fix: spurious Quill "Cannot register 'bullet'" warning on catalog create forms.** The compact `RichTextEditor` config listed `'bullet'` as a format — but Quill doesn't have a `'bullet'` format; bullet lists are the `'list'` format with the value `'bullet'` (configured via the toolbar's `{ 'list': 'bullet' }` button, which was already correct). Removed the bogus entry from `compactFormats` in `frontend/src/components/ui/RichTextEditor.tsx`. The warning surfaced in the console every time a catalog create form opened (`CatalogWorkspace > onNew > MedicationForm > RichTextField > RichTextEditor compact`). Bullet-list functionality was already working (it goes through the `'list'` format) — the warning was just Quill's registry complaining about an unknown name in the formats config. The full-mode `formats` array was already correct (lists `'list'` + `'indent'`, no `'bullet'`). One-line fix; tsc + 512 frontend tests still pass.

- **AI HITL: shared frontend for link proposals (Phase 2 of the `propose-link-hitl` plan).** Lands the frontend half of the modular link-proposal system so the existing create-forms can grow a "Related items" section in Phase 3. Four new files plus two small extensions to existing components; no behavior change for end users yet (Phase 3 wires it into the forms). **(1) `frontend/src/services/conceptEdges.ts` (new)** — thin client over `/concept-edges/*`: `getLinkSchema(srcType?, dstType?)` overloaded to return the matrix slice for any of the three shapes the backend endpoint emits (full / per-source / per-pair), `createLinksFor(srcType, srcId, links[], opts?)` that loops `createEdge` per link and collects a per-link `{ok, edge_id?, error?}` result so a single hit doesn't abort the loop, and `selectionsToLinkInputs(selections)` — the canonical mapping from the picker's `CatalogSelection[]` to the service's `LinkInput[]` (kept as a named export so the form doesn't duplicate the conversion). Default write opts: `source='ai', status='approved', tenant_scoped=true` — matching the HITL "human is the approval" contract. **(2) `frontend/src/hooks/useLinkSchema.ts` (new)** — module-level cached discovery hook. First mount pays the network cost; every subsequent mount (across any form in the session) reads synchronously from the cache. Two access modes via overload: `useLinkSchema()` returns the full matrix as `LinkSchemaRow[]`; `useLinkSchema('medication')` returns the per-source projection as `Record<dst_type, relations[]>` (derived client-side from the cached full matrix, so asking for a new srcType doesn't re-fetch). Failed fetches clear the cached promise so the next mount retries. Test-only `_resetLinkSchemaCacheForTests` is exported for unit-test isolation. **(3) `frontend/src/components/catalog/catalogRelationTypes.ts`** — adds `filterRelationGroups(groups, allowed)` to filter the global `RELATION_OPTION_GROUPS` to a subset of relation values. Used by the per-destination-type relation dropdown filter so a `medication→concept` chip shows only TREATS/CONTRAINDICATES/INDICATES/RISK_OF (etc.) instead of the full 19-relation list. **(4) `frontend/src/components/catalog/CatalogItemPicker.tsx`** — adds an optional `getRelationsForType?(catalogType) => RelationOptionGroup[] | undefined` prop. When provided, each chip's `<RelationTypeSelect>` shows only the relations valid for THAT chip's destination type (e.g. medication→concept vs medication→biomarker show different relation lists). Falls back to the global `relationPicker.options` / `RELATION_OPTION_GROUPS` when not provided or when the callback returns undefined — existing callers (`CatalogRelationsEditor`) are unchanged. The change is two `useMemo`-free inline per-chip reads, kept in the chips-mode AND cards-mode branches. **(5) `frontend/src/components/ai/hitl/LinksSection.tsx` (new)** — thin wrapper around `CatalogItemPicker` (relation mode) that uses `useLinkSchema(srcType)` to discover which destinations are valid for the form's primary entity, restricts `allowedTypes` accordingly, and feeds `getRelationsForType` to the picker for per-chip relation filtering. A form declares only `srcType="medication"` and `<LinksSection>` auto-renders every destination the matrix allows — no per-form link-catalog knowledge. Three modes: `'editable'` (default — full picker), `'summary'` (read-only chips for the HITL card), `'readonly'` (reserved alias for future denser layouts). Endpoint-type normalisation handled internally (`vaccine` ↔ `immunization`); non-catalog endpoint types the matrix might advertise (observation, doctor, examination, document) are hidden from the picker today since no instance-picker is available for them. The form owns the state (`value` / `onChange`); persistence happens in the form's `onSubmit` via `createLinksFor` AFTER the primary create returns — `<LinksSection>` itself never persists. **Tests:** 7 new in `services/conceptEdges.test.ts` — `getLinkSchema` URL construction for the 3 overloads, `createLinksFor` happy-path / per-link-failure-continues / opts-passthrough / empty-input / non-Error-rejection paths, `selectionsToLinkInputs` mapping + relation-stripping. 6 new in `hooks/useLinkSchema.test.ts` — first-mount fetch, per-source projection, cache-across-mounts (single network call for N mounts), error-then-retry, plus `deriveSchemaForSource` pure helper. `vi.hoisted` + `vi.mock` is the right vitest pattern for ESM exports (named-import spies don't intercept; vitest exports are frozen at module-eval). All 512 frontend tests pass; `tsc --noEmit` clean; `eslint` clean on every touched file.

- **AI HITL: renamed `propose_*` tools for catalog-vs-instance clarity (fixes the "create new medication" misrouting).** The user reported that asking the AI to "create a new medication" mis-selected the patient-instance tool (`propose_add_medication` → prescription) when they meant the catalog tool (`propose_create_medication_definition` → drug definition). Three-layer fix at the LLM-facing surface. **(1) Tool function names** — `propose_add_medication` → **`propose_prescribe_medication`**, `propose_create_medication_definition` → **`propose_define_medication`**, `propose_add_biomarker_to_examination` → **`propose_record_biomarker_result`**, `propose_create_biomarker_definition` → **`propose_define_biomarker`**. The new verbs are scope-explicit: "define" = catalog (reference definition), "prescribe"/"record" = patient-instance. The LLM picks a tool by its name + docstring, so this single change fixes the bulk of misrouting. **(2) Cross-referencing docstrings** — every paired tool's docstring now opens with an explicit "Use `propose_define_X` INSTEAD when…" pointer to its sibling, so when the LLM is hovering between two tools the docstring pulls it to the right one. Concrete trigger phrases included ("X isn't in the system yet" → define; "I'm taking X" → prescribe). **(3) System prompt** — new "CATALOG vs INSTANCE" subsection in `prompts.py` `CHAT_SYSTEM_PROMPT` + `GENERAL_CHAT_SYSTEM_PROMPT` that names the two layers, pairs the verbs, lists trigger phrases for each, and tells the LLM to ASK one clarifying question when intent is genuinely ambiguous rather than guess wrong. The MULTI-PROPOSALS example was also updated to use the new `propose_prescribe_medication` / `propose_define_biomarker` names. **Stable contract preserved:** the `task_type` routing strings inside the task payload (`add_medication`, `create_biomarker_definition`, etc.) are **deliberately unchanged** because they are shared with the integration SDK's `proposal_type` discriminator (`integrations/sdk/proposals.py:44-49`) — a future unified review UI depends on that alignment. Only the LLM-facing tool function names were renamed; backend routing, frontend `registry.tsx` keys, integration tests, and the SDK spec are all untouched. Doc comments referencing the old tool names refreshed in `docs/AI_SYSTEM.md` (task-types table gained a "Naming note" callout), `integrations/sdk/catalog.py` + `proposals.py` (docstrings), and three frontend form preface comments (`MedicationDefinitionForm`, `BiomarkerDefinitionForm`, `AddBiomarkerForm`) — purely cosmetic, no functional change. Tests: one reference to the old tool name in `test_ai_history_reconstruct.py:94` updated; `test_catalog_proposal_service.py` docstring refreshed. 56 hitl + catalog-proposal + integration-proposal tests pass; ruff clean; tsc + eslint clean.

- **AI HITL: modular link proposals in create forms (Phase 1 of the `propose-link-hitl` plan; see `dev/plans/propose-link-hitl-2026-07-21.md`).** Lets the AI attach a validated `links[]` payload to any existing `propose_*` tool call so the user creates a primary entity AND its knowledge-graph links to existing catalog items in a single confirmation. Previously "create Metformin AND link it to the disease it TREATS" required two HITL confirmations chained across auto-resume turns; now it's one. **Single source of truth:** `LINK_SCHEMA` in `backend/app/ai/tools/propose_link.py` — a declarative matrix mapping `(src_type, dst_type)` → valid `ConceptRelationType` values. Consumed by (a) `validate_relation_combo` (pure server-side guard), (b) the new `get_link_schema` read tool in `taxonomy.py` (LLM discovery — avoids bloating the system prompt with the 22-relation × 11-endpoint matrix), (c) the new `GET /api/v1/concept-edges/schema?src_type=&dst_type=` REST endpoint (UI discovery), and (d) the frontend `<LinksSection>` component (Phase 2 — filters destination + relation dropdowns from the same matrix via the REST endpoint). A form's "what links it supports" is just `LINK_SCHEMA` filtered by its primary `srcType` — no per-form declaration code. **Orchestrator:** `build_link_specs(db, tenant_id, src_type, raw_links, *, primary_existing_id=None)` validates each LLM-supplied link, resolves the destination endpoint via `resolve_endpoint` (UUID passthrough; slug/name fallback via `catalog_search_service`), and snapshots a `{dst, relation, properties, duplicate_of}` payload into `proposed_payload.links`. Invalid combos are silently dropped (drop-and-report — kept vs dropped count surfaces in the tool result so the LLM self-corrects on the next turn). When the primary already exists, `check_existing_edge` queries `concept_edges` for an existing match so the form can show a "Link exists" badge and skip on submit; when the primary is being newly created, dedup is skipped entirely. **Wired into four existing tools:** `propose_create_clinical_event`, `propose_prescribe_medication` (was `propose_add_medication` — see the rename entry above), `propose_define_biomarker` (was `propose_create_biomarker_definition`), `propose_define_medication` (was `propose_create_medication_definition`) each gained an optional `links: Optional[List[dict]] = None` argument. `propose_record_biomarker_result` (was `propose_add_biomarker_to_examination`) is intentionally untouched (lab-result additions don't carry ontology links — those live on the biomarker definition). **Resolver backfill:** `_resolve_doctors`, `_resolve_observations`, `_resolve_documents` added to `concept_endpoint_resolver.py` — all 11 `EdgeEndpointType` values now have proper labels (previously fell through to the `"{type}:{id-prefix}"` fallback). **Security unchanged from existing HITL pattern:** AI never writes; the form's Approve click triggers `POST /concept-edges` (the canonical RBAC/tenant-scoped endpoint). Three layers of validation: tool-layer matrix, `ConceptEdgeCreate` Pydantic schema, `ConceptService.create_edge` (which also checks concept endpoints exist). No new endpoint for writes; only the metadata `GET /schema`. **Tests:** 24 new in `tests/test_propose_link.py` — `LINK_SCHEMA` integrity (every endpoint + relation references a valid enum, no duplicates, covers the promised combos), `validate_relation_combo` accept/reject paths, `relations_for_*` + `serialize_full_schema` shapes, `build_link_specs` orchestrator (empty input, valid snapshot, invalid relation drop, unknown endpoint drop, malformed entries drop, dedup-when-primary-exists, skip-dedup-when-primary-new, mixed batch), the 4 REST endpoint shapes (full / filter-by-src / filter-by-pair / bad-type 400), and a guard that all 11 endpoint types have registered resolvers. 2265 existing tests still pass (4 pre-existing kind-filter failures on `main` unaffected by this change). ruff clean across all touched files.

- **Docs: aligned public docs with the integrations-sdk-improvements pass (items 1–5).** Doc-only commit — no code, no test impact. Eight files updated to reflect the five improvements that shipped on this branch (rate-limit cooldown, webhook HMAC helpers, document dedup, notification digest, setup config): (1) `docs/INTEGRATIONS_SDK.md` — §3.1 now mentions `IntegrationRateLimitError.retry_after_seconds` propagation; §3.3 documents the new exception field + Redis cooldown contract with a code example; §3.9 builder table gained a `.digest_key(key)` row; §3.10 dedup contract notes that the same pattern now applies to documents; §3.13 `DocumentPull` field list + code example + idempotency section rewritten (was claiming "no document-level dedup columns" — now false); new §3.14 "Webhook Signature Verification (Reusable Helpers)" documents `integrations.sdk.webhook_security`; §3.8 OAuth example `setup(config)` signature widened to `dict | None = None` + `super().setup(config)` call; line 218 "robust HTTP" → "HTTP that retries and maps status codes consistently" (positioning fix per the documentation skill's anti-fluff rule). (2) `docs/INTEGRATIONS_FRAMEWORK.md` — highlight #5 mentions rate-limit cooldown key + updated pipeline; highlight #6 cross-references the new webhook helper module; highlight #13 documents the document-level dedup pattern. (3) `docs/NOTIFICATION_SYSTEM.md` — `notifications` schema row includes `dedup_key?` + `dedup_expires_at?`; `emit()` signature block shows the new kwargs; numbered "emit does" list opens with a step 0 for the dedup-hit short-circuit; new "Digest collapsing (`digest_key`)" subsection; elevated-HR example includes `.digest_key(...)`; operator checklist includes `NOTIFICATION_DEFAULT_DIGEST_TTL_SECONDS`. (4) `docs/STATUS.md` — added a new "Integrations SDK improvements pass" entry under "Completed > Backend" with one sub-bullet per item; fixed the stale "Document-level dedup deferred" sentence in the F+G+C entry. (5) `docs/API.md` — sync-endpoint 429 row notes the cooldown key; webhook row notes the SDK delegation. (6) `docs/FHIR_R4_FACADE.md` — `DocumentReference` row notes the new `meta.tag[]` provenance stamping. (7) `docs/docs-tree.json` — refreshed SEO descriptions for `notification-system`, `integrations-framework`, and `integrations-sdk` to advertise the new capabilities (digest collapsing, rate-limit cooldown, webhook HMAC helpers, document dedup). (8) `integrations/dev_dummy/docs/capabilities.md` — moved 5 of 7 items from "recommendations" to a new "Recently shipped" section (the integrations-sdk-improvements pass closed them); added a `setup(config)` row to the provider-hooks table; updated the notifications + webhook + documents rows to note the new demonstrations.

- **Integrations SDK improvements pass (5 items; plan: `dev/plans/integrations-sdk-improvements-2026-07-21.md`).** Five independent improvements surfaced while expanding `dev_dummy` into a reference implementation. Each shipped as its own commit; full details in the plan. **Item 1 — `retry_after_seconds` on `IntegrationRateLimitError` + worker cooldown:** `integrations/sdk/exceptions.py` gained an optional `retry_after_seconds` field; `integrations/sdk/http.py::_retry_request` captures the last seen `Retry-After` header and propagates it onto the raised exception; `integration_sync_service` exposes three new helpers (`set_rate_limit_cooldown`, `is_rate_limited`, `clear_rate_limit_cooldown`) that write a Redis key `sync_cooldown:{integration_id}` with TTL clamped to [60s, 1h]; `SyncResult.retry_after_seconds` carries the value; the worker skips the integration while the cooldown exists + writes it on a rate-limited sync outcome. Degrades gracefully when Redis is down (logs + falls back to the per-instance `sync_interval` throttle). **Item 2 — webhook HMAC helpers extracted to `integrations/sdk/webhook_security.py`:** `verify_hmac_signature` (constant-time HMAC-SHA256, strips the conventional `sha256=` / `sha256:` prefixes used by GitHub/Slack/Stripe) + `verify_canonical_signature` (the two-way-API canonical form `METHOD\npath\n[timestamp\n]body` with optional ±5min replay-skew window) + `get_signature_header` (case-insensitive lookup over the conventional header names). The endpoint (`app/api/v1/endpoints/integrations.py::_verify_webhook_signature` / `_verify_api_signature`) and the `dev_dummy` provider both rewritten as thin wrappers; behaviour preserved byte-for-byte. Module boundary: imports only stdlib, no `app.*` coupling. **Item 3 — document-level dedup via `source_integration_id` + `external_id`:** `documents` table had no integration-dedup columns; the contract was "providers advance their own cursor" (fragile under crashes; unavailable to webhook-driven ingestion). Migration `d1o2c3u4m5e6` (chains off `g1h2i3t4l5pr`) adds `source_integration_id` (FK → `user_integrations`, ON DELETE SET NULL) + `external_id` + partial unique index `uq_document_integration_dedup` on `(tenant_id, patient_id, source_integration_id, external_id)` — mirrors the pattern on examinations + clinical_events. `document_service.ingest_document_bytes` gains `source_integration_id` + `external_id` kwargs; on dedup hit returns the existing row as-is (no re-file-write, no re-OCR dispatch); race window recovered via IntegrityError. `DocumentPull` SDK spec gains `external_id`; engine wires the integration's own id through automatically. `dev_dummy` now sets `external_id="dev_dummy_demo_report_v1"`. **Item 4 — notification digest collapsing via `dedup_key` + TTL:** `NotificationSpec` gains an optional `digest_key` (provider-supplied); the builder exposes `.digest_key(key)`. Migration `n1o2t3i4f5y6` adds `dedup_key` + `dedup_expires_at` + a non-unique composite index. `notification_service.emit` gains `dedup_key` + `dedup_ttl_seconds` kwargs; before insert it looks up an existing notification with the same key whose `dedup_expires_at` is in the future and returns it as-is (no re-fan-out, no re-push). TTL clamped to [60s, 7d], default 6h, tunable via `settings.NOTIFICATION_DEFAULT_DIGEST_TTL_SECONDS`. Design note: NO unique partial index (Postgres partial indexes can't reference `now()` in the predicate; the TTL semantics require multiple historical rows per key). Engine forwards `spec.digest_key` to `emit`. `dev_dummy` tags the elevated-HR notification with `digest_key="dev_dummy:elevated_heart_rate:patient/{uuid}"` so consecutive syncs collapse. **Item 5 — `setup(config)` gets real `SystemIntegration.global_config`:** `integrations/base.py` widened to `config: dict | None = None`; the registry builds a per-domain config map from `SystemIntegration` rows and forwards each to `provider.setup(system_config)` instead of the legacy empty dict; `dev_dummy` adds a `setup(config)` override (logs the config + calls `super()`) so authors have a copy-paste example. **Verification:** 5 commits, 113 new tests, all 586 integration-related tests pass; ruff clean across every touched file. Plan reference: `dev/plans/integrations-sdk-improvements-2026-07-21.md`.

- **Integrations SDK: turned `dev_dummy` into a complete reference implementation exercising every SDK capability.** The `dev_dummy` integration was a thin pull-only demo that left most opt-in hooks undemonstrated — authors writing real integrations had no single place to copy from. This release expands it into the canonical reference. The provider (`integrations/dev_dummy/provider.py`, organised by `§A`–`§P` capability markers) now implements every `BaseHealthProvider` opt-in: `handle_webhook` with HMAC-SHA256 signature verification against the Fernet-encrypted `webhook_secret` config field, `handle_api_request` (a 4-route REST surface: `GET status` / `GET cursor` / `POST reset` / `POST echo`), `supports_tools` + `get_tools` returning two synthetic LangChain `StructuredTool` objects, `supports_clinical_events` + `pull_clinical_events` emitting a sample flu-episode `ClinicalEventCreate` (with `external_id` for engine dedup), `supports_examinations` + `pull_examinations` emitting an annual-checkup `ExaminationCreate`, `supports_catalog_proposals` + `pull_catalog_proposals` auto-applying a "Sleep Quality Score" biomarker via the `biomarker_proposal` constructor, `supports_hitl_proposals` + `pull_hitl_proposals` + `handle_proposal_resolution` queueing a "Stress Index" concept for human review (with cursor-advancement on resolve), `supports_documents` + `pull_documents` synthesising a small text lab report once per instance (cursor-based idempotency), plus all four notification kinds (alert / hitl / system / agent×critical) and the existing custom-action surface (gained a third action `show_status`). The config flow (`integrations/dev_dummy/config_flow.py`) demonstrates the remaining `BaseConfigFlow` hooks: `max_instances_per_user = 3`, `get_secret_fields() == ["webhook_secret"]` (round-trips through the inherited `prepare_for_storage` / `prepare_for_read` / `decrypt_for_use` defaults), plus a comprehensive JSON schema with a toggle per opt-in so each capability can be exercised in isolation. Manifest bumped to `2.0.0`; description refreshed. Two latent bugs fixed: `_latest_value` previously parsed `effective_datetime` as a string (would `TypeError` under a bare except, so the helper returned the FIRST matching obs instead of the LATEST); renamed to `_latest_numeric` with a proper datetime-aware comparison and an explicit skip for categoricals. The `sensor_malfunction` notification branch (HR > 200) was dead code (simulation never produced values that high) — added a `simulate_sensor_glitch` config toggle (5% chance per sync of an out-of-range HR) so the critical-alert path is reachable and tested. Stale docs (`docs/overview.md` claimed OAuth + webhook that didn't exist, `docs/testing.md` claimed a "10% failure rate" that wasn't implemented) rewritten to match the actual capabilities; new `docs/capabilities.md` provides a hook→toggle→file map. Test coverage: 51 new unit tests in `backend/tests/test_dev_dummy_provider.py` — one per capability, plus regression guards for the two bugfixes. No DB / Redis / OAuth provider required to run the tests. ruff clean; all 387 existing integration + SDK tests still pass.

- **Docs: aligned public docs with the integrations F + G + C workstreams.** Doc-only commit — no code, no test impact. Four files updated: (1) `docs/INTEGRATIONS_SDK.md` gained three new sections — **§3.11 Catalog Proposals** (the `supports_catalog_proposals` / `pull_catalog_proposals` hooks + `CatalogProposal` spec + the four `kind` payloads + the idempotency contract per kind + the role limitation for concept/edge writes), **§3.12 HITL Proposals** (the `supports_hitl_proposals` / `pull_hitl_proposals` / `handle_proposal_resolution` hooks + `IntegrationProposalSpec` + the state machine diagram + the three new resolver endpoints + the relationship to the chat-side AI HITL), and **§3.13 Document Pull** (the `supports_documents` / `pull_documents` hooks + `DocumentPull` spec + the per-sync caps + the cursor-must-advance idempotency contract). A new **See Also** footer cross-links FRAMEWORK, API, NOTIFICATION_SYSTEM, FHIR_R4_FACADE, and the three reference providers. (2) `docs/INTEGRATIONS_FRAMEWORK.md` gained three new highlight bullets (#11 Catalog Proposal Contributions, #12 Integration HITL Proposals, #13 Document Pull) alongside the existing #10; the §4 + §5 `run_sync` pipeline-stage sentence refreshed to list every opt-in stage in order (pull → convert → biomarker-map → telemetry-split → clinical events → examinations → catalog proposals → HITL proposals → documents → notifications → push → log → commit). (3) `.opencode/skills/integrations/SKILL.md` (agent-facing authoritative reference, gitignored) updated across §1 codebase map (3 new SDK modules, 3 new services, 1 new model, 1 new schema), §5.1 method table (7 new rows), §5.11/5.12/5.13 (new sections mirroring the SDK doc), §8.3 routes table (3 new HITL resolver endpoints), §10 "Adding X" bullets (3 new recipes), §13.1 debug table (10 new symptom→cause rows for F+G+C edge cases), §13.3 manual testing (curl example for HITL resolve). (4) `docs/docs-tree.json` `integrations-sdk` SEO description refreshed to advertise the new capability hooks; `fhir-r4-facade` description corrected from "18 conformance resources" → "19" (`EpisodeOfCare` was added earlier and the description drifted).

- **Integrations SDK (Workstream C.2 of follow-ups pass, plan: `dev/plans/integrations-sdk-followups-2026-07-21.md`): documents opt-in hook + engine wiring + per-sync caps.** New SDK opt-in hooks on `BaseHealthProvider` mirror the established `supports_X` / `pull_X` pattern: `supports_documents() -> bool` (default `False`) gates the pull, and `pull_documents(integration) -> List[DocumentPull]` (default `[]`) returns the specs. New `DocumentPull` Pydantic spec in `integrations/sdk/documents.py` (re-exported from `integrations.sdk`) carries `filename`, `content: bytes` (raw — provider fetches upstream), `content_type`, `examination_external_id` (links to an exam from `pull_examinations`), `category_concept_slug`, and `include_in_extraction`. Pydantic chosen over the plan's `@dataclass` to match the post-F SDK convention. Engine wiring in `integration_sync_service.run_sync` after the HITL-proposals block: per-spec, the engine resolves `examination_external_id` via a hoisted `exam_by_external_id` map (built in the examinations block), resolves `category_concept_slug` via `resolve_concept_by_slug`, enforces **both per-sync caps** before ingest, then delegates to `document_service.ingest_document_bytes` — the same canonical path the UI upload endpoint uses. Two new cap constants: `INTEGRATION_MAX_DOCS_PER_SYNC = 20` + `INTEGRATION_MAX_DOC_BYTES_PER_SYNC = 50 MiB`. `SyncResult.documents_pulled` / `documents_written` exposed; `records_synced` includes documents. No migration — `DocumentModel` already has every field C needs; document-level dedup deferred (providers advance their own cursor via `set_sync_cursor`). Tests: 7 new in `tests/test_integration_documents.py`. ruff clean.

- **Integrations SDK (Workstream C.1 of follow-ups pass, plan: `dev/plans/integrations-sdk-followups-2026-07-21.md`): extract `ingest_document_bytes` from `upload_document`.** The canonical ingestion path (`document_service.upload_document`) took a Starlette `UploadFile` — useless from a Celery / webhook / api-proxy context. New `ingest_document_bytes(*, filename, content, content_type, tenant_id, patient_id, owner_id, db, examination_id=None, include_in_extraction=True, category_concept_id=None) -> DocumentModel` takes raw bytes. The existing `upload_document` becomes a thin wrapper that reads the `UploadFile` in capped chunks (audit A4) and delegates. Same storage path, same DB shape, same FHIR validation gate — only the code location changed. The OCR dispatch moves **inside** `ingest_document_bytes` (best-effort, 4-arg `ocr_document.apply_async` form) so the engine path gets OCR for free; the endpoint's inline OCR trigger + `BackgroundTasks` broker-down fallback are dropped. Minor UX regression for the broker-down edge case (document is still saved; user can re-trigger extraction via the UI's `trigger_extraction` button); the unification is worth it. New `category_concept_id` kwarg not previously settable from the endpoint — UI uploads leave it NULL, the integration engine resolves it from `DocumentPull.category_concept_slug`. Tests: 7 new in `tests/test_document_service_bytes.py`.

- **Integrations SDK (Workstream G.4 fixups of follow-ups pass, plan: `dev/plans/integrations-sdk-followups-2026-07-21.md`): register model + CASCADE patient_id FK + baseline-test allowlist.** Four drive-by fixes surfaced by the full test suite after G.4 landed: (1) Register `IntegrationProposal` in `app/models/__init__.py` so it's part of `Base.metadata.tables` (required by `test_squashed_initial_schema` and similar metadata-driven checks). (2) Switch `integration_proposals.patient_id` FK from SET NULL to CASCADE per audit D9 — every patient_id FK in the schema must CASCADE so a patient's clinical record deletes atomically. Catalog proposals leave patient_id NULL, so CASCADE doesn't affect them. (3) Migration uses the same-line + single-quote `op.create_table('foo',` style as the consolidated baseline so the `test_every_model_table_is_created_by_the_baseline` substring check matches. Added a `POST_BASELINE_TABLES` allowlist to that test for tables added by named follow-up migrations (with a sanity check that the named migration actually creates the table — keeps the allowlist honest). (4) Fixed the migration's `downgrade()` — `batch_op.drop_index(..., table_name=...)` is invalid; the batch context already knows the table.

- **Integrations SDK (Workstream G.4 of follow-ups pass, plan: `dev/plans/integrations-sdk-followups-2026-07-21.md`): wire HITL-proposals opt-in into `run_sync`.** Adds the HITL-proposals opt-in block to `run_sync` (after the catalog-proposals step, before the push). `_opt_in(provider, 'supports_hitl_proposals')` gates `pull_hitl_proposals`; for each returned spec, the engine calls `integration_proposal_service.create_proposal` (dedup-aware insert). For newly-inserted rows only (NOT existing-and-returned ones, to avoid inbox spam on re-sync), it fires an HITL notification via the new `_emit_hitl_proposal_notification` helper. Notifications use `source=INTEGRATION, type=HITL_TASK, category=HITL, severity=WARNING` — the same shape as the chat-side HITL notifications but keyed on the integration source. The action button links to `/integrations/{provider}/{integration_id}/proposals/{proposal_id}` (forward-compatible — the SPA route doesn't exist yet; the review card + modal UI ships separately). Capped at `INTEGRATION_MAX_HITL_PROPOSALS_PER_SYNC = 20`. `SyncResult.hitl_proposals_pulled` / `hitl_proposals_inserted` exposed; `records_synced` includes the count. `create_proposal` now returns `(row, created)` so the engine can decide whether to fire the notification (only on actual inserts).

- **Integrations SDK (Workstream G.3 of follow-ups pass, plan: `dev/plans/integrations-sdk-followups-2026-07-21.md`): SDK hooks for HITL proposals.** Adds three opt-in hooks on `BaseHealthProvider` mirroring the `supports_X` / `pull_X` / handler pattern from B + E + F: `supports_hitl_proposals() -> bool` (default `False`, opt-in gate), `pull_hitl_proposals(integration) -> List[IntegrationProposalSpec]` (default `[]`), and `handle_proposal_resolution(integration, proposal_id, outcome) -> None` (default no-op, async — called by the resolver after approve/reject/cancel so the provider can advance a cursor, log audit, etc.). Names use `pull_*` for consistency with the established pattern. The `hitl_` prefix disambiguates from the auto-apply `pull_catalog_proposals` — a provider can opt into both (auto-apply for high-confidence entries, HITL for ones needing human judgement). Re-exports the spec + outcome + four typed constructors (`biomarker_hitl_proposal`, `medication_hitl_proposal`, `concept_hitl_proposal`, `edge_hitl_proposal`) from `integrations.sdk`.

- **Integrations SDK (Workstream G.2 of follow-ups pass, plan: `dev/plans/integrations-sdk-followups-2026-07-21.md`): proposal endpoints + resolver + SDK specs.** Adds the resolver, the three HITL endpoints, and the SDK spec module. `IntegrationProposalSpec` + `ProposalOutcome` Pydantic models + four typed convenience constructors in `integrations.sdk.proposals` wrap the same payload shape as F's `CatalogProposal` constructors but tagged for human review. `integration_proposal_service.resolve_proposal` performs the state machine (PROPOSED → CONFIRMED / DISMISSED / FAILED), delegates the approve path through `catalog_proposal_service.apply_proposal` (the same write path F.3 auto-applies through), stamps `resolved_*` fields, and fires the provider's `handle_proposal_resolution` callback (best-effort). Snapshots ORM attributes before any potential rollback to avoid `MissingGreenlet` when the session is expired. Three new endpoints on the integrations router: `GET /instance/{id}/proposals?status=` (list), `GET /instance/{id}/proposals/{proposal_id}` (one), `POST /instance/{id}/proposals/{proposal_id}/resolve` (resolve). New `_load_owned_integration(...)` helper extracted from the inline pattern repeated across this endpoint — stricter than the existing inline checks (includes `tenant_id` scoping). USER role can list + view but not approve (catalog writes require ADMIN+ under the catalog policy) — endpoint surfaces as 403. Re-resolve from a terminal state returns 409 (idempotent contract). Tests: 15 new in `tests/test_integration_proposals_api.py`.

- **Integrations SDK (Workstream G.1 of follow-ups pass, plan: `dev/plans/integrations-sdk-followups-2026-07-21.md`): `integration_proposals` model + persistence service.** Adds the persistence layer for workstream G. `IntegrationProposal` ORM model: a pending catalog write proposed by an integration, awaiting human review. Mirrors `HitlTaskStatus` (PROPOSED → CONFIRMED / DISMISSED / FAILED) so the existing HITL state machine applies. Deduped by a partial unique index on `(integration_id, dedup_key)` where `dedup_key` is the sha256 of canonical JSON of `(proposal_type, proposed_payload)`. Migration `g1h2i3t4l5pr` chaining off `e1x2a3m4i5n6`: creates the table + 8 indexes (basic lookups, list-by-status, tenant-wide, partial unique dedup). `integration_proposal_service` module: `create_proposal` (dedup-aware insert, returns `(row, created)` so the caller knows whether to fire a notification), `get_proposal` (integration-scoped read), `list_proposals` (status filter + newest-first with deterministic `(created_at, id)` tiebreaker), `count_proposals`, `compute_dedup_key`. Pydantic response + resolve-request schemas. Tests: 10 new in `tests/test_integration_proposal_service.py`.

- **Integrations SDK (Workstream F.3 of follow-ups pass, plan: `dev/plans/integrations-sdk-followups-2026-07-21.md`): wire catalog-proposals opt-in into `run_sync`.** Adds the catalog-proposals opt-in block to `run_sync` (after the examinations step, before the push). `_opt_in(provider, 'supports_catalog_proposals')` gates `pull_catalog_proposals`; the resolved actor calls `catalog_proposal_service.apply_proposal` for each item; per-item failures logged and don't abort the sync. Capped at `INTEGRATION_MAX_PROPOSALS_PER_SYNC = 50` items per sync. `SyncResult.proposals_pulled` / `proposals_applied` exposed; `records_synced` includes `proposals_applied`. Drive-by fix: `map_observations_to_biomarkers` was previously indented *inside* the per-observation patient-id-coercion loop and ran N times per sync instead of once — idempotent so just wasted work, but a real correctness risk if the call ever grew side effects. Dedented + comment explaining the one-call contract.

- **Integrations SDK (Workstream F.2 of follow-ups pass, plan: `dev/plans/integrations-sdk-followups-2026-07-21.md`): catalog proposals for medication / concept / edge kinds.** Extends `apply_proposal` with three new routers: `medication` → `medication_service.create_catalog_medication` (FHIR-validates + stamps scope via `DEFAULT_CATALOG_POLICY`). Idempotent on `(tenant_id, name)`. `concept` → `ConceptService.create_concept` with the actor's role + tenant_id. Validates `kind` against the `ConceptKind` enum. Idempotent on `slug`. `edge` → `ConceptService.create_edge` with `source=ConceptProvenance.INTEGRATION, status=APPROVED` — the only catalog-layer write path today that actually stamps the integration provenance (`ConceptEdge` is the only model with a dedicated `source` column). Idempotent on `(src_type, src_id, dst_type, dst_id, relation, tenant_id)`. Each router validates the payload shape via the matching `*Create` schema (or inline enum coercion for the strongly-typed concept/edge fields) before touching the DB. Role limitation: `ConceptService.create_concept` / `create_edge` require role ≥ ADMIN, so USER-role integration owners can't contribute concepts/edges. Tests: 9 new across the three kinds (happy path + idempotency + invalid-kind + invalid-UUID) in `tests/test_catalog_proposal_service.py`.

- **Integrations SDK (Workstream F.1 of follow-ups pass, plan: `dev/plans/integrations-sdk-followups-2026-07-21.md`): catalog proposals SDK shape + biomarker apply service.** Adds the opt-in `supports_catalog_proposals` / `pull_catalog_proposals` hook pair on `BaseHealthProvider` (mirrors the `supports_X` / `pull_X` shape from workstreams B + E), the `CatalogProposal` Pydantic spec + four typed constructors (`biomarker_proposal`, `medication_proposal`, `concept_proposal`, `edge_proposal`) in `integrations.sdk.catalog`, and the matching `apply_proposal` write-side service. F.1 wires only the `biomarker` router end-to-end — it validates the payload via `BiomarkerCreate`, resolves the preferred unit + class concept, stamps TENANT scope + the owning user via `DEFAULT_CATALOG_POLICY.assign_create_scope`, tags an explicit `meta_data["_provenance"] = "integration"` marker (the model has no dedicated provenance column), and is idempotent on slug. The other three kinds raise `NotImplementedError` until F.2 lands. Engine wiring + per-sync cap land in F.3. No migration needed (F is stateless — proposals aren't persisted; only the resulting catalog rows are). Closes the `ConceptProvenance.INTEGRATION` gap from workstream F of the integrations follow-ups plan. Tests: 6 new in `tests/test_catalog_proposal_service.py` + 2 new contract tests in `tests/test_integration_registry.py` (hook defaults + re-export identity).

- **Docs: aligned public docs with the integrations cleanup pass + stack 1 (workstreams A, D, B, E).** Doc-only commit — no code, no test impact. Four public docs updated to reflect capabilities that shipped on this branch: (1) `docs/INTEGRATIONS_SDK.md` gained a new **§3.10 "Clinical Events & Examinations (Opt-in Write Hooks)"** section documenting the `supports_clinical_events` / `pull_clinical_events` and `supports_examinations` / `pull_examinations` opt-in pairs, the dedup contract (provider sets `external_id`; engine stamps `source_integration_id` from `integration.id`), what the engine does on the provider's behalf (resolves a service-context actor via `integration_actor.resolve_integration_actor`, writes through `clinical_event_service.create_event` / `examination_service.create_examination`), a "when to use which" decision table, and a pointer to the bridge provider as the reference implementation for examination creation. §4 "Building FHIR Observations" gained a **"Categorical / string values"** subsection documenting `ObservationBuilder.set_value_string` (the FHIR R4 §3.1.1 `value[x]` mutual-exclusion rule, the categorical-no-normalization invariant). Verified zero stale `fetch_json` references remain — `rg fetch_json docs/` returns only one match inside the STATUS.md bullet describing the removal itself. (2) `docs/INTEGRATIONS_FRAMEWORK.md` gained a new **highlight #10 "Integration-Driven Write Hooks (Clinical Events & Examinations)"** alongside the existing Tool Exposure (#7), Notification Exposure (#8), and Secret Encryption (#9) contracts. (3) `docs/STATUS.md` "Completed" section gained a new **"Integrations SDK follow-ups pass"** entry summarizing the four workstreams (A: SDK robustness / shared `_retry_request` / full-jitter backoff / `fetch_json` removal; D: `resolve_integration_actor` service helper; B: clinical-events opt-in hook + migration `b1l2i3n4t5ev`; E: `examination_service.create_examination` chokepoint + migration `e1x2a3m4i5n6` + bridge migration that fixes the `category_id` silent-drop bug). (4) `docs/API.md` `examinations` and `clinical-events` sections gained notes that the write paths delegate to the canonical services and that responses now expose optional `source_integration_id` / `external_id` fields populated only on integration-sourced rows; the `POST` rows' dedup semantics are clarified (UI heuristic vs integration-key). The broader **H (Docs restructure)** workstream — API-ref table for the SDK, reserved config keys section, notifications-module section — is intentionally deferred to a batch pass at the end of the follow-ups plan; this commit only addresses stale-correctness.

- **Integrations SDK (Workstream E.3 of follow-ups pass, plan: `dev/plans/integrations-sdk-followups-2026-07-21.md`): examinations opt-in hook + engine wiring.** Completes workstream E (the largest capability addition in stack 1). New SDK opt-in hooks on `BaseHealthProvider` (`integrations/sdk/base.py`) mirror the `supports_clinical_events` pattern shipped in B.2: `supports_examinations() -> bool` (default `False`) gates the pull, and `pull_examinations(integration) -> List[ExaminationCreate]` (default `[]`) returns the exams. `ExaminationCreate` is re-exported from `integrations.sdk` so providers can build payloads without reaching into `app.schemas`. Providers set `external_id` on the payload (the upstream system's stable encounter id); `source_integration_id` is supplied by the engine (= the integration's own id), not by the provider. Engine wiring in `integration_sync_service.run_sync`: after the clinical-events step, the engine probes `_opt_in(provider, "supports_examinations")`, and if True, calls `pull_examinations`, resolves a service-context actor via `integration_actor.resolve_integration_actor(db, integration)` (workstream D), and writes each exam via `examination_service.create_examination(db, actor, payload, source_integration_id=integration.id, external_id=payload.external_id)` (workstream E.1's dedup-aware write path that the bridge provider was migrated to in E.2). Per-exam failures are logged but don't abort the sync — mirrors the clinical-events and push hook resilience pattern. `records_synced` in the sync log now also counts exams written. Tests: 3 new — `test_run_sync_wires_examinations_opt_in_hook` (source-level guard that run_sync references the hook + the create_examination call), `test_sdk_base_declares_examinations_opt_in_hook_with_safe_defaults` (contract test for the False/[] defaults), `test_examination_create_is_reexported_from_sdk` (the SDK re-export aliases the schema, not duplicates it). 283 integration + SDK + bridge + examination-service + clinical-event + actor tests green; ruff reports zero new errors on touched files.

- **Integrations SDK (Workstream E.2 of follow-ups pass, plan: `dev/plans/integrations-sdk-followups-2026-07-21.md`): bridge provider migrates to the canonical `examination_service.create_examination` (fixes a latent category-silent-drop bug).** The bridge's `_process_and_save_sync_data` (`integrations/health_assistant_bridge/provider.py`) inlined ~80 LOC of dedup + ORM construction for examinations. That code had gone stale in three ways: (1) it set `category_id=` — a column that **no longer exists** on the live `ExaminationModel` (renamed to `category_concept_id` when categories moved into the unified taxonomy), so the bridge silently dropped the category on every exam it created; (2) it referenced the deleted `ExaminationCategory` model (categories are now `Concept` rows with the `examination_category` kind tag); (3) it pre-generated UUIDs with `uuid.uuid4()` instead of letting the model's `gen_random_uuid()` server default do its job. E.2 replaces the entire exam-processing block with a call to `examination_service.create_examination(db, actor, payload, source_integration_id=integration.id, external_id=client_exam.id)`. The bridge now resolves a service-context actor via workstream D's `resolve_integration_actor`, builds an `ExaminationCreate` payload (with `category` as text so the service can run `MedicalProcessingService.resolve_category` — the modern concept-resolution path), and passes the integration provenance through. The returned exam's id is then used when parsing the nested records. Org-name → organization resolution stays provider-side (the service doesn't manage organizations). Bonus refinement to `examination_service.create_examination`: the heuristic UI dedup path now fires **only for pure UI callers** (defined as "no integration provenance at all" — both `source_integration_id` and `external_id` unset). An integration-sourced exam, even one missing `external_id` because the upstream system exposes no stable ids, no longer risks accidentally matching an unrelated UI row that happens to share date + category + notes. Tests: 1 new in `tests/test_health_assistant_bridge.py` — `test_bridge_routes_examinations_through_canonical_service` is a source-level guard that the bridge calls `create_examination`, references `resolve_integration_actor`, and does NOT construct `ExaminationModel` directly, reference `ExaminationCategory`, or set the stale `category_id=` field (uses a negative-lookbehind regex so the live `category_concept_id=` doesn't trip the check). 132 bridge + examination-service + c2-layering tests green; ruff reports zero new errors on touched files (4 pre-existing in the bridge untouched).

- **Integrations SDK (Workstream E.1 of follow-ups pass, plan: `dev/plans/integrations-sdk-followups-2026-07-21.md`): new `examination_service.create_examination` chokepoint + integration-key dedup.** The `POST /examinations` write path was inlined entirely in the route handler at `backend/app/api/v1/endpoints/examinations.py:46-128` — patient validation, category resolution, dedup, ORM construction, doctor linking, commit, and relationship reload all lived in the endpoint. That left integrations (the bridge provider, the future `supports_examinations` SDK hook from E.3) no clean call site, so the bridge bypassed dedup entirely with its own direct-ORM write — and its code is **stale** (uses `category_id` instead of the live `category_concept_id`). New `backend/app/services/examination_service.py` exposes a single function, `create_examination(db, current_user, payload, *, source_integration_id=None, external_id=None)`, that owns the full pipeline. The endpoint becomes a thin HTTP adapter (4 lines of handler body) that maps `NotFoundError` → 404. The service preserves **both** dedup paths from the original endpoint: (1) the **integration-key dedup** (precise, opt-in via kwargs) — when both `source_integration_id` and `external_id` are supplied, the service looks up by `(tenant_id, patient_id, source_integration_id, external_id)` and returns the existing row as-is; (2) the **heuristic UI dedup** (fuzzy, default-on) — when `auto_extract_metadata` is False, the service looks up by `(tenant_id, patient_id, examination_date, category_concept_id, notes)` to catch accidental re-submissions; bulk-placeholder uploads (`auto_extract_metadata=True`) bypass the heuristic check, preserving the original endpoint's batch-create semantics. **Bonus latent-bug fix:** the `source_integration_id` and `external_id` fields exist on `ExaminationCreate` (and have for a while), but the old endpoint never forwarded them to the ORM constructor — UI callers that set them were silently ignored. The service now honors them, with the explicit kwarg winning over the payload field when both are present. New migration `e1x2a3m4i5n6` adds the matching **partial unique index** `uq_examination_integration_dedup` on `(tenant_id, patient_id, source_integration_id, external_id)` — fires only when all four columns are non-NULL, so UI rows bypass it and integration-sourced rows are deduped at the DB layer. Same pattern B.1 shipped on `clinical_events`; catches the race window between the SELECT and INSERT. The `MedicalProcessingService` import is **lazy** inside the function body to avoid a circular import (`app.ai.pipeline.service` → `app.workers.task_logger` → `app.workers.ai_tasks` → back); a comment explains why the endpoint module can get away with a top-level import (route modules load later) but a service module can't. Tests: 9 new in `tests/test_examination_service.py` — integration dedup hit (no new row queued), integration-key SELECT filters on all four fields (compiles to SQL and asserts each predicate present), integration dedup miss creates with provenance populated (asserts `source_integration_id`/`external_id`/`tenant_id`/`created_by` on the ORM instance), UI-only path skips the integration-key lookup (asserts no `source_integration_id =` predicate in any query), heuristic dedup returns existing when `auto_extract_metadata=False`, `auto_extract_metadata=True` bypasses heuristic dedup (guards the bulk-placeholder upload flow), missing patient raises `NotFoundError`, source-level guard that the model declares both columns, and a source-level guard that the endpoint delegates to the service (no direct `ExaminationModel(...)` construction or inline `resolve_category` in the route handler). 250 tests green (conftest applies the new migration cleanly); ruff reports zero new errors on touched files (2 pre-existing E402 in `examinations.py` untouched).

- **Integrations SDK (Workstream B.2 of follow-ups pass, plan: `dev/plans/integrations-sdk-followups-2026-07-21.md`): clinical-events opt-in hook + engine wiring.** The smallest of the new capability hooks — providers can now pull longitudinal clinical events (hospital admissions, chronic conditions, pregnancies, ...) alongside observations, and the engine writes them through the existing `clinical_event_service.create_event` (which gained dedup in B.1). New SDK opt-in hooks on `BaseHealthProvider` (`integrations/sdk/base.py`) mirror the `supports_notifications` / `supports_tools` pattern: `supports_clinical_events() -> bool` (default `False`) gates the pull, and `pull_clinical_events(integration) -> List[ClinicalEventCreate]` (default `[]`) returns the events. `ClinicalEventCreate` is re-exported from `integrations.sdk` so providers can build payloads without reaching into `app.schemas`. New optional `external_id` field on `ClinicalEventCreate` — providers set it to the upstream system's stable encounter/episode id; the engine reads it off the payload and passes it through to `create_event`'s dedup-aware kwarg path. `source_integration_id` is **not** on the schema — the engine always supplies it (= the integration's own id), so providers can't fake their provenance. Engine wiring in `integration_sync_service.run_sync`: after `apply_telemetry_split`, the engine probes `_opt_in(provider, "supports_clinical_events")`, and if True, calls `pull_clinical_events`, resolves a service-context actor via `integration_actor.resolve_integration_actor(db, integration)`, and writes each event via `clinical_event_service.create_event(db, actor, ev, source_integration_id=integration.id, external_id=ev.external_id)`. Per-event failures are logged but don't abort the sync (mirrors the push hook's resilience). The new generic `_opt_in(provider, hook_name)` helper centralizes the `getattr` / callable-check / exception-swallowing dance so the engine can grow future opt-in hooks (`supports_examinations`, `supports_documents`, ...) without each call site re-implementing it. `clinical_event_service.create_event` also got a small refactor: `external_id` is now read via `external_id or payload.external_id` (so the dedup key set by the provider on the payload is honored even when the engine forgets to forward it explicitly), and `event_data = payload.model_dump(exclude={..., "external_id"})` (since the field now exists on the schema, it must be excluded from the dump to avoid passing it twice to the ORM constructor). Tests: 5 new — `test_run_sync_wires_clinical_events_opt_in_hook` (source-level guard that run_sync references the hook + the create_event call with `source_integration_id=integration.id`), `test_opt_in_helper_handles_missing_method`, `test_opt_in_helper_swallows_exceptions_from_capability_probe`, `test_sdk_base_declares_clinical_events_opt_in_hook_with_safe_defaults` (contract test for the False/[] defaults + bodies), `test_clinical_event_create_is_reexported_from_sdk` (the SDK re-export aliases the schema, not duplicates it). 246 integration + SDK + clinical-event + bridge tests green; ruff reports zero new errors on touched files.

- **Integrations SDK (Workstream B.1 of follow-ups pass, plan: `dev/plans/integrations-sdk-followups-2026-07-21.md`): clinical-event integration-provenance columns + dedup in `create_event`.** Lays the DB + service foundation for workstream B.2 (the SDK opt-in hook that lets integrations pull clinical events). Without these columns, every sync would create duplicate events for the same upstream encounter. New alembic migration `b1l2i3n4t5ev` adds two nullable columns to `clinical_events`: `source_integration_id` (FK → `user_integrations.id`, `ON DELETE SET NULL`) and `external_id` (free-text — the upstream system's encounter id). Plus a **partial unique index** `uq_clinical_event_integration_dedup` on `(tenant_id, patient_id, source_integration_id, external_id)` that only fires when all four columns are non-NULL — UI-created events (both fields NULL) bypass it, integration-sourced events are deduped automatically at the DB layer. Mirrors the existing pattern on `examinations` (`examination_model.py:53-59`). The `ClinicalEvent` model declares the columns + `to_dict()` serializes them. `clinical_event_service.create_event` gained two new optional kwargs — `source_integration_id` and `external_id` — that trigger dedup when **both** are supplied: a new `_find_integration_event` helper runs a SELECT keyed on all four fields, and if a row exists it's returned as-is (no duplicate insert, no notification re-fire). UI callers leave both as `None` and get the original create-always behavior — the kwargs are not part of `ClinicalEventCreate`, so the wire schema is unchanged and interactive callers can't accidentally set them. The partial unique index also catches the race window between SELECT and INSERT at the DB layer (a concurrent sync attempt that wins the INSERT raises `IntegrityError` instead of double-inserting). Tests: 6 new in `tests/test_clinical_event_integration_dedup.py` — dedup hit returns existing row (asserts no new row queued), dedup SELECT filters on all four fields (compiles the query to SQL and asserts each predicate is present, catching a regression that drops one), dedup miss creates with provenance populated (asserts `source_integration_id`/`external_id`/`tenant_id`/`created_by` on the queued ORM instance), UI-caller path with only `external_id` set skips the lookup entirely (asserts zero execute calls), symmetric path with only `source_integration_id` set also skips lookup, and a source-level guard that the ORM model still declares both columns. 177 tests green (conftest applies the new migration cleanly); ruff clean on touched files.

- **Integrations SDK (Workstream D of follow-ups pass, plan: `dev/plans/integrations-sdk-followups-2026-07-21.md`): service-context actor resolver.** New `app/services/integration_actor.py` with one function: `resolve_integration_actor(db, integration) -> TokenData`. The clean service-layer functions an integration will want to call (`clinical_event_service.create_event`, the future `examination_service.create_examination`, etc.) all take `current_user: TokenData` — for RBAC (the `check_*_access` helpers compare against `current_user.tenant_id` / `current_user.role`) and for audit provenance (`AuditMixin.created_by` / `updated_by` populate from `current_user.user_id`). There's no interactive user when a Celery worker runs `sync_active_integrations`, when a webhook fires, or when a two-way API proxy handler runs. Today's escape hatch is the bridge provider bypassing the service layer entirely and writing ORM rows directly — losing dedup, category resolution, doctor linking, audit provenance, and tenant scoping (and its code is also stale: uses `category_id` instead of `category_concept_id`). `resolve_integration_actor` fixes this generically: it derives a `TokenData` from the `UserIntegration`'s owning user (`user_id` + `tenant_id` + `role` + `email`), so service-layer writes from integration contexts go through the exact same code path as interactive UI writes — same RBAC, same audit, same tenant scoping. The role is **re-fetched from `UserModel` on every call** so role changes (e.g. an ADMIN demoted to USER after the integration was connected) take effect on the next sync without re-issuing tokens. The tenant_id comes from the user row, not the integration row, defending against the owner having been moved to a new tenant after connecting the integration. **No service-account-style "integration user" is created** — the integration acts as its owner, with whatever role the owner has. Raises `NotFoundError` if the owner row is gone (hard delete / FK cascade) so the caller can surface it as an integration ERROR state rather than silently writing under a NULL identity. Foundational for workstreams B (Clinical Events hook), C (Documents hook), and E (Examinations hook) — all three need this helper to call their respective services from non-interactive contexts. 5 new tests in `tests/test_integration_actor.py`: happy-path field population (id/tenant/role/email/is_service_account), role re-fetch + user-tenant-wins-over-integration-tenant invariant, `NotFoundError` on owner deletion, defensive `NotFoundError` on malformed `user_id=None` integration row (and confirms no DB query is issued in that case), and a sanity check that the produced `TokenData` is shaped for `check_patient_access`. C2 layering suite (115 tests) green — the new service module imports only from `app.core.errors`, `app.models.*`, `app.schemas.*`, and `sqlalchemy` (no endpoint imports); ruff clean.

- **Integrations SDK (Workstream A.3 of follow-ups pass, plan: `dev/plans/integrations-sdk-followups-2026-07-21.md`): vestigial `fetch_json` removed from the SDK base.** `BaseHealthProvider.fetch_json(integration, url, headers=None, params=None, max_retries=3)` was a pre-`http_request` GET helper at `integrations/sdk/base.py:283-338` that inlined its own retry loop — different backoff base (`2 ** attempt`) from the canonical `http_request` (`1.0 * 2**n`), no jitter, and a misleading final exception (`raise IntegrationRateLimitError` on **all** exhaustion paths, including non-429 ones). It had **zero provider overrides or call sites** across `integrations/` and `backend/` (verified via `rg "fetch_json" integrations/ backend/` → only the declaration hit). After A.1 + A.2 extracted the shared `_retry_request` primitive, `fetch_json` was the only SDK HTTP surface not routing through it. **Removed outright** (greenfield — no compatibility shim needed). Providers that need a robust GET now call `await http_request(self._http_client, "GET", url, ...)` from `integrations.sdk`, getting full-jitter backoff, `Retry-After` honoring, and the correct exception type per status code. The `asyncio` and `.exceptions` imports that were only used by `fetch_json` are dropped too. Documentation: `docs/INTEGRATIONS_SDK.md` §3.1 + §3.3 rewritten to reference the shared helpers (`http_request` / `fhir_search` / `fhir_create` / `fhir_conditional_update`) and the full-jitter retry policy; the local integrations skill's codebase map, SDK-base description, pull-data example, §5.1 method table, §5.4 exception table, §9 checklist, §10 robustness note, and §13.1 debugging row all updated (8 `fetch_json` references → 0; verified via `rg`). Tests: 2 new in `tests/test_integration_registry.py` — `test_sdk_base_no_longer_declares_fetch_json` (asserts the method is gone from `BaseHealthProvider`) and `test_sdk_base_no_longer_imports_asyncio_or_legacy_exceptions` (source-level guard that the now-unused imports stay gone, catching a partial revert that leaves them dangling). 109 SDK + integration tests green; ruff clean on touched files.

- **Integrations SDK (Workstream A.2 of follow-ups pass, plan: `dev/plans/integrations-sdk-followups-2026-07-21.md`): shared retry helper for all SDK HTTP calls.** Extracted a single `_retry_request(do_request, *, url, method, max_retries)` primitive in `integrations/sdk/http.py` that owns the SDK's entire retry/backoff/jitter/status-mapping contract (network errors → retry then `IntegrationDataError`; 429 → retry honoring `Retry-After` then `IntegrationRateLimitError`; 5xx → retry then `IntegrationDataError`; 401/403 → immediate `IntegrationAuthError`; everything else returned to the caller). The three helpers that previously inlined their own copies of this loop — `http_request` (`integrations/sdk/http.py`), `fhir_create` + `fhir_conditional_update` (`integrations/sdk/fhir.py`), and the OAuth/DCR/token-refresh `_request_json` (`integrations/sdk/auth.py`) — now all delegate to it. Consequences: (1) **`_request_json` is retry-aware for the first time** — pre-refactor, OAuth discovery / Dynamic Client Registration / token exchange / token refresh were all single-shot, so a transient 429 or 503 on a hospital IdP's load balancer surfaced as an immediate `IntegrationDataError`/`IntegrationRateLimitError`; now those calls get the same full-jitter backoff as every other SDK HTTP call. (2) **All four helpers now share one jitter implementation** (the `_backoff_delay` introduced in A.1) — pre-refactor `fhir_create` and `fhir_conditional_update` carried their own copies of the no-jitter `backoff = 1.0; backoff *= 2` loop and had already diverged subtly (`fhir_create` had no logger warnings, `fhir_conditional_update` did). (3) **`fhir_conditional_update`'s 412 path is preserved** — the helper returns the final response and the caller still decides what to do with non-retryable non-2xx; 412 remains a tuple return, not a raise. Cleaned up unused imports left in the wake of the refactor (`asyncio`, `IntegrationAuthError`, `IntegrationRateLimitError` from `fhir.py`; `IntegrationRateLimitError` from `auth.py`). Tests: 5 new — `tests/test_sdk_auth.py` (`test_discover_smart_retries_5xx_via_shared_handler` proves the prior single-shot path now retries; `test_discover_smart_429_retries_then_raises_rate_limit` proves the same for 429 → `IntegrationRateLimitError`); `tests/test_sdk_fhir.py` (`test_fhir_helpers_route_through_shared_retry_request` is a source-level guard that re-inlining the retry loop in either helper would trip; `test_fhir_create_retries_5xx_then_raises_data_error` confirms the post-refactor retry count matches `max_retries`; `test_fhir_conditional_update_412_still_returns_tuple` pins the 412 tuple-return contract). 95 SDK + integration tests green; ruff reports zero new errors on touched files (2 pre-existing errors in `base.py` / `secrets.py` untouched).

- **Integrations SDK (Workstream A.1 of follow-ups pass, plan: `dev/plans/integrations-sdk-followups-2026-07-21.md`): full-jitter retry backoff + bounded default `max_pages`.** Two defects in the SDK's HTTP/FHIR layer, both with thundering-herd failure modes. (1) **No jitter anywhere.** Every retry wait was a deterministic `base * 2**attempt` — when a backend recovers from a transient outage, all integration instances retry on the exact same tick, restressing the server. Added `_backoff_delay(attempt, base=1.0)` in `integrations/sdk/http.py` using the **full-jitter** scheme (AWS Architecture Blog, "Exponential Backoff and Jitter"): `uniform(0, min(base * 2**attempt, 60.0))`. Each client now picks an independent random wait in its theoretical cap, so a retry wave spreads out instead of stampeding. The 60-second ceiling bounds the worst-case wait at high attempt counts. `http_request` now calls `_backoff_delay` on every retry path (network error, 5xx); the 429 path still prefers a server-provided `Retry-After` when present (the server knows its own load better than our heuristic). (2) **`paginate_bundle` and `fhir_search` defaulted to `max_pages=None` (unbounded).** An integration that forgot the argument could walk a multi-million-entry FHIR Bundle indefinitely. Both now default to `DEFAULT_MAX_PAGES = 100` (exported from `integrations.sdk.http`); callers can still pass `max_pages=None` for an explicit opt-out when genuinely unbounded iteration is needed. Tests: 7 new in `tests/test_sdk_http.py` — `_backoff_delay` returns values in `[0, base * 2**attempt]` (sampled 50× per attempt across 8 attempts), the 60s ceiling kicks in at high attempts, `base` scales the range, `http_request`'s retry sleeps are jittered (captured via `monkeypatch` on `asyncio.sleep`), `Retry-After` overrides jitter when present, the default `max_pages=100` cap actually halts self-linking Bundle iteration, and `max_pages=None` is still a working escape hatch. 40 SDK HTTP + FHIR tests green; ruff clean on touched files.

- **Agent skills + cross-skill drift cleanup (Phase 4 of integrations-SDK cleanup pass, plan: `dev/plans/integrations-sdk-cleanup-2026-07-21.md`).** Three local agent skills — the canonical reference for any agent working in the codebase — had drifted against the actual codebase. (1) `.opencode/skills/integrations/SKILL.md` §1 listed `health_connect/` as "empty placeholder — scaffold only" but that directory was removed long ago; §1 also omitted `sdk/notifications.py` (12 KB, exporting `NotificationSpec` / `NotificationSpecBuilder` / `NotificationAction` / `NotificationTypeSpec`); §1 documentation table and §10 gotcha #13 both referenced `docs/USER_INTEGRATIONS_PLAN.md` as "OUTDATED" but the file was deleted in commit `0b9951c` (2026-07-20); §8.3 route table was missing the three notification routes (`GET /notification-types`, `PUT /{domain}/notification-types/{type_id}`, `POST /{domain}/notification-action/{integration_id}/{action_id}`) that exist in the actual code at `backend/app/api/v1/endpoints/integrations.py:828,892,963`; §10 gotcha #12 mis-stated that `ObservationBuilder.build()` "strips tzinfo (naive UTC) for asyncpg" — the opposite of what the code does (the tz-fix regression test at `tests/test_sdk_observation_builder_tz.py:30` pins that the builder must keep tzinfo; the rationale is now corrected). (2) `.opencode/skills/project-overview/SKILL.md` carried the same two stale items (`health_connect/` directory listing + `USER_INTEGRATIONS_PLAN.md` doc row); fixed in place, also added the missing `fhir_server/` to the integration list. (3) `.opencode/skills/documentation/SKILL.md` listed the deleted `USER_INTEGRATIONS_PLAN.md` in its docs-categorization table; row removed. Verified `rg "USER_INTEGRATIONS_PLAN|health_connect" .opencode/ docs/` now returns zero matches. Doc-only commit — no test impact; all changes are inside `.opencode/skills/`.

- **Integrations SDK: legacy OAuth stubs removed from the core base (Phase 3 of cleanup pass, plan: `dev/plans/integrations-sdk-cleanup-2026-07-21.md`).** The minimal core base at `integrations/base.py` (the abstract class the registry discovers providers through) declared a legacy OAuth trio — `get_auth_url` / `exchange_token` / `refresh_access_token` — that predates the SDK's `begin_oauth` / `complete_oauth` pair and the `SmartOAuth` helper (`integrations/sdk/auth.py`). No engine path called them; they returned `""` / `{}` / `{}` silently, which invited new integrations to override the wrong shape (the SDK base at `integrations/sdk/base.py` formally declares the new pair as `raise NotImplementedError` defaults, but the core base's silent trio was a footgun for anyone reading the core abstract class directly). **Breaking change, by design** — greenfield codebase, no users, no out-of-tree overrides to preserve: all three methods are **removed outright** (not deprecated). Providers that need OAuth subclass the SDK base and use `SmartOAuth` for the SMART-on-FHIR round-trip. The `import warnings` that a more conservative earlier version of this change had added is also dropped. 2 new tests in `tests/test_integration_registry.py` (`test_core_base_no_longer_declares_legacy_oauth_stubs` asserts none of the three names exist on `BaseHealthProvider`; `test_core_base_does_not_import_warnings_module` guards against a partial revert leaving a dangling import). 5 registry tests green; ruff clean on the changed lines (3 pre-existing ruff errors untouched).

- **Integrations: webhook sync path now routes through the shared telemetry/FHIR split helper (Phase 2 of cleanup pass, plan: `dev/plans/integrations-sdk-cleanup-2026-07-21.md`).** The webhook handler at `POST /api/v1/integrations/{domain}/webhook/{integration_id}` (`backend/app/api/v1/endpoints/integrations.py`) used to inline its own copy of the telemetry-vs-FHIR routing loop that `apply_telemetry_split` (`backend/app/services/integration_sync_service.py`) already encapsulated. Two consequences: (1) the two copies had already **diverged subtly** (the webhook copy matched `"8867-4" in slug OR "heart-rate" in slug`; the canonical helper also matches `slug == "heart_rate"` and treats the slug-detection chain as `elif`, so a future policy change had to be made in two places); (2) the webhook path **never stamped `performer[0].reference`** with the integration id (the inlined performer block was missing the `"reference": "Integration/{id}"` field that the manual + background paths set), so observations ingested via webhook didn't link back to their owning instance in the details-endpoint UI (`integrations.py:503-506,545-548`); (3) `post_sync_notifications` was always called with `telemetry_persisted=0, fhir_persisted=count` because the webhook path didn't track the split — acknowledged in an inline comment. The webhook handler now delegates to `apply_telemetry_split` (the same helper `run_sync` uses for manual + background sync, audit A4), the inlined copy and the redundant `b_defs_map` fetch (the helper does its own) are removed, and `post_sync_notifications` now receives the **real** per-channel counts. Net effect: all three sync entry points (manual, background, webhook) now produce identical observation pipelines, telemetry-class webhook data (heart rate, steps, etc.) lands in the TimescaleDB hypertable instead of bloating `fhir_observations`, and webhook-driven integration notifications report correct per-channel counts. 3 new source-level parity tests in `tests/test_integration_sync_telemetry_split.py` guard against a revert: `test_webhook_endpoint_uses_apply_telemetry_split` (handler delegates to the helper), `test_webhook_endpoint_no_longer_inlines_telemetry_loop` (the `"calories" in slug` literal and the `TelemetryDataModel(` constructor must not reappear in the handler), and `test_webhook_endpoint_passes_real_per_channel_counts_to_notifications` (`fhir_persisted=len(fhir_records)` and `telemetry_persisted=len(telemetry_records)` literals must survive). 18 telemetry-split + integration tests green; ruff reports zero new errors on touched files (8 pre-existing errors in `integrations.py` untouched).

- **Integrations SDK (Phase 1 of cleanup pass, plan: `dev/plans/integrations-sdk-cleanup-2026-07-21.md`): categorical-observation fix + API-surface gaps.** Three small independent defects, each capable of silently dropping data or steering integration authors at the wrong pattern. (1) **Fix (silent data drop): `ObservationBuilder` now supports categorical / string values.** `set_value(value: float, ...)` was the only value-setter on the SDK builder (`integrations/sdk/observation_builder.py`), and `build()` always emitted a `valueQuantity` — never a `valueString` — so any integration that needed categorical data (lab PCR results, sleep stages, subjective pain scores, questionnaire answers) had no SDK path. The reference bridge integration worked around this by poking `obs_builder._data["value_string"] = ...` as a private attribute (`integrations/health_assistant_bridge/provider.py`), but `build()` never read `_data["value_string"]`, so the categorical value was **silently discarded** before reaching the FHIR validator — the bridge's "SARS-CoV-2 PCR: POSITIVE" records never landed. Added a public `set_value_string(value: str)` chainable method that mirrors FHIR R4 §3.1.1's `value[x]` mutual-exclusion rule (last setter wins; calling `set_value` after `set_value_string` clears the string slot, and vice versa), and updated `build()` to emit `value_string` (omitting `value_quantity`) when set. `raw_value` / `normalized_value` / `relative_score` are left unset for categoricals — they're numeric concepts and downstream analytics must not try to plot a string on a numeric axis. The bridge provider now uses the public method (the private-attribute poke is gone). New test file `tests/test_sdk_observation_builder_categorical.py` (7 tests) covers the basic shape, mutual-exclusion contract in both directions, the no-numeric-normalization invariant, a FHIR R4 round-trip through `assert_valid_fhir`, and a regression test that reproduces the old buggy bridge pattern to prove it would have lost data. (2) **Surface gap: `fhir_create` is now re-exported from `integrations.sdk`.** The helper (`integrations/sdk/fhir.py`) — FHIR `POST /{Resource}` with the same retry/auth/rate-limit contract as `fhir_conditional_update` — was implemented but absent from `integrations/sdk/__init__.py`'s imports and `__all__`, so provider authors reading its docstring naturally tried `from integrations.sdk import fhir_create` and hit `ImportError`. Added to both lists; new re-export smoke test plus three behavioral tests (201 returns the resource, 401 → `IntegrationAuthError`, Bearer header sent when `access_token=` passed) appended to `tests/test_sdk_fhir.py`. (3) **Dead code: `NotificationSpec.source_ref` no longer carries a dead `if False` branch.** The field was declared `field(default_factory=list) if False else field(default_factory=dict)` (`integrations/sdk/notifications.py`) — the list branch was unreachable, so the field was effectively `default_factory=dict`, but the construct read like a typo and invited a future "fix" that would revert to a list. New `tests/test_sdk_notifications.py` (4 tests) pins the `NotificationSpec` defaults (incl. the `source_ref == {}` contract), the `to_payload` action/block serialization, the fluent `NotificationSpecBuilder`, and the `NotificationTypeSpec` defaults/`to_dict`. 39 new + 74 existing integration tests green; ruff clean on all touched files (4 pre-existing ruff errors in `health_assistant_bridge/provider.py` untouched).

- **Docs: documentation audit + sweep (`dev/audits/AUDIT-DOCS-2026-07-20.md`).** A full read-only audit of every file under `docs/` + the root README + `docs/docs-tree.json` found every public doc carried at least one drift item; one cross-link was broken; two public docs were actively misleading; and three count errors propagated across multiple docs. All Critical/High findings fixed in this commit:
  - **Critical accuracy:** (1) `docs/MOBILE_SYNC_APP.md` was an **undisclosed proposal** that fabricated `POST /api/v1/wearable/data`, `wearable_service.py`, and `WearableSyncPayload`/`WearableDataPoint` as if they existed — none do. Added a prominent RFC banner at the top, marked every fabricated reference as proposed/not-implemented, and pointed readers at the actual mobile-bridge surface (`integrations/health_assistant_bridge/`). (2) `docs/TASK_DEBUGGING_GUIDE.md` listed **six log-stage names that don't exist in code** (only `ocr_start` is emitted), claimed a "100-char truncation" that isn't implemented, gave a wrong `_categorize_error` return list (omitted the real `unknown` fallback; added nonexistent `config`/`timeout`), and showed a cleanup-status list that mismatched both the beat task (`ai_tasks.py:338-345`) and the startup-time predicate (`main.py:77-83`). All four tables rewritten against the actual code.
  - **Skill update:** dropped the "H1 must start with `# Health Assistant — `" rule from `.opencode/skills/documentation/SKILL.md`. The product name is already implicit (the docs live inside the Health Assistant repo/site); repeating it in every page title is redundant. New rule: H1 is just the topic name.
  - **Cross-cutting counts:** the FHIR R4 resource count was wrong everywhere — README/ARCHITECTURE/FHIR_R4_FACADE said **18**, STATUS/EXPORT_IMPORT/API said **15**, but the actual `RESOURCE_REGISTRY` exposes **19** (`backend/app/facade/registry.py:220-412`). Everyone was missing `EpisodeOfCare` (projection from `ClinicalEvent` via `to_fhir_episode_of_care_dict()`). Updated all 6 docs + added an `EpisodeOfCare` row to FHIR_R4_FACADE.md's resource table + an `If-Match`/`412 Precondition Failed` bullet on the PUT row. Also fixed README's stale "830+ pytest tests" → **1800+**, and rewrote `SEEDING_AND_DEMOS.md`'s stage table with the real 11 stages in the right order (`concepts` first, not `medications`; adds `diseases` + `vaccines`), sweeping CLINICAL_EVENTS.md and INSTALL.md to match.
  - **`PROJECT_STRUCTURE.md` full rewrite** — the highest-drift doc in the repo. Added 4 missing backend `app/` subdirs (`ai/`, `catalogs/`, `facade/`, `instances/`); replaced the stale `processors/ # AI/NLP processing pipeline` line (AI pipeline migrated to `app/ai/`); listed all 21 frontend page dirs (was 8); listed all 18 component dirs (was 9); added the 3 missing integrations (`fhir_server/`, `mcp_client/`, `base.py`); corrected file counts (Python ~100+ → ~520; TS ~150+ → ~525; docs ~10 → 28); dropped the stale `TODO` row for the biomarker-clinical-event binding API (shipped at `clinical_events.py:69-351`).
  - **Per-doc fixes:** (a) `AI_SYSTEM.md` Step 2 pointed at a `task_types` list inside `get_config_summary` that was removed — corrected to point at the `TaskType` enum in `backend/app/ai/providers/enums.py` (single source of truth); added the missing 6th propose tool `propose_anatomy_graph_generation`; added `app/ai/agents/` to the component table; dropped hardcoded "v0.3.0+" version references. (b) `NOTIFICATION_SYSTEM.md`: fixed wrong path `ai/tools/hitl/hitl_proposals.py` → `ai/tools/hitl_proposals.py`; corrected broadcast role (was `ADMIN`/`MANAGER` — actually `ADMIN`+`SYSTEM_ADMIN` only); added an explicit note that `EMAIL`/`SMS` channels are reserved/future. (c) `TAXONOMY.md`: replaced the fictional migration id `c4d5e6f7a8b9` with the real archived chain + consolidated baseline; added missing `IMAGES` + `AFFECTS` to the relation enum list (was 23, actual is 25); updated endpoint resolver count `7/11 → 8/11`. (d) `TENANCY_AND_USER_MANAGEMENT.md`: replaced stale `alerts` cascade entries with `notification_rules` (`AlertModel` was removed); softened "home installations" audience copy. (e) `TELEMETRY_AND_AGGREGATION.md`: moved continuous aggregates + compression from "Future Considerations" to a new "Implemented optimizations" section (both shipped in the consolidated baseline `8ddb7ef7ca4d`); documented the `telemetry_hourly`/`telemetry_daily` cagg tables. (f) `ARCHITECTURE.md`: updated Zustand slice list (4 → 14); renamed "Universal Parsing" → "Format-Tolerant Parsing"; replaced "robust for external interoperability" → "suited for"; updated mobile-sync framing to point at the bridge integration. (g) `DEVELOPMENT.md`: replaced stale `processors/ # AI logic (OCR, NLP)` with the correct `ai/` location; removed the stale "WebSocket: polling" Known Issue (real-time WS shipped). (h) `INSTALL.md`: fixed the only broken cross-link in the docs tree (`[CHANGES.md]` → `[../CHANGELOG.md]`); corrected the documented `/health` response (handler always returns `redis: "not_configured"`); reworded "For home users"; updated seed-catalog prose to list the actual stages. (i) `CLINICAL_EVENTS.md`: corrected occurrence storage location (was "stored in `occurrences` JSONB column"; actually stored in the dedicated `clinical_event_occurrences` table with the JSONB as a legacy fallback); updated seed-stage references (`stage 2` → `stage 5`). (j) `INTEGRATIONS_FRAMEWORK.md`: corrected `integration_tool_aggregator.py` → `app/ai/tools/aggregator.py`. (k) `INTEGRATIONS_SDK.md`: tightened the "15 min beat" sentence to the real 60-second beat + 15-min per-integration `sync_interval`; replaced "robust APIs" → "external APIs"; renamed the JSON example "Universal Smartwatch Sync" → "Acme Smartwatch Sync"; dropped "Universal" + "Robust" from two headings. (l) `DEVELOPMENT_PLAN.md`: the two "Upcoming Feature" sections described work that has largely shipped — reframed as "Shipped: …" with links to the implementations, leaving only the genuinely-remaining items as TODOs.
  - **Internal cleanup:** deleted `docs/AI_PROVIDER_TESTS.md` (zero inbound links, half-dead content — frontend test file path didn't exist — and the worst positioning violation in the repo: "ready for production use with comprehensive test coverage"); deleted `docs/TASK_PROGRESS_INDICATOR.md` (zero inbound links; documented interface was a subset of the real 6-prop component, missing `TaskLogViewer` integration + `DocumentDetail.tsx` usage); tightened `docs/RELEASE_PROCESS.md` wording ("updates" → "stages" for `CHANGELOG.md`/`RELEASE_PROCESS.md`, since `version_manager.py` only *stages* those two, it doesn't write them).
  - **Vocab sweep:** removed the remaining 14 banned-positioning phrase occurrences (comprehensive ×5, robust ×4, Universal ×4, "production use" ×1) across SCREENSHOTS source (`gallery.mjs`), STATUS, TASK_DEBUGGING, ARCHITECTURE, INTEGRATIONS_SDK, SEEDING_AND_DEMOS. No hard-banned phrases (HIPAA/GDPR-compliant, clinical-grade, medical diagnosis, seamless, etc.) were found. `docs-tree.json` itself was already clean (valid JSON, all icons valid, all 24 SEO descriptions contain "self-hosted", no missing-file refs).

- **Docs: full rewrite of `docs/API.md` to match the actual backend (audit `dev/audits/AUDIT-API-2026-07-20.md`).** The public REST API reference documented only **~127 of 298** endpoint handlers (~43% coverage) and several documented routes were wrong. Fixed everything. **Critical path/method drift corrected:** (1) the "Wearable Data" section's `/api/v1/wearable/*` paths never existed — renamed to **Telemetry** at the real `/api/v1/telemetry/*` mount (4 routes); (2) the Tenants section listed `POST /tenants`, `PUT /tenants/{id}`, `DELETE /tenants/{id}` — none exist; tenant CRUD lives at `/admin/tenants`, the public `/tenants` router exposes only `GET /tenants`, `GET /tenants/{id}`, `PATCH /tenants/{id}`; (3) the Integrations section documented legacy paths (`/enable`, `/disable`, `/sync/{id}`) — replaced with the real `/{domain}/config-flow`, `DELETE /instance/{id}`, `POST /instance/{id}/sync` + the OAuth start/callback, instance details/debug-logs/toggle-debug/custom-action routes. **14 previously-missing modules added** (~171 endpoints): Examinations (11), Biomarkers full surface (15; only `/remap` was documented before), Medications (12), Allergies (10), Anatomy (16), Concepts + Concept-Edges (11), AI Configuration (21), AI Assistance (9), Admin Tenants (13), Admin Integrations (3), Settings (8), Doctors (5), Organizations (5), Patient Layout (5). **Partial-coverage gaps closed:** Documents (+7 routes: list, PATCH, edit, presign, preview-temp, dicom-metadata, DELETE), Clinical Events (+12 routes: CRUD, link-observation, occurrences, link/unlink-anatomy, insights, type biomarker binding), Analytics (+3: anomalies, available-categories, category rollup), Auth (+`POST /auth/service-account`), Catalogs (+`GET /catalogs/relation-types`), Import (+`GET /import/jobs` list), WebSockets (+`WS /ws/tasks`), Admin (+3 SYSTEM_ADMIN routes), Notification Rules (+`GET /{id}`), FHIR R4 facade (15 registered resource types enumerated + `If-Match`/412/`ETag`). **Error handling section rewritten** against the actual global handler shape (`{message, detail, correlation_id}`); status-code table gained `405`, `409`, `412`, `413`, `415`, `502`. **New "Quick start — common flows" section** with six end-to-end curl examples (login → list patients → read observations; upload → extract → poll; push subscription + WebSocket; biomarker threshold alert; enable integration + sync; export → re-import). **New table of contents** for navigation. Removed the duplicate "Get Current User" and "Create User" sections, the stale "Legacy Note", and the duplicate `alerts` stub. Audit `dev/audits/AUDIT-API-2026-07-20.md` records the full before/after coverage matrix.

- **Docs: kept `TASK_DEBUGGING.md` as a re-scoped internal reference, deleted `USER_INTEGRATIONS_PLAN.md`, rewrote `TASK_DEBUGGING_GUIDE.md` as a clean reference.** (1) `docs/USER_INTEGRATIONS_PLAN.md` — **deleted** (had been temporarily restored, then dropped on re-evaluation). The doc was actively misleading as a reference: the `UserIntegration` class definition used `provider` (actual column is `domain`), still said `OAuth Credentials (Should be encrypted in a production environment)` (they ARE encrypted via Fernet now — audit B1), the `BaseHealthProvider` interface showed methods that don't match the real SDK in [INTEGRATIONS_SDK.md](docs/INTEGRATIONS_SDK.md), and the 5 API endpoints it listed (`/integrations/{provider}/auth-url`, `POST /wearable/data`, etc.) propagated the **exact same path drifts** the rest of this CHANGELOG entry fixes in `API.md`. The valuable bits (Health Connect / HealthKit on-device push pattern, cloud-vs-device strategy) are already covered in [MOBILE_SYNC_APP.md](docs/MOBILE_SYNC_APP.md) and [INTEGRATIONS_FRAMEWORK.md](docs/INTEGRATIONS_FRAMEWORK.md). Git history preserves the original. (2) `docs/TASK_DEBUGGING.md` — **kept on disk** (re-scoped, not deleted). Unlike the integrations plan, its content (TaskLogger / TaskProgressTracker / TaskTimeoutMonitor class internals, structured-log JSON schema, error categories, monitoring metrics + alert thresholds) accurately matches the current `backend/app/workers/task_logger.py`. Now opens with a clear "Scope of this doc / Status" header marking it internal (not in docs-tree.json) and pointing at [TASK_DEBUGGING_GUIDE.md](docs/TASK_DEBUGGING_GUIDE.md) as the canonical operator-facing troubleshooting workflow. (3) Rewrote `docs/TASK_DEBUGGING_GUIDE.md` from a postmortem-style narrative (opened with a specific incident: "3 documents stuck for 632 minutes", named filenames) into a clean 6-step reference: background on the task lifecycle + the 10-min/15-min/20-min thresholds, verify worker is alive, verify AI config, read structured logs, retry via API/UI, direct DB inspection; plus a symptom→cause→fix table and a prevention checklist. Two renderable **Mermaid** diagrams replace ASCII art and prose: a `stateDiagram-v2` of the document/examination status machine (uploaded → processing → aggregating → analyzing_text → completed, with failed/retry transitions) and a `flowchart TD` triage decision-tree that routes a stuck task from symptom to the right step (config / file / provider-outage / timeout / unknown → retry or DB-inspect), matching the same Mermaid-first convention already applied to CI_CD_SETUP, NOTIFICATION_SYSTEM, and TELEMETRY_AND_AGGREGATION. Audit `dev/audits/AUDIT-API-2026-07-20.md` items F1/F2/F3 updated accordingly.

- **Docs: replaced ASCII flow diagrams with renderable Mermaid.** The three prose flow diagrams that still used box-drawing characters in code fences — the CI/CD 4-stage pipeline (`docs/CI_CD_SETUP.md`), the notification fan-out model (`docs/NOTIFICATION_SYSTEM.md`: `emit` → `Notification` → `NotificationRecipient` / `NotificationDelivery` → Redis/Celery), and the telemetry→FHIR patient attribution chain (`docs/TELEMETRY_AND_AGGREGATION.md`: `TelemetryDataModel.device_id` → `UserIntegration` → `Patient.user_id`) — are now ` ```mermaid ` flowcharts so they render natively on GitHub and the docs site instead of depending on monospace alignment. File-tree listings (PROJECT_STRUCTURE, EXPORT_IMPORT, FHIR_R4_FACADE) and the UI mockup in TASK_PROGRESS_INDICATOR are intentionally left as plain code blocks — Mermaid has no faithful tree/mockup primitive.

- **Docs: new `CLINICAL_EVENTS.md` reference for the clinical-events type blueprint + seed format.** Previously the `clinical_event_types` seed schema was only documented across scattered mentions in ARCHITECTURE.md, SEEDING_AND_DEMOS.md, and API.md — never as a single canonical reference. The new `docs/CLINICAL_EVENTS.md` covers: the two-layer model (type blueprint vs. instance), the seed JSON envelope with a worked example, every top-level field (slug / name / category_slug / description / icon / color / schedule_kind / metadata_schema + the journey-template fields), the four `schedule_kind` values (`state` / `range` / `recurring` / `point`) and how each renders on the calendar, the `metadata_schema` field-type reference (`text` / `number` / `date` / `boolean` / `catalog-select`) including the validation rules for catalog-select fields (`CatalogType` / `CatalogRelationType` / `concept_kind` constraint), the category system (`event_category` concepts, the always-present system `general-event` fallback), the journey-template analytics fields (`default_duration_days` / `phases` / `milestones` / `severity_scale`), the frontend localization pattern (`events.type.{slug}.*` / `events.category.{slug}.*` i18n keys with backend-string fallback), the bilingual search behavior (English source + current locale), a table of all 9 shipped types, and a step-by-step "add a new type" checklist. Registered in `docs-tree.json` under "Core Systems" with SEO metadata; cross-linked from ARCHITECTURE.md (clinical_event_types + episodes/occurrences lines), SEEDING_AND_DEMOS.md (stage 2 row), API.md (Clinical Events section header), and TAXONOMY.md (the `event_category` ConceptKind row).

- **Fix (documents): PDF / image preview no longer blocked by `X-Frame-Options: DENY`.** The document preview (`AuthenticatedPdf` / `PdfViewer`, rendered as a same-origin `<iframe src="/api/v1/documents/{id}/download?token=...">`) was rejected by the browser because `security_headers_middleware` (`app/main.py`) stamped `X-Frame-Options: DENY` on **every** response unconditionally, including the inline download the preview embeds. The middleware now uses `setdefault` so the `DENY` baseline is preserved for all routes while a route that legitimately needs to be framed can opt out. The document download endpoint (`GET /api/v1/documents/{id}/download`) sets `X-Frame-Options: SAMEORIGIN` **only when serving a file inline** (`should_serve_inline` is `True` — PDFs, images, text; active-content types like svg/html/xml/js stay DENY and are forced to `attachment` anyway, so no clickjacking surface is opened). New regression test asserts the middleware honors an explicit per-route override without dropping the other baseline headers. Audit A7 test still green (the `DENY` default is intact on a normal route).

- **Frontend: PWA-safe in-app detail overlays for picker cards, reusing each entity's source-of-truth view.** The "open" affordance on `InstanceCard` / `CatalogItemCard` (the compact peek cards under `InstancePicker` / `CatalogItemPicker` in `displayMode="cards"`) no longer navigates away — it opens an **in-app overlay** so the form/modal behind it is never interrupted and the standalone PWA never exits to a browser tab (the prior new-tab approach broke the PWA shell on iOS and re-bootstrapped the whole SPA for an internal route). The overlay renders the entity's **existing rich component**, not a stripped variant: a new per-type **detail registry** (`detailViewRegistry`, the overlay counterpart of the browse `viewRegistry`) lets `InstanceCard` dispatch by type — examinations reuse `ExaminationPreview` (full biomarkers/notes/impressions/diagnoses/documents); observations resolve to their biomarker definition and render the same tabs the BiomarkerDetail page uses (`BiomarkerTrendTab` chart + `BiomarkerInfoTab` + `BiomarkerHistoryTab`) plus the extracted `BiomarkerKpiStrip` (Latest Result / Clinical Reference / Avg / Total Records); clinical events reuse `ClinicalEventCard`. Catalog cards already used `CatalogItemInfo`. Unmapped observations fall back to the shared `BiomarkerResultCard`. **Stack-safe `Modal`**: the cards live inside form modals (e.g. `MedicationForm` in `MedicationModal`), so opening an overlay stacks two Modals — Escape now closes only the topmost (resolved by live DOM order of `[data-modal]` panels, robust against React's child-before-parent effect timing) and the focus-trap no-ops when focus is in a child modal; single-modal behavior is unchanged. **DRY extractions**: `BiomarkerResultCard` and `BiomarkerKpiStrip` lifted out of `ObservationView` / `BiomarkerDetail` (both now consume them), and a `useBiomarkerDetailData` hook encapsulates the biomarker+trend load. **Bug fix**: `computeStatus` (`biomarkerUtils`) and the `useBiomarkers` observations path now coerce the FHIR `interpretation` CodeableConcept to a string (`codeableText`) — `getObservation(id)` returns an object where the list endpoint returned a string, which crashed `.toLowerCase()` when opening an observation overlay. Tests: stack-safe `Modal` (single/nested Escape), `InstanceCard` registry-dispatch, and the existing biomarker suites; tsc + eslint clean.

- **Frontend: open-in-detail links on picker cards now open in a new tab (forms aren't interrupted).** `InstanceCard` and `CatalogItemCard` — the compact peek cards rendered under `InstancePicker` / `CatalogItemPicker` in `displayMode="cards"` — already exposed an "open in domain" / "open in catalog" button (the `ExternalLink` affordance), but it navigated **same-tab**, so clicking it from inside a form/modal/detail-editor abandoned the in-progress edit. Both cards now default to opening that link in a **new tab** (`target="_blank"` + `rel="noopener noreferrer"` to prevent tab-nabbing), via a new `openInNewTab?: boolean` prop (default `true`). The prop is threaded through `InstancePicker` and `CatalogItemPicker` and auto-forwarded by `InstanceField` (it spreads `...pickerProps`), so every layer is overridable while the default flows to all callers. This covers every form/edit surface that renders these cards with zero per-form edits: the Record New Medication form (selected medication + linked examination), `MedicationDetail` + `DocumentDetail` (editable examination links), `ExaminationEventModal` (associated events), and `ClinicalEventForm` (related examinations + biomarkers). The existing `InstanceCard` test was strengthened to assert the new-tab default (`target` + `rel`) plus a same-tab opt-out case. Frontend tsc + eslint clean; 151 instance/catalog tests green.

- **Frontend: rich info card for a selected catalog definition (`CatalogItemPicker.displayMode="cards"`).** `CatalogItemPicker` (the search-and-select for catalog items — medication / biomarker / allergy / vaccine / concept / anatomy definitions) previously rendered a picked item only as a compact chip (label + remove). It now mirrors `InstancePicker`'s `displayMode: 'chips' | 'cards'` (+ a `renderCardFooter` slot): in `cards` mode each selection renders a new **`CatalogItemCard`** — the catalog counterpart of `InstanceCard` — that fetches the definition via `/catalogs/{type}/{id}`, caches it by `type:id` for the session, and shows a compact card (type icon + type chip + name + scope badge + open-in-catalog link + remove) whose body is **metadata-driven** via the existing `useFieldDescriptors` / `TYPE_FIELDS` registry, so it auto-adapts to every catalog type: a picked medication surfaces indications / dosage / contraindications / side effects, an allergy shows description / typical reactions, etc. (richtext → one-line snippet, chips → semantic `ChipList`, with a `maxFields` cap; loading skeleton + unavailable-state fallback; `invalidateCatalogItemCard` for cache busting). The Record New Medication form now uses `displayMode="cards"` so the selected medication's info is visible at a glance instead of a bare chip — matching the per-record `InstanceCard` already shown for the form's linked-examination field. The catalog-type → icon map was extracted to a shared `catalogTypeIcons.ts` module (single source of truth) so the new card and the `CatalogItemInfo` pane never drift. Default stays `chips`, so no other `CatalogItemPicker` caller is affected. Frontend tsc + eslint clean.

- **Records: unified instance browser/picker + cross-record linking.** A reusable, registry-driven system for searching and selecting **patient-scoped record instances** (examinations, medications, observations, documents, clinical events, allergies, vaccines) — the patient-records counterpart of the catalog `CatalogItemPicker`. Plan: `dev/plans/instance-browser-unified-picker-2026-07-16.md`. **Backend:** a registry-driven dispatcher `GET /api/v1/instances/search?q=&types=&patient_id=&limit=` (`app/instances/` + `app/services/instance_search_service.py`) fans an `ILIKE` query out across the requested entity types, each search function tenant- + patient-scoped. Security is enforced **centrally in the endpoint** (never per-entity): tenant isolation on every query; `check_patient_access` when `patient_id` is given (404 cross-tenant, 403 for an unlinked `USER`); tenant-wide browse (no `patient_id`) is **ADMIN/SYSTEM_ADMIN only** — `USER` gets 403 (the picker always binds the current patient context, so this is defense-in-depth against enumeration). 14 backend tests incl. the mandatory security cases (USER tenant-wide 403, USER other-patient 403, cross-tenant 404, `q`<2→422, tenant isolation, patient + type filters). **Frontend:** generic, domain-agnostic primitives in `components/instances/` — `InstanceBrowser` (presentational list/grid over a uniform `InstanceRow` projection, sort/search-highlight/picker-mode/Load-more/keyboard-nav), `InstancePicker` (controlled input: debounced type-ahead + Browse button, single/multi, relation binding via the catalog `RelationTypeSelect` reused verbatim), `InstanceBrowseModal` (wraps the existing `useFilterState`/`FilterBar` filter engine + a patient-guard; uses the existing `MasterDetailLayout` shell with a preview pane — list left, generic `InstancePreview` right, rich text via `FormattedText`), `InstanceField`, and a runtime `instanceRegistry`. Seven per-entity adapters (`features/instances/adapters/`) supply fetch (service list + client `q`/patient normalization, since most instance services lack a search param), search (the unified dispatcher scoped to type), facets, and row/selection/route projection; `services/instanceSearchService.ts` wraps the endpoint. **Per-type browse views** (`features/instances/views/` + a `viewRegistry`) let each entity reuse its purpose-built UI instead of the generic list: examinations render the robust `ExaminationCard` list + `ExaminationPreview` master-detail; observations render a biomarker card grid (via `useBiomarkers` + `biomarkerUtils`, trends-style); other types fall back to the generic `InstanceBrowser` + `InstancePreview`. Views are decoupled from the data adapters (registered at app entry) to avoid import cycles. Built entirely on existing primitives — no new filter/modal logic reinvented. **Consolidation:** retired the two genuinely-duplicated bespoke selector modals (`ExaminationSelectorModal` + `ObservationSelectorModal`, each one caller, near-identical) by routing the clinical-event form through `InstanceBrowseModal` (−325 lines); the write-back `ExaminationEventModal` now uses it for selection too (keeping its diff-and-save + per-event reason inputs). **New linking affordances** (where the data model supports it): `DocumentDetail`'s examination link is now editable (link/change/clear via `PATCH /documents/{id}`, was read-only); `MedicationDetail` gained a per-record editable examination link (was set only implicitly at creation); `MedicationForm` gained an optional linked-examination at create/edit. Allergy/vaccine→event linking is deferred — the model has no such link table (events link only to exams/observations/anatomy), and the plan forbids speculative link tables. 38 frontend tests + the 14 backend tests; tsc + eslint + vite build green; ruff clean.

- **AI chat: voice input (speech-to-text) + capability-based model features.** Two coupled changes that rework how models declare what they can do and add hands-free chat input. **(1) Capability-based models:** replaced the implicit "every model is a chat model" assumption with an explicit capability set. A new `AIModelCapability` enum (`text` / `vision` / `audio_input`) is stored as a JSONB `capabilities` array on `AIModel` (migration `s1t2m3o4d5e6`, default `["text"]` so existing models keep their meaning). A single source-of-truth mapping (`app/ai/providers/capabilities.py` `required_capabilities_for_task`) declares what each task needs — `chat`→text, `ocr`→vision, `transcription`→audio_input, every other text-generation task→text — so the AI-config Models UI now shows **feature toggles** (Text is always on; Vision / Audio Input are opt-in chips with icons + colored badges on the list row). A model can be multimodal (e.g. `gpt-4o` = text+vision) or purpose-built (`whisper-1` = audio_input). The `transcription` task type was added to `TaskType`, surfaced in the config summary (`AIConfigSummary.transcription`) + a new `voice_input` workflow, so admins can create a `transcription` task assignment pointing at an `audio_input` model. **(2) Voice input:** a new `POST /ai-assistance/transcribe` endpoint accepts a compressed audio upload, resolves the `transcription` assignment via a new `AIProviderService.get_stt_target` (validates the resolved model advertises `audio_input`; rejects a misconfigured chat-only assignment with a clear guard message), and POSTs it to the provider's `/audio/transcriptions` (OpenAI-compatible) returning `{text}`. Audio is **ephemeral** — never persisted (it may contain PHI); the resulting text is prompt-guard-scanned for audit correlation and then placed in the chat input box for the user to review/edit before sending (it then flows through the normal chat pipeline). Size (20 MiB) + MIME guards at the trust boundary; backend `app/ai/assistance/stt.py` (`STTTarget`, `TranscriptionError`, `transcribe_audio`) + new config (`AI_STT_MAX_AUDIO_BYTES`, `AI_STT_TIMEOUT_SECONDS`, `OPENAI_STT_MODEL`). **Frontend:** `useVoiceRecorder` hook records Opus/16 kHz mono/24 kbps via `MediaRecorder` (~90 KB / 30s — tiny uploads) with a 60s hard cap; `ChatVoiceButton` is a **single unified control** handling both interaction models by gesture — tap (down→up < 250 ms) = toggle (tap to start, tap to stop), press & hold = push-to-talk — with a live elapsed timer and a clear "unsupported/permission-denied" path. Transcribed text appends into the editable input box (preserves typed draft + focuses for review). i18n added (en + el). 12 backend tests (`test_stt_capabilities.py`) cover the capability mapping, baseline inclusion, and STT-target resolution/enforcement; the squash-schema test was relaxed to allow a baseline + incremental follow-ups. Frontend tsc + eslint + build green.

- **AI chat: multimodal image support — ask questions with images.** The agentic chatbot (both the side-panel drawer and the full-screen `/ai-assistant` page) now accepts image attachments so users can ask about lab-report scans, photos, charts, or medication labels. **Backend:** a new modular `app/ai/assistance/attachments.py` is the single source of truth for image handling — an `AllowedImageMime` enum (JPEG/PNG/WEBP/GIF; SVG deliberately excluded as a script vector), `validate_image_data_url` / `validate_chat_images` (MIME whitelist + per-image `AI_CHAT_MAX_IMAGE_BYTES` 8 MiB + per-request `AI_CHAT_MAX_IMAGES` 4 count enforcement at the trust boundary, fail-fast `ImageValidationError` subclassing `ValueError` so it surfaces as a localized guard SSE message with no SDK leak), `build_multimodal_content` (emits the OpenAI vision content-block schema — plain `str` when no images, `[{"type":"text"},{"type":"image_url"}]` otherwise), and `has_images`. `AIAssistanceRequest.images: List[str]` (RFC 2397 data URLs) flows through the `/assist` + `/stream` endpoints; `AIAssistanceService.assist` validates images for the `chat` task only and threads them through `_chat_stream` / `_general_chat`. User messages now persist `{"text", "images": [...]}` in the `ChatMessage.content` JSONB, and `reconstruct_history` rebuilds multimodal `HumanMessage`s for past turns so vision context survives across turns. The chat system prompt gained an "IMAGE & VISION INPUT" rule block (examine images, transcribe values, never fabricate illegible numbers, route "look at my uploaded document" to `get_document_content`). Image bytes bypass the text prompt-injection guard (the HITL wall remains the structural defence for clinical writes). **Frontend:** modular, reusable pieces — `types/ai.ts` gained `ChatImageAttachment` (data URL), a `ChatAttachmentStatus` enum + `PendingChatAttachment` interface for composer state, and `images?` on `Message`; a new `useChatAttachments` hook centralizes client-side validation (mirrors the backend limits so rejected files are never base64-encoded) + `FileReader` data-URL encoding for three input vectors (pick, drag-drop onto the input bar, clipboard paste); `ChatAttachmentPicker` (paperclip button + hidden `<input type=file multiple>`) and `ChatAttachmentPreviewRail` (thumbnail chips with remove + encoding spinner) compose into the modernized input bar; `ChatMessageImages` renders galleries inside bubbles with count-adaptive layouts (single large tile / side-by-side / 1+2 / 2×2 grid with "+N" overflow) and a keyboard-navigable full-screen lightbox. The send button enables image-only sends ("what's this?"), and history reload maps persisted `images` back into the bubbles. 13 backend unit tests (`test_chat_attachments.py`); frontend tsc + eslint + vite build green.

- **Frontend: catalog list rows are now fully clickable + anatomy form unified with the Anatomy Explorer.** Three coupled UX/consistency improvements to the Catalogs workspace and the Anatomy page. (1) In `CatalogBrowser`, each list row and card was selectable only via the inner title/description button — the rest of the row (empty space, the badges column) did nothing. Selection moved to the whole `<li>`/card (cursor-pointer), while the inner interactive elements (class/scope badges, the external-domain link, the picker Add button) keep working independently via `stopPropagation`. (2) Catalogs > Anatomy's create/edit form previously fell back to the bare `GenericCatalogForm` (name + description only), unlike the Anatomy Explorer's `AnatomyStructureForm` (name, slug, category, SNOMED code, description). A new **`AnatomyForm`** is registered in the `CATALOG_FORMS` registry and is now the single shared field set used by **both** surfaces — `AnatomyStructureForm` was refactored to render it. It matches the patterns of the other catalog forms: the anatomy **class** is a `CatalogItemPicker` (`conceptKind="anatomy_class"`, single-select, bound to `class_concept_id`) instead of a legacy uppercase-enum dropdown, and **coding system + code** are two separate fields (`standard_system` select + `standard_code` text) like ConceptForm/BiomarkerForm. (3) Added an **"Open in catalog"** link from both the Anatomy detail card (`/catalogs?type=anatomy&item=<id>`) and the Explorer toolbar (`/catalogs?type=anatomy`) so a structure can be opened/edited in the full catalog workspace. Writes for anatomy now route through the `/anatomy` domain endpoint (not `/catalogs/anatomy`) via a new anatomy entry in the `writeTarget.ts` dispatcher (mirroring how `concept` writes go through `/concepts`), so the class concept is resolved + global-item RBAC enforced by the anatomy service; `buildWritePayload` gained an `anatomy` branch. Dropped the legacy `category` field from the `anatomyService` TS types (`AnatomyListParams`/`AnatomyStructureInput`/`AnatomyStructurePatch`) and the `list()` method — the modern field is `class_concept_id`/`class_concept_slug`; `AnatomySearchPopup` now converts its category filter to a class slug at the API boundary, and `bodyPartService` creates with `class_concept_slug: 'other'`. Frontend tsc + eslint clean.

- **Frontend: catalog Info tab modernized — registry-driven, type-aware structured detail.** The catalog workspace's Info tab (the preview of a selected biomarker/medication/allergy/anatomy/vaccine/concept) was a flat `Object.entries()` dump with mechanically-derived labels and `JSON.stringify` blobs for structured fields. It is now driven by a per-type discriminated-union field registry (`components/catalog/info/fieldRegistry.ts` + `useFieldDescriptors` hook) grouping each catalog type's fields into titled sections (Identity / Coding / Clinical / Safety / Reactions / Schedule / Targets / Presentation / …) rendered by a thin orchestrator. Specialized renderers replace the raw values: `CodeBadge` (LOINC/SNOMED/CVX/ATC/ICD-10/FMA code + system badge + external lookup + copy), `ChipList` (semantic pills for aliases/side-effects/reactions/targets/kinds), `BooleanPill` (flags), `EnumBadgeField` (allergy category, concept status with toned colors), `RefRangesField` (biomarker stratified ranges as a compact table **+ a number-line band**), `DoseScheduleField` (vaccine doses + interval chips), `ColorSwatchField` + `IconPreviewField` (concept color/icon, glyph tinted by the concept color). Six generic, app-wide-reusable primitives were lifted to `components/ui/` (CopyButton, ChipList, CodeBadge, BooleanPill, InfoSection, KeyValueGrid, RangeBar) so biomarker-detail / medication-detail / FHIR views can adopt them later. Interaction additions: a live in-preview **field filter**, a related-summary chip (jumps to the Relations tab), a "Needs attention" completeness badge (missing code/unit/category), Copy-JSON, keyboard shortcuts (`e` edit / `r` relations / `h` history / `Esc` clear, ignored while typing), and a type-icon empty state. i18n keys added to en + el (39). **Zero backend changes** — a Phase-0 payload check confirmed every field was already serialized by the `/catalogs` adapters (incl. expanded `preferred_unit_symbol` + `reference_ranges[]`). Also fixed a Rules-of-Hooks violation the filter introduced (hooks now run before the `if (!item)` early return; a regression test mounts null→item→null) and cleared the pre-existing `react-hooks/exhaustive-deps` warnings in `CatalogWorkspace`. `tsc` + eslint (0 errors) + 350 frontend tests green. Unit-system toggle deferred (payload lacks full Unit rows for client-side conversion); inline single-field edit deferred (the full Edit form + `e` shortcut already cover it). Plan: `dev/plans/catalog-info-preview-modernization-2026-07-16.md`.

- **Database: squashed the migration chain into a single consolidated baseline.** The `alembic/versions/` directory carried 40 incremental migrations (the prior `0001` squash + 39 follow-ups accumulated over ~3 months of refactoring), many create-then-modify-then-drop cycles on the same objects. These are now replaced by **one** deterministic baseline — `8ddb7ef7ca4d_consolidated_baseline.py` — that reproduces the full current schema from an empty database. It was generated via `alembic revision --autogenerate` against the ORM metadata, then hand-augmented with the pieces autogenerate cannot infer: `CREATE EXTENSION` for `pgcrypto`/`pg_trgm`/`timescaledb`; the trigram + FTS tsvector + JSONB GIN expression indexes powering catalog hybrid search; the per-tenant unique indexes on `slug`/`mrn` (`(col, COALESCE(tenant_id, <sentinel>))`), the `concept_edges` dedup unique, and the partial `users.is_service_account` index; the explicit `fk_*` constraint names; and the guarded TimescaleDB hypertable + compression/retention policies + hourly/daily continuous aggregates on `telemetry_data`. The 58 `create_table` blocks are topologically ordered and the `tenants↔users` FK cycle is broken by deferring `tenants.owner_id`; the downgrade is an order-independent `DROP TABLE ... CASCADE`. `UUIDMixin.id` now also declares `server_default=text("gen_random_uuid()")` so DB-level UUID defaults survive (autogenerate had dropped them because the model previously carried only a Python-side default). No in-place upgrade path — existing databases must be dropped and recreated (`alembic upgrade head`); acceptable since the project has no users yet. Verified by `pg_dump` schema diff (functionally equivalent; only cosmetic differences remain — enum value ordering, unique-constraint vs unique-index on one table), an upgrade→downgrade→upgrade round-trip, and the full suite (1975 passed). Tests that hardcoded the old `0001` path/revision (`test_squashed_initial_schema.py`, `test_softdelete_mixin_alignment.py`) were rewritten to validate the new baseline dynamically; `docs/TAXONOMY.md` updated.

- **Seeds (audit B9 / F3): default catalog ships reference ranges, incl. stratified examples.** `data/seeds/default_catalog.json` previously defined 10 biomarkers with **no ranges at all**. It now carries widely-published standard adult reference ranges (marked as illustrative defaults — method-/population-dependent, not clinically authoritative) for all 10, plus **stratified examples** that exercise the resolver: hemoglobin (♂ 135–175 / ♀ 120–155 g/L), HDL (♂ ≥1.0 / ♀ ≥1.2 mmol/L), creatinine (♂ 60–110 / ♀ 45–90 µmol/L), and TSH (pediatric 0–18y 0.7–5.7 vs adult 0.4–4.0 mIU/L). The import pipeline now supports them: `BiomarkerCreate` carries `reference_ranges`, and `CatalogImportService` upserts them **idempotently** by stratification key (sex/age-window/unit) — re-seeding updates bounds in place without duplicating, and deliberately does NOT delete user-added ranges (a clinician's custom rows survive re-seed). Verified end-to-end: a fresh seed produces the expected rows; the dev DB was re-seeded. 3 regression tests (`test_audit_b9_seed_reference_ranges.py`) cover seed validity, range creation, and idempotent upsert + user-range preservation.

- **Frontend (audit B9 / F3): reference ranges editable on create + clearer editor layout.** Two fixes to the stratified-range editor in the biomarker form: (1) it was hidden on first creation (ranges need a parent id, which doesn't exist until the biomarker is saved). The editor is now a **pure draft editor** (stores ranges in the form's draft via `onChange`, makes no API calls itself), so it works identically in create and edit mode; the catalog workspace reconciles the draft against the server after the biomarker is saved via a new `biomarkerService.syncReferenceRanges` helper (POST new / PUT changed / DELETE removed / skip unchanged). (2) The cramped 7-column grid with detached labels was unreadable — each range is now a labelled card (Sex / Age min / Age max / Low / High / Unit) with uppercase field labels and a remove control. Frontend tsc + eslint + 245 tests green.

- **Fix (frontend): typing in a form modal no longer loses focus.** Every entity form (biomarker, patient, doctor, hospital, etc.) lost input focus on each keystroke — after typing one character the caret jumped out of the field. Root cause: `Modal`'s focus-management effect depended on `onClose`, and parents pass an inline `onClose` (`() => setOpen(false)`) whose identity changes every render. Each keystroke re-rendered the parent → the effect tore down (its cleanup restores focus to the pre-modal element, yanking the caret out) and re-ran (re-focusing the first field). Fixed by reading `onClose` through a ref so the Escape handler stays current while the effect is gated on `isOpen` only — focus is now captured once on open and restored once on close. Frontend tsc + eslint + 245 tests green.

- **Frontend (audit B9 / F3): stratified reference-range editor in the biomarker form.** The catalog > biomarkers > create/edit form now supports multiple demographic-specific ranges. A new `ReferenceRangesEditor` (repeating rows: sex / age window / unit / low / high) is shown in **edit mode** and manages ranges via the nested CRUD endpoints (POST on add, PUT on blur, DELETE on remove) — the server stays the source of truth, complementing the draft-based core fields that save through the generic catalog endpoint. Create mode shows a "save the biomarker first" hint (ranges need a parent id). The existing single "Ref. min/max" stays as the resolver's default/fallback range. Backend: the `/catalogs/biomarker` adapter now serializes each item's `reference_ranges` so the form has them on open. New types (`BiomarkerReferenceRange`) + service methods (`list/create/update/deleteReferenceRange`). 1 new backend test for the adapter serialization; frontend `tsc` + eslint clean.

- **Data architecture (audit B9 / F3): reference-range CRUD API + DB-driven `/reference-ranges`.** Completes the stratified-ranges feature with the missing management surface: nested REST endpoints `GET/POST /biomarkers/{biomarker_id}/reference-ranges` and `PUT/DELETE /biomarkers/{biomarker_id}/reference-ranges/{range_id}` (Pydantic `BiomarkerReferenceRangeCreate/Update/Response` with `low ≤ high` + sane age-window validation). Access is **inherited from the parent biomarker** — reads require the parent to be visible (global+tenant), writes are gated by `DEFAULT_CATALOG_POLICY.check_modify` on the parent's scope/ownership (a USER cannot manage ranges for a system biomarker → 403). `BiomarkerResponse` now serializes each biomarker's `reference_ranges`. Retired the hardcoded `GET /analytics/reference-ranges` dict (a parallel source of truth that drifted from the catalog) — it now reads the caller-visible biomarker catalog, and the dead frontend `getReferenceRanges` wrapper (never imported anywhere) was deleted. 7 endpoint tests (`test_audit_b9_reference_ranges_api.py`) cover the CRUD cycle, 404s, validation, the inherited-access model, the response shape, and the DB-driven ranges endpoint.

- **Data architecture (audit B9 / F3): stratified biomarker reference ranges.** `BiomarkerDefinition` carried a single global `reference_range_min`/`max`, which made `relative_score` and the status badge unreliable for anyone outside the "default" demographic (a range normal for a 60-year-old female is not normal for a 25-year-old male). Added a child table **`biomarker_reference_ranges`** holding 0..* ranges each scoped by `sex` / `age_min`-`age_max` / `unit_id` (mirroring FHIR `Observation.referenceRange` with `appliesTo` + `age`), plus a specificity-ranked resolver in **`app/services/reference_ranges.py`** (`resolve_reference_range` / `pick_reference_range` / `resolve_for_patient`) that picks the most-specific applicable row for a patient and falls back to the legacy global range when none matches — so a row constraining a dimension only applies when the patient's value for that dimension is *known* (an unknown age/sex/unit can't be assumed to fall inside a stratified window). Migration `r8b9c0d1e2f3` creates the table (with `low ≤ high` + sane age-window `CHECK` constraints, `ON DELETE CASCADE` from the biomarker) and backfills one catch-all row per biomarker from its legacy bounds. Wired into `analytics_service.get_biomarker_trends` — the user-visible status/range source now resolves by the patient's sex/age instead of the single global range. A shared `compute_relative_score` helper dedupes the scoring math. The legacy columns are retained (display sites + resolver fallback). 15 regression tests (`test_audit_b9_reference_ranges.py`) cover the model/constraints, the resolver's specificity + fallback semantics, DB integration via `resolve_for_patient`, CHECK-constraint enforcement, and CASCADE on biomarker delete.

- **Backend structure (audit C2): fixed the service→router layer inversion.** `clinical_event_service` imported the `check_*_access` helpers from `app.api.v1.endpoints.utils`, coupling the service layer to the router (and to `HTTPException`) — so the service couldn't be reused from Celery, the importer, or the FHIR facade without dragging in Starlette. The seven canonical access helpers (`check_patient/examination/medication/immunization/event/allergy/observation_access`) were moved to **`app/services/access.py`** (they already raise domain exceptions `NotFoundError`/`AuthorizationError`/`ValidationError` post-C1, mapped to HTTP status by the global `DomainError` handler — so the same code is now callable from any layer). The router-layer `endpoints/utils.py` was **deleted**; all 13 importers (11 endpoint modules, the service, and 2 tests) now import from `app.services.access`. The 4 `patch()` targets in `test_examinations.py` were updated to the new module. A new parametrized hygiene suite (`test_audit_c2_layering.py`, 109 checks) forbids any module under `app/services`, `app/ai`, `app/workers`, or `app/facade` from importing `app.api.v1.endpoints`, and pins that `clinical_event_service` and the endpoint modules resolve the helpers from the access module. Suite green (1938; the single unrelated planner-stats test is pre-existing).

- **Data architecture (audit B4): dropped the dead `VersionedMixin.is_current` column.** The audit flagged `VersionedMixin` as "dead data," but on inspection the claim was only half-true: `version` is a *live* feature (the FHIR R4 facade bumps it on every update, reads it for `If-Match` optimistic locking → HTTP 412, and exposes it in the `ETag` header — `test_fhir_r4_versioning.py`), while `is_current` was genuinely never queried anywhere in the codebase. Migration `q7a8b9c0d1e2` drops `is_current` from all 18 tables that carried it (leaving `version` intact); `VersionedMixin` now declares only `version`. The now-unused `Boolean` import and the stale `CatalogItemInfo.tsx` / catalog-adapter readonly-field references were cleaned up. Breaking change is acceptable (no users). 8 new regression tests (`test_audit_b4_versioned_mixin.py`) pin that `is_current` stays gone while `version` survives.

- **Backend structure (audit C9): dropped the stale `_db` filename suffixes.** `documents_db.py` / `document_service_db.py` were named after a long-deleted non-DB predecessor, implying an alternative implementation that no longer exists. Renamed to `documents.py` / `document_service.py` (`git mv` to preserve history); all import paths in `app/` + `tests/` updated. No behaviour change. Suite green.

- **Docs: aligned documentation with the 2026-07-15 audit changes.** `docs/ARCHITECTURE.md` documents the `fhir_observations` `patient_id` FK (`ON DELETE CASCADE`) and `document_id` UUID FK (`ON DELETE SET NULL`) (B2/B3). `docs/STATUS.md` gains a "Full-project audit (2026-07-15)" bullet. `docs/API.md`: the `/auth/refresh` response now shows the **rotated** `refresh_token` (A5) + new `POST /auth/logout` and `/auth/logout-all` endpoints; the `429` row is no longer "future" (A2 rate limiting is live); the auth endpoint summary updated. `docs/INSTALL.md` production checklist: rate limiting is built-in (A2) and baseline security headers are automatic (A7). `docs/AI_SYSTEM.md`: `INTEGRATION_SECRET_KEY` is fail-closed in prod (A11), not plaintext-with-warning. Agent skills (`.opencode/skills/`, local) corrected: `clinical-data` (Observation `patient_id` FK, B1 per-tenant unique pattern, B3 facade `?patient=` fix), `backend` (`core/errors.py` C1, `core/converters.py` C8), `project-overview` (D1 + SECRET_KEY gotchas).

- **Backend polish (audits C7 + C10 + B13; B12 verified resolved).** (C7) `SECRET_KEY` and `INTEGRATION_SECRET_KEY` are now proper Pydantic fields (`Optional[str] = None`) instead of `os.getenv(...)` baked in at class-definition time — the latter bypassed Pydantic and made the prod-guard validators untestable (the codebase's own comment flagged this exact pattern for VAPID). The existing dev-ephemeral/prod-refuse validators now work against a real field and accept test overrides. (C10) `process_document_sync` (the FastAPI `BackgroundTasks` OCR fallback when Celery/Redis is down) gained a docstring explaining why a synchronous direct-call path exists and that the name is intentional. (B13) Migration `o6f7a8b9c0d1` adds the `(session_id, created_at)` composite index on `chat_messages` (the chat-session load path is always `WHERE session_id = ? ORDER BY created_at`); the `AuditMixin.created_by`/`updated_by` columns were evaluated as **not worth indexing** (provenance columns, never a `WHERE` predicate → a ~20-table index migration would add write cost with no query benefit). (B12) the `medicationintent` enum case-mismatch is already resolved — the model's `values_callable` emits lowercase labels matching the DB type, confirmed via `alembic check` (no drift); the historical migration's uppercase column declaration is immutable history. Suite green (1822).

- **Backend structure (audit C8): de-duplicated `_uuid()`/`_now()`/`_parse_dt()`.** These three conversion helpers were re-implemented in five service modules (notification_service, notification_targets, notification_rule_service, export_service, import_service, integration_sync_service) — near-identical bodies differing only in which exceptions they caught. Extracted to `app/core/converters.py` as `to_uuid` / `utcnow` / `parse_dt`; each module now imports the shared implementation (aliased back to its private `_uuid`/`_now`/`_parse_dt` name so no call site changes). Behaviour is preserved exactly; the now-unused `datetime`/`timezone` imports were dropped. Suite green (1822).

- **Backend structure (audit C1): domain exception hierarchy + handler.** The service layer had only ~4 domain exceptions and 148 broad `except Exception` blocks; unknown DB errors were too often surfaced as `HTTPException(400, detail=str(e))`. Added `app/core/errors.py` — `DomainError` (base, 500) + `NotFoundError` (404) / `AuthorizationError` (403) / `ValidationError` (400) / `ConflictError` (409) / `ConcurrencyError` (409) — each carrying a safe, client-facing `detail` and a `status_code`. `main.py` registers a single `DomainError` handler that maps each to its HTTP status (logged at INFO as an expected client error, no correlation-id/stack leak). Migrated the canonical access-check helpers (`endpoints/utils.py` `check_patient/examination/medication/immunization/event/allergy/observation_access`) to raise these instead of `HTTPException` (same 404/403/400 contract via the handler), and updated `clinical_event_service` to catch `DomainError` — a first step toward the audit's C2 layer-inversion fix (services will catch domain exceptions, not `HTTPException`). `check_observation_access` now uses the new B3 `patient_id` column (with `subject` fallback). 3 regression tests (`test_audit_c1_domain_errors.py`) cover the hierarchy status codes, the handler mapping, and that the access helpers raise the right domain exception.

- **Data architecture (audit B3): typed `patient_id` FK on Observation + DiagnosticReport.** The patient lived only inside the FHIR `subject` JSONB (`{"reference":"Patient/<uuid>"}`) on `fhir_observations` and `fhir_diagnostic_reports` — forfeiting referential integrity, with no `ON DELETE CASCADE`, and forcing a JSON parse on every per-patient read. `Medication` already had a real `patient_id` FK; this applies the same pattern to the two remaining patient-owned clinical tables. Migration `n5e6f7a8b9c0` adds a maintained `patient_id UUID` column (FK → `fhir_patients.id` `ON DELETE CASCADE`, indexed) to both, backfilled from `subject` only where the referenced patient still exists (dangling `Patient/unknown` / deleted-patient refs stay NULL — the nullable FK permits it). All application write paths now keep `patient_id` in sync with `subject` via a shared `coerce_patient_id` helper (`fhir_service.create_observation`/`create_diagnostic_report`, the OCR pipeline, the import upserters, the telemetry→FHIR backfill worker, and the integration sync sites). `subject` remains the FHIR serialization projection; `patient_id` is the relational source of truth for joins/cascade/scoping. **Bonus bug fix:** the FHIR R4 facade `?patient=` search param now actually filters Observation/DiagnosticReport — `facade/crud._build_resource_filter` keys on `hasattr(model, "patient_id")`, which was a silent no-op before the column existed (the filter returned `None` and was dropped, so `GET /fhir/R4/Observation?patient=<id>` returned every tenant observation). 5 regression tests (`test_audit_b3_patient_id_fk.py`) prove the columns + CASCADE FKs, that both create paths derive `patient_id`, that a patient delete cascades, and the helper handles every reference shape.

- **Data architecture (audit B2): `Observation.document_id` is now a real UUID FK.** It was a free-text `String` that stored a document UUID as text, so deleting a `DocumentModel` left orphan observations with stale references and service code had to compare with `str(obs.document_id) == str(d.id)` because the types never lined up. Migration `m4d5e6f7a8b9` widens the column to `UUID`, nulls malformed/orphan values first (so the upgrade can't fail on a populated DB), and adds `FOREIGN KEY ... REFERENCES documents(id) ON DELETE SET NULL` — deleting a document now nulls the link instead of dangling it. All write/read paths updated to pass/compare `UUID` objects: `fhir_service.create_observation` (validates the UUID, drops malformed), `ai.pipeline.persistence` (`find_source_doc` returns the UUID; `save_observation` typed `Optional[UUID]`), `analytics_service` (removed the `cast(id, String)` and `str()`-compare smell — now plain `UUID == UUID`), `document_service_db` (UUID lookup + `::uuid`-cast raw SQL). Schema field `Optional[UUID]`. 3 regression tests (`test_audit_b2_document_id_fk.py`) prove the column type, FK rejection of a non-existent document, and the on-delete SET NULL cascade.

- **Data architecture (audit B1): per-tenant uniqueness for catalog/identity columns.** `slug`/`mrn` were `UNIQUE` globally on four tables even though `tenant_id` is nullable — so a system-wide (NULL tenant) row and a tenant override could not share a slug, and two tenants could not each carry a patient with the same MRN. Replaced the global unique with a `(col, COALESCE(tenant_id, <sentinel>))` unique index (the pattern already proven on `concepts`, migration `9a3f7c2e1b4d`) on `biomarker_definitions.slug`, `anatomy_structures.slug`, `clinical_event_types.slug`, and `fhir_patients.mrn` (migration `l3c4d5e6f7a8`). NULL-tenant (system) rows collapse into one synthetic tenant so two system rows still collide, while a system row and a tenant row — or two tenant rows — no longer do. **Deliberately excluded:** `users.email` (remains globally unique — it is the login identifier and `get_user_by_email` uses `scalar_one_or_none()`; the model is one-tenant-per-user) and `units.symbol` (no `tenant_id` column — units are a universal reference catalog). The global unique was strictly stricter than per-tenant, so the upgrade cannot fail on a populated DB. 3 regression tests (`test_audit_b1_tenant_unique.py`).

- **Data integrity (audit B10): added the obvious CHECK constraints.** The schema had exactly one `CHECK` (Patient `mrn_not_empty`); the rest of the domain invariants were enforced only by convention / application code, so a bad seed or a stray write silently stored unusable rows. Added 8 constraints (declared on the models' `__table_args__` + migration `k2b3c4d5e6f7`): `units.conversion_multiplier > 0`; `biomarker_definitions.reference_range_min <= reference_range_max` (NULLs allowed — an inverted range yields a nonsensical `relative_score`); `examinations.extraction_progress` / `documents.progress` / `export_jobs.progress` / `import_jobs.progress` all in `[0, 100]`; `ai_models.max_tokens > 0` and `ai_models.temperature BETWEEN 0 AND 2`. The migration clamps any pre-existing rows that would violate the new constraints before adding them (defensive — the project has no users, but a dev DB may carry seed data). 4 regression tests (`test_audit_b10_check_constraints.py`) assert the models declare the constraints, the DB has them, and enforcement rejects bad rows while accepting good ones.

- **Security (audit D1, frontend): logout no longer leaves cached PHI in IndexedDB.** The logout wipe in `utils/auth.ts` deleted an IndexedDB named `health_assistant-offline` (underscore) while the Dexie store in `services/db.ts` opens `health-assistant-offline` (hyphen), so the offline cache (cached patient data, biomarker trends, the pending-sync queue) survived across user switches on any browser where the `indexedDB.databases()` bulk-delete fallback is unavailable (Firefox/Safari — Chromium-only API). The DB name is now a single exported constant (`OFFLINE_DB_NAME`) in `db.ts`, imported by the logout wipe, so the two can never drift again. New `performTenantSwitch` regression test (below).
- **Fix (audit D5, frontend): `performTenantSwitch` no longer fires the token callback twice.** A duplicated `onTokensUpdated(...)` call (+ duplicate comment) meant the token setter ran twice on every tenant switch — harmless today but a real latent defect (any side-effect in the callback would double-apply). Removed the duplicate; new `performTenantSwitch.test.ts` pins that the callback fires exactly once.

- **Cleanup (audit E1–E6): deleted 5 dead files + retired the stub-`User` test mock.** Removed `app/models/tenant.py`, `app/models/document.py` (zero-reference non-SQLAlchemy stub classes whose real models are `*_model.py`), the merge artifacts `app/models/enums.py.orig` / `enums.py.patch`, and the placeholder `app/services/base.py`. The `app/models/user.py` stub `User` was only ever a 4-field mock shell used by `test_auth.py`/`test_users.py`; both now construct a transient real `UserModel` (with proper `Role` enum + `UUID` `tenant_id`) so they exercise the actual model instead of validating nothing. Zero behaviour change; 1805-test collection and the two affected suites are green.

- **Security (audit A1, critical): closed a second-order SQL-injection in the telemetry analytics path.** The biomarker `slug` is f-string-interpolated into raw SQL in `analytics_service.get_biomarker_trends` (`CAST(data->>'{slug}' AS FLOAT)` / `data ? '{slug}'`). Because `slug` is a user-writable string (any authenticated `USER` can `POST /biomarkers/` with `is_telemetry=true`), a crafted slug could exfiltrate cross-tenant data via UNION/error channels. Fixed with three layers of defence: (1) the `BiomarkerCreate` Pydantic schema now validates `slug` against `^[A-Za-z0-9_-]{1,80}$` on the API write path (input-only — the response schema still serialises any pre-existing row); (2) `analytics_service` now refuses to interpolate any slug that is not a strict identifier — unsafe slugs are skipped with a warning rather than interpolated (defence-in-depth, catches pre-existing rows or AI-pipeline/import writes that bypass the schema); (3) the AI ontology pipeline sanitises its AI-proposed slugs (`sanitize_slug`) before any DB lookup/insert so auto-expanded catalog entries are always safe. New helpers `is_safe_slug` / `sanitize_slug` in `schemas/biomarker.py`. 26 new regression tests (`test_audit_a1_slug_injection.py`) including an end-to-end test asserting an injection-payload slug never reaches the executed SQL.

- **Security (audit A6): endpoints no longer leak internal exception detail via `detail=str(e)`.** Broad `except Exception` blocks in the documents, import, AI-assistance, biomarker, and admin endpoints forwarded raw exception messages to clients (DB constraint names, file paths, PHI). Now 500-class errors re-raise so the global handler returns a generic 500 + correlation id; 400-class errors log server-side and return a generic message. Document upload no longer echoes the caller's `tenant_id` in the "Tenant not found" message. 10 regression tests (`test_audit_a6_error_leak.py`) statically assert the previously-leaky endpoints never embed `str(e)` in client detail.

- **Security (audit A3 + A4): upload file-type validation + inline-download XSS hardening + upload-size enforcement.** The document upload path accepted any file type and served downloads `inline` with no `nosniff`, so an uploaded `.svg`/`.html` ran as stored XSS at the app origin; `MAX_UPLOAD_SIZE` (50 MB) was defined but never checked, so a single request could exhaust server RAM. Fixed: (1) uploads are now restricted to a medical-document allowlist (PDF/images/DICOM/text — deliberately excluding `svg`/`html`/`xml`/`js` active-content types); (2) the download endpoint forces `Content-Disposition: attachment` for active-content extensions and sets `X-Content-Type-Options: nosniff` on every served file (defence-in-depth even if a bad type slipped past the allowlist); (3) uploads are read in 1 MiB chunks and rejected with `413` if they exceed the cap (`_read_capped`). 39 regression tests (`test_audit_a3_a4_upload_security.py`).

- **Security (audit A7–A13): medium/low hardening batch.** (A7) Every response now carries baseline security headers (`X-Content-Type-Options: nosniff`, `X-Frame-Options: DENY`, `Referrer-Policy`, `Strict-Transport-Security`). (A8) The prompt-injection guard gains an env-gated block mode — `PROMPT_GUARD_BLOCK_HIGH=true` forces high-risk inputs to be flagged for rejection at the boundary (off by default; HITL remains the structural defence). (A9) Removed the `print("[WS-DEBUG]…")` statements that leaked token-existence and user IDs to stdout. (A10) `get_document()` now takes an optional `tenant_id` and scopes the query; the authenticated document CRUD endpoints pass it (SYSTEM_ADMIN bypasses) so a forgotten re-check can't read cross-tenant. (A11) `encrypt_secret` fails closed (`RuntimeError`) in non-dev environments when `INTEGRATION_SECRET_KEY` is unset instead of silently storing plaintext; dev/test keeps the loud-warning fallback. (A12) The deprecated `?token=` WebSocket fallback now emits a deprecation warning. (A13) The anatomy-figure slug is sanitised to a strict basename with a containment check (`relative_to`) before any file write, blocking path traversal. 11 regression tests (`test_audit_a7_a13_hardening.py`).

- **Security (audit A2): Redis-backed rate limiting on authentication endpoints.** `/auth/login` (20/min), `/auth/register` (5/min), `/auth/refresh` (30/min), and `/auth/invite` (10/min) are now rate-limited per client IP via fixed-window Redis counters (`app/core/rate_limit.py`) so the limit holds across all workers. Degrades open (allows requests) if Redis is unreachable — rate limiting is defence-in-depth, not an availability gate. 7 regression tests (`test_audit_a2_rate_limit.py`).

- **Security (audit A5): refresh-token rotation + revocation.** Refresh tokens now carry a `type=refresh` claim and a unique `jti` tracked server-side in Redis (`app/core/token_store.py`). `/auth/refresh` **rotates**: the consumed `jti` is revoked and a brand-new refresh token is issued on every call (previously the same long-lived token was returned indefinitely, so a stolen token granted access for its full lifetime with no server-side invalidation). New `POST /auth/logout` (revoke one refresh token) and `POST /auth/logout-all` (revoke every refresh token for the user). An access token can no longer be replayed at `/auth/refresh` (the `type` claim is enforced). Degrades open if Redis is down. 9 regression tests (`test_audit_a5_refresh_rotation.py`). **Breaking:** clients must now use the *new* refresh token returned by each `/auth/refresh` call (rotation); old refresh tokens are single-use.

- **Fix: idempotent re-seed no longer nullifies `anatomy_structures.class_concept_id`.** The seed update path overwrote `class_concept_id` unconditionally — when `resolve_concept_by_slug` returned `None` (e.g. a concept missing its `ANATOMY_CLASS` kind tag from a prior partial seed), a valid class link was destroyed. The update path now only overwrites when resolution succeeds, preserving existing data. The `test_seed_order` regression test is reset-deterministic (wipes `anatomy_structures` first) so it no longer depends on accumulated test-DB state.

- **Test suite: all 18 pre-existing failures fixed (suite now fully green — 1738 passed).** Stale mock in `test_ocr_persistence_scoring` updated to include `class_concept`; obsolete `test_examination_categories_endpoints` removed (the router was deleted in the taxonomy unification — the RBAC property it guarded is now covered by `test_catalog_rbac`); `test_catalog_exact_match` updated to assert the hybrid (trigram+FTS+RRF) search that intentionally replaced the old substring-ILIKE; `test_audit_a1` slug-rejection cases moved to `BiomarkerCreate` (input-only validation).

- **Sidebar nav restructured: Patient Record/Clinical Data merged, Catalogs flattened, Notifications promoted.** The main sidebar had a conceptually muddy boundary — "Patient Record" (9 items) and "Clinical Data" (2 items) overlapped (biomarkers/medications are clinical data; examinations/documents are patient record), and "Patient Record" was an unstructured grab-bag mixing identity, analytics, treatments, alerts, and time-views. Changes: (1) **Clinical Data is merged into Patient Record**, now organised with internal `section` dividers (the same mechanism the Administration group already used) — *Clinical Record* (Examinations, Documents, Biomarkers, Correlative Analytics), *Treatments & Alerts* (Medications, Vaccinations, Allergy Alerts), *Timeline* (Clinical Events, Clinical Calendar) — so the 10-item group is scannable instead of a flat list. (2) **Notifications promoted to a top-level item** — it is app-level (a header bell already exists), not patient-scoped, so it no longer sits under Patient Record. (3) **Catalogs collapsed from a 6-item expanding group to a single flat link** — `/catalogs` already opens a unified tabbed workspace; the six `?type=` sub-items were redundant shortcuts that added nav noise. Result: 5 top-level items (Dashboard, Patient Record, Notifications, Catalogs, AI Assistant) + role-gated Administration, down from 6, with the mega-group made navigable via sections. New i18n keys (`section_clinical_record`/`section_treatments_alerts`/`section_timeline`) added to en + el; `clinical_data` key retained for compatibility.

- **Catalog: unified filter UI (all dropdown FacetChips) + category tag on every row + selected-value pills.** Filters were applied inconsistently across catalog types — `concept` (kind) and `anatomy` (class) used inline toggle chips in the toolbar while `biomarker`/`allergy`/`vaccine`/`medication` used popover-dropdown `FacetChip`s in a second toolbar row. Both are now **server-side `FacetChip` dropdowns** (`conceptKindFacet` with static `ConceptKind` enum options; `anatomy` class facet with fetched `anatomy_class` options via a `makeClassFacet` factory in `catalogFacetRegistry`), so every catalog type uses the same dropdown component and filtering composes correctly with pagination (server-side, before `limit`/`offset`) rather than only over the loaded 50-item page. The previously-dead `catalogFilter.serverParams` (computed by `useFilterState` but never consumed) is now wired into the `load()`/`loadMore()` API calls, with a stringified `serverFilterKey` dependency so client-side facet toggles don't trigger a refetch. Row tags were equally uneven — only `biomarker` and `anatomy` surfaced their category as a colored `ClassBadge`; `medication`/`allergy`/`vaccine`/`concept` showed only the scope badge. The three FHIR-catalog `to_dict()` methods now emit `class_concept_slug` + `class_concept_name` (the `class_concept` relationship is already `lazy="selectin"`, so no N+1) and concepts now surface `primary_kind` as a colored `KIND_COLORS` badge (concepts carry no class FK). The generic `ClassBadge` in `CatalogBrowser` falls back to `primary_kind` when no `class_concept` is present, and its click-to-filter now routes to the matching facet (kind for concepts, class otherwise). Finally, the removable **active-value pills** built into `FacetChip` (`showActivePills`) are now enabled in both the catalog workspace and the picker browse modal, so selected filter values render as removable chips next to each dropdown without having to open it (desktop-only; mobile sees the count summary on the trigger). Tests updated (facet registry + facet defs); `CatalogToolbar` lost the inline-chip props/render. The picker modal filters out server-mode facets (kind/class) since it filters in-memory and doesn't refetch.

- **ScaleSlider + occurrence-panel form polish.** (1) New reusable **`ScaleSlider`** (`components/ui/ScaleSlider.tsx`): a bounded number renders as a green→yellow→red gradient slider with a synced numeric input (bidirectionally in sync, clamps to `[min,max]` on blur), a colour-shifting thumb/input border (green=low → red=high via hue interpolation), and optional Mild/Severe captions. Pseudo-element thumb/track CSS lives in `index.css` via a `--scale-thumb-color` custom property. `DynamicMetadataForm` now upgrades any `number` field that declares both `min`+`max` to a `ScaleSlider` automatically (unbounded numbers stay plain inputs; no schema change — the seed's `intensity` already declares `min:1,max:10`). (2) **`MetadataField` gains an optional `placeholder`** (backend Pydantic + frontend type); the pain-episode `trigger` field now carries `"e.g. stress, weather change, lack of sleep"` so users understand its scope. (3) **Episode & Symptom Tracking panel reworked**: the bare `<input type="time">` is replaced with the existing clock-face `TimePicker`; the cramped `AnatomySearchPopup` is replaced with `CatalogField` (`allowedTypes={['anatomy']}`, single-select) matching the Specialized Details body-location picker; the occurrence `location` now stores the `{type,id,label}` selection object; the intensity slider gains the numeric input; and the card is restacked into full-width rows (date/time, intensity, body location) so the body-location field is no longer cropped. `ClinicalEventDetail` renders the occurrence location via `formatMetadataValue` and the now-dead `getBodyPartName`/`bodyParts`/`listBodyParts` cluster is removed. New test pins the `placeholder` round-trip.

- **Catalog picker: graph view + explorer-parity filters (list and graph).** The catalog picker browse modal now has a **List | Graph** toggle, and each view exposes the same filters its catalog-explorer counterpart does. **Graph view** (new `CatalogPickerGraph`, self-contained): renders the ontology graph via `ConceptGraphView` with its own filter strip — multi-select catalog-type chips (color-filled, with live node counts; default to the field's `allowedTypes`, toggle others on for cross-catalog context), multi-select concept-kind chips (hidden when the field locks a `concept_kind`), include-isolated toggle, depth buttons (∞/1–4 BFS from the clicked node), and relation-type chips (reusing `GraphRelationFilter`). Nodes whose catalog type is outside `allowedTypes` render **dimmed + non-clickable** (visible for relational context, never selectable — e.g. for a body-location field you see the full anatomy hierarchy plus connected body systems, but only anatomy nodes are clickable); already-picked nodes get a green accent. **List view**: keeps the type dropdown + scope + search and adds the per-type **`FilterBar`** facets (biomarker category/telemetry/coding/unit, allergy category/is_custom, vaccine coding_system, medication is_custom, concept status) via `useFilterState` + `getFacetsForType`, mirroring the workspace toolbar. `ConceptGraphView` (the shared graph renderer) gains two opt-in props — `disabledNodeIds?: Set<string>` (dim + click-guard) and `pickedNodeIds?: Set<string>` (green accent) — both no-ops when omitted, so the workspace ontology graph and relations graph are unaffected. The graph endpoint caps at 2500 edges with an amber truncation warning; an empty state points to list view when a catalog has no relationships.

- **Typed `metadata_schema` for clinical event types + unified catalog picker in dynamic forms.** `ClinicalEventType.metadata_schema` is now validated by a typed Pydantic model (`MetadataSchema`/`MetadataField`) backed by three new enums (`MetadataFieldType`, `CatalogType`, `CatalogRelationType`) — a malformed seed or API payload raises a precise error instead of silently rendering nothing (fail-loud). The legacy ad-hoc `source: "body-parts"` string is removed (breaking change, no users to migrate); a field now declaratively references any catalog via `{"type": "catalog-select", "catalogs": ["anatomy"]}` and can be narrowed to a concept kind via `concept_kind`. `DynamicMetadataForm` is rewritten with an **exhaustive `switch` over `MetadataFieldType`** (a missing branch is now a TS compile error via a `default: never` guard, closing the dead-`select`/`code`/`options`-rendered-nothing bug class). `catalog-select` fields render through a new thin **`CatalogField`** form wrapper over the existing **`CatalogItemPicker`**, so a metadata field can search any combination of biomarker/medication/allergy/anatomy/vaccine/concept catalogs from the same reusable component; the bespoke **`DynamicAnatomyField` is deleted**. The picked value is stored as `{type, id, label}` (single) or an array (multi); `ClinicalEventDetail` gains a robust `formatMetadataValue` that renders these without `[object Object]`. **`search_catalogs(kind=…)` + `GET /catalogs/search?kind=…`** narrow the concept catalog by `ConceptKind` (unknown kind → 400), letting the same picker power examination/event-category and specialty selection from one endpoint; `CatalogItemPicker` gains a `conceptKind` prop threaded to both inline type-ahead and the browse modal. The browse modal's "All catalogs" option is **disabled when the picker is scoped to a single distinctive catalog** (e.g. body location → anatomy) and now correctly respects `allowedTypes` in "All" mode (was leaking cross-catalog results). A latent infinite-render loop in `CatalogItemPicker` (`allowedTypes?.map(String)` created a new array each render, thrashing `useEffect`) is fixed via `useMemo`. Seed `clinical_event_types.json` migrated to the new shape; `SeedService` validates descriptors on load. 33 new tests (`test_metadata_schema.py`, `test_seed_clinical_event_types.py`, kind-filter cases in `test_catalog_search.py`). Plan: `dev/plans/clinical-event-catalog-fields-2026-07-14.md`.

- **New opencode skill: `documentation`.** A reusable skill (`.opencode/skills/documentation/SKILL.md`) codifying how to create, edit, and position Health Assistant's documentation: the docs architecture (`docs-tree.json` as the single source of truth for public nav + SEO metadata, no YAML frontmatter in markdown), the locked positioning rules (self-hosted, open-source, privacy-first, audience-neutral, FHIR stated but never leading), the SEO keyword priorities, the anti-fluff and anti-overclaim checklist, the internal-vs-public doc boundary, and the step-by-step recipe for adding a new doc. The `project-overview` skill's task→skill table and skill list now reference it.

- **Docs SEO + positioning pass across all public documentation.** Audited all 28 docs for search-engine discoverability (docs are published on the website) and alignment with the self-hosted, open-source, audience-neutral positioning. Changes: (1) **`docs-tree.json` enriched** with a `description` field on all 23 entries — the website consumes these for `<title>`/meta description tags; markdown files stay clean (no YAML frontmatter, which renders as raw text on GitHub). (2) **Six docs with wasted intros rewritten** — ARCHITECTURE, DEVELOPMENT, DEVELOPMENT_PLAN, STATUS, INSTALL, and PROJECT_STRUCTURE previously opened with "See STATUS.md…" (useless as meta descriptions); each now has a real 1–2 sentence description with keywords. (3) **TENANCY_AND_USER_MANAGEMENT.md neutralized** — removed "Home-First, Clinic-Ready" tagline, "Head of Household", "entire family", "family members" as positioning prose; kept `HOUSEHOLD` org-type and "Default Household" where they describe actual code behavior. (4) **Fluffy adjectives toned down** — "comprehensive", "seamlessly", "robust", "powerful", "production-ready" replaced with concrete language across ARCHITECTURE, DEVELOPMENT, STATUS, EXPORT_IMPORT, INTEGRATIONS_SDK, INTEGRATIONS_FRAMEWORK, MOBILE_SYNC_APP, TELEMETRY, SEEDING_AND_DEMOS. (5) **docs-tree.json restructured** — removed 6 internal docs from public navigation (AI_PROVIDER_TESTS, USER_INTEGRATIONS_PLAN [outdated], RELEASE_PROCESS, TASK_DEBUGGING [internal], TASK_PROGRESS_INDICATOR, UI_CAPTURE_PIPELINE); added SEEDING_AND_DEMOS (was linked from README but not in nav); improved nav titles to be keyword-rich.

- **README repositioned — family-first, FHIR demoted to "clinic-ready under the hood".** Reframed the README's hero, "What is", "What's different", and Features sections around household value (bring all your family's health data together, understand your results, track the long view, AI that helps carefully, private by default, for the whole household, make it yours, connect your tools) instead of leading with FHIR/interop architecture. Hero subtitle is now "Self-hosted health records for your household". Added a dedicated **"Clinic-compatible by design"** section lower in the doc that still states the FHIR R4 storage, the 18-resource R4 server facade, the unified catalogs, multi-tenant isolation, RBAC, export/import, and audit provenance — framed as the foundation that lets the same install grow into a clinic-grade record without a rewrite, not as the headline. Scope & Limitations softened to family-self-hosted tone (added "no managed hosted offering", "AI features optional — the rest works without an LLM", "wearable data is one-directional so far"). FHIR no longer appears in the hero, the value props, or any family-oriented feature bullet.

- **README rewrite — accurate, structured, no overclaiming.** Replaced the flat 15-bullet feature list with a grouped, scannable layout: hero with logo, table of contents, "what's different" value props, features organised by domain (clinical data model, workflows, AI, integrations, notifications, security & ops, platform), visual-tour thumbnail grid, a production Docker quick-start (`setup_env.py` → `docker compose … standalone up -d` → `create_system_admin.py`) alongside the dev path, a mermaid architecture diagram, the docs reorganised into the 5 `docs-tree.json` categories, the tech-stack table, and a new honest **Scope & Limitations** section. Toned down marketing language: dropped "Universal", the HIPAA-certification implication, "Full compliance with FHIR" (now "18 R4 resources, advanced conformance is roadmap"), and the generic "Async Processing" bullet (folded into Architecture). Surfaced previously-missing shipped features: the FHIR R4 server facade, Unified Catalog Registry + knowledge graph, Vaccinations/Immunizations, Export/Import backups, the HITL proposal model ("AI never writes clinical data"), the full notifications system, the security posture (Fernet-encrypted keys, prompt-injection guard, HMAC webhooks, audit provenance), the MCP client + webhook + bridge integrations, and PWA/offline/i18n. Added explicit "no medical certification, not HIPAA/GDPR-certified" and the telemetry-outside-FHIR tradeoff to Scope. All 23 doc links + 5 images + logo verified to resolve. Support block moved below features.

- **Popup/dropdown clipping fix on create forms (allergy / examination / medication / clinical events).** Calendar (DatePicker), AI assistant (AIAssistButton), and the selector family (SearchableDropdown, DoctorSelector, OrganizationSelector, CategorySelector, CategoryDropdown, UnitSelector, AnatomyTypeahead) previously rendered their dropdowns inline with `absolute z-50`. Inside modal forms with `overflow-hidden` + `max-h-[90vh]` + `overflow-y-auto`, those dropdowns were clipped at the form/modal boundary — invisible near the bottom edge or on small screens. **New reusable `Popover` primitive** (`components/ui/Popover.tsx`) renders via `createPortal(…, document.body)` with `position: fixed` coordinates from `getBoundingClientRect()`, **auto-flips** when there's no room below (renders above the trigger), **auto-shifts** horizontally to stay in the viewport, and **repositions on scroll/resize** (capture-phase scroll listener, so nested scroll containers inside modal forms also trigger it). All 8 popup components converted; outside-click handling now centralised in one place. Type-check clean; production build green.

- **Hybrid catalog search (trigram + FTS + RRF) for the agentic chatbot + REST + global search.** `search_catalogs`, `search_available_biomarkers`, `search_medications`, and `search_concepts` chatbot tools — plus the `/catalogs/search`, `/search`, and `/concepts/search` REST endpoints — now run through one unified hybrid pipeline in `catalog_search_service.py`. For each catalog, a single SQL runs three matchers (trigram `%` over name/slug/code + `websearch_to_tsquery` FTS over description/indications/info/aliases + `ILIKE` substring fallback) and fuses them via **Reciprocal Rank Fusion** (k=60). Cross-catalog hits are then globally RRF-ranked with a per-type floor so a strong biomarker match can't completely hide an allergy hit. The LLM-facing payload is now enriched: every hit carries `matched_on` (e.g. `["name", "alias", "text"]`), a `ts_headline` snippet showing the match context, and the `score`. Alias matching is fuzzy via `::text ILIKE` (so `"FBS"` now finds `"Fasting Glucose"` via its alias — previously only exact JSONB `@>` containment worked). New migration `j1a2b3c4d5e6` adds per-column trigram GIN indexes (`ix_<table>_<col>_trgm`) + FTS tsvector expression GIN indexes (`ix_<table>_fts`) on biomarker/medication/allergy/anatomy/vaccine/concept catalogs. 11 new tests (`test_hybrid_search.py`); the existing `search_*` per-domain functions keep their ORM-returning signatures (internal delegation only). Plan: `dev/plans/unified-catalog-architecture-2026-07-08.md` Phase 5.

- **Biomarker detail page reworked into a read-only tabbed view.** The detail page is now purely exploratory — all editing (config panel, inline info editor, delete) moved to the Catalog workspace. Six deep-linkable tabs (URL-synced `/:activeTab`, matching `ExaminationDetail`): **Trend** (the longitudinal chart, default landing), **Clinical** (`info`), **History** (observations table), **Insights** (AI), **Relations** (reuses the catalog `CatalogRelationsGraph`), **Technical** (metadata moved out of the sidebar). Tab bodies extracted into modular components under `components/biomarkers/tabs/`; chart-only state lives inside the Trend tab while shared state (date range, aggregation) stays lifted. Tab strip uses compact wrapping labels (no horizontal scroll) + a modular `TabInfoButton` (i) popover explaining each tab. The page-header subtitle no longer shows the system code (it's in the Technical tab).
- **Catalog forms: IoT telemetry toggle, rich-text editors, chip aliases.** (1) `BiomarkerForm` gains the `is_telemetry` IoT telemetry toggle (flipping it on save triggers the FHIR↔TimescaleDB migration via a confirmation modal in `CatalogWorkspace`). (2) Paragraph-style fields across all catalog forms (medication description/indications/contraindications, allergy/vaccine/concept/generic description) now use a compact rich-text editor (`RichTextField`) instead of plain textareas — the editor auto-detects Markdown (from LLMs) and converts to HTML on load; stored content renders formatted in the catalog Info tab via a new `FormattedText` component + `detectTextFormat` utility (HTML/Markdown/plain). `RichTextEditor` gained a `compact` toolbar variant for modal forms. (3) Aliases upgraded from comma-separated input to a reusable `ChipInput` (type + Enter/comma → removable pill, paste-splitting, Backspace-to-delete, dedupe).
- **Catalog: migration progress visible from the preview pane.** A new `BiomarkerMigrationWatcher` component shows the `MigrationProgressIndicator` (progress bar / failure + retry) inside the catalog preview pane for biomarkers, polling only the selected item (3s, with stall detection) so the FHIR↔telemetry migration can be monitored without leaving the catalog.
- **Graph: optional MiniMap.** `ConceptGraphView` + `CatalogRelationsGraph` gained a `showMiniMap` prop (default true); the biomarker Relations tab disables it (small container).
- **Fix: biomarker catalog edit crashed ("property 'category' has no setter").** The biomarker catalog adapter's `_writable()` filter used `hasattr`, which admitted the computed `category` `@property` (derived from `class_concept`) through to `setattr`. Now uses SQLAlchemy column inspection (matching the base adapter's fix for the identical `is_custom` crash) so `@property`/relationship/annotated fields are excluded from mass-assignment.

- **Taxonomy ↔ Catalogs merge:** concept CRUD folded into the unified Catalogs workspace; `TaxonomyManager.tsx` (1129 LOC) deleted; routes redirect to `/catalogs?type=concept`. `ConceptCatalogAdapter` is read-only (405 on writes); all concept writes flow through `ConceptService` (audit + restore + scope invariant). New `ConceptForm` + `KindChips` + `writeTarget.ts` dispatcher in the frontend. Whole-ontology graph view (`CatalogOntologyGraph`) with List|Graph toggle for all catalog types.
- **Anatomy relations unified into `concept_edges`:** the legacy `anatomy_relations` table is dropped; all ~62 anatomy hierarchy edges (PART_OF, SUPPLIED_BY, BRANCH_OF, etc.) now live in `concept_edges` as `src_type=anatomy, dst_type=anatomy`. The `AnatomyRelation` model + `AnatomyRelationType` enum are deprecated. 6 relation types added to `ConceptRelationType` (now 25 values).
- **Cross-catalog ontology graph:** new `GET /catalogs/graph` endpoint + `whole_catalog_graph()` function returns the full polymorphic graph (concepts, biomarkers, medications, anatomy, allergies, vaccines). Catalog-type filter chips + concept-kind sub-chips + depth BFS + PNG export + "include isolated" toggle.
- **Scope invariant fix:** `ConceptService.create_concept` now derives `scope` from `tenant_id` (was relying on the model default, producing inconsistent `tenant_id`/`scope` pairs).
- **Audit logging:** `ConceptService` now appends `CatalogAuditLog` rows for create/update/delete/restore when an `actor` is provided (REST + AI tools covered; seeds bypass).

### Added
- **Relation-type reference metadata + endpoint.** Each `ConceptRelationType` now carries a human-facing label, an icon, a grouping, and a one-line "when to use this" description, as a single source of truth in a new `app/catalogs/relation_types.py` registry (the "seed" for relation semantics). Exposed via `GET /catalogs/relation-types` → `{"items": [{value, label, group, description, icon}]}`. The registry fails fast at import if it drifts from the enum. The frontend caches it once per session (`loadRelationTypes`/`getRelationType` in `catalogService`) and the `RelationTypeSelect` dropdown renders each option with its icon (`DynamicIcon`, lucide SVG — same convention as catalog-type metadata) and a description, plus an info affordance. New backend test; catalog test suite green.

- **Reusable `CatalogItemPicker` — search-and-select for any catalog item(s).** A new modular, controlled component (`components/catalog/CatalogItemPicker.tsx`) for picking catalog items, usable anywhere a form needs one or many clinical references. Two discovery paths feed one selection: (1) **inline type-ahead** — a debounced cross-catalog search (`searchCatalogs`) with a Portaled results popover showing each hit as `<TYPE-chip> <label>`; (2) a **Browse button** inside the input that opens `CatalogPickerBrowseModal` — the full catalog browser in a popup with a searchable catalog-type dropdown defaulting to **All** (cross-catalog search) or a specific type (`listCatalogItems` with its own search + scope filter), and an Add/Added toggle per row. Supports `mode: 'single' | 'multi'` (single replaces, multi appends + dedupes by `type:id`), and an optional `relationPicker` that surfaces a per-chip relation-type dropdown so selections can carry a relation bind (e.g. `<AFFECTS, biomarker>`). Selected items render as removable chips below the input. The popup reuses the existing `CatalogBrowser` (gains backward-compatible `pickedIds`/`onTogglePick` props for the Add/Added affordance + multi-highlight) and `CatalogTypeSelect` (gains an optional `allowAll` for the cross-catalog entry). Relation options now live in one shared `catalogRelationTypes.ts` (mirror of the backend `ConceptRelationType` enum), replacing the hardcoded copy that was inlined in the editor.

### Changed

- **Sidebar + header user menu on shared library primitives** — the
  hand-rolled sidebar (490 lines: icon rail, hover flyouts, manual
  tooltips) is replaced by `SidebarNav` from `@neuronection/assistant-ui`
  driven by the new typed `config/mainNav.ts` registry (role filtering,
  active-route resolution and section dividers extracted as tested pure
  functions). The header user dropdown now renders in a Radix popover
  (keyboard + Escape + click-outside for free). Adds `nav.*` locale keys
  (en/el). Part of the family nav primitives program (ADR-0007).
- **`CatalogRelationsEditor` refactored onto `CatalogItemPicker`.** The inline cross-catalog search, the Portaled results popover, the relation-type `<select>`, the chip list, and the duplicated `RELATION_OPTIONS` are all removed — the editor is now a thin consumer that maps persisted `concept_edges` ↔ picker selections and reconciles via `createEdge`/`deleteEdge` on change (immediate persistence, same UX as before). ~110 lines of duplicated search/popover plumbing collapse into a `<CatalogItemPicker mode="multi" relationPicker>` call. New `catalogs.picker_*` + `common.remove`/`common.searching` i18n keys (en + el). `npm run build` (tsc strict) + `npm run lint` clean.

### Fixed
- **Catalog item edits not persisted + preview not refreshing.** Editing a catalog item (e.g. a medication) silently failed: the PUT returned a 500 (`property 'is_custom' of 'MedicationCatalog' object has no setter`) because the adapter's `_writable()` filter used `hasattr`, which admitted the computed `is_custom` `@property` (derived from `tenant_id`) — and relationships — through to `setattr`. `_writable` now restricts to real SQLAlchemy mapper columns (`inspect(model).columns`), so computed properties/relationships are excluded from mass-assignment across every catalog. On the frontend the failure was additionally hidden by an **empty `catch { /* toast */ }`** in `handleSave`/`handleDelete` (`CatalogWorkspace`) — the error was swallowed with no toast, so edits looked like they did nothing while relations (which the inline `CatalogRelationsEditor` persists immediately via its own edge calls) appeared to update. Both handlers now surface the backend `detail` via `toast.error`, show a success toast, patch the returned row straight into the local item list (so the Info preview reflects the edit instantly), disable the Save button while in-flight ("Saving…"), and bump a `relationsRevision` counter that drives a new `refreshKey` prop on `CatalogRelationsGraph` so the Relations graph refetches on save (it previously fetched once per item selection and stayed stale until re-selected, without losing the depth/view/node-selection state). `npm run build` (tsc strict) + `npm run lint` clean; 122 catalog tests green.

- **Catalog type dropdown chevron + relations graph fill.** (1) The catalog-type select's chevron sat immediately after the type name instead of at the right edge — the trigger's label span is now `flex-1` so the arrow is pushed to the far right of the button (standard dropdown affordance). (2) The Relations tab graph was locked to a fixed `h-[500px]` and didn't fill the preview pane — `CatalogRelationsGraph` now uses a `flex flex-col h-full` root with the graph canvas as `flex-1 min-h-[400px]` (and the cards view in a `flex-1` scroll area), so it expands to fill all available width and height of the preview pane instead of leaving dead space.

- **Catalog selection contrast + preview header.** (1) The active-row left accent only appeared on the first row because the `border-l-4`/`border-transparent` approach collided with the list's `divide-y` borders — switched the accent to a `box-shadow inset` so it renders on every selected row, and softened the selection fill (`bg-blue-100`→`bg-blue-50`, dark `/50`→`/30`; card `ring-2`→`ring-1 ring-inset`). (2) The preview-pane title was being squeezed out by four labeled action buttons (Edit/History/Delete/Open) — they're now compact icon-only buttons (hover-highlighted, tooltip on hover) so the item title keeps the full remaining width.

### Changed
- **Catalog list: stronger selected-item contrast.** The clicked/active catalog row was nearly invisible (`bg-blue-50/50`). List rows now get a solid `bg-blue-100` / `dark:bg-blue-900/50` plus a blue left-accent bar (`border-l-4 border-blue-500`, transparent when inactive so there's no layout shift); card view gets `bg-blue-50` / `dark:bg-blue-900/40` with a `ring-2 ring-blue-400` and blue border. Selection is now immediately obvious in both light and dark mode.

- **Catalog list: incremental pagination + keyboard navigation.** Two more list upgrades. (1) **Load-more pagination** — the workspace no longer hard-caps browse at 200 items; it now fetches 50 at a time (`limit`/`offset` against the existing `/catalogs/{type}` endpoint) and appends on a **Load more** button at the bottom of the scroll area (spinner state while fetching). This also unblocks the in-memory page-search introduced earlier — clicking Load more expands the searchable set beyond the first page (previously items beyond 200 were invisible + unsearchable). The `mine` scope stays a single all-at-once fetch since its ownership filter is client-side (offset pagination would be wrong on the user-scope set). Count header now reads `N of M` while loading pages, `M total` once complete. (2) **Keyboard navigation** — the list pane is focusable (click it, or it gets a subtle focus ring); **↑/↓** move the selection through the (filtered + sorted) rows and drive the preview pane, **Esc** clears the selection, and the selected row auto-scrolls into view (`scrollIntoView({block:'nearest'})`, which doesn't disturb click selection). A hover tooltip hints the shortcuts. New `catalogs.load_more` / `loading_more` / `keyboard_hint` i18n keys (en + el). `npm run build` (tsc strict) + `npm run lint` clean.

- **Catalog search moved to the global page-search; search-match highlighting + loading skeletons.** The catalog workspace no longer has its own search box — item search is now the shared header **SearchLauncher** in *Current Page* mode. `CatalogWorkspace` opts into page-search (`setIsPageSearchSupported(true)` on mount) and filters the loaded items **in-memory** against `pageSearchTerm` (the backend `?search=` param is no longer sent on browse); the local `search` state + the `<input>` in `CatalogToolbar` are removed. Two complementary list polish items ship with it: (1) **search-match highlighting** — a `Highlight` helper wraps case-insensitive matches of the active term in `<mark>` on each row's title and description (list + card views), so it's obvious *why* a row matched; `CatalogBrowser` gains a `searchTerm` prop. (2) **Loading skeletons** — the full-section spinner is replaced by animated skeleton rows (list) / cards (card view) that match the real layout, reducing perceived load time and layout shift. The header's active-search chip continues to show/clear the active term. `npm run build` (tsc strict) + `npm run lint` clean.

- **Catalog list: clickable tags, client-side sort header, and differentiated empty states.** Three upgrades to the catalog item list (`CatalogBrowser`). (1) **Clickable tags** — the scope badge and the taxonomy class badge on each row are now interactive: clicking a scope badge (System / Tenant / Yours) toggles the workspace scope filter to that tier (and marks itself active with a ring), and clicking a class badge toggles that class in the `?class=` filter (works for any catalog that carries a `class_concept_*`, not just anatomy); the `ScopeBadge` gained optional `onClick`/`active` props (it renders as a `<button>` when clickable, plain `<span>` otherwise, so the preview-pane badges stay non-interactive). (2) **Sort header** — a sticky bar above the scroll area offers client-side sort by **Name / Updated / Relations** (asc/desc toggle via the arrow icon; Updated/Relations default to desc) plus a live count that reads `N total` normally and `N of M` when a filter narrows the visible set (replaces the old count line buried at the bottom). (3) **Empty states** — instead of a flat "No items found." the list now distinguishes *no matches* (search/filter active → icon + hint + a **Clear filters** button that resets search/scope/class) from *no items yet* (fresh catalog → hint pointing at New). Also dropped the now-redundant per-row **View relations** button (the Relations tab in the preview already covers it; `onViewRelations` prop + the `viewRelations` handler removed). New `catalogs.sort_*` / `catalogs.count_of` / `catalogs.no_matches*` / `catalogs.empty_*` + `common.clear_filters` i18n keys (en + el). `npm run build` (tsc strict) + `npm run lint` clean.

- **Catalog list rows decluttered; edit/history/delete moved to the preview pane only.** Each list row was carrying five action buttons (Open / Edit / Relations / History / Delete) alongside inline tags, which crowded the title and overlapped it when names were long. The list now shows only the lightweight **View relations** + **Open in domain** inline actions; **Edit / History / Delete** live exclusively in the preview pane (a **Delete** button was added to the desktop preview header, and Edit/History/Delete icon buttons to the mobile full-screen popup, so mobile users keep access). Row layout reworked: the title gets its own full-width line (no longer shares a flex row with the scope/class badges → no truncation overlap), the description switched from a harsh 80-char slice to `line-clamp-2` (proper two-line wrap), and the scope + class badges moved to a dedicated right-aligned column. Row padding bumped (`py-3`→`py-3.5`) for a slightly taller, more legible item. `CatalogBrowser` is fully presentational now (its `onEdit`/`onDelete`/`onHistory` props are dropped — the workspace owns those actions). `npm run build` (tsc strict) + `npm run lint` clean.

- **Catalogs workspace reworked into a layered shell (Phase 3 of the catalogs UI rework).** The per-type workspace is now a master-detail-with-tabs instead of a Browser/Relations tab toggle: **Layer 1** is an always-visible item browser + an item-selector dropdown (default "All items") that sets `?item=`; **Layer 2** is a tab bar — **Info | Relations | <custom>**. The Relations tab is no longer disabled when nothing is selected: with "All" it renders a new **relation-centric index** (`CatalogRelationsIndex`) — every item with outgoing edges grouped by relation type (TREATS / AFFECTS / …), each clickable to select that item — reusing the single `?include=relations` fetch (no extra round-trip); with an item selected it shows that item's cross-catalog graph. The click-to-preview **drawer is removed** (`CatalogItemDrawer` deleted); item detail now renders inline in the **Info** tab (`CatalogItemInfo`). `CatalogBrowser` is now presentational (the workspace owns item-loading + the edit/audit modals, so the New button and the Info tab's Edit button share one modal). Selecting an item no longer forces a tab switch — it drives both Info and Relations. The **Ranges** (biomarker), **Interactions** (medication) and **Atlas** (anatomy) custom tabs are removed (the Info/Relations tabs + dedicated domain pages cover them); the custom-tab registry is retained for future tabs. New `catalogs.*` i18n keys (en + el). `npm run build` (tsc strict) + `npm run lint` clean (0 errors). See `dev/plans/catalogs-ui-rework-2026-07-09.md`.

### Added
- **Anatomy class chips + badges in the catalog (Phase 4 of the catalogs UI rework).** `/catalogs?type=anatomy` now shows each item's anatomy class as a colored badge (Organ / Region / System / Joint / …, read from the Phase-2 `class_concept_slug`/`class_concept_name` projection) and offers **multi-select class filter chips** in Layer 1 — the chips fetch the `anatomy_class` concepts (`GET /catalogs/concept?kind=anatomy_class`) and filter the list via `?class=organ,region,…`. The class badge is generic (renders on any catalog item that carries a `class_concept_name`, incl. biomarkers). `listCatalogItems` + the `CatalogItem` type gain `kind` / `class` / `class_concept_*`. The Anatomy Explorer's broken category filter is also unblocked: the landing "systems" fetch now uses `?class=system` (the legacy `?category=` was silently ignored by the backend) and system dots/labels read `class_concept_slug`/`class_concept_name`. `npm run build` (tsc strict) + `npm run lint` clean. See `dev/plans/catalogs-ui-rework-2026-07-09.md`.

- **Catalog class filter + projection (Phase 2 of the catalogs UI rework).** The taxonomy-class concept is now visible and filterable across catalogs, via one registry-driven mechanism. `GET /catalogs/{type}` gains `?class=<slug>` (comma-list OK) — the adapter resolves the slug → concept id and filters on the descriptor's `concept_link` FK column, so it works for *every* catalog that carries a `class_concept_id` (anatomy, biomarker, medication, allergy, vaccine) with no per-catalog code. Anatomy + biomarker items now serialize `class_concept_slug` + `class_concept_name` (read off the selectin-loaded `class_concept` relation) — so e.g. Thyroid shows as "Organ" instead of a bare UUID, and ALT shows its biomarker class. `GET /catalogs/concept?kind=<ConceptKind>` filters concepts by `primary_kind`. The dedicated `/anatomy` endpoint mirrors this: `?class=<slug>` + class projection on list/detail, and `POST/PATCH /anatomy` accept `class_concept_slug` (resolved to `class_concept_id`) so callers needn't know the UUID. 9 new tests (`test_catalog_concept_link.py`, `test_anatomy_class_projection.py`); 293 related tests green. See `dev/plans/catalogs-ui-rework-2026-07-09.md`.

### Removed
- **Legacy `AnatomyCategory` enum dropped (Phase 2, clean rebuild).** The uppercase `AnatomyCategory` enum (SYSTEM/REGION/ORGAN/…) — a vestige of the pre-concept `category` column — is deleted entirely. `data/seeds/anatomy_structures.json` is migrated to the lossless `class_concept_slug` form (e.g. `"category": "ORGAN"` → `"class_concept_slug": "organ"`); the `_ANATOMY_CATEGORY_TO_CONCEPT_SLUG` map + legacy fallback branch in `seed_service` and the inverse map in `seed_export_service` are removed (the `class_concept_slug` path is now the only path). The obsolete `test_anatomy_seed_falls_back_to_legacy_category` test is deleted. (Frontend `AnatomyCategory` type + Anatomy Explorer category chips are rewritten to class slugs in Phase 4.)

### Fixed
- **Seed ordering: `concepts` now seeds before `body_parts` (and `default_catalog`).** `SeedService._SEED_STAGE_NAMES` ran `body_parts` (anatomy) before `concepts`, so on a fresh DB the `class_concept_slug` → `class_concept_id` resolution found no `anatomy_class` concepts and **every anatomy row got `class_concept_id = NULL`** (0/54 resolved). The catalog class chips/filter/projection (Phase 2–4) therefore showed nothing until a re-seed over an already-populated DB. `concepts` is now first in the order (it's dependency-free; `body_parts`, `default_catalog`, `concept_edges`, and `biomarker_panels` all resolve concept slugs). New regression test `test_seed_order.py` asserts every seeded anatomy structure resolves its class (thyroid → `organ`/`Organ`).

- **Catalogs layering + sidebar nav (Phase 1 of the catalogs UI rework).** Two layering bugs and a nav mismatch are fixed via a principled stacking-context approach rather than renumbering z-indexes. (1) The item-detail drawer (`CatalogItemDrawer`) and the edit modal previously rendered *under* the sidebar's hover popups (`z-100`) and collapse handle (`z-960`); the Sidebar root now carries `isolate` (`isolation: isolate`), which scopes all of its internal z-indexes into their own stacking context so they can never overlay a body-level Portal again. (2) A documented Tailwind `zIndex` scale (`dropdown: 100`, `modal: 1000`, `drawer: 1100`, `toast: 1200`) replaces the pile of magic numbers; the drawer uses `z-drawer`, the edit modal + shared `<Modal>` use `z-modal`. (3) The edit modal is restructured into `flex flex-col max-h-[90vh]` (fixed header/footer, scrollable middle) and the inline relations-editor's target-search popover is now rendered via `<Portal>` at `z-dropdown` anchored to the input (`getBoundingClientRect`, repositioned on scroll/resize) — so it escapes the modal's scroll container and is no longer clipped (no more scrolling the form to see the full dropdown). (4) Sidebar "Catalogs" group: the **Anatomy** sub-item now points at `/catalogs?type=anatomy` (was `/anatomy`, which left the workspace) and a new **Concepts** → `/catalogs?type=concept` entry is added; new `common.concepts` / `common.anatomy` i18n keys (en + el). The `/anatomy` explorer route is unchanged (reachable via the catalog's "Open in domain view"). `npm run build` (tsc strict) + `npm run lint` clean (0 errors). See `dev/plans/catalogs-ui-rework-2026-07-09.md`.
- **Backend test pythonpath (pre-existing).** `pytest.ini` `pythonpath` now includes the repo root (`.` + `..`) so the `integrations` package (moved out of `backend/` to the repo root in commit 115f687) resolves — the test suite could not even load `conftest.py` before this.

### Added
- **Catalog item UX rework — type-specific forms, preview drawer, relations editor, item switcher, relation counts.** Replaces the thin generic editor with a richer, type-aware interaction model:
  - **Per-type forms** (registry at `components/catalog/forms/`): biomarker (slug/code/aliases/ref-range/unit), medication (indications/dosage/side-effects/contraindications), allergy (category enum/reactions), vaccine (CVX/target-diseases), generic fallback. Field lists mirror the backend create schemas.
  - **Inline relations editor** in the edit modal (`CatalogRelationsEditor`): manage the item's outgoing `concept_edges` — add a relation by picking a relation type + searching ANY catalog for the target (cross-catalog), remove existing edges as chips. Uses the polymorphic `POST/DELETE /concept-edges` endpoints.
  - **Click-to-preview drawer** (`CatalogItemDrawer`): clicking a row now opens a right-side slide-over showing the full item detail + scope badge + audit timestamp, with Edit / Relations / History / Open-in-domain as explicit actions (no longer drops straight into edit).
  - **Searchable item switcher** in the Relations tab (`CatalogItemSwitcher`): a combobox showing the current item, search-scoped to the active type, to swap `?item=` without leaving the tab.
  - **Relation counts in the list**: `GET /catalogs/{type}` gains `?include=relations` (new `count_relations` batch query — N+1-safe) annotating each item with `relation_count` + `relation_breakdown`; both list rows and cards show a `≁ N` chip. 3 new backend tests (`test_catalog_relation_counts.py`). `npm run build` + `npm run lint` clean.

- **Catalog relations — graph + cards view (Phase E).** The Relations tab now actually renders and offers two synchronized views. **Root-cause fix:** `ConceptGraphView`'s wrapper applied a hardcoded `h-full` *alongside* the caller's `h-[500px]` — two conflicting `height` declarations, so when `h-full` won (parent had no height) the graph was 0px tall and React Flow errored ("parent container needs a width and a height"). `h-full` is now the *overridable default* `className`, and `CatalogRelationsGraph` wraps the graph in an explicit `h-[500px]` container (mirroring the working TaxonomyManager pattern). New **Graph ⇄ Cards sub-tab toggle** within the Relations tab: the cards view (`CatalogRelationsCards`) lists the item's `concept_edges` grouped by relation type (TREATS / PREVENTS / AFFECTS / …), each card clickable → navigates to the connected item in the catalog. Clear **empty-state** prompt ("Select an item from the Browser tab…") when the Relations tab is opened with no item selected. `npm run build` (tsc strict) + `npm run lint` clean. See `dev/plans/catalog-access-control-and-ui-2026-07-08.md`.

- **Catalog unified UI — Phase D (list/card views + per-type custom tabs + left-rail scope filter).** Every catalog type now renders through one consistent, configurable UI shell. The Browser tab gains a **list/card view toggle** (compact rows vs. richer cards with scope badge + description + "Updated Xh ago" + actions, per plan §3.2). A new **per-type custom-tab registry** (`components/catalog/tabs/catalogTabs.tsx`) makes adding a tab = one component + one entry; three functional starters ship: **Biomarker → "Ranges"** (inline-editable reference-range table: min/max/unit per biomarker, scope-aware), **Anatomy → "Atlas"** (responsive card-grid index of body parts with standard codes + scope badges, linking to the Anatomy Explorer), and **Medication → "Interactions"** (expand-a-medication browser surfacing its `TREATS` / `CONTRAINDICATES` / `INDICATES` / `CAUSED_BY` edges from the polymorphic graph, grouped by relation type). Custom tabs render alongside the built-in Browser + Relations tabs in `CatalogWorkspace`. The **scope filter moved to the left rail** (All / System / Tenant / **Mine**) as a workspace-level control; "Mine" narrows to the current user's user-scope items (fetches `?scope=user` then filters by `created_by`); `CatalogBrowser` now accepts controlled `scopeFilter` / `onScopeFilterChange` props (hiding its in-toolbar `<select>` when the workspace drives it). New `catalogs.*` i18n keys (en + el): tab labels, view-toggle titles, column headers, help text. `npm run build` (tsc strict) + `npm run lint` clean. See `dev/plans/catalog-access-control-and-ui-2026-07-08.md`.

- **Catalog routing consolidation — Phase C.** All catalog browsing now lives at one unified `/catalogs?type={type}` route (all authenticated users), replacing the scattered `/{type}/catalog` per-type pages and the SYSTEM_ADMIN-only `/admin/catalogs` workspace. The unified `CatalogWorkspace` moved from `pages/Admin/Catalogs/` to `pages/Catalogs/` (no longer admin-prefixed) and dropped its "Administration" breadcrumb. The four legacy routes (`/biomarkers/catalog`, `/medications/catalog`, `/vaccines/catalog`, `/allergies/catalog`) now `<Navigate>` to `/catalogs?type=…` (no transition period), and `/admin/catalogs` redirects to `/catalogs` for stale bookmarks. The dedicated `BiomarkerCatalog`, `MedicationCatalog`, and generic `CatalogPage` components are **removed** (clean rebuild — their rich forms become custom tabs in Phase D; common name/description editing already works via `CatalogBrowser`). The sidebar "Catalogs" group sub-items now point at `/catalogs?type=…`, and `isSubActive` became query-string-aware so the right catalog type highlights. Every internal link retargeted: SearchLauncher (also fixes a latent bug where `/admin/catalogs?vaccine` used a bare flag instead of `?type=vaccine`), the biomarker + medication detail "Manage in Catalogs" buttons, the biomarker trends / medication list "open catalog" buttons, and the post-delete redirects. The settings nav's redundant `/admin/catalogs` entry is removed (the workspace is now top-level nav). The "concept" type's "Open Taxonomy Manager" link is now role-aware (`/admin/system/taxonomy` for SYSTEM_ADMIN, `/examinations/categories` for everyone else, since TaxonomyManager mounts at both). The legacy import page (`/admin/system/catalogs` → `CatalogManagement`) stays SYSTEM_ADMIN-only. `npm run build` (tsc strict) + `npm run lint` clean. See `dev/plans/catalog-access-control-and-ui-2026-07-08.md`.

- **Catalog audit log — Phase B (backend).** Every catalog CRUD operation now appends an immutable `CatalogAuditLog` row (who / what / operation / scope-change / item snapshot), giving a durable record of who changed what, when, and (for promote/demote) the scope transition. New `CatalogAuditLog` model (`app/models/catalog_audit_model.py`) + migration `f7a8b9c0d1e2` — append-only, with `user_email` and `item_name` denormalized so the trail survives user/item deletion; `tenant_id`/`user_id` are deliberately FK-less for the same reason (added to the tenant-FK test's documented exception set). New `app/services/catalog_audit_service.py` (`record` / `record_from_obj` / `list_history`) — recording is best-effort at two levels (the service swallows commit errors, and the adapter's `_audit` wraps the call) so an audit failure never aborts the parent catalog write. Wired into `BaseCatalogAdapter` + `BiomarkerCatalogAdapter`: create/update/delete/promote each append a row after commit (delete captures the item snapshot before the row is gone; promote records `from_scope`→`to_scope`). Each adapter gains a `catalog_type` attribute (stamped by `registrations.py`) so rows record the catalog type. `BiomarkerCatalogAdapter` now inherits from `BaseCatalogAdapter` (gaining the audit helpers + `catalog_type`) while keeping its Unit-join overrides. New endpoint `GET /catalogs/{type}/{id}/history` returns the trail newest-first, tenant-scoped (the item must be visible to the caller — 404 otherwise — so the trail never leaks cross-tenant). See `dev/plans/catalog-access-control-and-ui-2026-07-08.md`. 7 new tests in `test_catalog_audit.py` (create/update/delete/promote → audit rows, history endpoint ordering + tenant-scoping, best-effort audit failure).
- **Catalog audit log — Phase B (frontend).** The catalog browser now surfaces the ownership + audit model. Every item row shows a colored **scope badge** (`ScopeBadge` — system=violet, tenant=blue, yours=green, user=amber; the "yours" vs "user" distinction uses the current user id vs `created_by`) and a relative "Updated Xh ago" timestamp (via `date-fns` `formatDistanceToNow`, locale-aware). A scope **filter** dropdown narrows the list (`All` / `System` / `Tenant` / `User`, wired to the `?scope=` query param). A **History** button on each row opens an `AuditHistoryModal` (reusing the shared `<Modal>` shell) showing the item's audit trail newest-first — operation badge, scope transition (`from → to`), the denormalized `user_email`, and a relative timestamp. New `CatalogItem` / `CatalogScope` / `CatalogAuditEntry` types; `getCatalogItemHistory` service wrapper; `listCatalogItems` gains a `scope` param. New `catalogs.*` i18n namespace (en + el). `npm run build` (tsc strict) + `npm run lint` clean. See `dev/plans/catalog-access-control-and-ui-2026-07-08.md`.

- **Catalog ownership-based access control — Phase A (scope tiers).** Catalog access is now ownership-based instead of pure role-based, so any user can contribute without breaking curated data. Every catalog item (biomarker, medication, allergy, vaccine, anatomy, concept) carries an explicit `scope` of `system` | `tenant` | `user` (new `CatalogScope` enum + nullable-default `scope` column via migration `e6f7a8b9c0d1`, backfilled from `tenant_id`: NULL→system, non-NULL→tenant). The rewritten `CatalogAccessPolicy` decides access by `(role, item.scope, ownership)`: **any authenticated user may CREATE** — a SYSTEM_ADMIN create lands in system-scope (canonical, `tenant_id=NULL`), ADMIN/MANAGER in tenant-scope, USER in user-scope (visible to the whole tenant, editable only by the creator + admins). Update/delete of system-scope requires SYSTEM_ADMIN; tenant-scope requires ADMIN/MANAGER; user-scope requires the creator OR an admin. New `POST /catalogs/{type}/{id}/promote` endpoint transitions scope (user↔tenant needs ADMIN/MANAGER; anything involving system needs SYSTEM_ADMIN; promote-to-system clears `tenant_id`, demote-to-tenant sets it). `GET /catalogs/{type}` gains a `?scope=system|tenant|user` filter. The `CatalogServiceProtocol` gains `promote_scope()`; both `BaseCatalogAdapter` and the specialised `BiomarkerCatalogAdapter` are fully scope-aware (create stamps scope+tenant_id+created_by; update/delete consult ownership). The domain endpoints/services (biomarker, medication, allergy, vaccine) are migrated to the same scope+ownership model (a USER can now create via `/biomarkers/`, `/medications/catalog`, etc., landing in user-scope). `anatomy_structures` gained the `AuditMixin` columns (`created_by`/`updated_by`) so the ownership model is uniform across all catalogs. See `dev/plans/catalog-access-control-and-ui-2026-07-08.md`. 55 new tests in `test_catalog_scope_access.py` (create→role-derived scope, ownership matrix, promote/demote gating, scope filter, migration backfill) + updated RBAC/domain tests for the new USER-create semantics.

- **Catalog LLM tools (Unified Catalog Registry — Phase 8).** The agentic chatbot can now answer cross-catalog questions via two new tools registered in the tool registry. `search_catalogs(query, types?, limit?)` — ranked trigram search across every registered catalog (biomarkers, medications, vaccines, allergies, anatomy, diseases) in one call; returns `{type, id, label}` hits; auto-covers newly-registered catalogs. `explore_catalog_relations(type, id, max_depth?, relations?)` — multi-hop graph traversal over the polymorphic `concept_edges` graph; answers "which organ does this biomarker affect?", "what diseases does this vaccine prevent?", "what medications treat this disease?"; the `relations` whitelist prunes to specific edge types (e.g. `["PREVENTS"]`). Both tools are pure read paths over the registry (tenant-scoped, read-only — AI never writes; proposals go through HITL). The agent system prompt gains a CATALOG & KNOWLEDGE-GRAPH RULES section directing the LLM to prefer `search_catalogs` for cross-domain discovery and `explore_catalog_relations` for relational questions. A type alias (`vaccine` → `immunization`) keeps the LLM-facing vocabulary consistent with search results. See `dev/plans/unified-catalog-architecture-2026-07-08.md`. 9 new tests (search finds medication/disease, types filter, short-query empty, relations from medication + vaccine, relation whitelist, registration + get_tools inclusion).
- **Unified Catalogs admin workspace (Unified Catalog Registry — Phase 7).** One admin screen manages every clinical reference catalog. New `/admin/catalogs` page (`CatalogWorkspace`) renders a **registry-driven** left rail from `GET /catalogs` (UI meta from the backend `CatalogRegistry`) — so newly-registered catalogs appear automatically with no hardcoded nav. Each type offers two tabs: **Browser** (`<CatalogBrowser>` — searchable list + create/edit/delete via the `/catalogs/{type}` CRUD routes + "Open in domain view" deep links to existing detail pages) and **Relations** (`<CatalogRelationsGraph>` — the cross-catalog `concept_edges` graph, reusing the existing `ConceptGraphView` with an adapter that maps the `/catalogs/{type}/{id}/relations` response into the graph node/edge shapes; this is the visual "which organ does this biomarker affect? what treats this disease?"). The "concept" type links out to the existing `TaxonomyManager`. New `services/catalogService.ts` (typed wrapper for all `/catalogs` endpoints) + `types/catalog.ts`. The global search box (`SearchLauncher`) now navigates catalog-type results (anatomy, allergy, vaccine, concept) and renders type-appropriate icons — the mock-results fallback is removed (the backend `/search` already aggregates `/catalogs/search`). "Manage in Catalogs" links added to the biomarker + medication detail pages' toolbars. The settings nav replaces the separate "Clinical Ontology" + "Taxonomy" entries with one "Catalogs" entry → `/admin/catalogs`. Backend fix: the anatomy catalog's `ui.icon` corrected from the nonexistent `"Body"` to `"PersonStanding"` (a valid lucide-react export). `npm run build` (tsc strict + vite) + `npm run lint` clean. See `dev/plans/unified-catalog-architecture-2026-07-08.md`.
- **Diseases as concepts + FHIR CodeSystem/ValueSet (Unified Catalog Registry — Phase 6).** Illness/disease reference data now exists as first-class `kind=disease` concepts and is FHIR-queryable. New `data/seeds/diseases.json` seeds 57 curated ICD-10 disease concepts (top causes of morbidity/mortality + all vaccine-target diseases: measles, mumps, rubella, influenza, COVID-19, tetanus, diphtheria, pertussis, hepatitis B, HPV, cervical cancer, pneumococcal pneumonia) via a new `seed_diseases` stage in the ordered `seed_all` pipeline — diseases live as concepts so they inherit full CRUD/search/edges/FHIR for free via the concept layer (no new table). The cross-domain `concept_edges` graph is extended with the deferred Phase 3/5 edges: specialty `EXAMINES` disease (cardiology→CAD, endocrinology→diabetes, pulmonology→asthma, …), medication `TREATS` disease (metformin→T2DM, lisinopril→hypertension, atorvastatin→hypercholesterolemia, …), medication `CONTRAINDICATES` disease (aspirin→peptic-ulcer, metformin→CKD), and vaccine `PREVENTS` disease (MMR→measles/mumps/rubella, HPV→HPV+cervical-cancer, …) — the seed loader gained a `"vaccine"` endpoint branch (resolves `VaccineCatalog` by slug) so `GET /catalogs/{type}/{id}/relations` now answers "what does the MMR vaccine prevent?" and "what treats type-2-diabetes?" end-to-end. The FHIR R4 facade publishes the disease vocabulary as two new computed terminology resources: `GET /fhir/R4/CodeSystem/ha-diseases` (a `CodeSystem` with `content=complete` + every disease's ICD-10 code/display) and `GET /fhir/R4/ValueSet/ha-diseases` (a `ValueSet` composing the ICD-10 system) — both validate against `fhir.resources`. These are the first **computed** facade resources (no backing table): `ResourceEntry` gained optional `read_fn`/`search_fn` hooks that bypass the generic table-query dispatcher and project from the `concepts` table; the CapabilityStatement advertises both. `SeedExportService` gained `export_diseases` (+ `diseases.json` in `EXPORTERS`) as the strict inverse of `seed_diseases`, and `export_concepts` now excludes disease-kind concepts so re-exporting an unchanged DB remains a git no-op; the edge export's slug maps now cover `MEDICATION` (by name) + `IMMUNIZATION` (by slug) endpoints. See `dev/plans/unified-catalog-architecture-2026-07-08.md`. 24 new tests (5 disease-seed idempotency/code-coverage, 8 cross-domain edge resolution incl. graph traverse, 10 CodeSystem/ValueSet facade incl. CapabilityStatement + 405-on-write, 1 diseases-export round-trip). **Out of scope:** Anatomy `BodyStructure` FHIR resource (deferred per decision §16.2); frontend disease management folds into the existing TaxonomyManager (Phase 7).
- **Vaccines & immunizations (Unified Catalog Registry — Phase 5).** Vaccinations are implemented end-to-end, mirroring the medications pattern. New tables `vaccine_catalog` (CVX-coded reference definitions, with `class_concept_id` taxonomy hook) + `patient_immunizations` (dose-administered patient-instance records) via migration `d5e6f7a8b9c0`. REST CRUD at `/vaccines/catalog*` (RBAC: USER read-only, ADMIN/MANAGER tenant, SYSTEM_ADMIN global) + `/vaccines/patient/{id}` + per-record `/vaccines/{id}` (patient-access scoped via `check_immunization_access`). FHIR R4: `PatientImmunization.to_fhir_dict()` → canonical `Immunization` (status, vaccineCode, patient, occurrenceDateTime, lotNumber, protocolApplied.doseNumberPositiveInt); `VaccineCatalog.to_fhir_dict()` → `Medication`. Registered `Immunization` as a 16th facade resource (read + search-type) with a `param_filter` mapping `date`→`administered_at` + `vaccine-code`→JSONB text. Both write paths pass the `assert_valid_fhir` gate. Vaccines are registered in the `CatalogRegistry` (`VaccineCatalogAdapter`) + the `IMMUNIZATION` endpoint resolver, so they appear in `/catalogs/vaccine`, `/catalogs/search`, and the cross-catalog graph automatically. Seeded 7 CVX-coded vaccines (MMR, influenza, COVID-19 mRNA, Tdap, HepB, HPV, PCV) via a new `seed_vaccines` stage. See `dev/plans/unified-catalog-architecture-2026-07-08.md`. 11 new tests (catalog CRUD+RBAC, patient-instance CRUD+access, FHIR projections, facade search, registry integration, migration). **Deferred:** `PREVENTS` vaccine→disease edges (need disease concepts → Phase 6); dedicated frontend pages (Phase 7 — vaccines already surface in `/catalogs`).
- **Unified Catalog Registry — Phase 4: registry-driven cross-catalog search.** One dispatcher searches every registered catalog at once. `search_catalogs(db, tenant_id, q, types=, limit=)` (in `catalog_search_service.py`) iterates the `CatalogRegistry` and calls each adapter's new `search()` method — typo-tolerant `pg_trgm` trigram matching over the descriptor's `search_columns` with an `ilike` fallback, ranked by `similarity(label_column, q)`, returning uniform `{type, id, label}` hits. Exposed at `GET /catalogs/search?q=&types=&limit=` (anatomy, concepts, and allergies now surface, not just medications + biomarkers). The global `GET /search` catalog portion is rewired to delegate to the dispatcher, so newly-registered catalogs appear in global search automatically — the hardcoded per-entity blocks are gone. Adding a catalog to search = registering it (the descriptor already declares `search_columns`). See `dev/plans/unified-catalog-architecture-2026-07-08.md`. 9 new tests (dispatcher all-types/type-filter/short-query/tenant-scope/unknown-type, `/catalogs/search` endpoint, `/search` now includes anatomy+concept+allergy).
- **Unified Catalog Registry — Phase 3: cross-domain seed edges + single link system.** The polymorphic `concept_edges` graph is now the **only** cross-domain link system. Migration `c4d5e6f7a8b9` drops the two legacy biomarker link tables: `biomarker_relationships` (was dead code) and `biomarker_event_correlations` (its live readers — `clinical_event_service` recommended-biomarker insight + the `POST/GET/DELETE /clinical-events/types/{id}/biomarkers` CRUD — were rewritten first to query `concept_edges`, where biomarker↔clinical_event_type is a `MONITORS` edge with `correlation_type`/`description` on the edge's `properties` JSONB). The ORM classes, schema classes, and the legacy `scripts/seed_biomarkers.py` correlation writes are removed. `concept_edges.json` is extended with cross-domain biomarker→anatomy `AFFECTS` edges (creatinine→kidneys, tsh→thyroid, glucose→pancreas, lipids→cardiovascular-system) + biomarker→body-system `INDICATES` edges — these power the headline "which organ does this biomarker affect?" query via `GET /catalogs/{type}/{id}/relations`. The seed loader gained a `"medication"` endpoint branch (resolves `MedicationCatalog` by name), enabling medication→disease edges when Phase 6 seeds disease concepts. See `dev/plans/unified-catalog-architecture-2026-07-08.md`. 6 new tests (correlation CRUD via edges, seed loader, table-drop verification, model-registry cleanup). **Note:** medication→disease / specialty→disease edges are deferred to Phase 6 (require disease concepts).
- **Unified Catalog Registry — Phase 2: cross-catalog graph completion.** The polymorphic `concept_edges` link system is now finished and queryable end-to-end. New `app/services/catalog_graph_service.traverse()` does a depth-bounded (1–5), cycle-safe, tenant-scoped **recursive-CTE** traversal from any `(type, id)` start node, returning a display-ready `{start, nodes, edges}` payload (endpoints resolved via the resolver registry). Exposed at `GET /catalogs/{type}/{id}/relations?depth=&relation=&include_proposed=` — this is the headline query: "which organ does this biomarker affect? what treats that disease?" The endpoint-resolver registry now covers **7 of 11** `EdgeEndpointType` values (added medication, allergy, clinical_event_type). Migration `a1b2c3d4e5f7` adds `class_concept_id` (nullable FK→concepts.id, indexed) to `medication_catalog` and `allergy_catalog`, giving every catalog a native taxonomy classification on the established `<role>_concept_id` convention; adds the `AFFECTS` value to the `conceptrelationtype` PG enum (the biomarker→anatomy semantic link). **Decision refined:** the legacy `AllergyCategory` enum is **kept** (it backs the FHIR `AllergyIntolerance.category` closed value set on both the catalog and patient-instance models); `class_concept_id` is added alongside it. See `dev/plans/unified-catalog-architecture-2026-07-08.md`. 13 new tests (resolver extension, traverse depth/cycle/whitelist/proposed-filter, relations endpoint, migration column presence, AFFECTS round-trip).
- **Unified Catalog Registry — Phase 1: RBAC normalization + CRUD completion.** The uniform `CatalogAccessPolicy` (USER = read-only; ADMIN/MANAGER = tenant-scoped writes; SYSTEM_ADMIN = global writes + bypass) is now enforced across **all** catalog write surfaces — closing the pre-Phase-1 gaps where a `USER` could `DELETE /biomarkers/{id}` and biomarker reads leaked across tenants. Enforced both on the `/catalogs` meta-layer write routes (`POST/PUT/DELETE /catalogs/{type}[/{id}]`, now wired through the adapter `create/update/delete`) and on the domain endpoints (biomarker/medication/allergy catalog create/update/delete). `CatalogPermissionDenied` is mapped to HTTP 403 by a new global handler in `main.py`. Allergy catalog CRUD is **completed** (get-one `GET /allergies/catalog/{id}`, update `PUT`, delete `DELETE` were missing — now full round-trip + `AllergyCatalogUpdate` schema). Medication catalog **delete** added (`DELETE /medications/catalog/{id}`). Biomarker reads are now tenant-scoped (`or_(tenant_id == caller, tenant_id IS NULL)`) and bulk-delete is restricted to the caller's own tenant rows. Allergy/medication **patient-instance** routes are unchanged. See `dev/plans/unified-catalog-architecture-2026-07-08.md`. 25 new tests (15 RBAC parametrized over biomarker/medication/allergy × the role/scope matrix + 10 domain CRUD incl. the biomarker tenant-scope regression). AllergyCatalog `to_fhir_dict` deferred to the facade phase (the instance gate was already closed in audit C13; the catalog table has no FHIR JSONB).
- **Unified Catalog Registry foundation (Phase 0).** A new `app/catalogs/` package introduces a declarative `CatalogRegistry` + `CatalogDescriptor` contract that all clinical catalogs (anatomy, taxonomy, biomarkers, medications, allergies — vaccines/diseases in later phases) conform to, plus a uniform `CatalogAccessPolicy` RBAC value object and a `CatalogServiceProtocol`. Each catalog registers once (model, service adapter, search columns, concept-link, `EdgeEndpointType`, graph resolver, FHIR projector, UI metadata) and gains unified read access via a new thin `/catalogs` meta-layer: `GET /catalogs` (registered types + UI meta, drives the future admin workspace left rail), `GET /catalogs/{type}` (tenant-scoped list, global+caller's tenant), and `GET /catalogs/{type}/{id}` (get-one). The meta-layer **complements** (does not replace) the domain endpoints — it dispatches by type to the registered adapter. This is the foundation for cross-catalog search (Phase 4), the polymorphic graph (Phase 2), vaccines (Phase 5), diseases-as-concepts (Phase 6), the unified admin workspace (Phase 7), and two LLM tools (Phase 8). Domain endpoints are unchanged; the adapters tenant-scope reads correctly (the biomarker adapter fixes the legacy global-leak that Phase 1 will also fix on the domain endpoint). See `dev/plans/unified-catalog-architecture-2026-07-08.md`. 21 tests (12 registry unit + 9 real-DB API incl. cross-tenant isolation).
- **Bidirectional examination ↔ clinical-event surfacing.** An examination's REST response now includes a `clinical_events` array (id/title/status/reason of the journeys it belongs to, capped at 20), and its FHIR `Encounter` projection emits real `diagnosis[].condition.reference = "Condition/{id}"` entries for each linked `ClinicalEvent`. This closes a documented-but-unimplemented gap (the `to_fhir_dict` comment promised Condition references "when a ClinicalEvent row is linked via EventExaminationLink" — the code never produced them) and makes navigation between visits and journeys work in both directions. The examination list + detail endpoints now eager-load `event_links.event`; `ExaminationResponse` carries the new field. 1 real-DB test (REST surfacing + Encounter Condition reference).
- **`BiomarkerEventCorrelation` CRUD endpoints.** `POST /clinical-events/types/{type_id}/biomarkers` and `DELETE /clinical-events/types/{type_id}/biomarkers/{biomarker_id}` let admins manage which biomarkers are clinically relevant to monitor for a given journey type. Previously correlations were seed-script-only — there was no API or UI to define them. The create is idempotent on the (type, biomarker) pair (re-adding updates `correlation_type`/`description` in place rather than 409-ing). The `ClinicalEventEngine`'s `recommended_biomarkers` insight reads from these correlations end-to-end (create a correlation → it surfaces in `/clinical-events/{id}/insights`). 2 real-DB tests (CRUD lifecycle + recommendation flow-through).
- **Behavior-driving Clinical Event types + a `ClinicalEventEngine`.** `ClinicalEventType` is promoted from a passive form-schema holder into a journey *template* via four new JSONB columns (migration `f2c3d4e5f6a7`): `severity_scale`, `phases` (e.g. trimesters), `milestones` (with `alert_before_days`), and `default_duration_days`. A new pure-Python `ClinicalEventEngine` (`app/services/clinical_event_engine.py`) derives computed guidance from `(event, type)` with **no DB dependency** — current phase (from onset + phase offsets), upcoming/overdue milestones (absolute or resolved from `event_metadata` via `date_field`), recommended biomarkers (from `BiomarkerEventCorrelation`), and an `is_overdue` flag. Exposed at `GET /clinical-events/{id}/insights`. This is the modular extensibility core: a new journey type = a new JSONB template = new behavior, with no engine code change. 9 pure-Python engine unit tests + 3 real-DB endpoint tests (phase computation, overdue flag, template round-trip).
- **FHIR `EpisodeOfCare` projection — Clinical Events now back two FHIR resources.** A Health-Assistant "health journey" (a 9-month pregnancy, a 2-year dental alignment) is semantically a FHIR `EpisodeOfCare` — *"managing a condition over time, including when the patient is not actively being seen"* — not just a `Condition` (which captures only the problem). The same `ClinicalEvent` row now projects to BOTH: `Condition` via `to_fhir_dict` (the problem) and `EpisodeOfCare` via the new `to_fhir_episode_of_care_dict` (the journey). The two share an id (legal — FHIR ids are per-type) and `EpisodeOfCare.diagnosis[0].condition` references `Condition/{id}` so a client can walk between them. **No new table, no dual-write** — same hybrid-storage pattern. Registered at `/fhir/R4/EpisodeOfCare` with `patient`/`subject`/`status` search (the FHIR status values `active`/`finished`/`onhold`/`planned` translate back to the stored `ClinicalEventStatus` enum via a per-resource `param_filter`). The generic facade dispatcher now honors `ResourceEntry.to_fhir_dict_attr` (previously declared but unused), so one ORM model can back multiple FHIR resources through different projection methods. Reverse converter `fhir_to_episode_of_care_orm` round-trips. Backed by 3 real-DB tests (facade search incl. status translation, round-trip, versioning).
- **`EventAnatomyLink` promoted from dead code to the structured anatomy path.** The `event_anatomy_links` table existed (migration `4f61f50eb0be`) but had no `ClinicalEvent.anatomy_links` relationship, no endpoints, and zero references — anatomy was tracked ad-hoc in `event_metadata.body_part_id` JSONB instead. Phase 3b adds the relationship (cascade delete-orphan), `POST /clinical-events/{id}/link-anatomy` and `DELETE /clinical-events/{id}/unlink-anatomy/{anatomy_id}` endpoints, a `relation_type` discriminator (`primary_site` / `radiates_to` / `referred_to`), an `anatomy_links` field on the event response (with the anatomy name resolved via eager load), and a backfill migration (`f1b2c3d4e5f6`) that promotes every existing `event_metadata.body_part_id` into a real `EventAnatomyLink` row (`relation_type='primary_site'`, `ON CONFLICT DO NOTHING`). This unifies clinical events with the anatomy atlas already used by the detail page and makes body-site queries first-class.
- **`ClinicalEventOccurrence` model — discrete journey episodes promoted from untyped JSONB to a queryable first-class table.** `ClinicalEvent.occurrences` was a free-form JSONB array (no schema, no per-occurrence integrity, no way to query "all pain episodes with intensity ≥ 8 this quarter" or slice by body site). The new `clinical_event_occurrences` table (migration `f0a1b2c3d4e5`) gives each episode `occurred_at`, `title`, `severity`, `intensity` (1..10), `notes`, an optional `anatomy_id` FK to `anatomy_structures`, and a `metadata` JSONB. New endpoints `POST /clinical-events/{id}/occurrences` and `DELETE /clinical-events/{id}/occurrences/{occurrence_id}` manage them. The legacy `occurrences` JSONB column is retained for one cycle as read-back fallback; migration backfills every existing JSONB entry into a model row, and `to_dict().occurrences` sources from the model when non-empty (falling back to JSONB for rows written through the legacy payload). The occurrence shape is backward-compatible (`date`/`intensity`/`notes` legacy keys preserved) while adding the richer fields. Backed by 3 real-DB tests (CRUD, intensity validation, JSONB read-back).
- **`POST /api/v1/clinical-events/{id}/link-observation` endpoint.** Closes the asymmetry with `link-examination`: observation links were previously only manageable through the create/update full-replace payload. The new endpoint adds a single observation link (with notes) and rejects duplicates, mirroring the examination-link flow. The clinical-event frontend service gains a matching `linkObservationToEvent` call site.
- **Pagination on `GET /api/v1/clinical-events`.** `limit` (default 50, clamped to 200) and `offset` query params replace the unbounded list — previously every list call paid the full deep eager-load chain over all matching events.

### Changed
- **`VersionedMixin` is now active on `ClinicalEvent`.** Each `PUT /clinical-events/{id}` bumps `version` (previously the column was decorative — always 1), and both FHIR projections (`Condition` and `EpisodeOfCare`) emit the real `meta.versionId` instead of a hardcoded `"1"` (it was misusing the row id). This unblocks optimistic locking on the FHIR facade's `If-Match`/`ETag` path for clinical events.
- **Clinical Events: extracted a `ClinicalEventService` as the single mutation chokepoint.** All ~516 LOC of business logic (CRUD, full-replace link sync, soft-delete, lifecycle notifications, the eager-load chain) moved out of the endpoint module into `app/services/clinical_event_service.py`, leaving `clinical_events.py` as a thin HTTP adapter. The canonical eager-load chain (type→category_concept, examination_links, observation_links→biomarker→preferred_unit) is now defined once instead of duplicated in 5 places. `emit_event_notification` lives in the service so the FHIR facade and the import path can route through the same create/update/delete + link-sync + soft-delete + notification semantics as REST (previously each would have had to re-implement them). Backed by 12 real-DB conformance tests (lifecycle, soft-delete, every advertised Condition search param) + 2 Phase-2 tests (link-observation, pagination).

### Fixed
- **Clinical Events: `DELETE` now soft-deletes instead of hard-deleting.** `ClinicalEvent` carries `SoftDeleteMixin`, but `DELETE /api/v1/clinical-events/{id}` called `db.delete(event)` (hard delete) while the FHIR facade (`DELETE /fhir/R4/Condition/{id}`) tombstoned the same row — so deleting via REST permanently removed the row (and CASCADEd its links), destroying the audit trail the facade relies on for `410 Gone`. REST delete now sets `deleted_at`; `list_events` and `check_event_access` filter `deleted_at IS NULL`, so a soft-deleted event 404s from REST too. This unifies the two API surfaces and preserves audit history.
- **5 of 8 advertised FHIR Condition search params silently no-op'd.** `RESOURCE_PARAMS['Condition']` advertised `clinical-status`, `code`, `category`, `verification-status`, and `encounter`, but the generic dispatcher had no real handler for them against `ClinicalEvent` (the projection model): `clinical-status` mapped to a nonexistent `clinical_status` column, `code` ran the JSONB containment path against a `String` column, `category`/`encounter` need joins the dispatcher can't infer, and `verification-status` has no equivalent at all. A FHIR client searching `GET /fhir/R4/Condition?clinical-status=active` got unfiltered results — a conformance violation of the F8 "no advertised param may silently no-op" contract. Fixed: `clinical-status` falls back to the `status` column, `code` honors `String`-column equality (incl. `system|code` form), `category` and `encounter` resolve via per-resource EXISTS-subquery predicates through a new `ResourceEntry.param_filter` extension hook (joins to `clinical_event_types.category_concept` and `event_examination_links`), and `verification-status` is removed from the advertised set. Status/criticality/gender token matching is now case-insensitive via text-cast, fixing an `invalid input value for enum` crash when comparing a PG ENUM column to a lowercase FHIR token. 10 real-DB conformance tests pin every advertised param.
- **Clinical-event lifecycle notifications now fire on resolve and delete, not only on create.** Previously `POST /clinical-events` emitted a `CLINICAL` notification, but status changes (resolving a journey) and deletes were silent. Notifications are now routed through a single `_emit_event_notification` chokepoint (`created`/`updated`/`resolved`/`deleted`), with the resolve transition detected before mutation. Best-effort as before — failures are logged and never abort the parent write.

### Changed
- **Standardized all classification FKs into `concepts.id` on the `<role>_concept_id` naming convention.** The 6 entity tables that classify into the unified taxonomy used 4 different column-name styles — `examinations.category_id` and `clinical_event_types.category_id` hid their target table (read as a reference to a non-existent `categories` table), `biomarker_definitions`/`anatomy_structures` used `class_concept_id`, `doctors` used `specialty_concept_id`, and the relationships were resolved implicitly by SQLAlchemy single-FK guesswork. The two ambiguous columns are renamed to `category_concept_id` (migration `e1f2a3b4c5d6` renames the column + its FK constraint + its index in place, preserving data), the `category_entity` relationship becomes `category_concept`, and **every** concept relationship is now declared with an explicit `foreign_keys=[...]` so resolution no longer depends on there being a single concept FK per table (fragile the day a second is added). Backup import tolerates the legacy `category_id`/`category_details` JSON keys for one cycle alongside the new `category_concept_id`/`category_concept`. See `dev/plans/concept-fk-naming-convention-2026-07-07.md`.

### Fixed
- **`PUT /api/v1/examinations/{id}` returned a stale `category_concept` after a category change.** The endpoint loaded the exam via `check_examination_access` first (eagerly loading `category_concept` as `None`), then mutated `category_concept_id`, then reloaded — but the reload returned the same identity-map instance, and `selectinload` skipped the already-"loaded" relationship, so the response serialized `category_concept: null` (and the `category` name alias) even though the FK was correctly persisted. Fixed by adding `.execution_options(populate_existing=True)` to the post-commit reload so SQLAlchemy overwrites the stale relationship with fresh data.
- **`POST /api/v1/examinations` 500 on response serialization.** After the concept-FK rename, `ExaminationResponse.category_concept` was typed `Optional[Dict[str, Any]]`, but the endpoint returns the ORM `ExaminationModel` whose `.category_concept` is a `Concept` instance — Pydantic cannot coerce a populated ORM object into a bare `Dict` via `from_attributes` ("Input should be a valid dictionary ... input: <Concept object>"). Fixed by typing the field `Optional[ConceptResponse]` (a Pydantic model with `from_attributes=True`); same fix applied to `ExaminationSummaryResponse` and `ClinicalEventTypeResponse`. `ConceptResponse` is safe because `Concept.kind_tags` uses the eager `lazy="selectin"` strategy, so `kinds` is populated when the parent Concept loads (no async greenlet trap during sync serialization). 2 real-DB regression tests pin the contract (exam with + without a category).
- **`documents.category_concept_id` model/DB drift.** Migration `2b4ee2367046` added `documents.category_concept_id` (column + FK `fk_doc_category_concept`) to the database, but `DocumentModel` never declared it — so the live schema had a classification column the ORM did not know about (document classification still ran through the `entities["document_category"]` JSONB path, leaving `documents` inconsistent with every other classified table). The ORM now declares the column + `category_concept` relationship (DB already had it); the migration also adds the missing index. Full population/migration off the JSONB path is tracked as a follow-up. The same migration backfills the missing indexes on the rest of the concept-FK family that the ORM declared (`index=True`) but no prior migration had created: `anatomy_structures.class_concept_id`, `biomarker_definitions.class_concept_id`, `doctors.specialty_concept_id`, `concepts.parent_id`.

### Added
- **Concept-FK naming-convention + drift-detection regression tests** (`tests/test_concept_fk_naming_convention.py`, 13 cases). Pins the contract that every domain-specific FK into `concepts.id` matches `^[a-z_]+_concept_id$` (with `concept_kind_tags.concept_id` and `concepts.parent_id` as the documented owned-child / self-ref exceptions), that every `relationship("Concept")` resolves to a local concept-FK column (the `foreign_keys=` robustness guarantee), that the legacy `category_id`/`category_entity` attributes are gone, and — via a live-DB `information_schema` diff — that the ORM-declared concept columns and the DB-resident ones agree (catches the `documents.category_concept_id` drift class going forward).

- **Download the instance's seeds as a ZIP (UI + dev unpack helper).** A maintainer can now pull the curated taxonomy/anatomy/catalog off a running instance (including one on a different machine) without SSH: a "Seeds" download button on the **TaxonomyManager** page (`/admin/system/taxonomy`, SYSTEM_ADMIN-only) calls `GET /admin/seeds/export.zip` and saves a ZIP of the 9 seed-format JSON files. The download is read-only — the server never writes its own `data/seeds/`. The maintainer transfers the ZIP to their dev machine and runs `python scripts/unpack_seeds_zip.py <zip>` (backs up existing `data/seeds/*.json` to `.backup-<timestamp>/`, refuses unknown/path-traversal entries, then extracts), then reviews with `git diff data/seeds/`. Frontend `seedService.downloadSeedsZip()` mirrors the existing backup-download pattern.
- **Export an instance's data back into the shipped seeds (`SeedExportService` + CLI).** A curator can now build the canonical taxonomy/anatomy/biomarker/catalog set in a running instance via the UI + AI, then snapshot it into `backend/data/seeds/*.json` to replace the hardcoded seeds. New `app/services/seed_export_service.py` is the strict inverse of `SeedService` — one method per seed file — emitting the slug-keyed format (slug is the stable join key; `coding_system`+`code` carried as identity *evidence*, not the key). The CLI `python scripts/export_seeds.py` writes the files through a safety pipeline (stage → back up existing to `.backup-<timestamp>/` → atomic write), so the result is reviewed via `git diff data/seeds/`. Supports `--dry-run`, `--source global|<tenant-uuid>` (export the global taxonomy, or treat a template tenant as the source), `--out DIR`, `--no-backup`. Output is deterministic (sorted by natural key, fixed field order, only meaningful fields, regenerated metadata) so re-exporting an unchanged DB is a git no-op. 16 real-DB tests: per-file round-trip preservation against the shipped seeds, cross-export determinism, scoped export→re-ingest idempotency, the lossless-field guard, the backup/atomic pipeline, and the CLI surface.
- **Lossless seed read-side: anatomy `class_concept_slug` + biomarker concept-edge endpoints.** The seed ingest now reads `class_concept_slug` on `anatomy_structures.json` (preferred over the legacy ~8-value uppercase `category` enum), so anatomy whose class concept is outside the legacy set (e.g. a niche "lymphatic-structure" class) round-trips instead of silently falling back to "other-anatomy". The concept-edge ingest resolves a `biomarker` endpoint type by `BiomarkerDefinition.slug`, unblocking biomarker-level edges beyond panel membership. Both are backward compatible (legacy-only seed items keep working).
- **Backup export/import now carries the taxonomy + anatomy.** ZIP backups (`ExportType.FULL_BACKUP`) include three new sidecars so tenant-private customizations travel with the backup and survive a restore: `concepts.json` (tenant-scoped concepts + their multi-kind tags), `anatomy.json` (tenant-scoped body-part structures + the relations between them), and `concept_edges.json` (the polymorphic knowledge-graph edges — specialty→organ, biomarker→panel `MEMBER_OF`, AI-approved relationships). Import restores them in dependency order (concepts → anatomy → biomarkers/exams → edges) with UUID id-remapping: FK-bearing rows (examination `category_id`, biomarker `class_concept_id`, anatomy `class_concept_id`, edge endpoints) relink to the target's rows. References to global/seeded concepts that were deliberately not exported resolve by slug fallback against the target's re-seeded taxonomy. Edges with an unresolvable dangling endpoint are skipped with a warning rather than aborting the import. Restores are idempotent (upsert by slug / by natural key) so re-importing the same backup is a no-op. The `gather_anatomy` export is strictly tenant-scoped (`tenant_id == tid`) — it never leaks another tenant's `is_custom` anatomy. 17 new real-DB round-trip tests in `tests/test_export_import_taxonomy.py`.
- **Multi-kind concepts (taxonomy consolidation, part 2).** A concept can now carry multiple domain tags — "Blood Laboratory" is simultaneously an `examination_category`, a `biomarker_class`, and a `document_category` — instead of existing as 3 unrelated rows. The single-valued `concepts.kind` column is replaced by a many-to-many `concept_kind_tags` join table (reusing the existing `conceptkind` PG enum) plus a denormalized `primary_kind` column for cheap single-badge rendering. Migration `9a3f7c2e1b4d` is a pure schema change (create join table + `primary_kind`, backfill both from the legacy `kind`, swap the unique index from `(kind, slug, tenant)` → `(slug, tenant)`, drop `kind`); the 16 legacy duplicate concepts are de-duplicated in the seed data (`concepts.json`: 68 → 54 items), which is the single source of truth — the migration assumes a greenfield/empty concepts table (per the prior `consolidate_categories` note) and carries no hardcoded merge table. The new `concepts_with_kind()` helper replaces every legacy `Concept.kind ==` filter across 10 consumer files (services, AI pipeline, endpoints); `resolve_concept_by_slug` keeps its `kind` param (now an optional join-table filter) so resolvers needed no caller changes; `create_concept` accepts `kinds: List[ConceptKind]` (legacy single `kind` still wrapped for backward compat). Pydantic schema emits `kinds: List[str]` + `primary_kind`; the create endpoint accepts either `kinds: [...]` or legacy `kind`. `biomarker_category_to_concept_slug` drops the now-obsolete `-class` suffix. 6 new multi-kind model tests + updated concept/service/API/seed/taxonomy-tool tests; the dedup also fixed 3 previously-failing seed idempotency tests. Frontend (types, TaxonomyManager, ConceptGraphView) lands next.
- **Biomarker catalog auto-seeding.** `seed_default_catalog()` loads `data/seeds/default_catalog.json` (units + biomarker definitions) at startup via `CatalogImportService`, resolving the empty-catalog issue. Runs AFTER `seed_concepts` so `biomarker_class` concepts exist for `class_concept_id` resolution. **Biomarker panel memberships** (`seed_biomarker_panels()`) creates `MEMBER_OF` edges from biomarkers to panel concepts (e.g. LDL → Lipid Panel) from `data/seeds/biomarker_panels.json`. Both wired into `main.py` startup.
- **FHIR observation category on concept meta_data.** The `_BIOMARKER_CONCEPT_TO_FHIR_CATEGORY` and `_BIO_FALLBACK_FHIR_CATEGORY` hardcoded dicts in `persistence.py` are replaced by `meta_data["fhir_observation_category"]` stored directly on each `biomarker_class` concept in `concepts.json`. The FHIR mapping is now editable via the TaxonomyManager UI and lives with the concept (single source of truth). The `_fhir_observation_category()` function takes the concept object directly.
- **Unified taxonomy — Phase 7 (frontend).** Management UI and reusable components for the concept layer: `TaxonomyManager.tsx` — generic CRUD page with a domain switcher (all 16 ConceptKind values), inline editor with color/icon/alias/coding-system fields, icon picker from the Lucide catalog; `<TaxonomyTypeahead>` — server-backed debounced search component for picking concepts in forms (replaces free-text inputs); `conceptService.ts` — typed API wrapper for all /concepts + /concept-edges endpoints; `types/concept.ts` — full TypeScript types. Route: `/admin/system/taxonomy` (SYSTEM_ADMIN); `/examinations/categories` now renders TaxonomyManager. Sidebar item under Administration. Broken `examinationService` + `clinicalEventService` category functions repointed from the dropped endpoints to `/concepts?kind=examination_category` and `/concepts?kind=event_category`. TypeScript build + ESLint clean.
- **Unified taxonomy — Phase 6 (AI chatbot tool).** New `taxonomy` domain tool module auto-wired into the agentic chatbot via `@register_chat_tool("taxonomy")`: `search_concepts(search_term, kind)` — ranked trigram + alias search over the concept layer; `get_concept_neighborhood(concept_id, relation)` — one-hop graph traversal; `get_entity_concepts(entity_type, entity_id, relation)` — "what panels is this biomarker in?" / "what specialty does this doctor have?". All tools are live-read-only (AI never writes; proposals go through HITL). 4 tool tests including alias matching and error handling.
- **Unified taxonomy — Phase 5 (consolidation).** Drops the 4 old scattered category/group tables (`examination_categories`, `clinical_event_categories`, `biomarker_groups`, `biomarker_group_members`) and consolidates all category vocabulary into the unified `concepts` table. Migration `2b4ee2367046`: adds `class_concept_id` FK to `biomarker_definitions` + `anatomy_structures`, `specialty_concept_id` to `doctors`, `category_concept_id` to `documents`; repoints `examinations.category_id` + `clinical_event_types.category_id` FKs to `concepts.id`; drops old free-text/enum `category`/`specialty` columns; drops old tables. All 17 consumer files updated: `analytics_service` (clinical_groups from concept_edges), AI pipeline (`resolve_category` → concepts, form fillers), endpoints (examinations/clinical_events/biomarkers), schemas, export/import, seed service. Deleted `ExaminationCategory` model + endpoint + schema. 101 tests passing.
- **Unified taxonomy — Phase 4 (seeds).** Curated seed data for the concept layer: ~60 concepts covering 9 domains (specialties, body systems, examination categories, event categories, biomarker classes, biomarker panels, anatomy classes, document categories, ATC medication classes) + ~19 concept↔concept edges (specialty EXAMINES body_system, specialty PERFORMS examination_category, ATC class hierarchies via PART_OF). Idempotent upsert by `(kind, slug)` for concepts and `(src, dst, relation)` for edges; parent_slug resolution for hierarchical concepts. Integrated into startup seeding (`main.py` lifespan). 4 seed idempotency tests.
- **Unified taxonomy — Phase 3 (API endpoints).** REST endpoints for the concept layer: `GET/POST /concepts`, `GET/PUT/DELETE /concepts/{id}`, `GET /concepts/search?q=&kind=`, `GET /concepts/{id}/neighbors` (one-hop graph traversal), `GET/POST/DELETE /concept-edges`. Full RBAC enforcement (SYSTEM_ADMIN for global rows; ADMIN for tenant-scoped; USER read-only), tenancy filtering, and validation. Pydantic v2 schemas (`ConceptCreate/Update/Response`, `ConceptEdgeCreate/Response`, `NeighborResponse`). 11 HTTP integration tests covering CRUD, search, edge creation, neighbor traversal, RBAC (USER 403 on write, USER 200 on read), and validation errors.
- **Unified taxonomy — Phase 2 (service layer + search).** `ConceptService` provides full CRUD for concepts + edges with RBAC enforcement (SYSTEM_ADMIN for global rows; ADMIN for tenant-scoped; USER read-only), tenancy filtering (`or_(tenant_id == tid, tenant_id.is_(None))`), soft-delete-with-retire-if-referenced lifecycle, one-hop neighbor traversal, and entity→concept lookup ("what panels is this biomarker in?"). AI-proposed edges land as `status=PROPOSED` and are invisible to graph queries until approved. `search_concepts()` added to `catalog_search_service` — trigram + ilike + JSONB aliases containment, optionally filtered by `kind`, excludes retired/deleted. 20 real-DB integration tests covering CRUD, RBAC, tenancy isolation, edge validation, neighbor traversal, proposed-edge lifecycle, and ranked search.
- **Unified taxonomy / knowledge-graph foundation (Phase 1).** Introduces two new tables that form the backbone of a consolidated medical vocabulary system: `concepts` (controlled-vocabulary nodes for specialties, examination/event/biomarker/anatomy/document categories, biomarker panels, medication classes, diseases, body systems, etc.) and `concept_edges` (typed polymorphic edges between concepts and/or domain entities — `MEMBER_OF`, `EXAMINES`, `PERFORMS`, `TREATS`, …). This replaces the scattered `examination_categories`, `clinical_event_categories`, `biomarker_groups` tables and free-text `category` columns (consolidation migration follows in a later phase). Six new enums (`ConceptKind`, `ConceptStatus`, `ConceptProvenance`, `EdgeApprovalStatus`, `EdgeEndpointType`, `ConceptRelationType`). `coding_system` is a free string (not enum) to support open-ended terminology systems (LOINC, SNOMED, ATC, ICD-10, CVX, MeSH). Trigram GIN index on `name`+`slug` powers ranked search; JSONB GIN on `aliases` enables containment matching. Unique partial indexes use `COALESCE(tenant_id, sentinel)` to correctly handle global (NULL-tenant) rows. Migration `1a3dd1256035`.

### Fixed
- **Export/import jobs stuck in "Processing" forever (completed_at type bug).** `ExportService.complete_job`/`fail_job` and `ImportService._complete_job`/`_fail_job` passed `datetime.now(timezone.utc).isoformat()` — a **string** — into the `completed_at` column (`TIMESTAMP WITH TIME ZONE`). asyncpg rejects strings (it needs a real `datetime`), so the export/import actually succeeded (the ZIP was written) but the DB update marking the job `COMPLETED` crashed, and the fallback `fail_job` crashed the *same way* — leaving the job stuck in `PROCESSING` permanently and the UI never showed an error. Now passes the `datetime` object directly. 3 new real-DB regression tests cover `complete_job`/`fail_job` (the mocked export tests had skipped these, which is why it slipped through).
- **Biomarker class link (`class_concept_id`) was silently dropped on every backup round-trip.** The export emitted the concept *name* (e.g. `"Blood Laboratory"`) in the legacy `category` field, but the import's `biomarker_category_to_concept_slug` only swaps `_`→`-`, turning that name into `"blood laboratory"` (with spaces) which never matched the real slug `"blood-laboratory"` — so `class_concept_id` resolved to `None`. The export now also emits `class_concept_slug` (the canonical concept slug) and `CatalogImportService` resolves that ahead of the legacy name→slug path. The ontology-catalog-URL import (whose JSON uses the underscore convention `blood_laboratory`) is unaffected.
- **Examination category (`category_id`) was exported but never restored.** The `examinations.json` sidecar carried `category_id` + `category_details`, but `_restore_examinations` ignored both — imported exams always came back uncategorized. It now relinks the category (via the concept id-remap, with slug fallback to the target's re-seeded global taxonomy) and also restores the previously-dropped `organization_id`, `source_integration_id`, `external_id`, and `auto_extract_metadata` fields.

### Changed
- **Examination detail toolbar consolidated into an Actions dropdown.** The previously separate *Upload Results*, *Run AI Analysis*, *Edit*, and *Delete* buttons on the examination detail page are now grouped under a single **Actions** dropdown (`MoreVertical` trigger), decluttering the sticky toolbar. Each item retains its prior behaviour and iconography (Add Results / Take Photo on mobile, Full AI Reconstruction, Fast AI Extraction, Edit Details, Delete); the `ExaminationAIActions` (summarize/findings/analysis) button stays separate, and Save/Cancel still surface inline when global editing is active.
- **Repository tab "Include in AI" toggle now matches the FileCard style.** Both the list-view and grid-view document toggle in the examination Repository tab adopt the same subtle indigo chip used on the new-examination document `FileCard`: a `Sparkles` icon with gradient "AI Extract" / muted "AI Off" label on a translucent indigo/grey background (replacing the solid blue `Cpu` button). Added `aria-pressed` + accessible titles.
- **Category selector in Smart Extraction (single) mode.** The new-examination form now shows a **Category** picker (same `CategorySelector` + inline `AIAssistButton` as manual mode, with "Optional — AI will auto-extract if not set" hint) when Smart Extraction AI is enabled but Bulk Create is off. Previously the category was hardcoded to `Clinical` in smart mode; the create payload now sends the user's choice (or `undefined` so the backend auto-extracts).
- **Docs: seed export workflow + schema cheatsheet.** `docs/SEEDING_AND_DEMOS.md` gains §7 "Exporting an instance back to seeds" (identity model, the three delivery surfaces — UI button / `export_seeds.py` CLI / `unpack_seeds_zip.py` — the cross-machine transfer workflow, source scope, the anatomy-markers caveat). `docs/DEVELOPMENT.md` Seed System section adds the export/unpack scripts. The `seeding` skill gains a "Seed export (inverse)" subsection so future sessions know `SeedExportService` exists. New `backend/data/seeds/README.md` is a per-file field-schema cheatsheet (every seed file's required/optional fields + enum values, verified against the ingest code) plus the edit/export workflows.
- **Docs refresh: install/dev/seed.** INSTALL.md's first-time-seeding section now reflects that the full catalog (anatomy + taxonomy + default biomarker catalog) auto-seeds via `SeedService.seed_all()` on every startup — the standalone `seed_default_catalog.py` / `seed_anatomy.py` CLIs are no longer required for setup. DEVELOPMENT.md documents `scripts/reset-dev-db.sh` (wipe + restart the dev Postgres/Redis volumes) and adds a Seed System section covering `seed_all()`'s declarative stage ordering, the standard `{metadata, items}` envelope, the `{added, updated, skipped, errors}` stats contract, and per-stage re-seed. SEEDING_AND_DEMOS.md gains a §0 Seed Pipeline table + fixes the anatomy section for the `anatomy_structures.json`/`anatomy_relations.json` split (54 nodes + 62 edges; new `seed_all` log shape; manual re-seed via the service method, not the expansion-pack CLI).
- **Unified notification system — Phase 5 (role-based admin Notification Center).** Completes the notification system with an admin/system console. New `GET /notifications/admin/stats` endpoint aggregates delivery metrics (counts by source, category, and per-channel delivery status; cross-tenant for SYSTEM_ADMIN). A new `AdminNotificationCenter` component surfaces stat cards, a broadcast composer (tenant-scoped for ADMIN/MANAGER; system-wide cross-tenant for SYSTEM_ADMIN, gated accordingly), and a live admin feed. It appears as a role-conditional "Admin" tab in the Notification Center, visible only to ADMIN/MANAGER/SYSTEM_ADMIN — giving each role its appropriate scope (system admin sees cross-tenant + can broadcast system-wide; tenant admin sees/manages their tenant; regular users see only their personal inbox). i18n keys added for `en`/`el`.
- **Unified notification system — Phase 4 (source wiring).** The remaining notification sources now flow through the unified `emit()` pipeline. **HITL proposals**: the 6 AI propose-tools (`propose_create_clinical_event`, `propose_add_medication`, etc.) now emit an `AGENT`/`HITL_TASK` notification to the chatting user, so pending AI proposals surface in the notification inbox even after the chat is closed (with an "Open chat" action). **Integrations**: `integration_sync_service.run_sync` emits an `INTEGRATION_EVENT` to the integration owner when new wearable/lab data arrives, and a `SYNC_FAILURE` (warning) to the owner + tenant admins on auth/data errors (rate-limit and skip outcomes stay silent). **System broadcasts**: new `POST /admin/notifications/broadcast` endpoint — `ADMIN`/`MANAGER` broadcast to their tenant, `SYSTEM_ADMIN` may broadcast system-wide or target another tenant. **Clinical events**: creating a clinical event emits a `CLINICAL_EVENT` notification to the care team. The `ToolContext` gained a `user_id` field (threaded from the chat entry points) so HITL notifications can target the chatting user.
- **Unified notification system — Phase 3 (biomarker rules UI + allergy naming fix).** A new biomarker-rules builder lets users create custom biomarker-limit notifications ("get alerted when glucose > 140" or "out of normal range"): a searchable biomarker picker (showing each biomarker's normal range), a condition (operator + threshold or "out of normal range"), multi-recipient targeting (any combination of patients and doctors — a notification can be assigned to multiple recipients, not one-to-one), severity, and a per-rule cooldown to prevent alert storms. Existing rules can be enabled/disabled, edited, deleted, and test-fired. The builder is a new "Biomarker Rules" tab in the Notification Center. Rules are evaluated event-driven on observation create (Phase 1 backend). Separately, the misleadingly-named `/alerts` page (which actually showed allergies) is renamed `ClinicalAlerts` → `AllergyAlerts`, with the sidebar label updated to "Allergy Alerts" — freeing the "alerts" name for the biomarker-alert system. i18n keys added for `en`/`el`.
- **Unified notification system — Phase 2 (real-time delivery + user-scoped inbox).** The notification bell is now real-time and user-scoped: a new `/ws/notifications` WebSocket streams each user's notifications from a per-user Redis channel (`user:{id}:notifications`) authenticated via the `["bearer", token]` Sec-WebSocket-Protocol subprotocol, replacing the previous 30-second patient-scoped poll. The bell now works **without a patient selected** (so system/tenant notifications surface), renders severity dots, action buttons (deep-link to the relevant resource), and category icons for all notification types (reminder/alert/hitl/agent/system/integration/clinical_event), and shows a live/reconnecting indicator. The Notification Center page (`/notifications`) is rebuilt with Inbox / All / Reminders tabs, category + status filters, mark-all-read, and dismiss. The frontend `notificationService` and `notificationStore` are rewritten around the Phase 1 fan-out model (inbox items, optimistic read/dismiss, live-event handler). A new `useNotificationStream` hook manages the WebSocket lifecycle (auto-reconnect with backoff + fallback unread-count poll if the socket can't open). The service worker's obsolete `/notifications/{id}/delivered` PATCH is removed (delivery status is now tracked server-side by the push delivery worker). i18n keys added for `en`/`el`.
- **Unified notification system — Phase 1 backend (fan-out model).** The notification subsystem is rebuilt around a 3-table fan-out architecture: one immutable `Notification` event (with `source`/`category`/`severity`/`type`, nullable `patient_id`/`tenant_id` for system/tenant broadcasts), `NotificationRecipient` (per-user inbox state: unread/read/dismissed), and `NotificationDelivery` (per-recipient per-channel delivery log). A single `notification_service.emit()` entry point now produces every notification — it resolves target specs (`USER`/`PATIENT`/`DOCTOR`/`TENANT`/`SYSTEM`) into concrete user ids, fans out recipients + deliveries, optionally links a FHIR `Communication` for clinical sources, publishes to a per-user Redis channel, and enqueues push delivery. The orphaned `AlertModel` is replaced by `NotificationRule` (operator + value + biomarker + multi-recipient targets), evaluated **event-driven** on observation create (`fhir_service.create_observation` → `notification_rule_service.evaluate_and_fire`) with per-rule cooldown. New endpoints: `/notifications/inbox`, `/unread-count`, `/{id}/read|dismiss`, `/read-all`, `/notifications/admin` (tenant/cross-tenant feed), and `/notification-rules` CRUD + `/test`. The `deliver_notification` worker task now iterates per-recipient deliveries (dead-subscription pruning preserved). The legacy `/alerts` endpoints, `AlertModel`, and the dead `biomarker_update` event trigger are removed. Schema folded into the squashed `0001` migration (no backfill — greenfield). **Frontend (real-time bell, Notification Center, rule builder) lands in Phase 2.**
- **FHIR facade tenant bridge: service-account auth + X-Tenant override (F19, G10).** External systems can now connect to the FHIR facade (`/fhir/R4/*`) with a service-account JWT (minted by admins via `POST /auth/service-account`) instead of a user password. The CapabilityStatement documents both auth modes. `SYSTEM_ADMIN` can scope into any tenant via the `X-Tenant: <uuid>` header. Provenance records the external system identity (`client_id`) as a third agent entry on service-account-initiated writes. This is the stepping-stone before full SMART-on-FHIR (F1) — deferred to Stage 4.
- **Service-account JWT auth for external systems (F19).** New `POST /auth/service-account` endpoint (ADMIN/MANAGER/SYSTEM_ADMIN only) creates a password-less machine account (`is_service_account=True`) and returns a long-lived JWT carrying `is_service_account=True` + `client_id=<instance_name>`. The token works as a Bearer against the FHIR facade (`/fhir/R4/*`) and the REST API. Service accounts are bound to one tenant (the JWT's `tenant_id`). `TokenData` gained `is_service_account` + `client_id` fields. `/auth/login` now rejects service-account emails (403). `UserModel.hashed_password` is now nullable (service accounts have no password). Migration `f19a2b3c4d5e` adds the `is_service_account` column with a partial index. This is the tenant-bridge stepping-stone before full SMART-on-FHIR (F1) — external systems can now connect with a bearer token instead of a user password.
- **Push resilience: per-row 401-race retry, cursor integrity, batch isolation.** Three robustness fixes: (1) Push now retries once after a 401 race (token expired between the `get_live_token` check and the PUT) via `force_refresh` — mirroring the pull path's `_authorized_search` pattern. Previously a 401 mid-batch aborted the entire push. (2) The `last_pushed_at` cursor now only advances past *successfully* pushed rows (tracks the max `updated_at` among created/updated outcomes). If ALL rows fail, the cursor stays unchanged → full retry next cycle. Previously the cursor advanced to `now` unconditionally, so transiently-failed rows were never retried (silent data loss). (3) Per-row error isolation — one bad row counts as `error` and the batch continues; only `insufficient_scope` (H1) aborts the batch early.
- **Outbound push now POSTs a Provenance to the remote server (H3).** Hospitals typically require this for regulatory audit ("Health Assistant pushed this from integration X"). After each successful push (PUT 201/200), `_push_one` POSTs a Provenance with the remote Observation id as the target, the local Device as the agent, and a CREATE activity code. Best-effort: Provenance failure (server doesn't support it, network error) is logged and never aborts the push. The push result carries `provenance_created`/`provenance_failed` counters. New `fhir_create` (POST) primitive added to `sdk/fhir.py` alongside the existing `fhir_conditional_update` (PUT).
- **Outbound push requests write scope when push is enabled (H1).** `DEFAULT_SCOPES` was read-only (`patient/*.read`); a spec-strict SMART server rejects `PUT /Observation` with 403. New `PUSH_SCOPES` adds `patient/*.write`; `begin_oauth` passes `push_enabled` when `sync_direction` is `both` or `push_only`. The config-flow description documents that switching to push requires re-authorizing. 403 `insufficient_scope` is now detected — the push stops and the result carries an actionable "re-authorize with write permissions" warning instead of silently failing every row.
- **Error messages now include parsed FHIR OperationOutcome diagnostics.** `parse_operation_outcome` existed but was never called on any error path — all integration SDK raises used raw `response.text[:300]`, making hospital 422/400 rejections hard to debug. Both `fhir_conditional_update` and `http_request` now extract `OperationOutcome.issue[].diagnostics` (the most useful field — e.g. "code system https://loinc.org not recognized") before falling back to the raw body.
- **Outbound push: multi-component observations (blood pressure) + multi-range referenceRange + note preservation.** `fhir_observation_to_create` previously returned `None` for any observation lacking both `valueQuantity` and `valueString` — silently dropping blood pressure (the most common vital, which uses `component[]` for systolic + diastolic) and panels (BMP, CBC). The early-return now checks for `component[]` — these observations survive and their components are mapped onto the `Observation.component` JSONB column (added in Phase 6). `note[0].text` is mapped to the ORM `comment` field. Additionally, `_extract_reference_range` (renamed from `_convert_reference_range`) now returns the full canonical `referenceRange[]` list instead of flattening only the first range to `{min, max}` — multi-range observations (age-stratified, gender-specific) preserve all ranges with `type`/`appliesTo`/`text`/units intact.
- **OAuth hygiene: atomic state consume, 429 fix, token revocation, DCR guard.** Four robustness fixes in the integration SDK auth layer: (1) `OAuthStateStore.consume` now uses `GETDEL` (Redis ≥ 6.2) for a truly atomic read-and-delete — the old GET+DELETE had a TOCTOU race where duplicate OAuth callbacks could consume the same state. (2) The token-endpoint 429 response is now correctly classified as `IntegrationRateLimitError` (was `IntegrationAuthError` — rate-limited token endpoints would pause the integration instead of retrying). (3) `SmartOAuth.revoke(integration)` calls the SMART `revocation_endpoint` (RFC 7009) on disconnect — `DELETE /instance/{id}` now revokes tokens before deleting, preventing stale tokens on hospital servers. Best-effort: network errors are swallowed. (4) If a SMART server advertises a `registration_endpoint` but DCR returns no `client_id`, the flow now raises an actionable `IntegrationAuthError` instead of silently producing an authorize URL with an empty `client_id`.
- **Integration sync pipeline consolidated into shared `run_sync` helper.** The pull → convert → biomarker-map → telemetry-split → push → log pipeline was previously copy-pasted across 4 call sites (the Celery worker, the manual-sync endpoint, the webhook path, and the bridge provider). It now lives in a single `IntegrationSyncService.run_sync(integration, db, provider, *, source)` method that both the worker and the manual-sync endpoint delegate to. The shared helper also acquires the Redis dedup lock internally — fixing a race condition where the manual-sync endpoint didn't respect the worker's lock (concurrent Sync Now + beat could double-write). The 3-tier error contract (`IntegrationAuthError` → status=ERROR; `IntegrationRateLimitError` → retry; generic → log) is now uniform across both sites. The manual-sync endpoint returns HTTP 409 when a sync is already in progress (lock held), 401 on auth failure, 429 on rate limit, 500 on other errors.
- **`Observation.component` JSONB column added (prereq for H2 push).** The FHIR R4 `Observation.component` (`0..*`) — used by multi-component observations like blood pressure (systolic + diastolic) and panels (BMP, CBC) — is now a first-class JSONB column on the ORM model (migration `i7b8c9d0e1f2`, with a GIN index for future `component-code-value-*` search). The import converter (`fhir_to_observation_orm`), `_upsert_observation` (create + update paths), `to_fhir_dict()`, and `to_dict()` all carry it. The `ObservationCreate`/`Update`/`Response` schemas accept `component: Optional[List[Dict]]`. Without this column, the push path (H2, Phase 7) could not emit `component[]` because push reads `obs.to_fhir_dict()` which projects from ORM columns. **DB migration required** (`alembic upgrade head`).
- **`Observation.interpretation` now stores the canonical FHIR R4 list shape (was a flattened string).** The column type changed `String → JSONB` (migration `i6a7b8c9d0e1`) with a backfill that wraps legacy plain-string rows in `[{"text": value}]`. The import path (`fhir_to_observation_orm`), REST create path (`fhir_service.create_observation`), and OCR persistence path all normalize any input (string, list, or None) to the canonical `0..* CodeableConcept` list via the new `_normalize_interpretation` helper. The round-trip import → ORM → `to_fhir_dict()` now preserves the LOINC/OBSINT coding (previously collapsed to a lossy display string). `to_dict()` (frontend projection) still flattens to a single display string via the existing `_flatten_interpretation` helper, so the frontend contract is unchanged. **DB migration required** (`alembic upgrade head`).
- **`validate_and_filter_observations` no longer mutates the caller's list.** The FHIR write-time gate previously did `observations[:] = valid` (in-place mutation), so callers that held a reference to the original list silently saw a shorter list with no signal that items were removed. It now returns `(valid, dropped_count)` and leaves the input untouched. **Breaking API change** for any out-of-tree caller that relied on the in-place mutation — switch to unpacking the returned tuple.
- **FHIR Bundle import: reference routing + cross-tenant collision warnings.** Two import-correctness fixes: (1) `_apply_remap` no longer misroutes bare `urn:uuid:` references — an `Observation.encounter` given as `urn:uuid:abc` now correctly maps to `Encounter/<new>` (was silently mapped to `Patient/<new>`). A new `FIELD_HINT_TO_TYPE` map covers all reference-bearing fields (`subject`/`patient`→Patient, `performer`/`author`→Practitioner, `partOf`→Organization, `context`/`encounter`→Encounter, `device`→Device, `specimen`→Specimen); ambiguous hints (`sender`/`recipient`) are resolved by a bundle look-ahead (`{id → resourceType}` index built once per bundle). (2) `_resolve_id` no longer silently creates a new row when a bundle id exists in another tenant — the collision is now surfaced as a warning in the ImportJob result (e.g. `"Patient/<id> already exists in tenant <other>; created new with id <new>"`).
- **FHIR Bundle import now records a Provenance per entry (bulk-import audit trail).** Previously bulk imports left no audit trail of who imported what — only the `ImportJob` row itself tracked the importing user, with no per-resource Provenance. Each created/updated/deleted entry now records a Provenance via `record_provenance` (best-effort, never aborts the import). The Provenance carries the entry's resource type + id, the `CREATE`/`UPDATE`/`DELETE` activity code, the importing user as the agent, and an `ImportJob/<id>` reference in `entity_inputs` so bulk-import Provenance is distinguishable from facade-write Provenance. `restore_fhir_bundle` gained `actor_user_id` + `source_job_id` params (threaded from the job-driven import paths).
- **FHIR Bundle import now supports all 15 resource types (was 7).** The import dispatcher previously handled only Patient, Observation, MedicationStatement, AllergyIntolerance, DiagnosticReport, Organization, and Practitioner — silently dropping MedicationRequest, Condition, Encounter, Device, Communication, and Provenance on every bulk import (the `_TO_ORM` converter map already had converters for all 15; only the dispatcher branches were missing). All 15 non-DocumentReference types now import correctly: Condition → ClinicalEvent, Encounter → Examination, Device → DeviceModel, Communication → CommunicationModel, Provenance → ProvenanceModel (immutable — existing rows are not overwritten, counted as an idempotent no-op). MedicationRequest delegates to the same Medication table as MedicationStatement (distinguished by the `intent` discriminator). Genuinely-unsupported types (anything outside the FHIR R4 resources we model, e.g. `Coverage`) now surface a warning instead of being silently dropped.
- **FHIR Bundle import now honors `entry.request.method` + `ifNoneExist` (transaction-integrity fix).** Previously every entry in a posted Bundle was treated as POST (create-new) — a bundle authored as a transaction with `{"request": {"method": "PUT", "url": "Observation/<id>"}}` silently became a brand-new observation with a server-generated UUID, losing round-trip integrity with third-party EHR / SMART transaction Bundles. The import path now routes on the verb: `PUT` updates an existing same-tenant row or creates one **with the URL's id** (not a random uuid); `POST` + `ifNoneExist` performs a conditional create (skipped when a match exists — Patient `identifier` → `mrn` lookup today, unsupported forms log a warning and create rather than silently treat "couldn't match" as "no match"); `DELETE` soft-deletes (idempotent on missing id); `POST` (or no request block) keeps the historical create-new behaviour. **Breaking API change:** `ImportService.restore_fhir_bundle` now returns a `BundleRestoreResult` dataclass (attribute access: `.created` / `.updated` / `.deleted` / `.skipped` / `.errors` / `.warnings` / `.id_remap`) instead of the previous 5-tuple. Internal callers + `scripts/seed_demo.py` updated; any out-of-tree caller unpacking the old 5-tuple must switch to attribute access.
- **FHIR R4 facade: honest versioning + If-Match optimistic locking.** The CapabilityStatement per-resource `versioning` is now honestly `"no-version"` (we don't implement `vread` / `history-instance` / a version-history table). `If-Match` optimistic locking is now implemented: a PUT with `If-Match: W/"3"` against a version-2 row returns HTTP 412 Precondition Failed (RFC 7232). Garbage If-Match values are silently ignored (FHIR lenient on ETag form). The `VersionedMixin.version` column is still bumped on update and exposed in the ETag header. `vread` + `history-instance` remain deferred. Location header on create uses the spec-correct short form `<base>/Type/<id>` (allowed for `versioning="no-version"`; the versioned form is only required when `vread` is implemented).
- **FHIR R4 facade: `Provenance.agent.who` now references real FHIR resource types.** Previously `agent.who.reference` used `User/<id>` and `Integration/<id>` — neither is a FHIR resource, so external clients resolving the reference got 404. The agent block is now async and resolves both actor types via DB lookups: `user_id → Practitioner/<doctor.id>` (via `DoctorModel.user_id`, mirroring the F11 fix) and `integration_id → Device/<device.id>` (via `DeviceModel.owner_integration_id`). When resolution yields nothing (admin/manager with no Doctor row; orphan integration with no Device row), the agent is emitted in display-only form `{"display": "..."}` — spec-compliant and logged as "degraded" so audit-trail gaps are identifiable.
- **FHIR R4 facade: `DocumentReference.author` now resolves to a real Practitioner.** Previously `to_fhir_dict()` emitted `Practitioner/<owner_id>` but `owner_id` is a `ForeignKey` to `users.id` (User id, not Doctor id) — external FHIR clients resolving the reference got 404. Added a nullable `documents.practitioner_id` column (FK to `doctors.id`, `ON DELETE SET NULL`), backfilled from the existing owner→doctor mapping at migration time. `to_fhir_dict()` now emits `Practitioner/<practitioner_id>` when set and **omits** `author` entirely when NULL (rather than emit a wrong reference). The upload path (`document_service_db.upload_document`) and the edited-copy path both resolve the Practitioner id from the owner at upload time via a new `_resolve_practitioner_id` helper. Admin/manager uploads (no Doctor row) get no `author` reference rather than a wrong one.
- **FHIR write-time validation gate now covers every clinical write path.** Previously 4 service-layer write paths bypassed `assert_valid_fhir` (doctor/organization/medication/document services), so invalid FHIR could be persisted via the REST endpoints even though the facade and `fhir_service.*` were gated. All 4 services now call `assert_valid_fhir(obj)` right before `commit()`, mirroring the existing gate pattern: `doctor_service.create_doctor`/`update_doctor` (Practitioner), `organization_service.create_organization`/`update_organization` (Organization), `medication_service.create_catalog_medication`/`update_catalog_medication`/`add_patient_medication`/`update_patient_medication` (Medication + MedicationCatalog), `document_service_db.upload_document` + the edited-copy path (DocumentReference). Closes the audit's FHIR validation gate coverage table — every row now reads ✅.
- **FHIR R4 facade: `_elements` projection honored and `_format=xml` explicitly rejected.** `_elements` (comma-separated list of top-level fields) is now applied as a post-serialization projection in search results, keeping the requested fields plus the always-present `resourceType` / `id` / `meta` per FHIR R4 spec. `_format=xml` (and `application/fhir+xml`, RDF, TTL) is now explicitly rejected with a 400 OperationOutcome — previously silently returned JSON which was misleading. JSON-family formats (`json`, `application/json`, `application/fhir+json`) are accepted case-insensitively. `_include` / `_revinclude` chained search remain deferred (parsed but not yet executed).
- **FHIR R4 facade: Bundle entries now carry `search.mode`** (`"match"` for primary results, `"include"` for `_include` resources). Strict R4 clients expect this on every Bundle entry.
- **FHIR R4 facade: `_summary=count` and `_total` are now honored.** `_summary=count` (or `_summary=true`) returns a Bundle with an empty `entry[]` and the `total` (the main SELECT is skipped — cheap "how many match?" queries). `_total=none` skips the COUNT(*) query entirely AND omits the `total` key from the Bundle (default remains `_total=accurate` for backward compat).
- **FHIR R4 facade: search-param coverage now matches advertisement.** Following the F4 "advertise only what's implemented" principle: the dispatcher now honors the commonly-used subset of search params (previously accepted-but-silently-dropped), and the long-tail unimplemented params are removed from `RESOURCE_PARAMS` so they're no longer advertised in the CapabilityStatement. Newly-implemented: `value-quantity` (numeric comparator with FHIR prefix matrix), `performer`/`sender`/`recipient`/`agent`/`target`/`partof`/`parent`/`author` (JSONB reference containment with bare-UUID type hints), `criticality` (scalar enum), `medication` (delegates to the `code` column), `activity` (Provenance CodeableConcept), `identifier` (universal JSONB `{system,value}` containment), `name`/`family`/`given` (text-cast ILIKE on the JSONB name blob), `gender`/`active` (scalar tokens). Removed (not advertised): `Condition.severity`, `Encounter.class/reason-code/diagnosis/practitioner`, `Device.manufacturer/model`, `Medication.form`, `Provenance.entity`, `DocumentReference.author` (returns when F11 lands).
- **FHIR R4 facade: token / category search now uses JSONB `@>` containment semantics.** Three sub-bugs fixed: (1) `code` search previously inspected only `coding[0]` — multi-coding resources (e.g. an Observation carrying both a LOINC and a SNOMED coding) silently missed. The new helper `_jsonb_codeable_concept_match` builds a `@>` containment predicate so a CodeableConcept column matches the supplied fragment anywhere in its structure. (2) `category` search previously did `model.category.astext == value` against `Observation.category` which is a **list** of CodeableConcepts — the comparison matched nothing. The dispatcher now detects JSONB-list columns and emits a list-wrapped `@>` fragment (so any element matches); scalar enum columns like `AllergyIntolerance.category` keep the simple equality. (3) The `:not` token modifier (e.g. `code:not=1234-5`) is now parsed and negates the match. Other modifiers (`:above`/`:below`/`:in`/`:text`) and chained-reference search remain deferred. The `type` param (DocumentReference/DiagnosticReport) was previously unimplemented and now uses the same `@>` path.
- **FHIR R4 facade: date search now honors FHIR precision semantics.** Previously `_parse_fhir_datetime("2024")` was treated as exact `2024-01-01T00:00:00Z` instead of the whole-year range per the FHIR R4 spec. Date search now uses precision-aware ranges: year (`2024`) → `[2024-01-01, 2025-01-01)`; month (`2024-05`) → `[2024-05-01, 2024-06-01)`; day (`2024-05-15`) → `[2024-05-15, 2024-05-16)`; full datetime → exact instant. The full FHIR prefix matrix (`eq`/`ne`/`gt`/`ge`/`lt`/`le`/`sa`/`eb`/`ap`) is interpreted against these ranges (`eq2024` matches any instant in 2024; `gt2024` matches 2025+; etc.). Also fixes a DRY violation: the date branch in `crud._build_resource_filter` (the resource-specific filter builder) now routes through `DateFilter.to_orm_filter` (the same code path `_lastUpdated` uses), so the prefix matrix can no longer drift between the two implementations. Fixed a greedy day-precision branch that was silently truncating the time component of full datetime strings like `2024-05-15T13:30:00Z`.
- **FHIR R4 facade: sort columns now map to real ORM columns and align with filters.** Three corrections: `Condition?_sort=onset-date` now sorts on `onset_date` (previously referenced a non-existent `fhir_onset_datetime` column); `MedicationRequest?authored-on` sort now uses `created_at` to match the same-named filter (previously the filter used `created_at` while sort used `start_date` — they disagreed); and `Patient?_sort=name` now sorts by lowercase family name via a JSONB path expression (`LOWER(COALESCE(name -> 0 ->> 'family', name ->> 'family'))`) that handles both the list-of-HumanName (FHIR canonical) and single-HumanName-dict (legacy) storage shapes — previously it sorted by JSONB blob byte representation, so 'Smith' sorted before 'adams'.
- **FHIR R4 facade: pagination links now work end-to-end.** Previously the search Bundle's `first`/`last`/`previous`/`next` links carried a non-standard `_offset=<n>` query param while the parser only honored `page=N`, so following the `next` link always landed on page 1 (any non-trivial dataset was unreadable via standard pagination). The links now emit spec-standard `page=N` (matching HAPI / Vonk); the parser also accepts `_offset` on input as a tolerant alias so legacy links and clients that send `_offset` keep working.
- **FHIR R4 facade: CapabilityStatement now advertises only what's actually implemented.** Previously it falsely advertised system-wide `batch`, `transaction`, `history-system`, `search-system` interactions, a `/$validate` operation, and per-resource `versioning=versioned` / `updateCreate=true` — none of which the facade implements (POST to `/fhir/R4` is treated as create-one; no `/_search`, no `/_history`, no `vread`, no `history-instance`; PUT to a missing id returns 404). All false claims stripped. Per-resource `versioning` is now honestly `no-version`; `updateCreate` is `False`. This unblocks strict R4 conformance validators (Inferno/Touchstone) that fail servers advertising unimplemented capabilities. (`CapabilityStatement.rest.interaction`, `operation`, per-resource `versioning`.)
- **FHIR R4 facade: `CapabilityStatement.date` is now generated dynamically** at every `/metadata` call instead of being hardcoded.
- **FHIR R4 facade now advertises the correct FHIR version (`4.0.1`).** The CapabilityStatement previously declared `"fhirVersion": "4.3.0"` (R4B) while the path was `/fhir/R4/`, which fails strict R4 validators (Inferno, Touchstone). `FHIR_VERSION` is now `"4.0.1"` to match the path. Validation of the resources we emit continues to use the `fhir.resources` library (its R4B subpackage, since 8.x dropped its R4 subpackage); R4B is a backward-compatible superset of R4 for every field our models project, so the validator accepts exactly the FHIR JSON we produce while we advertise R4 honestly to clients.
- **DB-driven anatomy atlas with admin image management.** The hardcoded frontend atlas is replaced by an `anatomy_figures` table where each row is one view of one figure (e.g. `man-front`, `woman-back`) backed by a **WebP image** on disk under `UPLOAD_DIR/anatomy_figures/`. New `/api/v1/anatomy/figures` endpoints: `GET` (list), `GET /{slug}/image` (serves the WebP via `FileResponse`), and `POST`/`PATCH`/`DELETE` (SYSTEM_ADMIN only, multipart upload). New **Atlas Manager** admin page (`/admin/anatomy-atlas`) to create/edit/delete figures with an **interactive image cropper** — upload a source image, drag to select the view region (real pixel crop via canvas → WebP), live preview, no coordinate math. Adding new views (left side, right side) or figure groups is entirely through the UI.
- **Per-figure marker coordinates.** Organ markers are keyed by figure slug (`display.map.markers["man-front"]`) and normalized 0–1 against the image's pixel dimensions, so each figure owns an independent coordinate space. The PositionEditor and AnatomyExplorer pick a figure (grouped by figure_key → views). Existing markers were migrated (migration `b3d4e5f6a7c8`).
- **Modular Anatomy Graph Ontology.** Replaced the flat `body_parts` table with a directed-graph ontology (`AnatomyStructure` nodes + `AnatomyRelation` edges). 9 categories (SYSTEM, REGION, ORGAN, ORGAN_PART, TISSUE, CELL, SUBSTANCE, JOINT, OTHER) and 7 relation types (PART_OF, BRANCH_OF, DRAINS_INTO, ARTICULATES_WITH, INNERVATED_BY, SUPPLIED_BY, CONTINUOUS_WITH). Optional standard identifiers (SNOMED CT, LOINC, CUSTOM). Base seed of 54 nodes + 67 relationships ships with the application and is seeded automatically on startup.
- **Anatomy Explorer UI.** New `/anatomy` page with a custom 2D SVG body map (front/back views, clickable regions), searchable popup with category icons and SNOMED badges, organ location preview, and an interactive relationship graph (reactflow) showing parents/children/connections. Category filter chips and fullscreen graph modal.
- **Anatomy JSON Import API.** `POST /api/v1/anatomy/import` (SYSTEM_ADMIN) accepts JSON expansion packs with `nodes` and `edges` arrays. Slug-based upsert (never deletes), edge deduplication. Enables specialized deployments to import custom anatomy sub-graphs (e.g., detailed ophthalmology or neuroanatomy packs).
- **AI-Driven Anatomy Graph Generation.** New AI task type `define_anatomy_graph` and chatbot tool `propose_anatomy_graph_generation` allow the AI assistant to generate anatomical sub-graphs on demand via a human-in-the-loop review card.
- **Body Location in Clinical Events.** The Clinical Event form now includes a dedicated body location field (with AnatomySearchPopup + inline OrganPreview) visible for all event types. The Clinical Event detail page shows an organ location visualization section with a link to the Anatomy Explorer.
- **EventAnatomyLink.** M2M table bridging `ClinicalEvent` and `AnatomyStructure` for future graph-based clinical event queries.
- **Anatomy Position Editor.** SYSTEM_ADMIN-only drag-and-drop editor (Positions button on the Anatomy Explorer) for placing organ markers on the body silhouette. Markers auto-save normalized coordinates; front/back view is tagged automatically from drop position. The canonical way to fix or add a structure's map position — no pixel math or JSON editing.
- **Anatomy i18n.** Full `anatomy` translation namespace (EN/EL), including category and relation labels (Organ, Tissue, Part of, Branch of, …) and the explorer/editor UI strings.
- **`scripts/setup_env.py` now generates VAPID keys and prompts for the contact email.** A fresh `python scripts/setup_env.py` run produces a fully-configured Web Push section in `.env` with zero further operator action: a P-256 key pair (`VAPID_PUBLIC_KEY` / `VAPID_PRIVATE_KEY`) is generated using the existing `cryptography` dependency (no Node.js / `npx web-push generate-vapid-keys` needed), and Full Setup mode asks for `VAPID_ADMIN_EMAIL` with a smart default derived from the entered `APP_URL` hostname (`https://health.example.com` → suggests `admin@health.example.com`; `http://localhost:*` → falls back to `admin@example.com`). The user can accept the default (Enter) or override. Keys Only mode still generates the key pair but leaves `VAPID_ADMIN_EMAIL` commented (the config.py placeholder default applies — fine for dev, where missing VAPID keys are tolerated anyway). Closes the loop on the VAPID prod-guard (see *Changed* below): a fresh prod install now goes from `python scripts/setup_env.py` → `docker compose up` with Web Push working end-to-end.

### Changed
- **Patient REST schema now accepts FHIR R4 list cardinality.** `PatientCreate`/`PatientUpdate` fields `name`, `address`, `telecom` changed from `Optional[Dict[str, Any]]` (single object) to `Optional[List[Dict[str, Any]]]` to match FHIR R4 `0..*` cardinality. Posting canonical FHIR JSON like `{"name": [{"family": "Doe", "given": ["John"]}]}` previously returned 422; it now succeeds. Single-dict payloads are correctly rejected (the ORM column still tolerates legacy single-dict rows via `_coerce_*` read-side helpers). `PatientCreate`/`PatientUpdate` are not used by any current endpoint, so no caller breakage.
- **VAPID keys now required in production.** `VAPID_PUBLIC_KEY` / `VAPID_PRIVATE_KEY` env vars must be set when `APP_ENV != "development"`; the app refuses to boot otherwise. The fields were also changed from `os.getenv(...)` (which bypassed pydantic and baked in the value at class-definition time) to plain `Optional[str] = None`, so the existing `SECRET_KEY` / `INTEGRATION_SECRET_KEY` prod-guard pattern now applies to them. In development, missing keys continue to be tolerated (Web Push silently skipped). Operators running `scripts/setup_env.py` don't need to do anything extra — the wizard generates both keys and prompts for `VAPID_ADMIN_EMAIL` automatically (see *Added* above).
- **Lifespan startup is now fail-fatal in production.** Catalog seeding (medications, allergies, clinical event types, body parts, anatomy figures) and integration-registry initialization previously failed silently with a log line — the app would boot half-initialised with no medication/allergy catalog. In production (`APP_ENV != "development"`), these failures now re-raise after logging so the app refuses to boot; development retains the fail-soft behaviour. Stuck-extraction cleanup remains best-effort in both modes.
- **Worker DB engine consolidated to a worker-scoped singleton.** Celery tasks previously created a fresh `AsyncEngine` per invocation; the engine is now a single lazy-init singleton per worker process using `poolclass=NullPool`. Tasks do `db, engine = get_async_session()` → `await db.close()` in `finally`; `engine.dispose()` is never called per task (the engine is disposed on `worker_process_shutdown`). This eliminates the intermittent `RuntimeError: Event loop is closed` / `Future attached to a different loop` crashes on `check_notification_triggers`, `sync_active_integrations`, and other periodic tasks.
- **Anatomy figures switched from SVG to WebP.** SVG viewBox cropping was fundamentally broken (viewport zoom, not a real crop; width/height/viewBox mismatch stretched renders; SVG presentation attributes triggered browser CSS warnings). Replaced with raster WebP images: the crop editor produces a real pixel crop via `<canvas>`, the stored image IS the view, and the marker overlay's viewBox matches the image dimensions by construction — eliminating the entire class of alignment bugs. The four default figures ship as bundled WebP seeds (14–25 KB each, rasterized at 3× from the Wikimedia surface diagrams, CC BY-SA 3.0). Frontend Zustand store (`useAnatomyAtlas`) fetches figure metadata + authenticated image blobs lazily.
- **AnatomyExplorer figure picker.** The ♂/♀ gender toggle and Front/Back view toggle are replaced by a single grouped `<select>` listing all active figures.
- **AI subsystem reorganized under `backend/app/ai/`.** All AI/LLM code (previously scattered across `app/services/ai_*.py`, `app/processors/`, and `app/schemas/ai_*.py`) now lives under a single `app/ai/` package, split by concern: `providers/` (config + factory + enums/registry), `processors/{ocr,nlp}/`, `pipeline/` (extraction orchestration + persistence + ontology), `agents/` (the agentic-chat reasoning loop, HITL helpers, prompts), `tools/` (the chatbot tools split by domain + the integration aggregator), `assistance/` (form-fillers/definitions/icons), and `schemas/`. The three near-duplicate chat reasoning loops (`_chat_stream`, `resume_after_hitl`, `_general_chat`) collapsed into one `run_reasoning_loop(...)` in `agents/chat_agent.py`; AI Celery tasks moved to `workers/ai_tasks.py`. Internal refactor — no user-facing behaviour change and no public API break — but developers should update import paths. Notable mappings: `services/ai_assistance_service` → `ai/assistance/service`, `services/medical_processing_service` → `ai/pipeline/service`, `services/ai_chatbot_tools.ChatbotTools(...).get_tools()` → `ai.tools.get_tools(...)`, `services/integration_tool_aggregator` → `ai/tools/aggregator`, `schemas/ai_{nlp,assistance,config}.py` → `ai/schemas/{nlp,assistance,config}.py`. Branch `refactor/ai-services`.
- **Anatomy Explorer overhaul.** The page now follows app conventions: decoupled `PageHeader` (title/subtitle/icon render in the global header), `PageContainer`, shared `LoadingState`, full i18n, URL deep-linking (`/anatomy/:slug`), debounced server-side search (`?search=`), category-filter dimming on the relationship graph, a landing overview (systems grid + total count), and an "Add custom structure" form (uses the existing `POST`/`PATCH`/`DELETE` endpoints). `bodyPartService` is now a thin adapter over `anatomyService` (single source of truth for `/anatomy`).
- **Anatomy body diagram is now data-driven and figure-independent.** Replaced the hand-coded SVG silhouette with the Wikimedia "surface diagram" assets (male + female, CC BY-SA 3.0 — see `NOTICE`). An atlas registry (`frontend/src/components/anatomy/atlas.ts`) declares figures + per-view crop regions; adding a figure is one asset + one registry line. Organ markers use **normalized 0–1 coordinates** stored per-figure (`display.map.markers.man` / `.woman`) under a new JSONB `display` column (migration `39fffbc136ce`), so positions are resolution-independent and each body is positioned independently. Front/Back toggle crops the viewBox and filters markers; ♂/♀ gender toggle swaps the figure. The base seed ships male markers; female markers are added via the editor.
- **`GET /api/v1/anatomy` supports `?search=`** (ilike on name/slug/standard_code/description) and now orders by name. Added `PATCH`/`DELETE` `/api/v1/anatomy/{id}` for editing/removing structures (global structures require SYSTEM_ADMIN).
- **Unified AI badges.** Every AI surface now uses a single `<AIBadge>` convention: the default renders the gradient **"AI"** label + an **info icon** that opens the transparency popover (active model, provider + country flag, Local/Cloud, privacy notice, medical disclaimer); a custom `label` is shown only where it adds meaning (e.g. "Proposal", "Suggested Unit"). The trigger is now a real `<button>` (keyboard-accessible: Enter/Space toggle, Esc closes + restores focus, `aria-expanded`/`aria-haspopup`), with a new `size="sm"` variant for tight headers. Converged the ad-hoc indicators that previously rolled their own icons/text/tooltips — biomarker "AI Suggested Unit", HITL "AI Proposal" (Card + Modal, formerly a `Bot` icon with no tooltip), the Magic Fill modal header (had no AI marker at all), and the "AI Insights"/"AI Interpretation"/"AI Agent Capabilities" section titles — onto `<AIBadge>`. Standardized AI action-trigger icons to `Sparkles` (the lone `Wand2` outlier in the category manager is gone). Wired the previously-dead `ai_labels.magic_fill_ai`/`smart_extraction_ai` keys and moved the remaining hardcoded AI strings (incl. EN/EL) behind i18n; `ai_chat.hitl.proposal_label` shortened from "AI Proposal" to "Proposal" since the badge now carries the AI signal.

### Fixed
- **Celery periodic tasks crashed intermittently with `RuntimeError: Event loop is closed` / `Future attached to a different loop`.** Root cause: each task's `get_async_session()` created a fresh `AsyncEngine` with its own connection pool, and the `@async_task` decorator created a fresh `asyncio` event loop per task. asyncpg connection protocol transports bound to a previous task's loop resurfaced in subsequent tasks via SQLAlchemy's pool, hitting a closed loop. Fix: the engine is now a worker-scoped singleton using `poolclass=NullPool` so no DB connection is ever reused across loops; engine disposal is wired to Celery's `worker_process_shutdown` signal rather than per-task. Live production outage observed on `check_notification_triggers` (2026-06-27).
- **`relative_score` was never computed for OCR-sourced observations.** Every OCR-sourced observation had `relative_score = NULL`, silently disabling the biomarker engine's scoring feature (`_get_observation_status` step 2 in `analytics_service.py` was dead code for OCR data; everything fell through to the hardcoded `STANDARD_RANGES` dict). `save_observation` (in `app/ai/pipeline/persistence.py`) now computes it using the same formula as the integrations SDK's `ObservationBuilder.build()`: `(value - ref_min) / (ref_max - ref_min)` clamped to `[0.0, 1.0]` when both bounds are present with `max > min`; incomplete range (only one bound) → `0.5`; no range → `NULL`.
- **`normalized_value` had two bugs in the OCR persistence path.** (1) Skip-condition: it was only computed when the biomarker had a preferred unit *different* from the matched raw unit; auto-created biomarkers (no preferred unit) kept raw_value, mixing units across labs in trend history. (2) Wrong direction: `val_float * matched_unit.conversion_multiplier` converts raw → base SI, but the result was labelled as-preferred. Fixed: `normalized_value` is now always expressed in the biomarker's preferred unit (`raw → base → preferred`), falling back to base SI when no preferred unit is set so trends stay consistent across labs.
- **`MedicationCatalog` / `AllergyCatalog` dedup used substring matching.** The uniqueness check `Catalog.name.ilike(name)` treated `_` and `%` as wildcards, so `"Vitamin_B12"` would silently match an existing `"Vitamin-B12"` (underscore matches any single char) and skip the insert. Switched all 5 dedup sites (`ai/pipeline/ontology.py`, `seed_service.py` ×2, `import_service.py` ×2) to `func.lower(Catalog.name) == func.lower(name)` for strict case-insensitive exact match. Catalog *search* features (legitimate substring `ilike('%q%')`) are unchanged.
- **Cumulative extraction was triggered even when all OCR documents produced empty text** (blank pages, OCR failures). The else branch of `_check_trigger_cumulative` fired `cumulative_extraction.delay(...)` "anyway to handle metadata if needed", wasting an LLM call and producing hallucinated or empty impressions. The else branch now marks the exam completed (`extraction_status="completed"`, `impressions=""`) and skips the LLM call entirely.
- **Web Push dead subscriptions accumulated forever.** `webpush_service.send_web_push` swallowed every `WebPushException` including the 410 Gone / 404 Not Found cases that RFC 8030 specifies as "subscription permanently dead", returning `False`. The caller couldn't distinguish transient failure from permanent death, so dead rows in `notification_subscriptions` stayed `is_active=True` and every PUSH cycle re-attempted them. `send_web_push` now raises a new `SubscriptionExpired` exception on 410/404; `deliver_notification`'s PUSH loop catches it and marks the matching `NotificationSubscription.is_active = False`.
- **`doctors.py` list endpoint leaked internal exception text.** The handler was wrapped in a `try/except Exception` that did `print(...)` + `traceback.print_exc()` + re-raised as `HTTPException(500, detail=str(e))`, bypassing the global exception handler. The wrapper is removed; the global handler now logs with a correlation id and returns a sanitized 500.
- **`check_observation_access` raised `AttributeError` → HTTP 500** when an observation's `subject` JSONB dict had no `"reference"` key (`.get()` returned `None`, then `None.split()` raised). Now defensively coerces via `(observation.subject or {}).get("reference", "")` so an absent reference yields `""` and the downstream surfaces a clean 400.
- **Anatomy tests failed on second run** (`UniqueViolationError` on `anatomy_structures.slug`). `anatomy_structures.slug` has a globally-unique constraint (anatomy is an ontology — heart is heart in every tenant), but tests used stable slugs like `"test-heart"` and the suite has no per-test transaction rollback. Rows accumulated across runs in different tenants created by the per-call `system_admin_headers` fixture. Added an autouse module-level fixture that deletes from `anatomy_structures` before each test (CASCADE handles `anatomy_relations` + `event_anatomy_links`). The tables aren't seeded by migrations — the only data in them during tests IS test data — so truncating is safe.
- **"Create New" dropdown hidden behind the sidebar.** The CreateMenu popup (New Examination / New Medication / etc.) is rendered via a portal at `document.body`, but its `z-index` (200) was lower than the sidebar container's (`z-[950]` in `Layout.tsx`). Since the layout root doesn't establish its own stacking context, the sidebar painted over the portaled dropdown. Bumped the dropdown to `z-[1000]` so it now correctly floats above the navigation sidebar.
- **Anatomy SVG assets failed to open in XML editors** with "XML declaration allowed only at the start of the document" and undeclared namespace-prefix errors. The `<?xml ?>` declaration was moved above the license comment, and orphaned Inkscape/RDF metadata blocks (using stripped `sodipodi:`/`rdf:`/`cc:`/`dc:` prefixes) were removed. Both files now validate as well-formed XML with zero errors.

### Fixed
- **Notification periodic task crashed with "Future attached to a different loop".** `check_notification_triggers` delegated to `NotificationManager.process_due_triggers()`, which opened its own session via the global pooled app engine (`AsyncSessionLocal`, `pool_pre_ping=True`). In a Celery prefork worker that engine's asyncpg connections are bound to the master's (already-closed) event loop, so the pool's pre-ping checkout raised `RuntimeError: Future ... attached to a different loop` and teardown raised `RuntimeError: Event loop is closed` — scheduled/recurring medication reminders intermittently failed to fire. The task now owns a worker-scoped `NullPool` session (via `get_async_session()`, the same pattern as `sync_active_integrations`) and injects it into `process_due_triggers(session=...)` / `fire_notification(session=...)`. A failed trigger now rolls back the shared session so the remaining triggers in the cycle still process. Request-path callers are unchanged (they fall back to the app engine).

## [0.3.0-rc.6] - 2026-06-26

### Added
- **Customizable bind addresses for deployment.** Gitea ``deploy.yml`` now supports ``BACKEND_BIND``, ``FRONTEND_BIND``, and ``FLOWER_BIND`` via Gitea Repo Secrets (defaulting to ``127.0.0.1`` for local-only security). This allows multi-VM architectures where a reverse proxy on a different VM connects to the application services over a private interface.
- **Reusable test workflow (``.github/workflows/test.yml``).** The backend pytest suite and frontend lint+build test are now defined once in a reusable workflow (``on: workflow_call``) and called from the GitHub ``docker-publish.yml`` pipeline. The workflow includes the TimescaleDB + Redis service containers, the ``PYTHONPATH`` fix for the top-level ``integrations`` package, and bumps the frontend Node version to 20 (matching the frontend Dockerfiles). The Gitea ``deploy.yml`` keeps its test jobs inline (``test-backend`` + ``test-frontend`` as separate top-level jobs) because Gitea's ``act_runner`` collapses reusable-workflow jobs into a single "Set up job" node with no step-level visibility — inlining preserves separate job nodes and full step visibility on Gitea.

### Fixed
- **CI/CD ``test-backend`` job failed** with ``ModuleNotFoundError: No module named 'integrations'``. The Gitea workflow ran ``pytest`` from ``backend/`` with ``pythonpath = .`` (per ``pytest.ini``), which only puts ``backend/`` on ``sys.path`` — but ``app.core.integration_registry`` imports the top-level ``integrations`` package (sibling of ``backend/``). The pytest step now exports ``PYTHONPATH`` to mirror the production Docker setup (``PYTHONPATH=/app/backend:/app`` in ``docker/Dockerfile``), so both ``app/`` and ``integrations/`` are importable during tests.
- **Production backend container crashed on boot** with ``ModuleNotFoundError: No module named 'integrations'``. The Gitea ``deploy.yml`` built the backend image with ``-f ./backend/Dockerfile ./backend`` (build context = ``backend/`` only), so the top-level ``integrations/`` package — which ``app.core.integration_registry`` imports at startup — was never copied into the image. The ``backend/Dockerfile`` also lacked the ``COPY integrations`` and ``ENV PYTHONPATH`` lines that ``docker/Dockerfile`` already had. Fixed by consolidating to a single Dockerfile per image under ``docker/`` (see Changed below).
- **CI ``DATABASE_URL`` env var masked ``POSTGRES_PASSWORD`` in security-hygiene tests.** Both Gitea and GitHub workflows set ``DATABASE_URL=postgresql+asyncpg://admin:password@...`` (weak password "password") as an env var. Since ``config.py``'s ``assemble_db_connection`` treats ``DATABASE_URL`` as dominant when set, the strong ``POSTGRES_PASSWORD`` passed to ``Settings()`` in ``test_b13_production_accepts_strong_password`` was silently ignored — the weak URL won, and the validator correctly rejected it. Both workflows now pass ``POSTGRES_*`` parts instead, letting ``config.py`` assemble the URL so ``POSTGRES_PASSWORD`` is the actual source. ``DATABASE_URL`` remains as an optional override escape hatch; when explicitly set, the validator still rejects weak passwords in production.

### Changed
- **Dockerfiles consolidated under ``docker/``.** Deleted the duplicate ``backend/Dockerfile`` and ``frontend/Dockerfile``; ``docker/Dockerfile``, ``docker/Dockerfile.frontend``, and ``docker/Dockerfile.worker`` are now the single source of truth for all image builds (CI, GitHub ``docker-publish.yml``, and ``docker-compose.dev.yml``). All CI workflows and composes now build with repo-root context (``context: .``) and reference ``-f docker/Dockerfile[.frontend|.worker]``. ``docker/Dockerfile.frontend`` updated to use repo-root-relative ``COPY frontend/...`` paths. This fixes the production ``integrations`` import crash (the deleted ``backend/Dockerfile`` never copied ``integrations/`` into the image) and eliminates future drift between two sets of Dockerfiles.
- **GitHub ``docker-publish.yml`` now gates image pushes on tests.** Previously, the GitHub workflow built and pushed Docker images to ghcr.io with no test verification at all. Both ``build-and-push-backend`` and ``build-and-push-frontend`` now declare ``needs: [test]`` and only run after the reusable test workflow passes.
- **Gitea ``deploy.yml`` test jobs inlined** (``test-backend`` + ``test-frontend`` as top-level jobs with ``needs: [test-backend, test-frontend]`` on ``build-and-push``), and ``actions/checkout`` bumped from v3 to v4 across all jobs. Gitea renders top-level jobs as separate nodes, so backend/frontend test results are visible at a glance.

### Changed
- **People & Access list (`/admin/tenant/users`) is now clickable.** Selecting a person's name/avatar navigates to their detail page instead of using a separate "eye" action button. The link-record (chain) and change-role (shield) row actions were removed from the list — both operations now live on the detail page (`/admin/tenant/users/{id}`): access level is edited inline via an Access Level card, and clinical profiles (patients/doctors) are linked/unlinked from the Linked Clinical Profiles section. Added missing `common.close`, `common.no_patients`, and `common.no_doctors` i18n keys (EN/EL) that were referenced but absent.

## [0.3.0-rc.5] - 2026-06-25

### Fixed
- **Frontend Docker image build crashed** (`ReferenceError: crypto is not defined` from `serialize-javascript` via `@rollup/plugin-terser`). The frontend Dockerfiles (`frontend/Dockerfile`, `docker/Dockerfile.frontend`) used `node:18-alpine`, where `globalThis.crypto` is still behind the `--experimental-global-webcrypto` flag (unflagged only in Node 20+). Bumped base image to `node:20-alpine`. Node 18 also reached EOL in April 2025.

## [0.3.0-rc.4] - 2026-06-25

### Changed
- **Versioning & changelog workflow is now local-first by default.** `docs/RELEASE_PROCESS.md` and `docs/DEVELOPMENT.md` updated: `version_manager.py --git` (stage + commit + tag locally) is the default stop point; `--push` (publishes to every remote + triggers CI/CD) is opt-in and only used when explicitly requested. A prominent push-policy callout was added to `RELEASE_PROCESS.md`. All examples and quick checklists reordered to show the local-only path first.
- **Changelog rule made explicit** in `docs/DEVELOPMENT.md` and `docs/RELEASE_PROCESS.md`: every user-visible change must add a bullet under `## [Unreleased]` at commit time, proactively.

### Fixed
- **Frontend build broken** — `tsc` failed with `Cannot find namespace 'NodeJS'` on 5 files (`CitationButton.tsx`, `BiomarkerDetail.tsx`, `DocumentList.tsx`, `ExaminationDetail.tsx`, `ExaminationList.tsx`) that used `NodeJS.Timeout` for `setInterval`/`setTimeout` return types. `tsconfig.json` `types` only includes `vite/client` + `vitest/globals` (no `@types/node`), so the namespace was unresolvable. Switched to the browser-idiomatic `ReturnType<typeof setTimeout/setInterval>`, which needs no `@types/node` and is runtime-identical.
- **Removed all private references from public docs.** `docs/AI_SYSTEM.md`, `docs/API.md`, `docs/STATUS.md`, and `docs/RELEASE_PROCESS.md` no longer reference internal/private tooling; links replaced with public doc cross-references (`INTEGRATIONS_SDK.md`, `AI_SYSTEM.md §4.1`).
- **`docs/RELEASE_PROCESS.md` cutting-a-release / promote-RC / catch-up sections** no longer default to `--git --push`; they default to `--git` (local) with `--push` shown as an explicit opt-in step.
- **Stale version string** in project metadata corrected (`0.3.0-alpha` → `0.3.0-rc.3`).

## [0.3.0-rc.3] - 2026-06-25

### Security
- **All 58 Dependabot alerts resolved** (1 critical, 26 high, 28 moderate, 3 low) across backend (pip) and frontend (npm). `npm audit` now reports 0 vulnerabilities.
- **Backend: replaced unmaintained `python-jose` with `PyJWT`** (CVE-2024-33664/33663 — algorithm confusion, DoS). `ecdsa`, `rsa`, and `passlib` (transitive deps of python-jose, unused in code) removed entirely. `security.py` updated: `JWTError` → `jwt.PyJWTError`.
- **Backend: bumped vulnerable packages** — `cryptography` 47→49 (vulnerable OpenSSL wheels), `starlette` 1.3.0→1.3.1 (`request.form()` DoS), `langsmith` 0.8.14→0.9.1 (TracingMiddleware file read), `langchain` 1.3.7→1.3.11 (path traversal/sandbox escape), `pydantic-settings` 2.14.1→2.14.2 (symlink secrets_dir bypass), `fastapi-mail` 1.6.4→1.6.5 (cryptography 49 compat).
- **Frontend: bumped `axios` → 1.18.1** — fixes 22 CVEs including SSRF, prototype pollution, credential leak, header injection, and CRLF injection.
- **Frontend: bumped `vitest` 1.1→4.1.9** (critical CVE — arbitrary file read/execute when UI server is listening). Required major bump of `vite` 5.4→6.4.3, `@vitejs/plugin-react` → 4.7.0, `vite-plugin-pwa` → 1.3.0.
- **Frontend: bumped `i18next-http-backend` → 3.0.6** (path traversal/URL injection), `react-router-dom` → 6.30.4 (open redirect), `postcss` → 8.5.15 (XSS via `</style>`), `@typescript-eslint/*` 6→7.18.0 (transitive `minimatch` ReDoS).
- **Frontend: pinned `react-quill-new` ~3.7.0 + npm `overrides` for `quill` 2.0.2** (Quill XSS via HTML export).
- **Frontend: `npm audit fix` patched transitive deps** — `flatted`, `lodash`, `picomatch`, `brace-expansion`, `follow-redirects`, `form-data`, `serialize-javascript`, `fast-uri`, `js-yaml`, `@babel/*`, `esbuild`.
- **Added `.github/dependabot.yml`** — weekly automated dependency checks with auto-PR for direct deps across `pip` (backend), `npm` (frontend + integrations TS SDK), `docker`, and `github-actions` ecosystems.

## [0.3.0-rc.2] - 2026-06-25

**Release candidate — tenant administration, deployment hardening, UI overhaul.**

### DB foundation: migration squash + schema hardening

**Single deterministic baseline replaces the 62-migration chain.** Existing
databases must be dropped and recreated (`alembic upgrade head` from empty).
There is no in-place upgrade path from the prior chain — the historical
migrations are archived under `backend/alembic/versions_archived/` for
traceability but no longer executed.

The squashed `0001_initial_schema.py` is fully idempotent (re-runnable),
guards all TimescaleDB DDL behind an extension-availability check, creates
all 20 PG enum types via `DO $$ ... EXCEPTION WHEN duplicate_object` blocks,
and seeds examination categories with deterministic uuid5-based IDs.

#### Migrations (21 audit items closed)
- **D7**: `Medication.intent` column is now a proper PG enum (was `VARCHAR(50)`).
- **D8**: `TenantMixin` now declares `ForeignKey("tenants.id", ondelete="CASCADE")` — 29 tables got the FK. Deleting a tenant purges all owned data instead of orphaning it. `TelemetryDataModel` overrides `tenant_id` without FK (TimescaleDB hypertable limitation).
- **D9**: Patient deletion cascades to ALL clinical tables. Previously `examinations`, `documents`, `fhir_devices`, `chat_sessions` used `SET NULL` (orphaned rows); now `CASCADE` (full record removal).
- **D10 + E2**: `documents.entities` converted from `JSON` to `JSONB` + GIN index for `@>` / path queries.
- **D11**: Dead `SENT` value in `notificationstatus` enum dropped (was never in the Python enum).
- **D12**: `role` enum no longer uses the `COMMIT` workaround for `ALTER TYPE ADD VALUE`.
- **D13**: Examination category seed UUIDs are deterministic (`uuid5` + `ON CONFLICT DO NOTHING`).
- **D14**: All TimescaleDB DDL (hypertable, continuous aggregates, retention/compression policies) is guarded — plain-PG dev/test databases now migrate successfully without the extension.
- **D16**: Dropped dead `notifications.fhir_resource_type` column (never honored).
- **D17**: `export_jobs.completed_at` + `import_jobs.completed_at` converted from `TEXT` to `TIMESTAMPTZ`.
- **D18**: `BodyPartModel.slug` is now `UNIQUE` (matches every other slug column in the codebase).
- **D19**: `CHECK` constraint on `fhir_patients.mrn` prevents empty-string MRNs.
- **D22**: Consistent index naming across the schema.
- **D23**: Idempotent enum creation (all via `DO` blocks or `create_type=False`).
- **K12**: Stale migration docstring archived.

#### Performance indexes
- **E1**: Expression index on `Observation.subject->>'reference'` + `DiagnosticReport.subject->>'reference'`. This is the most-used query pattern in the codebase (12+ call sites). `EXPLAIN` confirms the planner now uses an Index Scan instead of a full-table scan within tenant.
- **E3**: Indexes on `biomarker_group_members.biomarker_id` + `.group_id` (FK reverse-lookups).
- **E4**: Indexes on `biomarker_relationships.source_biomarker_id` + `.target_biomarker_id`.
- **E5**: Composite `(tenant_id, timestamp)` index on `telemetry_data` for tenant-wide analytics.
- **E6**: Indexes on FHIR sort columns — `fhir_patients.birth_date`, `fhir_medications.start_date`, `fhir_communications.sent`.

#### Migration upgrade instructions
```bash
# Existing dev databases must be reset (no in-place upgrade path):
PGPASSWORD=admin123 psql -h localhost -p 5433 -U admin -d postgres \
  -c "DROP DATABASE IF EXISTS health_assistant;"
PGPASSWORD=admin123 psql -h localhost -p 5433 -U admin -d postgres \
  -c "CREATE DATABASE health_assistant OWNER admin;"
cd backend && alembic upgrade head
```

### P0 stabilization pass (2026-06-22)

**7 critical/high fixes + a redesigned dev workflow + production-grade Docker hardening** following the comprehensive re-audit.

### Security
- **Tenant-isolation breaches closed** — `task_monitor` (B1), `notifications` incl. previously-unauth `/delivered` (B2, B3), `alerts` (B4), document `/preview` PHI exfil hole (B5), `/auth/register` tenant impersonation (B7 — invite-token flow + `pg_advisory_xact_lock` bootstrap race fix + new `POST /auth/invite`), `integration_api_proxy` (B8 — optional HMAC via `api_secret` + `X-Api-Signature`).
- **Pipeline data-loss / race fixes** — `_persist_results` re-extraction wrapped in `begin_nested()` SAVEPOINT (C2); `_check_trigger_cumulative` per-exam `pg_try_advisory_xact_lock` (C3); `sync_active_integrations` per-integration Redis lock `sync_lock:{id}` `SET NX EX 600` (C4); `migrate_biomarker_data` telemetry→FHIR now resolves patient per-row via `device_id → UserIntegration → user_id → Patient.user_id` and aborts cleanly when attribution is impossible (C1).
- **Worker hygiene** — `cleanup_stuck_extractions` threshold 15 → 20 min (5-min margin beyond Celery hard `task_time_limit=900s`) (A5); startup cleanup now filters `updated_at < threshold` so rolling restarts don't kill in-flight exams (A6).
- **Schema & facade conformance** — `SoftDeleteMixin` added to all 9 FHIR-exposed models (`Patient`, `Observation`, `DiagnosticReport`, `Medication`, `AllergyIntolerance`, `OrganizationModel`, `ExaminationModel`, `ClinicalEvent`, `DocumentModel`); the facade's `_soft_delete_predicate` is now non-None for all of them and reads return `410 Gone` instead of `404 Not Found` (D6 + F3). `OrganizationModel` gained `TimestampMixin` (`created_at`/`updated_at`) (D15). Migration `c4a8e7f2b1d9` realigns the index naming convention. The legacy `idx_*_deleted_at` partial indexes that were invisible to SQLAlchemy autogenerate are dropped; the `ix_*_deleted_at` indexes the models declare are created.
- **Regression coverage** — 11 new test files (`tests/test_task_monitor_isolation.py`, `test_notifications_isolation.py`, `test_alerts_isolation.py`, `test_documents_preview_auth.py`, `test_auth_register_isolation.py`, `test_integration_api_proxy_hmac.py`, `test_stuck_extraction_cleanup.py`, `test_medical_processing_savepoint.py`, `test_check_trigger_cumulative_race.py`, `test_sync_active_integrations_lock.py`, `test_migrate_biomarker_attribution.py`, `test_softdelete_mixin_alignment.py`). Full suite: **830 tests pass**.
- **AI provider `api_key` is now encrypted at rest** (Fernet, via `INTEGRATION_SECRET_KEY`) and **masked in every API response** (`***<last4>`, plus a new `has_api_key` boolean). Plaintext keys persisted before this release must be migrated with `PYTHONPATH=. python scripts/encrypt_existing_api_keys.py` (supports `--dry-run`, idempotent). The factory layer reads plaintext only via the new `AIProviderModel.get_api_key_plaintext()` accessor at LLM-instantiation time.
- **Scope checks on `/ai-config/providers/{id}`, `/providers/{id}/with-models`, `/providers/{id}/models`, `/models/{id}`, `/providers/{id}/fetch-external-models`** — previously any authenticated user could read any provider (including its key) by UUID. New `verify_provider_access` / `verify_model_access` helpers enforce USER/TENANT/SYSTEM scope on all 7 entry points.
- **`fetch-external-models` SSRF guard**: in production (`DEBUG=False`) the `api_base` must be `http(s)://` and not point at a loopback / private / link-local address.
- **Telemetry endpoints are now tenant-scoped**. `/telemetry/data`, `/telemetry/data/summary`, and `/telemetry/anomalies` previously accepted only `device_id`; a user who guessed another tenant's `device_id` could read its data. All three endpoints now require and filter on the caller's `tenant_id`.
- **Global exception handler no longer leaks `str(exc)` in production**. 500 responses now include a `correlation_id`; the full detail is logged server-side with the same id. `DEBUG=True` preserves the verbose detail for developer convenience.
- **Tenant-scoped FHIR single-resource reads** — `get_observation`, `get_diagnostic_report`, `get_medication`, and `delete_observation` now accept an optional `tenant_id` parameter and filter on it (defense in depth at the service level). All `/fhir/*/{id}` endpoints pass `current_user.tenant_id`.
- **Prompt-injection guard** (`app/utils/prompt_guard.py`) — heuristic detector for 8 OWASP LLM01 patterns (instruction-override, role-switch, role-marker-injection, prompt-extraction, jailbreak-mode, delimiter-escape, rule-injection). `scan_prompt_injection()` → `{safe, risk, matches, snippets}` with risk escalation (1 match = medium, 2+ = high). Non-blocking — logs WARNING, HITL wall remains the structural defence. `DEFENSE_PREAMBLE` prepended to both chat system prompts. The `assist()` dispatcher runs every input through the guard before the LLM.
- **SVG sanitizer rewritten** (`app/utils/svg.py`) — compiled regexes for all event-handler quoting forms (double-quoted, single-quoted, unquoted), `javascript:`/`vbscript:`/`data:text/html` URL protocols in `href`/`xlink:href`, and dangerous elements (`<script>`, `<foreignObject>` — both paired and self-closing). Optimization passes preserved.
- **WebSocket hardened** (`app/api/v1/endpoints/websockets.py`) — prefers `Sec-WebSocket-Protocol` subprotocol auth (`["bearer", token]`) over URL query string; fixes unawaited `asyncio.sleep(0.1)` busy-loop (10 Hz → 1 Hz via `get_message(timeout=1.0)`); logs errors before `close(1011)`; sends 30s keepalive ping.
- **AuditLog provenance** (`app/services/audit_service.py`) — `log_audit_action()` helper (best-effort, own session, never raises) wired into `POST /fhir/Observation`, `DELETE /fhir/Observation`, `POST /fhir/DiagnosticReport`, `POST /fhir/Medication`. Captures who/what/when + old/new value diff.
- **Insecure DB password default removed** — `POSTGRES_PASSWORD` no longer defaults to `admin123` in `config.py`; production `@model_validator` refuses known-weak credentials; `.env.example` uses `CHANGE_ME` placeholder.
- **Webhook HMAC-SHA256 verification** — integrations can set `user_config["webhook_secret"]`; when present the route verifies a constant-time HMAC-SHA256 signature over the raw body. Supports `X-Webhook-Signature`, `X-Webhook-Signature-256`, and GitHub-style `X-Hub-Signature-256`. Backward-compatible (no secret = legacy UUID-as-secret).
- **Auth on integration listing** — `GET /integrations/available` and `GET /integrations/{domain}/documentation` now depend on `get_current_user` (any role).
- **AI debug `print()` leaks removed** — two `print()` calls in `_define_biomarker`/`_define_medication` that dumped user input + LLM output to stdout replaced with `logger.debug`.
- **CORS fallback hostname fixed** — `app.health_assistant.com` (underscore — invalid RFC 1123) → `app.health-assistant.com`.
- **Catalog search SQL hardened** — `_set_similarity_threshold` validates float in [0, 1] before inlining into `SET pg_trgm.similarity_threshold` (PostgreSQL `SET` doesn't accept bind params).

### Fixed
- **`deliver_notification` `NameError` on every PUSH delivery** — `tasks.py` imported `datetime` as a module but called `datetime.now(timezone.utc)` with `timezone` unbound. Push notifications were silently 100% broken (status never advanced past `PENDING`). Fixed.
- **`admin.py` was entirely broken at import-call boundary** — imported nonexistent `async_session_maker`, wrong `TaskLogger`/`TaskProgressTracker` arity, called nonexistent methods. The frontend's `adminService.ts` uses both endpoints (`/admin/catalogs/import/url` + `/file`) so the feature was dead in prod. Rewritten against real signatures in `app.workers.task_logger`.
- **`list_examination_categories` raised `NameError`** — used `or_(...)` not imported at module scope. Re-flagged from 0.3.0-alpha; now actually fixed.
- **5 lowercase role-string comparisons in `documents_db.py`** (`update/edit/trigger_extraction/extraction_status/delete` endpoints) compared against `["admin", "manager"]` while enum values are uppercase. Admin/manager bypass never matched → admins couldn't perform their jobs on these endpoints. Now uses `Role.ADMIN.value` / `Role.MANAGER.value` / `Role.SYSTEM_ADMIN.value`.
- **Three PG enum drifts** that crashed on first use of the missing values: `medicationstatus` missing `INACTIVE`/`CANCELLED`; `allergycriticality` had wrong token (`unable_to_assess` underscore vs `unable-to-assess` hyphen rename); `aiscope` missing `ORGANIZATION`. Fixed by migration `7a9c2e1b4f3a` (idempotent, uses `DO $$ … IF NOT EXISTS … END $$`).
- **`fhir_communications` schema drift** — `CommunicationModel` uses `VersionedMixin` but the table lacked `version` + `is_current` columns and declared `tenant_id` `NOT NULL` (model inherits nullable from `TenantMixin`). ORM attribute access + facade PUT version-bump crashed. Fixed by migration `b3f1d52a9c7e`.
- **`notifications.communication_id`** column existed in DB but was missing from the `Notification` model. Added to model + surfaced in `to_dict()`; index created by migration `b3f1d52a9c7e`.
- **`list_observations` now applies its filters.** The function accepted `patient_id`/`code`/`start_date`/`end_date` but silently ignored them, returning every observation in the tenant. Cross-patient data exposure. Results are also ordered by `effective_datetime DESC`.
- **`/fhir/Observation/history` no longer raises `TypeError`.** It was calling `get_observation(patient_id, code, period)` — wrong arity. New `get_observation_history` service fn + reordered routes so `/Observation/history` is matched before `/Observation/{observation_id}`.
- **`sync_active_integrations` (background Celery task) now routes telemetry to TimescaleDB.** Every pulled observation previously landed in `fhir_observations` regardless of `BiomarkerDefinition.is_telemetry`, breaking the AI telemetry tools. New shared helper `app.services.integration_sync_service.apply_telemetry_split` — also wired into the manual-sync endpoint for DRY.
- **`/telemetry/anomalies` no longer raises `TypeError`.** Was calling the synchronous `AnomalyDetector.detect_biomarker_anomalies(device_id, metric, period)` with the wrong arity and an `await`. New wrapper `get_telemetry_anomalies` fetches history and feeds the detector correctly.
- **`AIModel.__table_args__` typo corrected** (was missing trailing `__`, silently dropped the `idx_ai_models_provider_active` composite index). Migration `f1a2b3c4d5e6` creates the index on existing databases.
- **Dead `processors/fhir_mapper.py` deleted** — broken import (`from app.models.fhir.observation import Observation` — module doesn't exist). Would have raised `ModuleNotFoundError` if imported.
- **`from app.models import *` works again** — removed the stale `WearableDataModel` alias (renamed to `TelemetryDataModel`); `TelemetryDataModel` is now exported in `__all__`.
- **`telemetry_service.get_telemetry_data` and `get_telemetry_summary` are no longer stubs.** Real tenant-scoped `SELECT` + aggregate queries against the `telemetry_data` hypertable.
- **OHLC double-aggregation fixed** (`analytics_service.py`) — the raw-table path set `avg_col = "AVG(col)"` and the SQL template wrapped it in `AVG(AVG(col))` — invalid SQL, silently caught by the except handler, producing empty charts for any non-cagg-stride bucket (all sub-hour/day + 1-week/month aggregations). Now uses single-level `AVG(col)`/`MAX(col)`/`MIN(col)`. Added `_ALLOWED_TELEMETRY_BUCKETS` whitelist to guard the `INTERVAL '{bucket}'` f-string interpolation.
- **`relative_score` boundary logic fixed** (`analytics_service.py`) — `_get_observation_status` used `< 0 → Low` / `> 1.0 → High` on a value clamped to [0, 1], so every score short-circuited to Normal. Now only returns Normal for strictly-interior scores (0 < s < 1); boundary values (0.0/1.0) defer to the explicit reference-range comparison.
- **Magic Fill live date** (`ai_assistance_service.py`) — hardcoded `"Today's date is 2026-03-22."` replaced with `datetime.now(timezone.utc)` injection for both `today_iso` and `current_year`.

### Added
- **AI Chatbot — HITL auto-resume continuation**: after the user resolves a human-in-the-loop task card (approve/reject), the agent automatically gets a continuation turn via `POST /sessions/{id}/resume`. The backend reads resolved outcomes from the `tasks` JSONB (never trusted from the client), builds a structured `[HITL RESOLUTION FEEDBACK]` message with per-task status/result/error, and streams a new response. The agent acknowledges what was saved, chains dependent proposals (e.g. define biomarker → add to exam via auto-resume), and respects dismissed items. Guardrails: fires at most once per message, race-gated, suppressed on session reloads.
- **AI Chatbot — HITL Continue button (partial resume)**: when the user answers some review cards but leaves others pending, a "Continue (N unanswered)" button appears. Clicking it triggers the resume with partial answers — the LLM is told which items were skipped and instructed not to auto-repropose them. When all cards are resolved, auto-resume fires immediately (no button needed).
- **AI Chatbot — parallel HITL proposals**: the system prompt now allows multiple independent `propose_*` calls in a single turn (e.g. "add medications X, Y, Z" → three parallel review cards). Dependent actions are split across turns via the auto-resume chaining mechanism.
- **`HitlTaskStatus` enum** (`backend/app/models/enums.py`): `(str, Enum)` with `PROPOSED | CONFIRMED | FAILED | DISMISSED` + a `terminal()` classmethod. Replaces scattered string-literal status comparisons across the backend. `HitlResolutionRequest.status` is now typed as the enum (Pydantic validates at the boundary). Backward-compatible: compares equal to plain strings, so existing JSONB data works unchanged.
- **`TERMINAL_HITL_STATUSES`** frontend constant (`registry.tsx`): ReadonlySet mirroring the backend `terminal()` helper — single source of truth for "which statuses mean the user finished acting."
- **AI Chatbot — human-in-the-loop (HITL) `add_medication` proposal**: the agentic chat can propose adding a medication; the user reviews/edits in a card + modal and explicitly confirms before anything is saved. Resolves the medication against the medication catalog automatically to ensure correct identifiers.
- **Powerful Catalog Search (pg_trgm)**: Replaced basic substring ILIKE matching with trigram similarity (`pg_trgm`) and GIN indexes across all catalogs (Medications, Biomarkers, Allergies, Clinical Event Types/Categories). This provides robust typo-tolerance for search and AI tools.
- **Unified Catalog Search Service**: A new `catalog_search_service.py` to power all catalog search functions across the backend.
- **Agent Capabilities Inspector**: A new modal inside the AI Chat Interface (accessible via the Wrench icon) that lists all available LangChain tools to the LLM. Features tool descriptions and dynamically parses the `args_schema` to display the exact input arguments, types, and descriptions required for each tool.
- **Data Source Inspector ID Visibility**: The raw UUID is now visible at the bottom of the data source inspector mini-cards (DataMiniPage) for precise referencing.
- **AI Chatbot — human-in-the-loop (HITL) `add_biomarker_to_examination` proposal**: the agentic chat can propose adding a lab-result biomarker to an examination; the user reviews/edits in a card + modal and explicitly confirms before anything is saved. The AI never writes — commit flows through the canonical `POST /fhir/Observation` endpoint, FHIR-validated at write time via `assert_valid_fhir`. The target exam resolves from the open exam in the chat context **or** an explicit `examination_id` the AI resolves via `get_recent_examinations` (hard-fails on unknown / cross-patient exam). Second HITL task type after `create_clinical_event`. See [AI_SYSTEM.md §4](docs/AI_SYSTEM.md).
- **Integrations SDK OAuth2 + SMART auth module** (`integrations/sdk/auth.py`): reusable Authorization Code + PKCE primitives, SMART discovery (`.well-known/smart-configuration`), **Dynamic Client Registration** (users enter only the server URL), encrypted `OAuthTokenStore`, Redis-backed `OAuthStateStore`, and a composed `SmartOAuth` (incl. `force_refresh` for refresh-on-401). Foundation for all cloud integrations (FHIR, Fitbit, Withings, …). 23 unit tests. See [INTEGRATIONS_SDK.md §3.8](docs/INTEGRATIONS_SDK.md).
- **Integrations SDK HTTP + FHIR helpers** (`integrations/sdk/http.py`, `integrations/sdk/fhir.py`): token-aware `http_request` (Bearer inject, retry/backoff, SDK exception mapping) + `paginate_bundle` (follows `link[rel=next]`); and `fhir_search`, `fhir_observation_to_create`, `parse_operation_outcome`. Reused by Stage 2 (client) and the future Stage 3 (facade). 19 unit tests.
- **FHIR Server integration** (`integrations/fhir_server/`): SMART Patient/Standalone Launch connect + **bounded FHIR search pull**. Each sync runs `Observation?patient=<remote>&_lastUpdated=gt<cursor>&_count=100&_sort=_lastUpdated` (+ optional `category`), maps each FHIR Observation to the local patient via `ObservationCreate`, and feeds the Biomarker Engine. Push is deferred to Stage 2b.
- **Platform OAuth round-trip**: `POST /{domain}/oauth/start` + `GET /{domain}/oauth/callback` (generic; secured by a one-shot Redis `state`). New `BaseConfigFlow.is_oauth` flag and `BaseHealthProvider.begin_oauth` / `complete_oauth` hooks (opt-in; existing integrations untouched).
- **Enable/disable integration script** (`backend/scripts/enable_integration.py`): headless toggle for a system integration domain (dev/CI equivalent of the admin UI).
- **FHIR Server auth modes**: per-instance `auth_mode` (`smart` | `none`). `smart` runs the full SMART round-trip (hospitals/sandbox); `none` is **tokenless** — for local/open FHIR servers (e.g. a vanilla HAPI FHIR, which has no SMART module and 404s on `/.well-known/smart-configuration`); the instance goes straight to `ACTIVE`. `fhir_search` is decoupled from `SmartOAuth` (takes an optional `access_token`).
- **Docker dev stacks**: `docker/docker-compose.dev-db.yml` (Postgres+TimescaleDB on 5433 + Redis on 6379, ports pinned to match `backend/.env`) and `docker/fhir-test-server/docker-compose.yml` (local HAPI FHIR R4 on `${HAPI_PORT:-8080}` for offline Stage 2 testing). `docker/init-test-db.sh` auto-creates `health_assistant_test`. See [docker/README.md](docker/README.md).
- **FHIR seed script** (`backend/scripts/seed_fhir_server.py`): POSTs a Patient + LOINC-coded lab/vital sample Observations (glucose, lipids, CBC, BP, HR, SpO₂, …) with reference ranges, spread over N months, to a FHIR server for testing the pull path.
- **Export & Import (Backup) System**: Comprehensive data export and import at patient/group/system scopes with three formats (FHIR R4B Bundle, full BagIt-style ZIP backup, catalog-only). Includes FHIR validation via `fhir.resources`, SHA256 manifest verification, cross-tenant id remapping, and a Celery-driven async job system. Admin-only UI at `/settings/export-import` with export configuration, drag-and-drop restore, live job polling, and detailed job modals. See [EXPORT_IMPORT.md](docs/EXPORT_IMPORT.md).
- **FHIR Converter** (`services/fhir_converter.py`): Bidirectional ORM ↔ FHIR R4B conversion for Patient, Observation, MedicationStatement, AllergyIntolerance, DiagnosticReport, Organization, Practitioner.
- **Modal component** (`components/ui/Modal.tsx`): Reusable accessible modal with Portal, ESC-to-close, overlay click, and scroll lock.

### Changed
- **Dev processes now run under [honcho](https://github.com/nickstenning/honcho)** via `Procfile.dev`. `scripts/run-dev.sh` keeps the bootstrap (venv/deps/migrations/admin/frontend-deps/Redis-preflight) but replaces manual PID tracking + cleanup trap with `exec honcho start`. Single Ctrl+C stops everything cleanly; if any process crashes, honcho stops the whole group so the error is impossible to miss (instead of jobs silently queuing in PENDING).
- **Flower added at http://localhost:5555** for dev visibility (parity with the docker-compose Flower service). `flower==2.0.1` was already in requirements.
- `--force-celery` flag deprecated (no-op with backward-compat message; honcho owns the worker lifecycle now). `--force-stop` now also kills `honcho start` and port 5555.
- **Production Docker hardening**:
  - **`beat` service added** to both `docker-compose.yml` and `docker-compose.prod.yml`. Previously silent broken state in Docker deploys — periodic tasks (`cleanup_stuck_extractions`, `check_notification_triggers`, `sync_active_integrations`) never fired, breaking medication reminders, recurring notifications, stuck-exam cleanup, and integration auto-sync.
  - **Migrations moved out of the backend container** into a one-shot `migrate` service (`restart: "no"`, runs `alembic upgrade head`, exits). Backend/worker/beat depend on it with `condition: service_completed_successfully`. Fixes the parallel-replica race + boot-coupling.
  - **Flower now requires HTTP basic auth** (`--basic-auth=${FLOWER_USER}:${FLOWER_PASSWORD}`) and binds to `127.0.0.1` by default. Previously exposed unauthenticated on all interfaces — anyone with network access could inspect task payloads + retry jobs.
  - **Healthchecks** added for backend (`curl /health`), worker (`celery inspect ping`), flower (`curl /`). Combined with `init: true` for proper PID 1 signal handling.
  - **Resource limits** on backend (1G/1cpu) and worker (2G/2cpu) via `deploy.resources.limits`. Worker `--concurrency=${CELERY_WORKER_CONCURRENCY:-2}` + `--max-tasks-per-child=${CELERY_MAX_TASKS_PER_CHILD:-100}` to release memory leaked by ML/OCR libraries.
  - **Log rotation** on every service (`max-size: 10m`, `max-file: 3` via YAML anchor). Without this, `json-file` logs grow unbounded.
  - **All ports bind to `127.0.0.1` by default** (`BACKEND_BIND`, `FLOWER_BIND`, `FRONTEND_BIND` env overrides). Forces reverse-proxy use in prod.
  - `SECRET_KEY` + `INTEGRATION_SECRET_KEY` now passed through to worker/beat/flower via a shared `*backend-env` YAML anchor (previously missing — broke integration sync decryption + JWT issuance inside worker tasks).
  - `curl` added to both Dockerfiles for healthchecks.
  - `.env.example` rewritten with grouped sections and inline generator commands.
  - Removed obsolete `version: '3.8'` from both compose files (modern Compose spec ignores it).
- **Biomarker Citations & Telemetry Tooling**: The system prompt now strictly enforces referencing biomarkers by their `id` (UUID) instead of `slug`. Backend tools (`get_biomarker_history` and `get_aggregated_biomarker_trends`) have been updated to accept `id` instead of `slug` to prevent collision bugs.
- **AI Chatbot Biomarker Tool**: `search_available_biomarkers` tool was upgraded from unindexed Regex (`~*`) to indexed trigram search, improving performance and accuracy.
- **`ChatbotTools` examination context**: `ChatbotTools` now accepts `examination_id`, threaded from the chat context in both `_stream_chat` and `_general_chat`, so the propose/inspect tools can target the active examination.
- **Write-time FHIR validation (FHIR architecture Stage 1.1)**: every `fhir_service.create_*` / `update_*` now calls `assert_valid_fhir()` before persisting, so invalid FHIR can never be stored — the root-cause fix for the shape-drift bug class. A dedicated handler maps `FhirSerializationError` → HTTP 400 (more specific than the global 500). See [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) § "Data Serialization & FHIR Interoperability".
- **Headless `AddBiomarkerForm`**: extracted the biomarker-entry form out of `AddBiomarkerModal` into a reusable headless component (catalog search + FHIR Observation build + prefill path), shared by the manual modal and the HITL `AddBiomarkerHandler`. `AddBiomarkerModal` is now a thin portal wrapper.
- **Doctor `address` schema is FHIR-correct**: `DoctorResponse` / `DoctorCreate` / `DoctorUpdate.address` is now `Optional[List[Address]]` (FHIR `Practitioner.address` is `0..*`). The frontend reads `address?.[0]` (mirroring `Organization`). Previously single-typed, which conflicted with FHIR-list-shaped storage.
- `Patient.to_dict()` returns the primary name as a single `HumanName` object (frontend contract); `Patient.to_fhir_dict()` emits `List[HumanName]` (FHIR). Storage shape (dict|list) is normalized on read via `_primary_human_name` / `_coerce_human_name_list`.
- `Observation.to_fhir_dict()` cleans `valueQuantity` (`_clean_quantity`): drops empty-string `unit`/`code`/`system` and keeps the `system`+`code` pair intact.
- `Medication.to_dict()` / `to_fhir_dict()` use `_enum_value` for `status` (tolerates enum-or-string pre-flush state).
- Replaced the stub `ImportService` (in-memory, non-persisting) with a real DB-backed service that validates FHIR resources and upserts by natural key.
- `Observation.to_dict()` now includes `subject`, `performer`, `comment`, and `value_codeable_concept` (previously missing FHIR fields).
- `DiagnosticReport` now has a `to_dict()` method (was missing).
- Export/import services use the resolved `UPLOAD_DIR` from `document_service_db.py` (with fallback chain) instead of `settings.UPLOAD_DIR` directly.

### Fixed (FHIR conformance — runtime)
- **SDK-built Observations no longer silently dropped by FHIR validation.** `ObservationBuilder.build()` stripped tzinfo "for asyncpg compat" (asyncpg handles tz-aware natively), so `isoformat()` produced e.g. `'2026-06-20T22:39:56.471381'` which failed the FHIR R4 regex. Every pulled observation from `dev_dummy` (and any future SDK provider) was being dropped by `assert_valid_fhir`. The builder now keeps tzinfo; new `fhir_isoformat()` helper on the ORM side defends against naive datetimes anywhere else. The dropped-count now also surfaces to the integration UI: the manual-sync response and `IntegrationSyncLog` carry `dropped_invalid` / `status="partial"` instead of reporting a silent "success" with zero metrics.

### Fixed (legacy)
- **HITL tasks lost on stream interruption**: tasks were only saved at the END of the `_chat_stream` generator. If the stream was interrupted (LLM error, client disconnect) after the `[HITL_TASK]` SSE chunk but before completion, the task was never persisted — breaking `/resolve` (404) and `/resume`. Tasks are now **proactively saved** the moment they're detected; `update_message_fields` patches the final content when the stream completes normally.
- **`"object async_generator can't be used in 'await' expression"`**: `resume_after_hitl` is an async generator (uses `yield`) — the `/resume` endpoint was incorrectly awaiting it. Fixed: `async for chunk in service.resume_after_hitl(...)` (no await).
- **`_general_chat` missing HITL system prompt**: the non-streaming chat path had no HUMAN-IN-THE-LOOP section in its system prompt, despite running the same HITL tool-call branch. Now mirrors `_chat_stream`.
- **`GET /api/v1/doctors` 500** (`ResponseValidationError: ... 'address' Input should be a valid dictionary ... input: [{...}]`): root-caused to a Pydantic schema / FHIR cardinality mismatch (the schema typed `address` single while stored JSONB + FHIR use a list). Fixed at the schema level (see "Changed") rather than masked by a silent coercing validator. The FHIR cardinality + fail-loud rules are now documented in [ARCHITECTURE.md](docs/ARCHITECTURE.md).
- **FHIR export abort on `name`/`valueQuantity.code`**: legacy shape drift in stored JSONB. Export is fail-loud by design (`ExportError`); the serializers now normalize via `_coerce_human_name_list` / `_clean_quantity`.
- **`UniqueViolationError` on `Patient.mrn`**: empty-string `mrn` collided under the UNIQUE constraint (Postgres treats `''` as equal, unlike `NULL`). `create_patient`/`update_patient`/`import_service` now normalize `mrn` → `NULL` for empty/whitespace.
- **Frontend blank page** (`patient.name.given is undefined`): 3 non-defensive name-access sites (`PatientDetail`, `MedicationList`, `CalendarPage`) hardened with optional chaining.
- **Enum value mismatch**: `ExportScope` and `ExportType` enums now use `values_callable` so SQLAlchemy sends lowercase values (`patient`) matching the DB enum, not the uppercase names (`PATIENT`).
- **FHIR Observation.category dropped on pull**: FHIR `category` is `0..*` (a list) but `ObservationCreate.category` stores a single dict; `fhir_observation_to_create` now coerces via `_first_codeable_concept`. Previously every categorized observation failed validation and was silently skipped (0 pulled from a real server).

### Removed (dead code)
- **`POST /import` + `GET /import/status/{id}` placeholder routes.** Both were legacy stubs that returned redirect messages ("Use POST /import/backup, /import/fhir, …" / "Legacy in-memory jobs are no longer tracked"). Zero frontend callers (all imports go through `/import/backup` and `/import/jobs/{id}`); zero test coverage.
- **`UnitConverter` class** (`backend/app/services/unit_converter.py`, 81 lines) — defined but zero callers anywhere in `backend/app`, `backend/tests`, or `backend/scripts`. Deleted.
- **`services/__init__.py` re-exports** — `UnitConverter`, `AnomalyDetector`, `MedicationInteractor`, `NotificationService` were re-exported but no caller imported via the package; everyone uses direct path imports (`from app.services.anomaly_detector import AnomalyDetector`). File emptied (kept as package marker) with a comment explaining the convention.
- **Frontend deps `graphql` and `graphql-request`** removed from `package.json`. The `api/graphql.ts` client was deleted in 0.3.0-alpha (zero production callers); the dependency entries were left behind. Lockfile updated.

### Changed (API surface consolidation)
- **Deprecated the misleadingly-named `/fhir/*` ORM-shape router.** Patient/Observation CRUD moved to proper domain endpoints (`/patients/*`, `/observations/*`); medication create consolidated under `/medications/*` (new `GET /medications/{id}` for citation lookups). The `/fhir/R4/*` facade is now clearly the interop-only surface; the frontend uses domain endpoints. The frontend's `types/fhir.ts` was split into `types/patient.ts` + `types/observation.ts` (the old name was misleading — these types mirror ORM-shape, not FHIR R4). `services/fhirService.ts` was split into `patientService.ts` + `observationService.ts`.
- **Dead code removed**: GraphQL client (`api/graphql.ts`, defined but zero production callers); 6 unused `/fhir/*` routes (DiagnosticReport CRUD, duplicate Medication create/list, Observation history); the legacy single-layout `PUT /patients/{id}/layout` slot is still available but the live dashboard uses the per-user `/patients/{id}/layouts/*` routes.

### Docs
- `docs/DEVELOPMENT.md` — Flower URL, honcho flow, manual-start warning that running `uvicorn` standalone silently breaks background jobs.
- `docs/INSTALL.md` — security checklist updated with `FLOWER_USER`/`FLOWER_PASSWORD`, `BACKEND_BIND`, reverse-proxy requirement.
- `docs/STATUS.md` — added note that the comprehensive 2026-06-22 re-audit superseded the "29 items resolved" claim (110 new findings: 25C/32H/30M/23L), tracked in the local stabilization plan.
- `docs/PROJECT_STRUCTURE.md` — clarified `docker-compose.yml` (dev/staging) vs `docker-compose.prod.yml` (prod); added `Procfile.dev`.
- Project docs — fixed false claim that `main.py` has "celery auto-heal" (`check_and_start_celery()` was removed; celery is managed by the process supervisor). Documented the honcho dev workflow.

### Operational notes for deploy
1. **Set `INTEGRATION_SECRET_KEY`** (Fernet key) in `.env`. Generate one with:
   ```bash
   python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
   ```
2. **Run the api_key backfill** after deploying:
   ```bash
   cd backend && PYTHONPATH=. python scripts/encrypt_existing_api_keys.py --dry-run
   cd backend && PYTHONPATH=. python scripts/encrypt_existing_api_keys.py
   ```
3. **Run migration `f1a2b3c4d5e6`** (`alembic upgrade head`) — creates the previously-dropped `idx_ai_models_provider_active` index.
4. **Frontend follow-up recommended**: `ProviderManager.tsx` should surface the new `has_api_key` field for cleaner edit UX (currently the masked value pre-fills the edit input — functional, but not ideal).
5. **Set `POSTGRES_PASSWORD`** explicitly in `.env` — the insecure `admin123` default was removed. Production boot will refuse known-weak values.
6. **Webhook HMAC**: integrations that want HMAC verification should set `user_config["webhook_secret"]` to a strong random string. The sender must compute `HMAC-SHA256(secret, raw_body)` and send the hex digest in the `X-Webhook-Signature` header.
7. **WebSocket frontend migration**: update the frontend `useWebSocket` hook to connect with `new WebSocket(url, ["bearer", token])` so the token travels via the subprotocol instead of the query string. The query-string fallback remains for backward compatibility.
8. **AuditLog**: the `audit_logs` table is now populated for FHIR create/delete operations. Query it for provenance: `SELECT * FROM audit_logs WHERE resource_type = 'Observation' ORDER BY created_at DESC`.

### Tests
- **205 new tests** across the full security pass: 87 from the first pass + 118 from the second pass. See the changelog below "Branch progress" sections for the file/coverage matrix.

### Post-rc.1 batch (32 commits since `v0.3.0-rc.1`)

#### Added — tenant administration & switching
- **Tenant admin service + endpoints** (`backend/app/services/tenant_admin_service.py`, `backend/app/api/v1/endpoints/admin_tenants.py`): comprehensive admin management for tenants — create/update/suspend/delete, member management, stats. 546 new tests in `test_admin_tenants.py`.
- **Tenant switcher UI** (`frontend/src/components/layout/TenantSwitcher.tsx`, `TenantSwitchBanner.tsx`, `store/slices/tenantSwitchSlice.ts`): SYSTEM_ADMIN can switch active tenant context from the header dropdown; state synced from JWT; re-switching allowed. `SYSTEM_ADMIN` treated as a global role, not a tenant member.
- **Tenant detail page** (`frontend/src/pages/Admin/TenantDetail.tsx`): full tenant management UI with members, stats, and settings.
- **Tenant management page** (`frontend/src/pages/Admin/TenantManagement.tsx`): expanded list view with create/suspend/delete actions.

#### Added — deployment & CI
- **Standalone production flavor** (`docker/docker-compose.standalone.yml`): all-in-one production deployment (backend + worker + beat + flower + frontend + nginx) for single-host installs. Production-ready `nginx.conf` with SSE and WebSocket support.
- **GHCR Docker publishing** (`.github/workflows/docker-publish.yml`): GitHub Action builds and publishes images to `ghcr.io`; images renamed to `health-assistant-*` (hyphen consistency); `latest` tag applied to main branch.
- **Environment setup script** (`scripts/setup_env.py`): interactive Python script to auto-generate `SECRET_KEY` + `INTEGRATION_SECRET_KEY` and write `.env` from `.env.example`.
- **Renamed dev compose** — `docker-compose.yml` → `docker/docker-compose.dev.yml` for clarity against the standalone/prod flavors.

#### Added — frontend UX
- **User profile page** (`frontend/src/pages/Account/MyAccount.tsx`): identity, linked patient/doctor records, account metadata; accessible to all authenticated users via avatar dropdown.
- **Settings sidebar layout** (`frontend/src/components/settings/SettingsLayout.tsx`): dedicated sections (Preferences, Security, Appearance, AI Config, Integrations, Notifications, Export/Import). Settings menu group removed from main sidebar.
- **NoPatientState component** (`frontend/src/components/ui/NoPatientState.tsx`): reusable empty-state for all patient-scoped pages — adapts to tenant state (prompts selection / guides creation on fresh instances). Rolled out across Dashboard, Clinical Alerts, Calendar, Medications, Events, Documents, Examinations, Biomarker Trends, Correlative Analytics, Biomarker Detail, Notifications, Integrations.
- **Smart dashboard layout generator** — overview / vitals / clinical / compact presets; auto-saves generated layouts; improved packing algorithm.
- **Dashboard card registry + 4 new cards** (`frontend/src/components/dashboard/cardRegistry.ts`): `HealthSummaryCard`, `MultiBiomarkerComparisonCard`, `RangeGaugeCard`, `AnomalyCard`. Old `Alerts`/`RecentDocuments`/`VitalStats` cards replaced.
- **Patient detail summary cards** — replaced tabbed PatientDetail with rich summary cards: `BiomarkerSummary`, `ClinicalEventSummary`, `ExaminationSummary`, `ScheduleSummary`, plus updated `AllergySummary` / `MedicationSummary`. Shared `SummaryCardHeader` component.
- **Reusable UI components**: `CreateMenu` (global create-action dropdown), `OpenPageButton`, `InfoTooltip` (refactored), `useBiomarkerChange` + `useCreateIntent` hooks.
- **Default dashboard layout refined** — Unified Health Calendar, Clinical Alerts, Biomarker Trends, Latest Documents widgets by default; aligned with `seed_demo.py` data.
- **UI presentation tooling** (`scripts/capture_ui.sh`, `frontend/tests-e2e/ui-capture/`): Playwright-based screenshot pipeline; `docs/images/` populated with desktop screenshots + visual tour GIF; `docs/UI_CAPTURE_PIPELINE.md` + `docs/SCREENSHOTS.md` added.
- **Preferences page** (`frontend/src/pages/Settings/Preferences.tsx`) and **Security page** (`frontend/src/pages/Settings/Security.tsx`) — extracted from the removed `Profile.tsx`.

#### Changed — environment configuration
- **Single root `.env` consolidation** (`c5b6be7`) — eliminated the `backend/.env` vs root `.env` drift. `config.py` now uses `_resolve_env_file()`: 3-tier precedence (`HA_ENV_FILE` env var → walk-up from `__file__` to nearest `.env` → None for docker/prod). `backend/.env` and `backend/.env.example` deleted. `run-dev.sh` exports root `.env` so direct-uvicorn/IDE/script launches work without honcho. Docker compose files pass previously-missing env vars (`VAPID_ADMIN_EMAIL`, `MCP_*`, `UPLOAD_DIR=/app/uploads`, `AI_AGENT_MAX_ITERATIONS`, `APP_URL`).
- `.env.example` relocated to repo root; rewritten with grouped sections + inline generator commands.
- `docs/INSTALL.md` simplified — Quickstart focuses on standalone production path; dev setup moved to `docs/DEVELOPMENT.md`.
- `docker/README.md` refactored to remove redundant installation info; FHIR docker directory renamed to `docker/fhir-test-server/` for clarity.

#### Fixed
- **`create_system_admin.py` slug + error handling** — initial tenant now gets a generated slug; error messages point to root `.env`.
- **Docker compose env + seeding issues** — optional variables given empty fallback defaults (suppresses warnings); lowercase enum bug in `allergies.json` seed fixed.
- **Database password validation** — `config.py` now validates the password directly from `DATABASE_URL` (not just `POSTGRES_PASSWORD`).
- **Postgres hostname in Docker for Alembic** — properly resolved for migration runs inside containers.
- **Dashboard infinite loading on fresh instances** — `loadLayout` early-return left `isLoading` stuck true; fixed.
- **PWA update toast during screenshot capture** — suppressed when running the UI capture pipeline; native PDF viewer toolbars disabled via `#toolbar=0&navpanes=0` in embedded iframes.
- **Mini chart dots** — visible dot now rendered for single-point biomarker mini charts.
- **`/users/me` 404 for switched SYSTEM_ADMIN** — fixed tenant context resolution.

#### Docs
- `docs/TENANCY_AND_USER_MANAGEMENT.md` — replaced false three-tier hierarchy with accurate data model (recursive Organizations, peer entities, correct `User→Patient/Doctor` 1:N and `Dept↔Doctor` N:N cardinalities).
- `docs/DEVELOPMENT.md` — docker compose command fix, manual dev setup instructions.
- `docs/INSTALL.md` — separated bash commands into individual blocks, clarified AI settings as optional fallbacks, Buy Me a Coffee link in README.
- `docs/SEEDING_AND_DEMOS.md`, `docs/UI_CAPTURE_PIPELINE.md`, `docs/SCREENSHOTS.md` — new docs for the presentation pipeline.

---

## [0.3.0-rc.1] - 2026-06-21

**Release candidate — cleanup pass following the API surface consolidation in 0.3.0-alpha.**

### Fixed
- **RBAC gate on global examination categories.** `PATCH /examination-categories/{id}` previously had a `# TODO: Implement super-admin check here` placeholder; tenant admins could mutate global (`tenant_id=None`) categories. The route now requires `Role.SYSTEM_ADMIN` for global-category mutations. Tenant-scoped categories are unaffected. Locked in by 7 new tests in `test_examination_categories_endpoints.py`.
- **`test_import_backup_task_runs_service` no longer fails.** The test's `FakeImportService.run_import` was missing the `config=None` kwarg that the real `ImportService.run_import(job_id, archive_path, owner_id, config=None)` requires. The fake raised `TypeError` inside the task wrapper, returning `status="failed"`. Full backend test suite now green: 776 passed.

### Removed (dead code)
- **`POST /import` + `GET /import/status/{id}` placeholder routes.** Both were legacy stubs that returned redirect messages ("Use POST /import/backup, /import/fhir, …" / "Legacy in-memory jobs are no longer tracked"). Zero frontend callers (all imports go through `/import/backup` and `/import/jobs/{id}`); zero test coverage.
- **`UnitConverter` class** (`backend/app/services/unit_converter.py`, 81 lines) — defined but zero callers anywhere in `backend/app`, `backend/tests`, or `backend/scripts`. Deleted.
- **`services/__init__.py` re-exports** — `UnitConverter`, `AnomalyDetector`, `MedicationInteractor`, `NotificationService` were re-exported but no caller imported via the package; everyone uses direct path imports (`from app.services.anomaly_detector import AnomalyDetector`). File emptied (kept as package marker) with a comment explaining the convention.
- **Frontend deps `graphql` and `graphql-request`** removed from `package.json`. The `api/graphql.ts` client was deleted in 0.3.0-alpha (zero production callers); the dependency entries were left behind. Lockfile updated.

### Changed
- **Stale docstrings refreshed** in `backend/app/api/v1/endpoints/fhir_r4.py` and `backend/app/facade/__init__.py` — both still referenced "the legacy ORM-shape `/fhir/*` router (which the frontend keeps using)" after the router was deleted in 0.3.0-alpha. Now describe the facade as the interop-only surface.

---

## [0.2.0] - 2026-06-14

### Added
- **AI Telemetry Integration**: Enabled the AI Chatbot to access high-frequency telemetry data (heart rate, steps, etc.) stored in TimescaleDB using new aggregated tools.
- **Biomarker Discovery Tool**: Added `search_available_biomarkers` tool to the AI with regex support, allowing the LLM to identify correct metric slugs and data types (Telemetry vs Clinical).
- **Aggregated Trends Tool**: Created `get_aggregated_biomarker_trends` providing OHLC (Open-High-Low-Close) data to the AI, ensuring context window protection via record limiting.
- **Configurable Reasoning Loop**: Moved the hardcoded AI reasoning loop limit to a multi-tiered configuration system (Global Default -> System DB -> Tenant Override).
- **Persistent System Settings**: Implemented a `SystemSetting` database table and model for managing global application configurations via the UI.
- **AI Agent Admin UI**: Added a new "Agent Settings" management tab in the AI Configuration pages for both System and Tenant scopes.

### Changed
- Refactored `AnalyticsService.get_biomarker_trends` to support explicit `start_date` and `end_date` parameters for both clinical and telemetry data.
- Optimized AI tool-calling logic to prefer exact matches and prevent cross-metric data contamination (e.g., Heart Rate vs HRV).
- Updated AI System Prompts with strict biomarker routing rules and discovery-first logic.
- Bumped the default AI reasoning loop limit to 20 iterations.

### Fixed
- **Audit Column Mismatch**: Resolved a `ProgrammingError` by adding missing `created_by` and `updated_by` columns to the `system_settings` table via migration.
- **Fuzzy Match Hijacking**: Fixed a bug where substring matching in biomarker trends would return wrong data if a requested slug was a substring of another metric.
- **Analytics Endpoint Argument Mismatch**: Fixed a bug where positional arguments were incorrectly passed to `get_biomarker_trends` in the analytics endpoint, causing empty results due to a missing database session.
- **Timezone Inconsistency**: Fixed a `TypeError` (offset-naive/aware conflict) by standardizing all clinical timestamp columns (`effective_datetime`, `onset_date`, etc.) to use `TIMESTAMPTZ` via database migration and model updates.

---

## [0.1.0] - 2026-06-10

### Added
- **Centralized Versioning System**: Implemented a single source of truth for versioning loaded dynamically in FastAPI and root health endpoints.
- **Project Versioning Manager**: Created a unified CLI script (`scripts/version_manager.py`) to easily query, set, or bump semantic versions across all backend, frontend, and document files.
- **Reusable AppVersion Component**: Designed a theme-compatible, responsive `<AppVersion />` UI component for displaying the semantic version in both the Sidebar and the Login page.
- **Household-First Multi-Tenancy**: Added zero-config multi-tenant setup for home environments where the first registered user auto-scales to `SYSTEM_ADMIN` and households are dynamically isolated.
- **Identity & Record Linking**: Implemented dynamic linking connecting system login accounts to patient and doctor clinical records.
- **Biomarker-Event Bindings**: Programmed structural links connecting qualitative clinical events (like "Myopia") directly to quantitative biomarker observations (like "Visual Acuity").
- **Anatomical Body Part Mapping**: Expanded clinical events with physical anatomical coordinates (`BodyPartModel`) allowing symptoms and findings to bind to specific body systems.
- **Universal Health Calendar**: Developed an interactive health calendar component that integrates medical visits, medication timelines, and chronic event logs.
- **Drag-and-Drop Dashboards**: Added modular dashboard panels (`VitalStats`, `TrendsCard`, `AllergyAlertsCard`) with persistent grid locations saved to Zustand.

### Changed
- Refactored `Sidebar.tsx` and `Login.tsx` to consume the new `<AppVersion />` component.
- Updated core backend configurations to declare legacy API keys/variables as safe local fallback configurations.
- Audited and sanitized the repository in preparation for public release on GitHub, including masking potential secret keys/domains in documentation.
- Standardized all clinical and system enums to uppercase characters with automated database migration.
- Restructured frontend layouts using responsive `MasterDetailLayout` and `StickyToolbar` wrappers for optimized mobile viewing.

### Fixed
- **Pytest Suite Refactoring (92/92 Passing)**: Expanded tests count from 41 to 92, fixing database connection leaks, testing mock typings, and isolating conftest sessions.
- **Container Testing Sidecars**: Integrated PostgreSQL and Redis database service sidecars directly into testing workflows to eliminate dependency issues.
- **WebSocket & CORS Blockers**: Replaced local loopback connections with relative endpoints and configured Vite servers to allow binding over any local router hosts.
- **PageHeader Rendering**: Solved recursive rendering loop in PageHeader and optimized Zustand state subscriptions to increase application responsiveness.

---

## 2026-03-24

### Added
- **Asynchronous Notification Framework**: Implemented a modular system for handling medical reminders, clinical alerts, and system events.
- **Web Push (VAPID) Integration**: Added support for native browser notifications in the PWA, allowing alerts to be received even when the app is closed.
- **Automated Medication Reminders**: The system now automatically generates recurring notification triggers when new medications are prescribed.
- **Biomarker Event Hooks**: Integrated real-time event triggers for new observations, enabling future threshold-based alarms (e.g., high glucose).
- **Notification Center**: A new reactive global UI component with unread badges and status management for in-app messages.
- **Notification Management Page**: Dedicated dashboard to view active scheduled triggers (Next Run times) and historical delivery logs.
- **Smart Reminders UI**: Added a dedicated management component to the Medication detail page for creating custom alarms.
- **Custom Service Worker**: Implemented `sw.ts` with `injectManifest` to handle background push payloads and interaction logic.

### Changed
- Migrated from legacy `Alerts` system to the new `NotificationTrigger` and `Notification` models for better scalability and FHIR compliance.
- Updated PWA configuration to support advanced background capabilities.

## 2026-03-23

### Added
- **Clinical Events**: Introduced a comprehensive system for tracking longitudinal health narratives such as pregnancies, chronic pain, dental treatments, and surgical recoveries.
- **Global Events Dashboard**: A new top-level menu item and page providing a cross-patient view of all ongoing and historic clinical events.
- **Interactive Episode Tracking**: Events now support high-precision logging of individual occurrences (episodes) with date, time, intensity (1-10), and body location.
- **Type-Specific Metadata**: specialized schemas for different event types (e.g., Gestational Age for Pregnancy, Mechanism of Injury for Accidents, Diopters for Vision).
- **Bi-directional Visit Mapping**: Seamlessly link examinations to clinical events with specific clinical reasons. Associations can be managed directly from the Examination edit mode.
- **Enhanced Internationalization**: Full English and Greek support for all clinical event labels, placeholders, and status indicators.

### Changed
- Refactored `AssociatedEvents` into a modular, reusable component with "Compact" and "Detailed" rendering modes.
- Updated `PatientDetail.tsx` to support routable tabs via URL (`/history` and `/events`).
- Improved navigation consistency: clicking event badges in any list now navigates to the specialized Event Detail page.

### Fixed
- **Timezone Aware Dates**: Resolved `DataError` (offset-naive/aware conflict) by making all clinical event date columns timezone-aware in PostgreSQL.
- **Medication Modal Typings**: Fixed a critical frontend bug where the `onClose` handler was incorrectly typed as a boolean.
- **Build Integrity**: Cleaned up numerous TypeScript errors and unused imports across core frontend components.

## 2026-03-11

### Added
- **Global Patient Management**: Top-level header now contains a strict Patient selection dropdown that dynamically filters all dashboards, charts, and document views across the entire application context.
- **Examinations Platform**: Documents are now grouped categorically by individual medical visit instances ("Examinations"). Added new Models, Migrations, and CRUD endpoints to support this.
- **Rich-Text Medical Notes**: Doctors/Users can now write, edit, and save full HTML markdown notes directly into an Examination using a WYSIWYG editor (`react-quill`).
- **Dynamic AI Visualizer Factories**: Examinations page now dynamically mounts specific React UI components (Lab Results Table, Dual-Pane Imaging viewer) depending on what type of document the AI identified inside the visit.
- **DICOM (.dcm) Support**: The backend now natively extracts binary metadata from RAW DICOM files, converts the pixel matrix to an internal image buffer, and serves it seamlessly inline to the browser.
- **AI Categorization Framework**: The background OpenAI OCR agent now automatically categorizes any uploaded document (or unstructured image) into clinical buckets (e.g. Ophthalmology, Cardiology, Laboratory Tests, etc.) with safe fallbacks (`Other`).

### Changed
- Refactored `Dashboard.tsx` to automatically pull unique Biomarkers directly from the user's historical AI dataset.
- Refactored `/api/v1/users/me` endpoint to hit the database dynamically and construct full JWT tokens injected with correct `tenant_id` bindings.
- Modified File Response handlers to explicitly use `inline` content disposition and dynamic MIME guessing so PDFs/Images embed safely into frontend components instead of strictly downloading.
- Swept the `backend/` directory clean; moved all 9 stray maintenance/DB debug scripts into a dedicated `backend/scripts` folder.

### Fixed
- Fixed OpenAI JSON string parsing. (Previously if the LLM outputted ```json wrappers, it would trigger a parser failure and crash to the fallback local NLP).
- Fixed cross-chart timestamps. All FHIR observations now strictly extract their analytical graph dates from the parent `Examination` date, NOT the day the file was technically uploaded.
- Fixed 500 error in `get_patient` where the `to_dict()` serialization method was absent from the SQLAlchemy schema.
- Silenced numerous Pylance `MockResult` redeclaration warnings in testing suites.
- All 41 backend automated endpoints tests are currently passing natively (`100%` coverage on new workflows).
